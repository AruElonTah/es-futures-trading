---
phase: 00-provider-validation-spike
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01-PLAN.md
  - 00-02-PLAN.md
files_modified:
  - .planning/research/spike-0/comparison-table.md
  - .planning/decisions/0001-data-provider.md
  - scripts/spike/verify_adr.py
  - scripts/spike/verify_artifacts.py
autonomous: false
requirements:
  - FND-10
tags:
  - phase-0
  - adr
  - data-provider
  - vendor-comparison

must_haves:
  truths:
    - "A committed ADR at .planning/decisions/0001-data-provider.md documents (a) Twelve Data's verified ES coverage as of execution date, (b) TradingView MCP as the chosen v1 primary feed with rationale, and (c) Databento as the named futures-aware swap candidate — satisfying FND-10 verbatim."
    - "The ADR uses MADR format with YAML frontmatter so its body bytes can be hashed for `runs.adr_hash` traceability (FND-08 + OPT-04 downstream dependency)."
    - "The ADR cites all 6 Phase 0 spike artifacts by relative path, and the cited files all exist on disk at the time of the closing commit."
    - "The vendor comparison table re-verifies pricing for Twelve Data, Databento, and Massive/Polygon as of execution date — pricing pages change."
    - "Two assertion scripts (verify_adr.py, verify_artifacts.py) can be re-run by any future contributor to confirm the Phase 0 deliverables remain valid."
    - "The ADR contains an explicit 'When to revisit' section with concrete trigger conditions, per RESEARCH.md Pitfall #4."
  artifacts:
    - path: ".planning/research/spike-0/comparison-table.md"
      provides: "Markdown vendor-comparison table for Twelve Data / Databento / Massive (Polygon) / Interactive Brokers historical / CME DataMine, with ES coverage / MES coverage / live + historical / pricing as-of execution date / Python SDK / verification source. Plus a 'do not swap to' list."
      min_lines: 30
    - path: ".planning/decisions/0001-data-provider.md"
      provides: "The MADR ADR with YAML frontmatter (status, deciders, date, adr_id=0001, slug=data-provider), Context, Decision Drivers, Considered Options (≥4), Decision, Consequences, Verification artifacts, Pros and Cons of the Options, and 'When to revisit' section."
      contains: "adr_id: 0001"
    - path: "scripts/spike/verify_adr.py"
      provides: "Stdlib-only script that asserts the ADR file exists, parses YAML frontmatter, contains required keys (status, deciders, date, adr_id, slug), and includes all required markdown sections by H2 header. Exits 0 on success, non-zero with a clear diagnostic on failure."
      min_lines: 40
    - path: "scripts/spike/verify_artifacts.py"
      provides: "Stdlib-only script that asserts the 6 expected files in .planning/research/spike-0/ + the ADR all exist and are non-empty (>= 100 bytes for markdown, >= 200 bytes for JSON). Exits 0 on success, prints which files are missing or undersized on failure."
      min_lines: 30
  key_links:
    - from: ".planning/decisions/0001-data-provider.md"
      to: ".planning/research/spike-0/twelvedata-probe.json"
      via: "Cited by relative path in the Verification artifacts section"
      pattern: "twelvedata-probe.json"
    - from: ".planning/decisions/0001-data-provider.md"
      to: ".planning/research/spike-0/tv-mcp-transcript.log"
      via: "Cited by relative path in the Verification artifacts section"
      pattern: "tv-mcp-transcript.log"
    - from: ".planning/decisions/0001-data-provider.md"
      to: ".planning/research/spike-0/tv-restart-test.log"
      via: "Cited by relative path in the Verification artifacts section"
      pattern: "tv-restart-test.log"
    - from: ".planning/decisions/0001-data-provider.md"
      to: ".planning/research/spike-0/spy-bar-budget.md"
      via: "Cited by relative path in the Verification artifacts section"
      pattern: "spy-bar-budget.md"
    - from: ".planning/decisions/0001-data-provider.md"
      to: ".planning/research/spike-0/comparison-table.md"
      via: "Cited by relative path in the Considered Options / Verification artifacts sections"
      pattern: "comparison-table.md"
    - from: "scripts/spike/verify_adr.py"
      to: ".planning/decisions/0001-data-provider.md"
      via: "yaml.safe_load on the frontmatter block; markdown header presence check"
      pattern: "0001-data-provider.md"
---

<objective>
Close Phase 0 by writing the futures-aware vendor comparison table, authoring the MADR ADR that locks the v1 data-provider decision, and committing two assertion scripts so the Phase 0 deliverables remain re-verifiable.

Purpose: This is the gate that says Phase 0 is "done." FND-10 (the only Phase 0 requirement) mandates a committed ADR documenting Twelve Data ES coverage, the chosen v1 primary feed, and the eventual swap candidate. Plans 1 and 2 produced raw evidence; this plan synthesizes that evidence into a decision artifact every Phase 4+ optimization run will reference by hash (FND-08 + OPT-04 downstream). Plan 3 cannot start until Plans 1 and 2 have shipped their artifacts to `.planning/research/spike-0/`.

Output:
- `.planning/research/spike-0/comparison-table.md` (re-verified vendor pricing as of execution date)
- `.planning/decisions/0001-data-provider.md` (the MADR ADR — Phase 0 closing artifact)
- `scripts/spike/verify_adr.py` (assertion script for ADR shape)
- `scripts/spike/verify_artifacts.py` (assertion script for spike-0 directory completeness)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/phases/00-provider-validation-spike/RESEARCH.md
@.planning/phases/00-provider-validation-spike/00-01-SUMMARY.md
@.planning/phases/00-provider-validation-spike/00-02-SUMMARY.md
@.planning/research/spike-0/twelvedata-probe.json
@.planning/research/spike-0/spy-bar-budget.md
@.planning/research/spike-0/tv-mcp-tools.json
@.planning/research/spike-0/tv-mcp-transcript.log
@.planning/research/spike-0/tv-restart-test.log

<interfaces>
<!-- The ADR template is the MADR skeleton from RESEARCH.md §"ADR Template" lines 336–452. -->
<!-- The executor MUST use that exact skeleton as the starting point and adapt per the spike findings. -->

MADR frontmatter required keys (RESEARCH.md §"ADR Template"):
```yaml
---
status: accepted          # or `proposed` if a blocker emerged; the planner expects `accepted`
deciders: [Nathaniel Greene]   # per pre-planning user answer (matches local git identity)
date: <YYYY-MM-DD>        # the actual execution date, not 2026-05-14 hard-coded
adr_id: 0001
slug: data-provider
supersedes: []
superseded_by: []
tags: [data, foundation, phase-0]
---
```

Mandatory H2 sections (required by both MADR convention and the verify_adr.py check):
1. `## Status`
2. `## Context`
3. `## Decision Drivers`
4. `## Considered Options`
5. `## Decision`
6. `## Consequences`           ← MUST contain a `### When to revisit` H3 subsection (RESEARCH.md Pitfall #4)
7. `## Verification artifacts` ← MUST list all 5 cited file paths
8. `## Pros and Cons of the Options`

Considered Options must include AT LEAST these 5 (from RESEARCH.md §"Considered Options" lines 378–385):
1. TradingView MCP primary + Twelve Data secondary  (this is the chosen option in the default scenario)
2. Twelve Data primary on SPY (proxy)
3. Databento primary
4. Polygon / Massive Futures primary
5. Interactive Brokers historical via `ib_async`

Decision branching — the executor MUST adapt the ADR based on what Plans 1 and 2 found:

**Scenario A (expected, per research hypothesis):**
- Plan 1's probe shows Twelve Data does NOT serve ES (commodities_ES.body.data is empty OR no CME match).
- Plan 2's smoke test succeeded with `RESULT: conclusive` on the restart cycle.
- Decision: Option 1 (TV MCP primary + Twelve Data SPY secondary).
- Swap candidate: Databento (GLBX.MDP3 dataset).

**Scenario B (Twelve Data has ES — surprise positive):**
- Plan 1's probe shows non-empty data on /commodities?symbol=ES with CME exchange.
- Plan 2's smoke test succeeded.
- Decision: REVISIT — the ADR should still pick TV MCP primary for v1 (real-time CME, integrated chart, free) but document that Twelve Data ES is also available for headless backfills. Update the "When to revisit" section to remove "Twelve Data does not cover ES" as a trigger.

**Scenario C (TV MCP smoke failed — exit code 2/3/4/6):**
- Plan 2 did not produce a conclusive transcript.
- Decision status: `proposed` (not `accepted`). The ADR documents the blocker, and Phase 0 closes with the decision pending the blocker's resolution. The executor must escalate this to the operator via a checkpoint before writing the ADR — Phase 1 cannot proceed without a green Plan 2.

**Scenario D (restart test inconclusive — exit code 5):**
- Smoke happy path succeeded but restart cycle did not observe a real failure.
- Decision: Option 1 still defensible, but the Consequences section must say "restart resilience UNVERIFIED — Phase 6 TVBridge supervisor MUST validate this empirically before relying on auto-restart semantics."

Pricing data sources to re-verify in this plan (RESEARCH.md §"Futures-Aware Vendor Comparison" — pricing changes frequently):
- Twelve Data: https://twelvedata.com/pricing
- Databento: https://databento.com/pricing
- Massive (formerly Polygon.io) futures: https://massive.com/futures and /pricing
- Interactive Brokers historical: https://interactivebrokers.github.io/tws-api/historical_limitations.html (pacing rules; no monthly pricing — uses brokerage account)

Vendor comparison table columns (RESEARCH.md §"Comparison Table" lines 466–474):
| Vendor | ES coverage | MES coverage | Live | Historical | Monthly cost (as-of) | Python SDK | Verification source |

The 'do not swap to' list (RESEARCH.md §"The 'do not swap to' list"):
- CME DataMine (institutional pricing)
- yfinance for futures (unreliable)
- Alpaca (no futures)

verify_adr.py spec (RESEARCH.md §"Validation Architecture" line 663):
- Stdlib-only (no `yaml` package — Phase 1 has not yet shipped pyproject.toml). Parse the YAML frontmatter by extracting the block between the two `---` lines and using a simple key:value parser sufficient for the MADR frontmatter shape. The frontmatter values are simple strings, lists of strings, and dates — no nested mappings needed. (Alternatively, the executor can document that PyYAML is required and `pip install pyyaml` in `.venv-spike` from Plan 2 — that venv already exists. The PyYAML approach is robust; the stdlib approach is fragile. Prefer PyYAML since the venv exists.)
- Checks: file exists, frontmatter parses, required keys present, all 8 H2 sections present (by simple line-startswith scan), `Verification artifacts` section contains at least 5 path references to `.planning/research/spike-0/...` files, body length is between 1KB and 20KB (Nygard's "≤ 4 pages" upper bound is roughly 12KB; 20KB is a safety ceiling).

verify_artifacts.py spec (RESEARCH.md §"Validation Architecture" line 663):
- Stdlib-only (`pathlib`, `sys`).
- For each of the 7 files in the manifest (6 in spike-0 + the ADR), check exists() and stat().st_size >= the minimum (markdown: 100 bytes; JSON: 200 bytes; logs: 500 bytes).
- Print PASS/FAIL per file. Exit 0 if all pass, exit 1 listing failures otherwise.
</interfaces>

<environment>
- Platform: Windows 11 / PowerShell. Repo path contains a space. Use `pathlib.Path` and explicit `encoding="utf-8"` for all file I/O.
- Python 3.11+ available via `.venv-spike\Scripts\python.exe` (provisioned by Plan 2 Task 1). PyYAML can be installed there as a Plan 3 dep without polluting any future Phase 1 venv.
- All artifacts from Plans 1 and 2 are committed and on disk. Plans 1 and 2 are in wave 1; this plan is in wave 2.
- The operator (`deciders:`) is "Nathaniel Greene" per pre-planning user answer.
- The current date should come from `datetime.now()` at execution time, not be hardcoded to 2026-05-14 (the RESEARCH.md date).
</environment>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify vendor pricing and write comparison-table.md</name>
  <files>.planning/research/spike-0/comparison-table.md</files>
  <action>
    RESEARCH.md cached vendor pricing as of 2026-05-14 (Twelve Data tiers, Databento tiers, Massive's opaque pricing, IB pacing limits). Re-verify these against the live pricing pages because pricing changes frequently.

    Steps:

    1. Visit (via WebFetch tool) and capture key pricing for each:
       - `https://twelvedata.com/pricing` — extract Free / Grow / Pro / Ultra tier prices, credits/min, daily caps, and the 1min-interval-tier gate
       - `https://databento.com/pricing` — extract Standard / Plus / Unlimited monthly prices and the usage-based historical pricing line
       - `https://massive.com/futures` and `https://massive.com/pricing` — note whether futures-tier pricing is now publicly available (RESEARCH.md Assumption A5 says it is opaque as of 2026-05-14 — verify if that's still true)
       - For IB, no live page needed — RESEARCH.md's pacing rules (60 historical reqs / 10 min) are stable; cite the URL.

    2. If any pricing has CHANGED from RESEARCH.md, the comparison table MUST reflect today's values, not the cached values. Add a one-line "RE-VERIFIED <date>" footnote per row indicating the verification status (RE-VERIFIED <date>, or UNCHANGED, or OPAQUE).

    3. Write `.planning/research/spike-0/comparison-table.md` with this structure:

       ```
       # Futures-Aware Data Vendor Comparison

       **Verified:** <YYYY-MM-DD from datetime.now()>
       **Source:** Phase 0 spike, re-verified against live vendor pricing pages.
       **Companion to:** .planning/decisions/0001-data-provider.md

       ## Comparison Table

       | Vendor | ES coverage | MES coverage | Live | Historical | Monthly cost (verified <date>) | Python SDK | Verification source |
       | --- | --- | --- | --- | --- | --- | --- | --- |
       | Twelve Data | NO (catalog excludes CME futures) | NO | Yes (WS for stocks/forex/crypto, NOT futures) | Yes (5000 bars/call, full SPY 1m history) | Free (8 cred/min, 800/day) → Grow $79 → Pro $229 → Ultra $999 [RE-VERIFIED <date>] | `twelvedata` (official) | twelvedata.com/pricing |
       | Databento | YES (CME Globex MDP 3.0) | YES (same dataset) | Yes (flat-rate sub) | Yes (15+ years L0 schemas, usage-based) | Standard $199/mo, Plus $1,399/mo, Unlimited $3,500/mo [RE-VERIFIED <date>] | `databento` (official) | databento.com/pricing |
       | Massive (Polygon Futures) | YES (CME Globex incl. CBOT/CME/NYMEX/COMEX) | Likely YES | Yes (WS + REST) | Yes (aggregates, trades, quotes) | OPAQUE — requires sales conversation (or PUBLIC <details> if RE-VERIFIED <date> shows it changed) | `massive-com/client-python` | massive.com/futures |
       | IB historical (`ib_async`) | YES (real ES) | YES | Yes (during TWS session) | Yes (pacing-limited: ~60 historical reqs / 10 min) | $0 if IB account exists | `ib_async` community fork | interactivebrokers.github.io/tws-api/historical_limitations.html |
       | CME DataMine | YES (canonical) | YES | No (historical only) | Yes (full tick / book) | Institutional pricing only — call for quote | None (BBR / S3 model) | Industry standard |

       ## Why Databento is named as the swap candidate

       <Three bullet points from RESEARCH.md §"Why Databento is named as the swap candidate" — verbatim cost-shape, single-dataset, SDK rationale. Update any dollar figures if Databento pricing changed in Step 2.>

       ## Do-not-swap-to list

       - CME DataMine — institutional only; pricing requires a sales contract.
       - `yfinance` for futures — does not reliably serve continuous CME contracts.
       - Alpaca — confirmed has no futures.

       ## Findings from the Phase 0 spike

       - **Twelve Data probe result (from twelvedata-probe.json):** <one-line summary based on probes.commodities_ES.body — either "ES is NOT in Twelve Data's commodities catalog (data array empty)" or "ES surprisingly present on /commodities — see twelvedata-probe.json probes.commodities_ES.body"; the executor MUST inspect the JSON and emit the matching line>
       - **TV MCP smoke (from tv-mcp-tools.json + tv-mcp-transcript.log):** <"All 4 required tools present; happy-path completed with <N> bars retrieved; quote staleness <X>s confirming real-time CME subscription" — derived from the actual artifacts>
       - **TV MCP restart cycle (from tv-restart-test.log):** <"RESULT: conclusive — CDP disconnect/reconnect pattern observed; MCP stdio pipe stayed alive" — or the actual verdict from the log's final line>
       ```

    4. Save with `encoding="utf-8"` and a single trailing newline. UTF-8 because the file may contain non-ASCII characters from vendor pages and the repo is Windows.

    5. If a vendor pricing page is JS-only (RESEARCH.md A5 says Massive returns title only when scraped), the comparison table emits "OPAQUE — pricing page returned no machine-readable content on <date>" rather than fabricating numbers.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p = '.planning/research/spike-0/comparison-table.md'; if (-not (Test-Path $p)) { exit 10 }; $s = (Get-Item $p).Length; if ($s -lt 1500) { Write-Host \"size $s lt 1500\"; exit 11 }; $body = Get-Content $p -Raw -Encoding UTF8; foreach ($v in 'Twelve Data','Databento','Massive','IB historical','CME DataMine') { if ($body -notmatch [regex]::Escape($v)) { Write-Host \"missing vendor: $v\"; exit 12 } }; foreach ($a in 'Comparison Table','Why Databento','Do-not-swap-to','Findings from the Phase 0 spike') { if ($body -notmatch [regex]::Escape($a)) { Write-Host \"missing section: $a\"; exit 13 } }; Write-Host 'comparison-table-OK'; exit 0"</automated>
  </verify>
  <done>
    `.planning/research/spike-0/comparison-table.md` exists with all 5 vendor rows, the "Why Databento" rationale, the do-not-swap-to list, and the Phase 0 findings section sourced from Plans 1 and 2 artifacts. Pricing is re-verified as-of execution date.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the MADR ADR (0001-data-provider.md)</name>
  <files>.planning/decisions/0001-data-provider.md</files>
  <action>
    Use the MADR skeleton from RESEARCH.md §"ADR Template" lines 336–452 as the starting point. Adapt to the scenario determined by Plans 1 and 2's findings.

    Steps:

    1. **Determine the scenario** by reading the artifacts:
       - Open `.planning/research/spike-0/twelvedata-probe.json`. Check `probes.commodities_ES.body`. If `.data` is empty or absent → Scenario A (the expected negative). If non-empty with CME match → Scenario B. Record the verdict.
       - Open `.planning/research/spike-0/tv-mcp-tools.json`. Check `all_required_present`. If false → Scenario C (escalate to operator before writing the ADR).
       - Open `.planning/research/spike-0/tv-restart-test.log`. Check the last line for `RESULT: conclusive` or `RESULT: inconclusive`. If inconclusive → Scenario D.
       - Branching summary: A or D allows `status: accepted`. B allows `status: accepted` with revised consequences. C requires escalation (write Task 2.5 below).

    2. **For Scenario C only:** STOP. Write a single line to stdout: "BLOCKER: Plan 2 did not produce required tools / conclusive transcript. Cannot author ADR with status=accepted. Escalate to operator." Exit task with a non-zero code so the orchestrator surfaces it. Do NOT proceed to step 3.

    3. **Write `.planning/decisions/0001-data-provider.md` with this structure** (UTF-8, single trailing newline):

       a. **Frontmatter** (top of file, fenced by `---`):
          ```yaml
          ---
          status: accepted
          deciders: [Nathaniel Greene]
          date: <YYYY-MM-DD — datetime.now().strftime('%Y-%m-%d')>
          adr_id: 0001
          slug: data-provider
          supersedes: []
          superseded_by: []
          tags: [data, foundation, phase-0]
          ---
          ```
          (For Scenario D: keep `status: accepted` but add a note in Consequences.)

       b. **Title line:** `# 0001 — Data Provider for v1 (TradingView MCP as primary, Twelve Data as secondary)`
          (For Scenario B: change to `# 0001 — Data Provider for v1 (TradingView MCP primary, Twelve Data with ES coverage as secondary)`.)

       c. **`## Status`** — one paragraph stating "Accepted" (or scenario-specific status) and the date.

       d. **`## Context`** — adapt from RESEARCH.md §"Context" lines 354–367. Update the second paragraph to reflect the actual Twelve Data probe finding (cite `twelvedata-probe.json` by relative path).

       e. **`## Decision Drivers`** — keep RESEARCH.md's 5 bullets verbatim (ES coverage today / cost / reproducibility / failure isolation / headless+CI compat).

       f. **`## Considered Options`** — 5 options as in RESEARCH.md lines 378–385. Verbatim is fine.

       g. **`## Decision`** — adapt from RESEARCH.md §"Decision" lines 386–389. Update the rationale paragraph based on:
          - Actual spike findings (cite specific files)
          - Today's date
          - The current Twelve Data ES verdict from Step 1
          For Scenario B, the rationale changes: TV MCP still primary (real-time CME + integrated chart + zero cost), but Twelve Data ES becomes an available secondary in addition to SPY.

       h. **`## Consequences`** — adapt from RESEARCH.md §"Consequences" lines 391–404 (Positive / Negative / Neutral subsections).

          Add a **`### When to revisit`** H3 subsection (RESEARCH.md Pitfall #4) listing concrete triggers:
          - ORB strategy survives walk-forward with positive OOS Sharpe across at least 3 folds
          - Operator commits to ≥ 4 weeks of daily forward paper-trading
          - Databento Standard tier ($199/mo as-of <date>) is approved as a budget line
          - TV MCP CDP disconnect rate exceeds 1/hour during normal operation (would force a swap regardless of strategy edge)
          - Twelve Data adds ES to its catalog with documented OHLCV coverage (would re-open Option 2)

          For Scenario D, add a Negative bullet: "Restart-cycle resilience was UNVERIFIED in Phase 0 (restart test ran but did not observe a real failure cycle — see tv-restart-test.log final line). Phase 6 TVBridge supervisor MUST run an empirical resilience test as a precondition before relying on auto-restart."

          Add this Negative bullet for ALL scenarios (A/B/C/D): "MCP Python SDK 1.0's `stdio_client` does not expose the subprocess's stderr to the client — diagnostic output from the `tradingview-mcp-jackson` server is not directly capturable via the canonical SDK pattern. Phase 0's spike works around this by writing a placeholder note to `tv-mcp-stderr.log` (see Plan 02 Task 2). Phase 6 TVBridge MUST use `subprocess.Popen` directly (or a custom MCP transport) to capture server stderr for real operational observability."

       i. **`## Verification artifacts`** — list ALL FIVE cited paths with one-line descriptions:
          - `.planning/research/spike-0/twelvedata-probe.json` — Raw probe results for Twelve Data catalog endpoints.
          - `.planning/research/spike-0/spy-bar-budget.md` — 2-year SPY 1m backfill rate-limit math + tier recommendation.
          - `.planning/research/spike-0/tv-mcp-tools.json` — TV MCP tool inventory proving the 4 required tools exist.
          - `.planning/research/spike-0/tv-mcp-transcript.log` — Happy-path smoke transcript (set symbol / set timeframe / get OHLCV / quote).
          - `.planning/research/spike-0/tv-restart-test.log` — Restart-cycle log with explicit BEFORE/AFTER markers.
          - `.planning/research/spike-0/comparison-table.md` — Re-verified vendor comparison table.
          (That's 6 paths — RESEARCH.md said "all 5"; the comparison table is the sixth. Include all 6.)

       j. **`## Pros and Cons of the Options`** — adapt from RESEARCH.md lines 416–446. Verbatim is fine but update any pricing references that changed in Task 1.

       k. **`## Notes for the planner`** — RESEARCH.md lines 450–452 verbatim (about `runs.adr_hash` and FND-08 + OPT-04 downstream).

    4. **Length check.** The rendered file should be 1.5 KB ≤ size ≤ 20 KB. If it exceeds 20 KB, trim the Pros/Cons section (it's the most padded). If it's under 1.5 KB, the ADR is missing required content — re-check sections.

    5. **Forensic self-check:** before writing, verify all 6 cited paths exist. If any are missing, abort with a clear error — never cite a non-existent file.

    Reference RESEARCH.md lines 322–460 for the full skeleton.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p = '.planning/decisions/0001-data-provider.md'; if (-not (Test-Path $p)) { exit 10 }; $s = (Get-Item $p).Length; if ($s -lt 1500 -or $s -gt 20000) { Write-Host \"size $s out of 1500..20000\"; exit 11 }; $body = Get-Content $p -Raw -Encoding UTF8; foreach ($h in '## Status','## Context','## Decision Drivers','## Considered Options','## Decision','## Consequences','## Verification artifacts','## Pros and Cons of the Options') { if ($body -notmatch [regex]::Escape($h)) { Write-Host \"missing section: $h\"; exit 12 } }; if ($body -notmatch '### When to revisit') { Write-Host 'missing: When to revisit'; exit 13 }; foreach ($f in 'twelvedata-probe.json','spy-bar-budget.md','tv-mcp-tools.json','tv-mcp-transcript.log','tv-restart-test.log','comparison-table.md') { if ($body -notmatch [regex]::Escape($f)) { Write-Host \"not cited: $f\"; exit 14 } }; foreach ($k in 'adr_id: 0001','slug: data-provider','Nathaniel Greene') { if ($body -notmatch [regex]::Escape($k)) { Write-Host \"missing key: $k\"; exit 15 } }; Write-Host 'adr-md-OK'; exit 0"</automated>
  </verify>
  <done>
    `.planning/decisions/0001-data-provider.md` exists, 1.5 KB ≤ size ≤ 20 KB, contains all 8 required H2 sections + the `### When to revisit` subsection, cites all 6 verification artifacts by relative path, has frontmatter with `adr_id: 0001` and `deciders: [Nathaniel Greene]`, and the decision is consistent with the scenario derived from Plans 1 and 2.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write verify_adr.py and verify_artifacts.py assertion scripts</name>
  <files>
    scripts/spike/verify_adr.py
    scripts/spike/verify_artifacts.py
  </files>
  <action>
    Per RESEARCH.md §"Validation Architecture" lines 660–674, the planner must commit two assertion scripts that re-verify the Phase 0 deliverables on demand. Both are stdlib-first; PyYAML can be installed into `.venv-spike` from Plan 2 to make `verify_adr.py` robust.

    **scripts/spike/verify_adr.py:**

    1. Header comment:
       ```
       """Verify the Phase 0 ADR shape.
       PHASE 0 SPIKE — DO NOT IMPORT FROM PRODUCTION CODE.
       Re-runnable: `.venv-spike/Scripts/python.exe scripts/spike/verify_adr.py`.
       Exits 0 if the ADR passes all checks, non-zero with a clear diagnostic otherwise.
       """
       ```

    2. Imports: `pathlib.Path`, `sys`, `re`. Try to import `yaml`; if missing, fall back to a tiny `_parse_simple_yaml(text: str) -> dict` parser that handles flat `key: value` lines, lists in `[a, b]` form, and ignores comments. Sufficient for MADR frontmatter.

    3. Constants:
       - `ADR_PATH = Path(".planning/decisions/0001-data-provider.md")`
       - `REQUIRED_FM_KEYS = {"status", "deciders", "date", "adr_id", "slug", "supersedes", "superseded_by", "tags"}`
       - `REQUIRED_SECTIONS = ["## Status", "## Context", "## Decision Drivers", "## Considered Options", "## Decision", "## Consequences", "## Verification artifacts", "## Pros and Cons of the Options"]`
       - `REQUIRED_SUBSECTION = "### When to revisit"`
       - `CITED_ARTIFACTS = [".planning/research/spike-0/twelvedata-probe.json", ".planning/research/spike-0/spy-bar-budget.md", ".planning/research/spike-0/tv-mcp-tools.json", ".planning/research/spike-0/tv-mcp-transcript.log", ".planning/research/spike-0/tv-restart-test.log", ".planning/research/spike-0/comparison-table.md"]`
       - `SIZE_MIN_BYTES = 1500`, `SIZE_MAX_BYTES = 20000`

    4. Checks (function `main() -> int`):
       a. Assert `ADR_PATH.exists()` else return 10 with "ADR not found at <path>".
       b. Read file, check `SIZE_MIN_BYTES <= size <= SIZE_MAX_BYTES`; else return 11.
       c. Extract frontmatter block: split on `---` lines, take index 1 if `text.startswith('---')`. Else return 12.
       d. Parse frontmatter (yaml.safe_load or fallback). Assert all `REQUIRED_FM_KEYS` are present in the parsed dict. Else return 13 with which key(s) missing.
       e. Assert `fm["adr_id"]` equals `"0001"` or `1` (string or int — accept both). Else return 14.
       f. For each section in `REQUIRED_SECTIONS`, assert `re.search(rf"^{re.escape(s)}\b", body, re.MULTILINE)` matches. Else return 15 listing missing sections.
       g. Assert `REQUIRED_SUBSECTION` is present (substring check is fine — it's a unique phrase). Else return 16.
       h. For each path in `CITED_ARTIFACTS`, assert the path string is present in the body. Else return 17 listing uncited.
       i. Print `PASS: ADR shape OK (size=<bytes>, sections=<count>, citations=<count>)`. Return 0.

    5. Entry point: `if __name__ == "__main__": sys.exit(main())`.

    **scripts/spike/verify_artifacts.py:**

    1. Header comment (same pattern as verify_adr.py).

    2. Imports: `pathlib.Path`, `sys`.

    3. Manifest constant:
       ```
       MANIFEST = [
           (".planning/research/spike-0/twelvedata-probe.json", 200),
           (".planning/research/spike-0/spy-bar-budget.md",     500),
           (".planning/research/spike-0/tv-mcp-tools.json",     200),
           (".planning/research/spike-0/tv-mcp-transcript.log", 500),
           (".planning/research/spike-0/tv-mcp-stderr.log",      50),
           (".planning/research/spike-0/tv-restart-test.log",   500),
           (".planning/research/spike-0/comparison-table.md",  1000),
           (".planning/decisions/0001-data-provider.md",       1500),
       ]
       ```

    4. Loop over MANIFEST. For each (path, min_size):
       - Print `FAIL: missing <path>` and add to failures list if `not Path(path).exists()`.
       - Else print `FAIL: <path> is <bytes> bytes (need >= <min_size>)` and add to failures if `stat().st_size < min_size`.
       - Else print `PASS: <path> (<bytes> bytes)`.

    5. After the loop:
       - If failures: print a summary line `=== <N> failure(s) ===` and return 1.
       - Else print `=== ALL <total> ARTIFACTS PRESENT AND NON-EMPTY ===` and return 0.

    6. Entry point: `if __name__ == "__main__": sys.exit(main())`.

    Both scripts MUST run without modification under `.venv-spike\Scripts\python.exe` (PyYAML is optional for verify_adr.py — if not installed, the fallback parser handles MADR frontmatter).

    After writing, run both scripts as a self-test and confirm they exit 0 against the current state of the repo (the ADR from Task 2 + all 7 cited artifacts).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "& '.venv-spike/Scripts/python.exe' scripts/spike/verify_artifacts.py; if ($LASTEXITCODE -ne 0) { Write-Host 'verify_artifacts FAILED'; exit 20 }; & '.venv-spike/Scripts/python.exe' scripts/spike/verify_adr.py; if ($LASTEXITCODE -ne 0) { Write-Host 'verify_adr FAILED'; exit 21 }; Write-Host 'both-verify-scripts-OK'; exit 0"</automated>
  </verify>
  <done>
    `scripts/spike/verify_adr.py` and `scripts/spike/verify_artifacts.py` exist, are stdlib-runnable, and both exit 0 against the current ADR + spike-0 directory.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator review of the ADR before final commit</name>
  <files>.planning/decisions/0001-data-provider.md</files>
  <action>
    The ADR is the Phase 0 closing artifact. RESEARCH.md §"Sampling Rate" line 668 explicitly says: "Phase gate: Manual review of the ADR by the operator. Until the operator reads the ADR and accepts it, Phase 0 is not done."

    The executor presents the ADR's path and key sections to the operator for review BEFORE the final commit. The executor MUST NOT proceed past this checkpoint without explicit operator approval.

    Pre-checkpoint automation (already done in Task 3): both verify scripts pass.

    Present to operator:
    1. Path: `.planning/decisions/0001-data-provider.md`
    2. The Decision section's first paragraph (the actual decision being locked in).
    3. The Consequences section's `### When to revisit` triggers.
    4. The list of 6 cited verification artifacts (all of which exist on disk).
    5. A reminder that this ADR will be hashed and referenced by every Phase 4+ optimization run as `runs.adr_hash` (FND-08 + OPT-04). Editing it post-commit means writing a superseding ADR, not amending this one.
  </action>
  <what-built>
    The MADR ADR has been authored, the supporting comparison table has been re-verified against live vendor pricing, and two assertion scripts are in place so the Phase 0 deliverables are re-verifiable on demand. All checks pass automatically. The decision is now ready for the operator to read end-to-end and accept.
  </what-built>
  <how-to-verify>
    1. Open `.planning/decisions/0001-data-provider.md` in an editor or markdown viewer.
    2. Read the Decision section. Confirm it names: (a) v1 primary feed (expected: TradingView MCP), (b) v1 secondary feed (expected: Twelve Data on SPY), (c) the eventual swap candidate (expected: Databento).
    3. Read the Consequences > When to revisit section. Confirm the trigger conditions match your intent.
    4. Spot-check one cited artifact — e.g., open `.planning/research/spike-0/twelvedata-probe.json` and confirm it contains the expected probe results (Twelve Data ES verdict).
    5. If anything looks wrong, type "edit" with a description of what to change — the executor will revise the ADR (this becomes a follow-up auto task). If everything is correct, type "approved" to proceed to the final commit.
  </how-to-verify>
  <verify>
    <automated>powershell -NoProfile -Command "& '.venv-spike/Scripts/python.exe' scripts/spike/verify_adr.py; if ($LASTEXITCODE -ne 0) { exit 10 }; & '.venv-spike/Scripts/python.exe' scripts/spike/verify_artifacts.py; if ($LASTEXITCODE -ne 0) { exit 11 }; exit 0"</automated>
  </verify>
  <done>
    Operator has read the ADR's Decision section and the When-to-revisit triggers, has spot-checked one cited artifact, and has typed "approved". Both verify scripts still exit 0.
  </done>
  <resume-signal>
    Type "approved" to commit. Type "edit: <description>" to revise the ADR before commit.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit Plan 3 artifacts (Phase 0 closing commit)</name>
  <files>
    .planning/research/spike-0/comparison-table.md
    .planning/decisions/0001-data-provider.md
    scripts/spike/verify_adr.py
    scripts/spike/verify_artifacts.py
  </files>
  <action>
    This is the Phase 0 closing commit. RESEARCH.md "Commit policy" line stated: "The ADR commit is the Phase 0 closing commit."

    Steps:

    1. Final pre-commit sanity: re-run both verify scripts. Both must exit 0.

    2. Stage exactly these 4 files (do NOT use `git add -A` — `.venv-spike/` and `.env` may exist locally):
       ```
       git add .planning/research/spike-0/comparison-table.md .planning/decisions/0001-data-provider.md scripts/spike/verify_adr.py scripts/spike/verify_artifacts.py
       ```

    3. Confirm staging is correct:
       ```
       git diff --cached --name-only
       ```
       The output must be exactly those 4 paths.

    4. Commit via SDK:
       ```
       gsd-sdk query commit "docs(00): plan 3 — vendor comparison + provider ADR (closes phase 0)" --files .planning/research/spike-0/comparison-table.md .planning/decisions/0001-data-provider.md scripts/spike/verify_adr.py scripts/spike/verify_artifacts.py
       ```

    5. Post-commit verification:
       - `git show --stat HEAD` — confirm exactly the 4 files.
       - `git log --oneline -5` — confirm 3 phase-0 commits in order: plan 1 (twelve data probe), plan 2 (tv mcp smoke), plan 3 (vendor comparison + ADR).
       - Re-run `verify_artifacts.py` one final time against the committed state — exit 0.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$files = git show --pretty='' --name-only HEAD; foreach ($f in '.planning/research/spike-0/comparison-table.md','.planning/decisions/0001-data-provider.md','scripts/spike/verify_adr.py','scripts/spike/verify_artifacts.py') { if ($files -notmatch [regex]::Escape($f)) { Write-Host \"missing: $f\"; exit 10 } }; & '.venv-spike/Scripts/python.exe' scripts/spike/verify_artifacts.py; if ($LASTEXITCODE -ne 0) { Write-Host 'final verify_artifacts failed'; exit 11 }; $log = git log --oneline -5; if ($log -notmatch 'plan 1' -or $log -notmatch 'plan 2' -or $log -notmatch 'plan 3') { Write-Host 'phase-0 commit sequence incomplete'; exit 12 }; Write-Host 'phase-0-closed'; exit 0"</automated>
  </verify>
  <done>
    Plan 3's 4 files are in a single commit titled `docs(00): plan 3 — vendor comparison + provider ADR (closes phase 0)`. The git log shows 3 sequential Phase 0 commits (plan 1, plan 2, plan 3). verify_artifacts.py exits 0 against the committed state. Phase 0 is closed; Phase 1 can begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebFetch → vendor pricing pages | Untrusted HTML; treated as informational only, not executable. |
| Plans 1 + 2 artifacts → ADR | Verified by Plans 1 and 2 verify gates; cited by path. |
| ADR → future Phase 4+ runs | Hashed and referenced as `runs.adr_hash`; the ADR is effectively content-addressed downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12 | Information Disclosure | Vendor pricing fetched via WebFetch could include tracking pixels / non-public info | accept | Public pricing pages only; no auth, no cookies; the comparison table records only published prices. |
| T-00-13 | Tampering | ADR cites a non-existent artifact path | mitigate | Task 2 step 5 verifies all 6 cited paths exist before writing the ADR; Task 3's verify_adr.py re-checks at commit time and on demand. |
| T-00-14 | Repudiation | ADR cannot be re-validated by future contributors | mitigate | Task 3 ships verify_adr.py + verify_artifacts.py; both are stdlib-runnable and committed. Future contributors run them to confirm the ADR shape and the cited files' existence. |
| T-00-15 | Spoofing | Vendor pricing page returns stale or cached content masquerading as current | accept | Each table row gets a `RE-VERIFIED <date>` footnote; future contributors comparing today's table against the ADR date can detect drift. |
| T-00-16 | Elevation of Privilege | A surprise-positive (Scenario B) is silently treated as Scenario A | mitigate | Task 2 step 1 explicitly branches the ADR based on `probes.commodities_ES.body` content; verify_adr.py does not enforce the scenario but the operator's Task 4 review does. |
</threat_model>

<verification>
- [ ] `.planning/research/spike-0/comparison-table.md` exists with all 5 vendor rows, pricing re-verified as of execution date.
- [ ] `.planning/decisions/0001-data-provider.md` exists with MADR frontmatter (`adr_id: 0001`, `deciders: [Nathaniel Greene]`, today's date), all 8 H2 sections, the `### When to revisit` H3 subsection, and 6 cited verification artifacts.
- [ ] `scripts/spike/verify_adr.py` and `scripts/spike/verify_artifacts.py` are stdlib-runnable and both exit 0.
- [ ] The operator has reviewed the ADR and typed "approved" at the Task 4 checkpoint.
- [ ] All 4 Plan-3 files are in a single atomic commit titled `docs(00): plan 3 — vendor comparison + provider ADR (closes phase 0)`.
- [ ] `git log --oneline -5` shows 3 Phase 0 commits in sequence: plan 1, plan 2, plan 3.
- [ ] FND-10 acceptance criteria from ROADMAP.md Phase 0 success criteria are all met:
  - (1) ADR committed at the documented path with all 3 mandatory findings (Twelve Data coverage, TV MCP rationale, swap candidate)
  - (2) Plan 2 TV MCP smoke + restart artifacts exist under `.planning/research/spike-0/`
  - (3) Plan 1 Twelve Data probe + budget worksheet exist under `.planning/research/spike-0/`
</verification>

<success_criteria>
- Phase 0 is closed. The Phase 1 author writing `seed_bars.py` can read the ADR + spy-bar-budget.md and know exactly which provider to implement first, at which tier, with which pacing.
- The Phase 6 author writing `TVBridge` can read the ADR's Consequences section and know exactly which TV MCP failure mode to design the supervisor around.
- Every Phase 4+ optimization run can hash `0001-data-provider.md` and log the result in `runs.adr_hash` for forensic recall (FND-08 + OPT-04).
- A future contributor can re-run `verify_adr.py` + `verify_artifacts.py` in seconds to confirm the Phase 0 deliverables are still intact.
</success_criteria>

<output>
After completion, create `.planning/phases/00-provider-validation-spike/00-03-SUMMARY.md` summarizing:
- ADR scenario chosen (A / B / C-blocked / D-restart-inconclusive)
- Decision: v1 primary feed, v1 secondary feed, named swap candidate
- The 4 most material `### When to revisit` triggers
- Path: `.planning/decisions/0001-data-provider.md` (the ADR — cited by every Phase 4+ optimization run)
- Phase 0 status: CLOSED — Phase 1 unblocked
</output>
