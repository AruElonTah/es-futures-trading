---
phase: 04-optimization-grid-walk-forward
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - packages/api/src/api/routes/optimizations.py
  - packages/api/src/api/app.py
  - packages/api/tests/test_optimizations.py
  - apps/web/app/optimizations/page.tsx
  - apps/web/package.json
  - apps/web/app/dashboard/page.tsx
autonomous: false
requirements:
  - OPT-07
  - OPT-08
  - OPT-09

must_haves:
  truths:
    - "GET /optimizations returns a list of opt_runs rows sorted by created_at desc"
    - "GET /optimizations/{run_id}/results returns opt_results rows sorted by oos_sharpe DESC"
    - "GET /optimizations/{run_id}/heatmap validates axis_x/axis_y against ORBConfig field whitelist (no SQL injection)"
    - "GET /optimizations/{run_id}/heatmap returns {x: [...], y: [...], z: [[...]]} 2D grid"
    - "Next.js /optimizations page loads without SSR crash (react-plotly.js uses dynamic + ssr:false)"
    - "Leaderboard table renders edge_ratio > 2 cells with a red background class"
    - "Dashboard header has an 'Optimizations' link to /optimizations"
  artifacts:
    - path: "packages/api/src/api/routes/optimizations.py"
      provides: "GET /optimizations, GET /optimizations/{run_id}/results, GET /optimizations/{run_id}/heatmap"
      exports: ["router"]
    - path: "apps/web/app/optimizations/page.tsx"
      provides: "Next.js /optimizations page — leaderboard + Plotly heatmap"
      contains: "dynamic"
    - path: "packages/api/tests/test_optimizations.py"
      provides: "API route tests: empty list, results shape, heatmap 2D grid"
  key_links:
    - from: "packages/api/src/api/app.py"
      to: "optimizations.router"
      via: "app.include_router(optimizations_routes.router)"
      pattern: "include_router"
    - from: "apps/web/app/optimizations/page.tsx"
      to: "GET /optimizations/{run_id}/heatmap"
      via: "TanStack Query useQuery at 2s interval while status=running"
      pattern: "useQuery"
    - from: "heatmap endpoint"
      to: "opt_results table axis columns"
      via: "whitelist validation before DuckDB GROUP BY query"
      pattern: "ALLOWED_AXES"
---

<objective>
Full vertical slice closure: FastAPI optimization routes + Next.js /optimizations page with leaderboard and Plotly heatmap. After this plan, a user can navigate to /optimizations and inspect OOS-ranked results from a completed run.

Purpose: Closes the end-to-end user story for Phase 4. The CLI (Wave 2) produces rows in DuckDB; this plan surfaces them in the web UI with the ranked leaderboard and interactive 2-param heatmap.
Output: optimizations.py route file, updated app.py registration, Next.js /optimizations page, API tests, dashboard header link.
</objective>

<execution_context>
@C:\Users\Admin\Desktop\Day Trading\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\Desktop\Day Trading\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Desktop\Day Trading\.planning\ROADMAP.md
@C:\Users\Admin\Desktop\Day Trading\.planning\phases\04-optimization-grid-walk-forward\04-CONTEXT.md
@C:\Users\Admin\Desktop\Day Trading\.planning\phases\04-optimization-grid-walk-forward\04-RESEARCH.md
@C:\Users\Admin\Desktop\Day Trading\.planning\phases\04-optimization-grid-walk-forward\04-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types the executor needs — extracted from existing codebase -->

From packages/api/src/api/app.py:
- app: FastAPI instance
- app.state.store: DuckDBStore  (set in lifespan)
- Pattern: app.include_router(router_module.router) after CORS middleware lines
- Existing routers: bars_routes.router, backtests_routes.router
- CORS allow_methods=["GET"] — optimizations routes are GET-only (fits existing policy)

From packages/api/src/api/routes/backtests.py (pattern to follow):
- router = APIRouter() at module level
- @router.get("/backtests") reads from request.app.state.store._conn
- Dependency injection via request: Request parameter (not Depends(get_store) — check backtests.py actual pattern)

From packages/trading-core/src/trading_core/storage/duckdb_store.py (Wave 1 extensions):
- DuckDBStore.read_opt_results(run_id: str) -> list[dict]
- DuckDBStore._conn.execute(sql, params).fetchall() for raw queries

ORBConfig field whitelist for heatmap axis validation:
- Valid axis names: "opening_range_minutes", "atr_stop_mult", "r_target"
- These are the only columns in opt_results that correspond to grid axes
- Define ALLOWED_AXES = frozenset({"opening_range_minutes", "atr_stop_mult", "r_target"}) in optimizations.py

From apps/web/app/dashboard/page.tsx:
- Header area has title + ETClock + ConnectionStatus components
- Add "Optimizations" link (Next.js <Link href="/optimizations">) in the header area
- Do NOT modify the chart or equity panes (D-07 from Phase 3 context: cold-load state preserved)

react-plotly.js installation (from 04-RESEARCH.md):
- pnpm add react-plotly.js plotly.js (from apps/web directory)
- pnpm add -D @types/react-plotly.js
- Versions: react-plotly.js@2.6.0, plotly.js@3.5.1, @types/react-plotly.js@2.6.4
- MUST use dynamic(() => import('react-plotly.js'), { ssr: false }) — SSR crash otherwise

Next.js App Router conventions (from apps/web/AGENTS.md note):
- Read node_modules/next/dist/docs/ for any Next.js API used
- "use client" directive required for any component using hooks or dynamic import
- app/optimizations/page.tsx is a new route under the App Router
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI optimization routes + app registration + API tests</name>
  <files>
    packages/api/src/api/routes/optimizations.py,
    packages/api/src/api/app.py,
    packages/api/tests/test_optimizations.py
  </files>
  <read_first>
    packages/api/src/api/app.py,
    packages/api/src/api/routes/backtests.py,
    packages/trading-core/src/trading_core/storage/duckdb_store.py
  </read_first>
  <behavior>
    ROUTE FILE (optimizations.py):
    - router = APIRouter() at module level
    - ALLOWED_AXES = frozenset({"opening_range_minutes", "atr_stop_mult", "r_target"}) — security whitelist (T-04-03-01)
    - GET /optimizations: query opt_runs table, return list of dicts with run_id, strategy_id, status, total_combos, completed_combos, fold_count, created_at (ISO string). Mirror backtests.py pattern for DuckDB access via request.app.state.store._conn (or Depends — match actual backtests.py pattern).
    - GET /optimizations/{run_id}/results: query opt_results WHERE run_id=? ORDER BY oos_sharpe DESC NULLS LAST. Return list of dicts with all opt_results columns. Edge_ratio values may be None — serialize as null in JSON.
    - GET /optimizations/{run_id}/heatmap: query params axis_x: str = Query(...), axis_y: str = Query(...). Validate BOTH axis_x and axis_y are in ALLOWED_AXES; if not, raise HTTPException(status_code=422, detail=f"Invalid axis '{axis_x}'. Allowed: {sorted(ALLOWED_AXES)}"). SQL (use parameterized for run_id; column names from whitelist are safe to interpolate after validation):
        SELECT {axis_x}, {axis_y}, AVG(oos_sharpe) as oos_sharpe
        FROM opt_results
        WHERE run_id = ?
        GROUP BY {axis_x}, {axis_y}
        ORDER BY {axis_y} ASC, {axis_x} ASC
      Transform to {x: sorted unique axis_x values, y: sorted unique axis_y values, z: 2D list indexed [y_idx][x_idx]}.
      If no results found, return {"x": [], "y": [], "z": []}.
    - GET /optimizations/{run_id}: return single opt_runs row; 404 if not found. Include completed_combos and total_combos for progress polling.

    APP REGISTRATION (app.py):
    - Add: from api.routes import optimizations as optimizations_routes
    - Add: app.include_router(optimizations_routes.router) after existing include_router calls
    - No CORS changes needed (routes are GET-only, CORS already allows GET from localhost:3000)

    API TESTS (test_optimizations.py):
    - Use TestClient from starlette.testclient (same pattern as existing API tests)
    - test_get_optimizations_empty: GET /optimizations with empty DuckDB returns 200 + []
    - test_get_opt_results_unknown_run: GET /optimizations/nonexistent-id/results returns 200 + []
    - test_heatmap_invalid_axis: GET /optimizations/{id}/heatmap?axis_x=evil&axis_y=atr_stop_mult returns 422
    - test_heatmap_valid_shape: seed opt_results rows (3x3 grid of opening_range_minutes x atr_stop_mult), call heatmap endpoint, assert response has keys "x", "y", "z" and len(z) == len(y) and len(z[0]) == len(x)
    - test_get_optimizations_returns_list: insert one opt_runs row, assert GET /optimizations returns list with one item containing run_id
  </behavior>
  <verify>
    <automated>uv run pytest packages/api/tests/test_optimizations.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>
    optimizations.py exists with router containing 4 endpoints (GET /optimizations, /{id}, /{id}/results, /{id}/heatmap);
    app.py registers optimizations_routes.router;
    test_heatmap_invalid_axis passes with 422 response;
    test_heatmap_valid_shape passes — z is a 2D list;
    test_get_optimizations_empty passes with 200 + empty list;
    uv run pytest packages/api/tests/ -x -q exits 0 (no regressions)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 created FastAPI routes for /optimizations and registered them in app.py. Task 2 (below) will create the Next.js /optimizations page. Before writing the frontend, verify the API is reachable.
  </what-built>
  <how-to-verify>
    1. Start the API: uv run uvicorn api.app:app --host 127.0.0.1 --port 8000 --workers 1
    2. curl http://127.0.0.1:8000/optimizations
       Expected: 200 OK with body [] (empty list — no runs yet)
    3. curl "http://127.0.0.1:8000/optimizations/test-id/heatmap?axis_x=evil&axis_y=atr_stop_mult"
       Expected: 422 Unprocessable Entity with detail mentioning "Invalid axis"
    4. curl "http://127.0.0.1:8000/optimizations/test-id/heatmap?axis_x=opening_range_minutes&axis_y=atr_stop_mult"
       Expected: 200 OK with body {"x":[],"y":[],"z":[]}
    5. Ctrl+C to stop the server.
  </how-to-verify>
  <resume-signal>Type "approved" when all four checks pass, or describe which check failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Next.js /optimizations page + plotly install + dashboard header link</name>
  <files>
    apps/web/app/optimizations/page.tsx,
    apps/web/package.json,
    apps/web/app/dashboard/page.tsx
  </files>
  <read_first>
    apps/web/app/dashboard/page.tsx,
    apps/web/package.json,
    .planning/phases/04-optimization-grid-walk-forward/04-RESEARCH.md
  </read_first>
  <behavior>
    INSTALL DEPENDENCIES (run from apps/web/ or repo root):
    - pnpm add react-plotly.js plotly.js
    - pnpm add -D @types/react-plotly.js
    - Verify package.json now contains react-plotly.js and plotly.js entries.

    OPTIMIZATIONS PAGE (apps/web/app/optimizations/page.tsx):
    - "use client" directive at top (required for hooks + dynamic import)
    - dynamic import: const Plot = dynamic(() => import("react-plotly.js"), { ssr: false })
    - Import type { Layout, PlotData } from "plotly.js" for type annotations
    - TanStack Query useQuery for fetching run list: GET /optimizations (poll refetchInterval=2000 while any run has status="running")
    - State: selectedRunId (useState<string | null>(null)), axisX (useState("atr_stop_mult")), axisY (useState("opening_range_minutes"))
    - When selectedRunId is set: useQuery for GET /optimizations/{selectedRunId}/results and GET /optimizations/{selectedRunId}/heatmap?axis_x={axisX}&axis_y={axisY}

    LEADERBOARD TABLE:
    - Columns: Rank (1-based index), param_hash (first 8 chars), opening_range_minutes, atr_stop_mult, r_target, OOS Sharpe (oos_sharpe, 3 decimal places), IS Sharpe (is_sharpe), IS/OOS Edge Ratio (edge_ratio, 2 decimal places | "—" if null), OOS Return (oos_return as %), Fold Count (fold_idx max + 1 inferred from grouped data)
    - edge_ratio cell: if edge_ratio > 2.0, apply CSS class for red background (e.g., className="bg-red-900 text-red-200" using Tailwind if available, or inline style={{ backgroundColor: '#7f1d1d', color: '#fca5a5' }})
    - Clicking a row sets selectedRunId to that row's run_id
    - Table is sorted by oos_sharpe DESC (server already returns it sorted — preserve order)

    AXIS SELECTORS for heatmap:
    - Two <select> dropdowns: axis_x and axis_y, options = ["opening_range_minutes", "atr_stop_mult", "r_target"]
    - When selection changes, refetch heatmap query

    PLOTLY HEATMAP:
    - Only render <Plot> when heatmap data.z.length > 0
    - data prop: [{type: "heatmap", z: data.z, x: data.x, y: data.y, colorscale: "RdYlGn", zmin: -1}]
    - layout prop: { title: "OOS Sharpe — 2-Param Heatmap", xaxis: {title: axisX}, yaxis: {title: axisY}, paper_bgcolor: "#0a0a0a", plot_bgcolor: "#111", font: {color: "#e0e0e0"}, width: 600, height: 400 }
    - Wrap <Suspense fallback={<div>Loading chart...</div>}> around <Plot> component

    DASHBOARD HEADER LINK (apps/web/app/dashboard/page.tsx):
    - Add import Link from "next/link" if not already imported
    - In the header area (near the title / ETClock), add: <Link href="/optimizations" className="...">Optimizations</Link>
    - Do NOT modify chart panes, equity curve pane, or any existing data hooks
    - Minimal surgical change: add one Link element in the header JSX only
  </behavior>
  <verify>
    <automated>cd "C:/Users/Admin/Desktop/Day Trading/apps/web" && pnpm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    apps/web/package.json contains react-plotly.js, plotly.js, @types/react-plotly.js;
    apps/web/app/optimizations/page.tsx exists with "use client" + dynamic import of react-plotly.js with ssr:false;
    pnpm run build exits 0 (no TypeScript or build errors);
    /dashboard page.tsx has a Link to /optimizations in the header (visible in source);
    No modifications to Chart, EquityCurve, or data-fetching hooks in dashboard/page.tsx
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP query params → heatmap SQL | axis_x and axis_y from query string; must not reach SQL until validated |
| equity_curve_path in opt_results → file serve | Path stored in DuckDB; if served as file download, must be validated relative to OPT_ROOT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | SQL injection via axis_x/axis_y in heatmap endpoint | mitigate | Validate both params against ALLOWED_AXES = frozenset({"opening_range_minutes", "atr_stop_mult", "r_target"}) before any SQL construction; return 422 if not in whitelist; test_heatmap_invalid_axis asserts this gate |
| T-04-03-02 | Tampering | Path traversal via equity_curve_path in /results response | accept | Phase 4 returns equity_curve_path as a string (not file download); file serving deferred to Phase 7. Note in code comment for Phase 7 implementor. |
| T-04-03-03 | Denial of Service | Plotly.js 3.5.1 large bundle size slowing initial page load | accept | Single-operator localhost app; bundle size is not a production concern; dynamic() import defers loading until page renders |
</threat_model>

<verification>
Full end-to-end verification after all tasks:

1. API tests pass:
```
uv run pytest packages/api/tests/ -x -q
```
Expected: all tests pass including test_optimizations.py.

2. Frontend builds cleanly:
```
cd apps/web && pnpm run build
```
Expected: exits 0, no TS errors.

3. Full suite regression check:
```
uv run pytest packages/trading-core/tests/ packages/api/tests/ -q
```
Expected: all existing Phase 3 tests remain green.

4. Manual: navigate to http://localhost:3000/optimizations — page loads without SSR crash, shows leaderboard table (empty if no runs), axis selectors visible, heatmap placeholder renders.
</verification>

<success_criteria>
- packages/api/src/api/routes/optimizations.py: 4 endpoints with ALLOWED_AXES whitelist enforced on heatmap axis params
- packages/api/src/api/app.py: optimizations_routes.router registered
- test_heatmap_invalid_axis: 422 response for non-whitelisted axis params
- test_heatmap_valid_shape: {"x": [...], "y": [...], "z": [[...]]} response shape
- apps/web/app/optimizations/page.tsx: "use client" + dynamic react-plotly.js import with ssr:false + leaderboard table with edge_ratio red-flag styling + axis selectors + Plotly heatmap
- apps/web/app/dashboard/page.tsx: Link to /optimizations added in header
- pnpm run build exits 0 (no TypeScript errors)
- uv run pytest packages/api/tests/ -x -q exits 0 (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization-grid-walk-forward/04-03-SUMMARY.md`
</output>
