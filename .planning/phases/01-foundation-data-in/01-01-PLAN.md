---
phase: 01-foundation-data-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - package.json
  - .gitignore
  - packages/trading-core/pyproject.toml
  - packages/trading-core/src/trading_core/__init__.py
  - packages/trading-core/tests/__init__.py
  - packages/trading-core/tests/conftest.py
  - packages/trading-core/tests/test_import.py
  - packages/api/pyproject.toml
  - packages/api/src/api/__init__.py
  - packages/api/tests/__init__.py
  - packages/api/tests/conftest.py
  - packages/api/tests/test_import.py
  - packages/tv-bridge/pyproject.toml
  - packages/tv-bridge/src/tv_bridge/__init__.py
  - packages/tv-bridge/tests/__init__.py
  - packages/tv-bridge/tests/conftest.py
  - packages/tv-bridge/tests/test_import.py
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/tailwind.config.ts
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - data/duckdb/.gitkeep
  - data/parquet/.gitkeep
  - data/logs/audit/.gitkeep
  - config/system.yaml
autonomous: true
requirements:
  - FND-01
  - FND-02
must_haves:
  truths:
    - "`uv --version` and `pnpm --version` succeed on the operator's Windows machine"
    - "`uv sync` from a clean clone produces a working `.venv` with all pinned dev + runtime deps in `uv.lock`"
    - "`uv run python -c \"import trading_core, api, tv_bridge\"` succeeds (3 workspace members importable)"
    - "`pnpm install` produces a committed `pnpm-lock.yaml`"
    - "`pnpm --filter web build` succeeds on Windows (Tailwind v3, Next 16.2, React 19)"
    - "`pnpm --filter web tsc --noEmit` exits 0"
    - "`uv run pytest -q` collects and passes the 3 trivial import tests"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv workspace root with members + dev-deps + pytest config"
      contains: "[tool.uv.workspace]"
    - path: "uv.lock"
      provides: "platform-resolution-complete lockfile (committed)"
    - path: "packages/trading-core/pyproject.toml"
      provides: "trading-core package metadata + runtime deps (pandas, pyarrow, duckdb, pydantic, httpx, structlog, pandas-market-calendars, uuid6, rich, mcp, concurrent-log-handler)"
    - path: "packages/api/pyproject.toml"
      provides: "es-api package; depends on trading-core via uv workspace source"
    - path: "packages/tv-bridge/pyproject.toml"
      provides: "tv-bridge empty package; Phase 6 home; depends on trading-core"
    - path: "package.json"
      provides: "JS workspace root; pinned packageManager pnpm@9.15.0"
      contains: "\"packageManager\": \"pnpm@9.15.0\""
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace declaring apps/*"
    - path: "apps/web/app/page.tsx"
      provides: "Next.js 16.2 placeholder home page"
  key_links:
    - from: "packages/api/pyproject.toml"
      to: "packages/trading-core"
      via: "[tool.uv.sources] trading-core = { workspace = true }"
      pattern: "workspace = true"
    - from: "packages/tv-bridge/pyproject.toml"
      to: "packages/trading-core"
      via: "[tool.uv.sources] trading-core = { workspace = true }"
      pattern: "workspace = true"
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "pnpm workspace resolution"
      pattern: "packageManager.*pnpm"
---

<objective>
Lay down the entire Phase 1 workspace skeleton so every subsequent plan has a working `uv sync` + `pnpm build` floor. This is Wave 0 + Wave 1 combined: install missing toolchains (uv, pnpm), author the Python + JS workspace roots, stub the 3 Python packages as empty-importable members per D-02/D-03, and initialize `apps/web/` per D-01.

Purpose: Implements D-01 (JS scaffold ships in Phase 1, runnable Next.js stub), D-02 (3-package Python split), and discovers Windows toolchain issues NOW instead of mid-Phase-3.

Output: Repo where `uv sync && pnpm install && pnpm --filter web build && uv run pytest -q` all succeed from a clean checkout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-in/01-CONTEXT.md
@.planning/phases/01-foundation-data-in/01-RESEARCH.md
@.planning/phases/01-foundation-data-in/01-VALIDATION.md
@.gitignore
@.env.example
@CLAUDE.md

<interfaces>
<!-- Versions are non-negotiable. Authoritative source: 01-RESEARCH.md §Standard Stack -->

Python package versions (in trading-core/pyproject.toml dependencies):
- pandas>=2.2,<3.0 (NEVER 3.0; pinned upper bound per CLAUDE.md "What NOT to Use")
- pyarrow>=17.0,<18.0 (single-minor pin for data_hash byte-stability per Pattern 7)
- duckdb>=1.0,<2.0
- pydantic>=2.13,<3.0
- pydantic-settings>=2.5
- httpx>=0.27,<0.28
- structlog>=25.0
- concurrent-log-handler (latest; required for Windows-safe rotation per Pitfall WatchedFileHandler)
- pandas-market-calendars>=5.3,<6.0
- uuid6>=2025.0,<2026.0
- rich (latest)
- mcp>=1.0,<2.0

Root dev-deps in /pyproject.toml [tool.uv]:
- pytest>=8.0,<9.0
- pytest-asyncio>=0.24,<0.25
- pytest-cov
- hypothesis>=6.150,<7.0
- respx
- freezegun
- pre-commit
- mypy (added for MD-01 static check in Plan 04)
- vectorbt==1.0.0 (FND-02 requires it pinned in uv.lock even though Phase 1 does not use it)

requires-python = ">=3.11,<3.13" everywhere

JS versions in apps/web/package.json (do NOT use Tailwind v4 — see RESEARCH.md O-1):
- next: 16.2.x
- react: ^19 / react-dom: ^19
- typescript: ^5
- lightweight-charts: 5.2.0 (installed but unused in Phase 1)
- @tanstack/react-query: ^5
- zustand: ^5
- tailwindcss: ^3.4 (NOT 4.x)
- postcss / autoprefixer (Tailwind v3 prerequisites)

Root package.json:
- packageManager: "pnpm@9.15.0"
- private: true
</interfaces>

<canonical_skeletons>
The full file bodies for pyproject.toml (root + 3 packages), pnpm-workspace.yaml, root package.json, and apps/web/app/page.tsx are in 01-RESEARCH.md §Code Examples lines 1235–1384. Read them verbatim — do not invent alternate layouts.
</canonical_skeletons>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install uv + pnpm; initialize uv workspace root; stub 3 Python packages</name>
  <files>pyproject.toml, uv.lock, packages/trading-core/pyproject.toml, packages/trading-core/src/trading_core/__init__.py, packages/trading-core/tests/__init__.py, packages/trading-core/tests/conftest.py, packages/trading-core/tests/test_import.py, packages/api/pyproject.toml, packages/api/src/api/__init__.py, packages/api/tests/__init__.py, packages/api/tests/conftest.py, packages/api/tests/test_import.py, packages/tv-bridge/pyproject.toml, packages/tv-bridge/src/tv_bridge/__init__.py, packages/tv-bridge/tests/__init__.py, packages/tv-bridge/tests/conftest.py, packages/tv-bridge/tests/test_import.py, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Code Examples (lines 1235–1338) for the exact pyproject.toml skeletons
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Environment Availability (lines 1460–1481) for installation order
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §D-02 + §D-03 + §D-04 for package shape + tests layout
    - .gitignore (current state — extend without rewriting)
    - CLAUDE.md §Stack Summary for version pins (master pin table)
  </read_first>
  <action>
    PowerShell-first task. Step 1: Install uv via `powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"`; verify `uv --version` reports `0.11.x` or later. Step 2: Enable pnpm via `corepack enable; corepack prepare pnpm@9.15.0 --activate`; verify `pnpm --version` reports `9.15.x`. Step 3: Author root `/pyproject.toml` exactly as in RESEARCH.md §Code Examples — name `es-futures-trading`, requires-python `>=3.11,<3.13`, `[tool.uv.workspace] members = ["packages/trading-core", "packages/api", "packages/tv-bridge"]`, `[tool.uv] dev-dependencies` with pytest 8.x / pytest-asyncio 0.24.x / pytest-cov / hypothesis 6.150+ / respx / freezegun / pre-commit / mypy / `vectorbt==1.0.0` (FND-02 requires this pinned even though unused in Phase 1), and `[tool.pytest.ini_options] asyncio_mode = "auto"` + testpaths for all three packages. Step 4: Create the three `packages/<name>/pyproject.toml` files verbatim from RESEARCH.md lines 1270–1338 — trading-core deps include pandas/pyarrow/duckdb/pydantic/pydantic-settings/httpx/structlog/concurrent-log-handler/pandas-market-calendars/uuid6/rich/mcp; api depends on `trading-core` + fastapi 0.136 + uvicorn[standard] 0.32 via `[tool.uv.sources] trading-core = { workspace = true }`; tv-bridge depends only on `trading-core` via same workspace source. Step 5: Create empty `src/<name>/__init__.py` files with a single docstring line each. Step 6: Create per-package `tests/__init__.py` + `tests/conftest.py` (empty or single docstring) + `tests/test_import.py` containing a one-line test that imports the package's top module (`def test_module_imports(): import trading_core` for trading-core; same shape for api / tv_bridge). Step 7: Add the `pyarrow>=17.0,<18.0` upper-bound pin with an inline TOML comment `# pinned for data_hash byte-stability — see 01-RESEARCH.md Pattern 7`. Step 8: Extend `.gitignore` with `.venv/`, `data/duckdb/*.duckdb`, `data/parquet/`, `data/logs/audit/*.jsonl*` (keep existing `.env` / `.env.local` / `.venv-spike/` lines). Step 9: Run `uv sync` from repo root; it must produce a `.venv/` AND commit-clean `uv.lock`. Step 10: Run `uv run pytest -q` — should collect and pass exactly 3 trivial import tests. All paths must use forward slashes in TOML; all file writes go through pathlib in any helper script. No conftest fixtures yet — Plan 02 / 03 / 04 will extend conftest.py. The trading-core tests/conftest.py for now is just `# Plan 01-02 + 01-03 + 01-04 extend this file with shared fixtures.`
  </action>
  <verify>
    <automated>uv --version; pnpm --version; uv sync; uv run python -c "import trading_core, api, tv_bridge"; uv run pytest -q</automated>
  </verify>
  <done>
    `uv --version` reports `0.11.x`+; `pnpm --version` reports `9.15.x`; `uv sync` completes without error; `.venv/` and `uv.lock` exist; `uv run python -c "import trading_core, api, tv_bridge"` exits 0; `uv run pytest -q` reports exactly 3 passed (one per package); `uv.lock` contains literal strings `vectorbt = { version = "1.0.0"` (or equivalent uv-lock TOML form), `pandas = { version = "2.2`, `pydantic = { version = "2.13`, `fastapi = { version = "0.136`, `duckdb = { version = "1.`, `structlog = { version = "25.`, `httpx = { version = "0.27`, `hypothesis = { version = "6.`, `respx`, `freezegun` (FND-02 grep gate; verify each one).
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize JS workspace + Next.js stub via pnpm; build + typecheck on Windows</name>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml, apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.ts, apps/web/postcss.config.mjs, apps/web/tailwind.config.ts, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §"apps/web/ — Next.js stub setup" (lines 1340–1384) for the create-next-app command + placeholder page.tsx + pnpm-workspace.yaml + root package.json
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Open Question O-1 (lines 1424–1428) for Tailwind v3 recommendation
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Open Question O-6 for packageManager pinning rationale
    - CLAUDE.md §Frontend for version pins (Next 16.2, React 19, lightweight-charts 5.2.0, Tailwind v3)
  </read_first>
  <action>
    Author root `/package.json` per RESEARCH.md §Code Examples lines 1372–1383 — `name: es-futures-monorepo`, `private: true`, `packageManager: pnpm@9.15.0`, scripts `dev/build/typecheck` filtered to `web`. Author `/pnpm-workspace.yaml` with `packages: ["apps/*"]`. Then bootstrap `apps/web/` — preferred path: run `pnpm create next-app@latest apps/web --typescript --tailwind --app --eslint --import-alias "@/*" --src-dir=false --no-turbopack --use-pnpm` (the `--yes` flag is unreliable across create-next-app versions; pass each flag explicitly). If `create-next-app` enables Tailwind v4 by default (verify in `apps/web/package.json` after init), DOWNGRADE to v3 in the same task: `cd apps/web; pnpm remove tailwindcss @tailwindcss/postcss; pnpm add -D tailwindcss@^3.4 postcss autoprefixer; pnpm exec tailwindcss init -p`. After tooling is on v3, replace `apps/web/app/page.tsx` with the verbatim placeholder from RESEARCH.md lines 1349–1364 (uses `font-mono`, `bg-black`, `text-gray-200`). Replace `apps/web/app/globals.css` with the standard Tailwind v3 directives (`@tailwind base; @tailwind components; @tailwind utilities;`). Edit `tailwind.config.ts` so the `content` array includes `./app/**/*.{ts,tsx}`. Then add Phase 3 libs that are installed-but-unused: `pnpm add lightweight-charts@5.2.0 @tanstack/react-query@^5 zustand@^5` from `apps/web/`. Add a one-line comment to `apps/web/README.md` (create if missing) recording the v3 choice with a link back to RESEARCH.md O-1. Verify build + typecheck via the root scripts. The path contains a space (`C:\Users\Admin\Desktop\Day Trading`) — confirm pnpm handles it (no quoting hacks should be needed for pnpm 9.x, but flag if any command emits path-related errors). Commit `pnpm-lock.yaml` (do NOT add to .gitignore).
  </action>
  <verify>
    <automated>pnpm install; pnpm --filter web build; pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>
    `pnpm install` produces `pnpm-lock.yaml` at repo root; `pnpm --filter web build` exits 0 (Next compile + lint success); `pnpm --filter web exec tsc --noEmit` exits 0; `apps/web/package.json` declares `tailwindcss` with caret-major `^3.` (NOT `^4.`); `apps/web/package.json` lists `lightweight-charts` exactly as `5.2.0`, `next` matching `16.2.x`, `react` and `react-dom` matching `^19`; `package.json` at root contains literal string `"packageManager": "pnpm@9.15.0"`; the placeholder page renders the literal string `ES Futures Trading System` somewhere in the build output (`grep -R "ES Futures Trading System" apps/web/.next/server/app/` matches).
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold data/ + config/ directories and full acceptance smoke</name>
  <files>data/duckdb/.gitkeep, data/parquet/.gitkeep, data/logs/audit/.gitkeep, config/system.yaml</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Recommended Project Structure (lines 284–346) for the data/ tree shape
    - .planning/phases/01-foundation-data-in/01-VALIDATION.md §Wave 0 Requirements for the directory list (`data/duckdb/`, `data/parquet/`, `data/logs/audit/`)
    - .gitignore (already gitignores `data/` per Task 1 — confirm `.gitkeep` exception is needed)
  </read_first>
  <action>
    Create the three runtime directories with `.gitkeep` markers so the tree is committable but the contents stay gitignored. Adjust `.gitignore` if needed so `data/**` is gitignored but `data/**/.gitkeep` is allowed (use a negation pattern like `!data/**/.gitkeep` AFTER the `data/...` excludes). Create `config/system.yaml` as a single-line placeholder: `# Plan 01-05 (seed_bars + runs writer) wires Pydantic Settings to read this file.` (the actual Settings model lands in Plan 02 per RESEARCH.md). Then run the full Phase 1 acceptance smoke from repo root: `uv sync && uv run pytest -q && pnpm install && pnpm --filter web build && pnpm --filter web exec tsc --noEmit`. All four MUST succeed. Document any path-with-space issues that surface inline as a `## Known Quirks` line in `.planning/phases/01-foundation-data-in/01-RESEARCH.md` — only if observed; do not add speculative notes.
  </action>
  <verify>
    <automated>uv sync; uv run pytest -q; pnpm install; pnpm --filter web build; pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>
    `data/duckdb/.gitkeep` + `data/parquet/.gitkeep` + `data/logs/audit/.gitkeep` exist; `config/system.yaml` exists with the placeholder comment; `.gitignore` keeps `data/**/.gitkeep` allowed; the full acceptance smoke (uv sync → pytest → pnpm install → pnpm build → pnpm tsc) exits 0 for all five commands; `git status` shows no untracked files outside the intentional new ones.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → install scripts | uv installer pulled from astral.sh; pnpm pulled via corepack (signed by Node) |
| repo root → committed lockfiles | uv.lock + pnpm-lock.yaml must be deterministic across re-runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | pyproject.toml dependency versions | mitigate | Exact upper-bound pins per CLAUDE.md "What NOT to Use" table; lockfile committed; `<3.0` on pandas, `<2.0` on duckdb, `==1.0.0` on vectorbt, `<18.0` on pyarrow (Pattern 7 byte-stability) |
| T-01-01-02 | Information Disclosure | accidental commit of `.venv/` or `node_modules/` | mitigate | `.gitignore` extends to `.venv/`, `data/duckdb/*.duckdb`, `data/parquet/`, `data/logs/audit/*.jsonl*`; lockfiles ARE committed; .gitkeep negation pattern is explicit |
| T-01-01-03 | Tampering | corepack pulling untrusted pnpm version | accept | corepack ships with Node; pnpm pin is explicit `9.15.0`; checksum verified by Node's corepack on first invocation |
</threat_model>

<verification>
After all 3 tasks complete:
1. `uv --version` and `pnpm --version` both succeed
2. `uv sync` completes from a clean clone (delete `.venv/` and re-run as the proof)
3. `uv run pytest -q` reports 3 passed
4. `pnpm install` followed by `pnpm --filter web build` and `pnpm --filter web exec tsc --noEmit` all exit 0
5. `uv.lock` contains the FND-02 mandated version pins (vectorbt 1.0.0, pandas 2.2.x, FastAPI, Pydantic v2, DuckDB, structlog, httpx, pytest, hypothesis, respx, freezegun)
6. `.gitignore` blocks `.venv/`, `data/duckdb/*.duckdb`, `data/parquet/`, `data/logs/audit/`; allows `data/**/.gitkeep`
7. Workspace members importable: `from trading_core import *`, `from api import *`, `from tv_bridge import *` all succeed
8. Tailwind 3 confirmed in `apps/web/package.json` (NOT v4)
</verification>

<success_criteria>
Plan 01-01 is complete when:
- `uv sync` from a clean clone produces `.venv/` + `uv.lock` with all FND-02-required pins
- 3 Python workspace members are importable and have a trivial passing test each
- `apps/web/` builds + typechecks on Windows
- All toolchain commands (`uv`, `pnpm`, Node) are installed and discoverable on PATH
- `data/` runtime directory tree is committed (via `.gitkeep`) and gitignored for contents
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-in/01-01-SUMMARY.md` documenting: uv + pnpm versions installed, observed Windows path-with-space behavior (any quoting issues), any Tailwind-v4-to-v3 downgrade that happened during create-next-app, and the exact `uv.lock` line for vectorbt as evidence of FND-02 compliance.
</output>
