---
phase: 01-foundation-data-in
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
files_modified:
  - packages/trading-core/src/trading_core/storage/__init__.py
  - packages/trading-core/src/trading_core/storage/schema.sql
  - packages/trading-core/src/trading_core/storage/duckdb_store.py
  - packages/trading-core/src/trading_core/storage/runs.py
  - packages/trading-core/src/trading_core/data/twelvedata.py
  - packages/trading-core/src/trading_core/data/tradingview.py
  - packages/trading-core/tests/conftest.py
  - packages/trading-core/tests/test_duckdb_store.py
  - packages/trading-core/tests/test_runs.py
  - packages/trading-core/tests/test_twelvedata_source.py
  - packages/trading-core/tests/test_tradingview_source.py
autonomous: true
requirements:
  - FND-08
  - MD-01
  - MD-02
  - MD-03
  - MD-04
must_haves:
  truths:
    - "`DuckDBStore.upsert_bars(df, provider=...)` is idempotent: running it twice with identical input produces zero net row changes and zero data divergence"
    - "DuckDB `bars` schema has composite PK `(symbol, timeframe, ts_utc)`; `ts_utc` is `TIMESTAMPTZ`; `rollover_seam BOOLEAN NOT NULL DEFAULT FALSE`"
    - "`DuckDBStore` writes Hive-partitioned Parquet via `COPY ... PARTITION_BY (symbol, year(ts_utc), month(ts_utc)) OVERWRITE_OR_IGNORE`"
    - "`RunsWriter.write_run(...)` persists run_id (uuid7), git_sha, data_hash, param_hash, seed, adr_hash, started_at, finished_at, status, notes"
    - "`adr_hash()` returns `sha256(.planning/decisions/0001-data-provider.md bytes)`"
    - "`data_hash(df)` is deterministic across re-runs: sort-by-(symbol,tf,ts) + project + pyarrow parquet bytes with compression=None, use_dictionary=False, write_statistics=False"
    - "`TwelveDataSource.fetch_bars(...)` uses raw httpx (not the official SDK), reads `api-credits-left` response headers, raises `RateLimited` on 429"
    - "`TwelveDataSource` redacts `apikey` query param in structlog audit log entries (replaces with `<TWELVEDATA_API_KEY>` sentinel)"
    - "`TradingViewDataSource.fetch_bars(...)` calls `data_get_ohlcv` via the mcp ClientSession; publishes `DegradedStateEvent` on the EventBus when CDP disconnects (does not raise from the iterator)"
    - "Both adapters mypy-validate against the `DataSource` Protocol from Plan 02"
  artifacts:
    - path: "packages/trading-core/src/trading_core/storage/schema.sql"
      provides: "DDL single source of truth for bars/bar_gaps/instruments/runs tables"
      contains: "PRIMARY KEY (symbol, timeframe, ts_utc)"
    - path: "packages/trading-core/src/trading_core/storage/duckdb_store.py"
      provides: "Connection + schema load + ON CONFLICT upserts + Parquet partition writes"
      contains: "ON CONFLICT (symbol, timeframe, ts_utc) DO UPDATE"
    - path: "packages/trading-core/src/trading_core/storage/runs.py"
      provides: "uuid7-based run_id + git_sha + adr_hash + param_hash + data_hash recipe"
      contains: "def data_hash"
    - path: "packages/trading-core/src/trading_core/data/twelvedata.py"
      provides: "Httpx-based Twelve Data adapter; reads rate-limit headers; raises RateLimited on 429"
      contains: "class TwelveDataSource"
    - path: "packages/trading-core/src/trading_core/data/tradingview.py"
      provides: "mcp.ClientSession-based TradingView adapter; assumes CDP up; publishes DegradedStateEvent on disconnect"
      contains: "class TradingViewDataSource"
  key_links:
    - from: "packages/trading-core/src/trading_core/storage/duckdb_store.py"
      to: "packages/trading-core/src/trading_core/storage/schema.sql"
      via: "ensure_schema() reads schema.sql verbatim and executes it"
      pattern: "schema\\.sql"
    - from: "packages/trading-core/src/trading_core/storage/runs.py"
      to: ".planning/decisions/0001-data-provider.md"
      via: "adr_hash() = sha256(read_bytes()) — FND-08 audit chain"
      pattern: "0001-data-provider"
    - from: "packages/trading-core/src/trading_core/data/twelvedata.py"
      to: "trading_core.data.protocols.DataSource"
      via: "TwelveDataSource implements DataSource (mypy-verified)"
      pattern: "TwelveDataSource"
    - from: "packages/trading-core/src/trading_core/data/tradingview.py"
      to: "trading_core.events.bus.EventBus"
      via: "Publishes DegradedStateEvent on CDP disconnect instead of raising"
      pattern: "DegradedStateEvent"
---

<objective>
Ship the storage + provider-adapter layer that turns the Protocol seam into real data flow: DuckDB schema + upserts + Parquet partitioning, runs-table writer with the full reproducibility hash recipe, plus the two `DataSource` implementations (Twelve Data REST and TradingView MCP) that Plan 05's `seed_bars.py` composes.

Purpose: Implements MD-01 (DataSource Protocol live impls), MD-02 (TradingViewDataSource via MCP), MD-03 (TwelveDataSource via httpx for SPY/secondary), MD-04 (DuckDB + Hive Parquet upsert), FND-08 (runs table + adr_hash + data_hash recipe).

Output: Plan 05's `seed_bars.py` can `from trading_core.data.twelvedata import TwelveDataSource` + `from trading_core.storage.duckdb_store import DuckDBStore` + `from trading_core.storage.runs import new_run_id, git_sha, adr_hash, param_hash, data_hash` and compose the end-to-end backfill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-in/01-CONTEXT.md
@.planning/phases/01-foundation-data-in/01-RESEARCH.md
@.planning/phases/01-foundation-data-in/01-VALIDATION.md
@.planning/phases/01-01-SUMMARY.md
@.planning/phases/01-02-SUMMARY.md
@.planning/phases/00-provider-validation-spike/00-01-SUMMARY.md
@.planning/phases/00-provider-validation-spike/00-02-SUMMARY.md
@.planning/decisions/0001-data-provider.md
@.planning/research/spike-0/spy-bar-budget.md
@scripts/spike/twelvedata_probe.py
@scripts/spike/tv_mcp_smoke.py
@CLAUDE.md

<interfaces>
<!-- DuckDBStore public API -->
DuckDBStore:
  - def __init__(self, db_path: pathlib.Path)
  - def ensure_schema(self) -> None  # reads schema.sql, executes
  - def upsert_bars(self, df: pd.DataFrame, *, provider: str) -> int  # returns rows touched
  - def upsert_gaps(self, gaps_df: pd.DataFrame, *, provider: str, run_id: str) -> int
  - def write_run(self, *, run_id, git_sha, data_hash, param_hash, seed, adr_hash, started_at, finished_at, status, notes) -> None
  - def write_parquet_partition(self, *, symbol: str, start_utc, end_utc) -> None  # COPY TO data/parquet/bars partitioned
  - def close(self) -> None
  - def __enter__/__exit__ for `with DuckDBStore(...) as store:` usage

runs.py public functions:
  - def new_run_id() -> str  # uuid6.uuid7() string
  - def git_sha() -> str  # subprocess git rev-parse HEAD, falls back to 'unknown'
  - def adr_hash(adr_path: Path = Path(".planning/decisions/0001-data-provider.md")) -> str
  - def param_hash(args: dict) -> str  # canonical JSON sha256
  - def data_hash(df: pd.DataFrame) -> str  # sort + project + parquet bytes sha256

TwelveDataSource:
  - class TwelveDataSource (implements DataSource Protocol)
  - name = "twelve_data"
  - def __init__(self, settings: Settings, *, client: httpx.AsyncClient | None = None, pacing_seconds: float = 9.0)
  - async def fetch_bars(symbol, timeframe, start, end) -> pd.DataFrame
  - async def subscribe_bars(symbol, timeframe) -> AsyncIterator[Bar]  # may raise NotImplementedError in v1 (CONTEXT.md scope: backfill only — note this in docstring)
  - Raises: RateLimited on HTTP 429; DataSourceUnavailable on 5xx; GapDetected if returned series has internal gaps

TradingViewDataSource:
  - class TradingViewDataSource (implements DataSource Protocol)
  - name = "tradingview_mcp"
  - def __init__(self, settings: Settings, *, bus: EventBus, mcp_server_path: Path | None = None)
  - async def fetch_bars(symbol, timeframe, start, end) -> pd.DataFrame  # calls data_get_ohlcv via mcp.ClientSession
  - async def subscribe_bars(symbol, timeframe) -> AsyncIterator[Bar]  # polling implementation
  - On CDP disconnect: publishes DegradedStateEvent(source="tradingview_mcp", reason=...) to bus; does NOT raise from the iterator
  - Documents Pitfall 9: stdio_client does not expose subprocess stderr (Phase 6 will replace with custom Popen transport)
</interfaces>

<canonical_skeletons>
RESEARCH.md verbatim code (line refs):
- schema.sql DDL — lines 700–749
- UPSERT_BAR_SQL — lines 751–767
- Hive-partitioned Parquet write — lines 772–784
- runs.py (uuid7, git_sha, adr_hash, param_hash, data_hash) — lines 786–845
- Bar model already exists from Plan 02

Phase 0 reference patterns (DO NOT IMPORT, only mimic):
- scripts/spike/twelvedata_probe.py — stdlib HTTP probe; lifts the rate-limit-header parsing pattern
- scripts/spike/tv_mcp_smoke.py — mcp.ClientSession + stdio_client; lifts the allowlist + restore-on-exit patterns (Phase 1 TradingViewDataSource does NOT need the restore-on-exit — that was a spike concern)
</canonical_skeletons>

<env_keys>
- TWELVEDATA_API_KEY (read from .env via Settings.twelvedata_api_key SecretStr). The query param `apikey=<value>` is the Twelve Data convention.
- TV MCP server path: `C:\Users\Admin\tradingview-mcp-jackson\` (operator-managed; provided via Settings or via a TradingViewDataSource constructor kwarg). Adapter assumes CDP is already enabled on port 9222 (Phase 6 owns bootstrap).
</env_keys>

<pitfall_callouts>
- RESEARCH.md §Pitfall 2 (lines 992–997): use `INSERT ... ON CONFLICT (symbol, timeframe, ts_utc) DO UPDATE SET col = EXCLUDED.col` — DO NOT use the literal `INSERT OR REPLACE` syntax (silent-fail bugs DuckDB #14133 / #20743). This is the documented expansion of CONTEXT.md's `INSERT OR REPLACE` decision and is semantically equivalent.
- RESEARCH.md §Pitfall 4 (lines 1006–1010): Bar timestamps are OPEN time (MD-06). Both Twelve Data and TV use open-time. Add a SQL column comment `-- bar OPEN time, UTC (MD-06)` next to `ts_utc TIMESTAMPTZ`.
- RESEARCH.md §Pitfall 6 (lines 1019–1023): DO NOT use the official `twelvedata` Python SDK — it hides rate-limit response headers that are the only reliable pacing signal. Use raw `httpx.AsyncClient` and read `response.headers.get('api-credits-left')`.
- RESEARCH.md §Pitfall 9 (lines 1080–1082): mcp SDK `stdio_client` cannot expose subprocess stderr. Phase 1 TradingViewDataSource operates blind to server-side stderr; document this in the module docstring.
- Phase 0 SUMMARY 00-02 §"mid-restart transient state": TV CDP can attach to a partially-loaded target where `_activeChartWidgetWV` is undefined. The adapter must tolerate this — call `tv_health_check` (`api_available=true` gate) before relying on `data_get_ohlcv`.
- Phase 0 SUMMARY 00-01: catalog endpoints emit NO rate-limit headers; only `/time_series` does. TwelveDataSource only calls `/time_series` (catalog work was Phase 0 only).
</pitfall_callouts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DuckDB schema + DuckDBStore (ON CONFLICT upserts + Parquet partitioning) + idempotency test</name>
  <files>packages/trading-core/src/trading_core/storage/__init__.py, packages/trading-core/src/trading_core/storage/schema.sql, packages/trading-core/src/trading_core/storage/duckdb_store.py, packages/trading-core/tests/test_duckdb_store.py, packages/trading-core/tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pattern 6 (lines 699–784) — DDL + UPSERT_BAR_SQL + Parquet PARTITION_BY
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pitfall 2 — INSERT OR REPLACE workaround
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §Claude's Discretion paragraph 3 — composite PK (symbol, timeframe, ts) + INSERT OR REPLACE semantics + rollover seam via calendar method
    - .planning/REQUIREMENTS.md §MD-04 (DuckDB + Hive Parquet idempotent upsert; single-writer convention)
  </read_first>
  <behavior>
    - `DuckDBStore(tmp_path / 'x.duckdb').ensure_schema()` creates tables `bars`, `bar_gaps`, `instruments`, `runs` with the SQL columns + PK from schema.sql
    - `bars` PK is composite `(symbol, timeframe, ts_utc)`; `ts_utc` is `TIMESTAMPTZ`; `rollover_seam BOOLEAN NOT NULL DEFAULT FALSE`; `provider VARCHAR NOT NULL`; `ingested_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`
    - `store.upsert_bars(df, provider='twelve_data')` inserts 390 rows on first call; running the SAME upsert on the SAME df produces 0 net rows added; running with df' that updates `close` for one row updates that row and leaves all others byte-identical
    - Upsert uses `INSERT INTO bars (...) VALUES (...) ON CONFLICT (symbol, timeframe, ts_utc) DO UPDATE SET open=EXCLUDED.open, high=EXCLUDED.high, low=EXCLUDED.low, close=EXCLUDED.close, volume=EXCLUDED.volume, rollover_seam=EXCLUDED.rollover_seam, provider=EXCLUDED.provider, ingested_at=CURRENT_TIMESTAMP` (per Pitfall 2)
    - `store.write_parquet_partition(symbol='SPY', start_utc=..., end_utc=...)` produces `data/parquet/bars/symbol=SPY/year=2024/month=01/data_*.parquet` files; running again overwrites partitions (OVERWRITE_OR_IGNORE)
    - `store.upsert_gaps(gaps_df, provider='twelve_data', run_id='r1')` inserts rows into `bar_gaps`; idempotent re-run on same input produces 0 new rows
    - `with DuckDBStore(path) as store:` automatically closes the connection on exit; passing a non-existent parent directory raises a meaningful error
  </behavior>
  <action>
    Author `storage/schema.sql` verbatim from RESEARCH.md Pattern 6 lines 702–748: tables `bars`, `bar_gaps`, `instruments`, `runs`. Inline SQL comments must match the DDL (Pitfall 4: `ts_utc TIMESTAMPTZ NOT NULL  -- bar OPEN time, UTC (MD-06)`). Author `storage/duckdb_store.py` with `class DuckDBStore` providing the public API listed in `<interfaces>` above. `ensure_schema(self)` reads `schema.sql` from the module directory via `importlib.resources` (or `Path(__file__).parent / "schema.sql"`) and executes it. `upsert_bars(self, df, *, provider)` uses the `UPSERT_BAR_SQL` from RESEARCH.md lines 754–767 — `INSERT INTO bars ... ON CONFLICT (symbol, timeframe, ts_utc) DO UPDATE SET col = EXCLUDED.col`. Use `conn.executemany(UPSERT_BAR_SQL, rows)` where rows is a list-of-tuples built from `df.itertuples(index=False)`. `write_parquet_partition` uses the COPY pattern from RESEARCH.md lines 775–781 — `COPY (SELECT * FROM bars WHERE symbol = ? AND ts_utc >= ? AND ts_utc < ?) TO 'data/parquet/bars' (FORMAT PARQUET, PARTITION_BY (symbol, year(ts_utc), month(ts_utc)), OVERWRITE_OR_IGNORE)`. `upsert_gaps` is structurally identical to `upsert_bars` but targets the `bar_gaps` table. `write_run` lands in Task 2 alongside `runs.py` — for now, leave a `# Task 2 wires write_run` stub or implement it inline if convenient. Single-writer convention enforced by docstring + the assumption that only the FastAPI process (Phase 3) and CLI scripts (Plan 05) instantiate `DuckDBStore` — Plan 1 stores no enforcement code beyond the comment. Extend `tests/conftest.py` with a `tmp_duckdb_path` fixture (uses pytest's `tmp_path` fixture to return `tmp_path / "test.duckdb"`). Write `tests/test_duckdb_store.py` covering every `<behavior>` bullet. Use the `synthetic_spy_day` fixture from Plan 03's conftest to produce a 390-row DataFrame. The idempotency test runs `upsert_bars(df)` twice and asserts `SELECT COUNT(*) FROM bars` is 390 after both runs AND that the per-row hash of all rows is byte-identical between runs (use Plan 04 Task 2's `data_hash` once authored, OR compute a temporary local hash here and update the test to use `data_hash` once Task 2 lands). All paths use `pathlib.Path`; no string concatenation. Make duckdb_store.py import-safe even if `data/duckdb/` does not exist yet (`ensure_schema` is idempotent — `CREATE TABLE IF NOT EXISTS` per the SQL).
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_duckdb_store.py -q</automated>
  </verify>
  <done>
    test_duckdb_store passes including the idempotency check; `grep -n "ON CONFLICT" packages/trading-core/src/trading_core/storage/duckdb_store.py` matches at least 2 occurrences (bars + bar_gaps); `grep -n "INSERT OR REPLACE" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns ZERO matches (Pitfall 2 anti-pattern excluded); `grep -n "PARTITION_BY\|OVERWRITE_OR_IGNORE" packages/trading-core/src/trading_core/storage/duckdb_store.py` matches at least once each; `grep -nE "PRIMARY KEY \(symbol, timeframe, ts_utc\)" packages/trading-core/src/trading_core/storage/schema.sql` matches exactly once on the bars table.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunsWriter (uuid7, git_sha, adr_hash, param_hash, data_hash recipe) + write_run integration</name>
  <files>packages/trading-core/src/trading_core/storage/runs.py, packages/trading-core/src/trading_core/storage/duckdb_store.py, packages/trading-core/tests/test_runs.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pattern 7 (lines 786–847) — full runs.py body
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Open Question O-4 (lines 1442–1446) — data_hash sort + project + pyarrow bytes recipe
    - .planning/REQUIREMENTS.md §FND-08 (runs row contains git_sha/data_hash/param_hash/seed; reproducibility CI in Phase 3)
    - .planning/decisions/0001-data-provider.md (the ADR whose bytes go into adr_hash)
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §Claude's Discretion paragraph 3 — full 10-field runs row spec
  </read_first>
  <behavior>
    - `new_run_id()` returns a string that parses as a valid UUIDv7 (validate via `uuid6.UUID(...)`)
    - `git_sha()` returns the current HEAD's full SHA when run inside this repo; returns `'unknown'` when run outside a git repo
    - `adr_hash()` returns `sha256(.planning/decisions/0001-data-provider.md bytes).hexdigest()`; running it twice returns the same hex
    - `param_hash({'symbol':'SPY','tf':'1m','from':date(2024,1,1)})` returns a deterministic hex sha256; reordering keys yields the same hash (sorted JSON)
    - `data_hash(df)` is deterministic across re-runs: build the same df twice, hash both, expect identical hex; sort the rows in a different order before passing, expect same hex (sort-by-(symbol,timeframe,ts_utc) is internal)
    - `data_hash(df_with_one_close_changed) != data_hash(df_original)`
    - `store.write_run(...)` persists a row in `runs` with all 10 fields (run_id, git_sha, data_hash, param_hash, seed, adr_hash, started_at, finished_at, status, notes)
    - Querying `SELECT run_id, adr_hash FROM runs ORDER BY started_at DESC LIMIT 1` after a write returns the values written
  </behavior>
  <action>
    Author `storage/runs.py` verbatim from RESEARCH.md Pattern 7 lines 791–845. Module-level constant `ADR_PATH = Path(".planning/decisions/0001-data-provider.md")` — but accept `adr_hash(adr_path: Path | None = None)` so tests can pass a tmp path. `data_hash(df)` does: sort by `(symbol, timeframe, ts_utc)`, project to `[symbol, timeframe, ts_utc, open, high, low, close, volume, rollover_seam, provider]`, use `pyarrow.Table.from_pandas(df, preserve_index=False)` + `pq.write_table(table, buf, compression="none", use_dictionary=False, write_statistics=False)`, then sha256 the buffer bytes. Add an inline TOML-style comment in pyproject.toml (Plan 01 territory but verify it's there) explaining the `pyarrow>=17,<18` pin reason. Implement `DuckDBStore.write_run(self, *, run_id, git_sha, data_hash, param_hash, seed, adr_hash, started_at, finished_at, status, notes)` as a simple INSERT into the `runs` table (no upsert needed — run_id is unique per CLI invocation). Write `tests/test_runs.py` covering every `<behavior>` bullet. For the `git_sha` test, run `git rev-parse HEAD` in a subprocess and compare to `git_sha()` output. For the `adr_hash` test, use `pytest.fixture` to write a known-bytes file under `tmp_path` and assert `hashlib.sha256(known_bytes).hexdigest() == adr_hash(tmp_path/'adr.md')`. For `data_hash` determinism, build the same df twice with different row orderings and assert the hash is identical. Use freezegun on the `started_at` / `finished_at` test to lock the timestamps.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_runs.py packages/trading-core/tests/test_duckdb_store.py::test_write_run_round_trip -q</automated>
  </verify>
  <done>
    test_runs passes; `write_run` round-trip test (added to test_duckdb_store.py) passes; `grep -n "compression=\"none\"\|compression='none'" packages/trading-core/src/trading_core/storage/runs.py` matches (Pattern 7 recipe); `grep -n "use_dictionary=False\|write_statistics=False" packages/trading-core/src/trading_core/storage/runs.py` matches both; `grep -n "uuid6\.uuid7\|uuid7()" packages/trading-core/src/trading_core/storage/runs.py` matches; running `uv run python -c "from trading_core.storage.runs import adr_hash, new_run_id; print(adr_hash(), new_run_id())"` prints a 64-char hex + a UUID-shaped string.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TwelveDataSource (httpx; rate-limit headers; redaction; respx mock tests)</name>
  <files>packages/trading-core/src/trading_core/data/twelvedata.py, packages/trading-core/tests/test_twelvedata_source.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Alternatives Considered (lines 166–177) — "Use raw httpx, not the official SDK" + Pitfall 6
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pitfall 6 (lines 1019–1023) — `api-credits-left` header reading is the only reliable pacing signal
    - .planning/research/spike-0/spy-bar-budget.md — 9-second pacing on Free tier; 40 calls / ~5 min wall-clock for 196,560 bars
    - .planning/phases/00-provider-validation-spike/00-01-SUMMARY.md — what the probe established (redaction sentinel `<TWELVEDATA_API_KEY>`, no headers on catalog endpoints)
    - scripts/spike/twelvedata_probe.py — reference pattern (DO NOT IMPORT — carries `PHASE 0 SPIKE` sentinel)
    - .planning/REQUIREMENTS.md §MD-03 (secondary `TwelveDataSource` for SPY)
    - packages/trading-core/src/trading_core/data/protocols.py (Plan 02) — the Protocol this implements
  </read_first>
  <behavior>
    - `TwelveDataSource(settings)` constructs; reads `settings.twelvedata_api_key.get_secret_value()` once at fetch time (not at construction)
    - `await source.fetch_bars('SPY', '1m', start, end)` returns a DataFrame with columns `[symbol, timeframe, ts_utc, open, high, low, close, volume]` indexed by ts_utc (or with ts_utc as a column — pick one convention and document; the Bar model uses ts_utc as a field, so column form is preferred for adapter output)
    - The HTTP request hits `https://api.twelvedata.com/time_series` with query params `symbol=SPY&interval=1min&start_date=...&end_date=...&format=JSON&apikey=<value>&timezone=UTC`
    - After each call, `response.headers.get('api-credits-left')` is read and logged via structlog at INFO level with the literal key name `credits_left`
    - On HTTP 429: raises `RateLimited` immediately (no retry); the structlog log line contains `event="ratelimited"` and `status=429`
    - On HTTP 5xx: raises `DataSourceUnavailable`
    - Log entries that include the request URL redact the `apikey` query param value: replace it with the literal string `<TWELVEDATA_API_KEY>` before logging
    - Default pacing 9 seconds is honored: when fetching multiple windows back-to-back, the adapter sleeps ≥ 9s between requests (respx test verifies the sleep via `freezegun` or by mocking `asyncio.sleep` and asserting it was awaited with `≥9.0`)
    - `subscribe_bars` raises `NotImplementedError("Twelve Data live subscribe is out of scope for Phase 1; backfill only — use TradingViewDataSource for live")`
  </behavior>
  <action>
    Author `packages/trading-core/src/trading_core/data/twelvedata.py`. Imports: `httpx`, `pandas as pd`, `re`, `asyncio`, `structlog`, `pathlib.Path` (typing), `datetime`. `class TwelveDataSource:` with `name = "twelve_data"`. Constructor accepts `settings: Settings`, optional `client: httpx.AsyncClient | None`, optional `pacing_seconds: float = 9.0`. `_redact_url(url: str) -> str` substitutes the `apikey=...` value with `<TWELVEDATA_API_KEY>` using `re.sub(r"apikey=[^&]+", "apikey=<TWELVEDATA_API_KEY>", url)`. `async def fetch_bars(symbol, timeframe, start, end)`: validates start/end are tz-aware UTC (raises `ValueError` otherwise); constructs the URL; logs `event="fetch_bars.request"` with `url=self._redact_url(url)`; awaits httpx call (or uses `self._client` if provided — for respx mocking); reads `response.headers.get('api-credits-left')` and `response.headers.get('api-credits-used')`; on 429 raises `RateLimited` (parse the `code` and `message` from JSON body if available); on 5xx raises `DataSourceUnavailable`; on 2xx parses the `values` array into a DataFrame, converts each `datetime` to `pd.Timestamp(..., tz="UTC")` (Twelve Data returns local-exchange time per their docs — but with `timezone=UTC` query param it returns UTC; verify in the respx fixture by including a sample response with `"datetime": "2024-01-02 14:30:00"` and asserting the adapter normalizes correctly); add `symbol`, `timeframe`, `provider="twelve_data"` columns; return the DataFrame. After every successful call, `await asyncio.sleep(self._pacing_seconds)`. `async def subscribe_bars`: raises `NotImplementedError` with the message from `<behavior>`. Write `tests/test_twelvedata_source.py` using `respx` to mock `/time_series`: (a) happy path returns a DataFrame with expected columns + UTC timestamps; (b) HTTP 429 raises `RateLimited` and log line contains `event="ratelimited"` (capture via `caplog` or `structlog.testing.capture_logs`); (c) HTTP 503 raises `DataSourceUnavailable`; (d) log entries contain the redaction sentinel and NEVER the literal API key value (use a synthetic key like `"FAKEKEY12345"` in the settings fixture and assert the string is absent from all captured log entries); (e) pacing: mock `asyncio.sleep` and assert it was called with ≥ 9.0 after a successful fetch; (f) `subscribe_bars` raises NotImplementedError. Sleep mocking pattern: `monkeypatch.setattr("trading_core.data.twelvedata.asyncio.sleep", AsyncMock())` and assert call_args[0][0] >= 9.0.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_twelvedata_source.py -q</automated>
  </verify>
  <done>
    test_twelvedata_source passes including all 6 cases; `grep -rn "from twelvedata" packages/trading-core/src/trading_core/data/twelvedata.py` returns ZERO matches (Pitfall 6 — official SDK NOT used); `grep -n "<TWELVEDATA_API_KEY>" packages/trading-core/src/trading_core/data/twelvedata.py` matches at least once (redaction sentinel embedded); `grep -n "api-credits-left" packages/trading-core/src/trading_core/data/twelvedata.py` matches; running `uv run python -c "from trading_core.data.twelvedata import TwelveDataSource; print(TwelveDataSource.name)"` prints `twelve_data`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: TradingViewDataSource (mcp.ClientSession; CDP-assumed-up; DegradedStateEvent on disconnect)</name>
  <files>packages/trading-core/src/trading_core/data/tradingview.py, packages/trading-core/tests/test_tradingview_source.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §System Architecture Diagram (lines 181–265) — TV adapter role
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pitfall 9 (lines 1080–1082) — mcp stdio_client cannot expose subprocess stderr
    - .planning/phases/00-provider-validation-spike/00-02-SUMMARY.md §"Decisions Made" + §"key-decisions" — CDP-mode launch is Phase 6 territory; mid-restart transient state (`_activeChartWidgetWV` undefined); `quote_get` returns bar-start time
    - scripts/spike/tv_mcp_smoke.py — reference pattern for `stdio_client` + `ClientSession` + tool allowlist + chart restore (Phase 1 does NOT restore — just consume; remove the spike-only restore-on-exit)
    - .planning/REQUIREMENTS.md §MD-02 (TradingViewDataSource via MCP `data_get_ohlcv`)
    - packages/trading-core/src/trading_core/events/bus.py (Plan 03) — for `DegradedStateEvent` publishing path
  </read_first>
  <behavior>
    - `TradingViewDataSource(settings, bus=event_bus)` constructs; does NOT spawn the MCP server at construction time
    - `await source.fetch_bars('ES', '1m', start, end)` opens a `stdio_client` + `ClientSession` to the `tradingview-mcp-jackson` server, awaits `session.initialize()` with a 15s timeout, calls `tv_health_check` (asserts `api_available == True` BEFORE calling `data_get_ohlcv`), then calls `data_get_ohlcv(symbol='CME_MINI:ES1!', timeframe='1m', count=..., end=...)` and returns the bars as a DataFrame
    - If `tv_health_check.api_available == False` OR the session.initialize times out OR the subprocess exits unexpectedly: the adapter publishes `DegradedStateEvent(source='tradingview_mcp', reason=<reason>, ...)` to the `bus` and raises `DataSourceUnavailable` from `fetch_bars` (publishing-not-raising is reserved for `subscribe_bars`)
    - `subscribe_bars` is a long-running iterator that polls `data_get_ohlcv` periodically; on CDP disconnect it publishes `DegradedStateEvent` and yields nothing (does NOT raise from the iterator — per RESEARCH.md Pattern 1 Protocol contract; the caller in Phase 6's TVBridge can re-establish)
    - Symbol mapping: Phase 1 adapter maps `'ES'` → `'CME_MINI:ES1!'` and `'MES'` → `'CME_MINI:MES1!'` via a private `_symbol_map` dict; `'SPY'` raises `ValueError("TradingViewDataSource does not support SPY in Phase 1; use TwelveDataSource for SPY backfill")`
    - Module docstring documents Pitfall 9 (stdio_client stderr inaccessible) and notes that Phase 6's TVBridge owns CDP bootstrap
  </behavior>
  <action>
    Author `packages/trading-core/src/trading_core/data/tradingview.py`. Imports: `mcp` SDK (`mcp.ClientSession`, `mcp.client.stdio.stdio_client`, `StdioServerParameters`), `asyncio`, `pandas as pd`, `structlog`, `pathlib.Path`. Module docstring opens with a sentence documenting Pitfall 9 + the CDP-bootstrap-is-Phase-6 fact. `class TradingViewDataSource:` with `name = "tradingview_mcp"`. Constructor accepts `settings: Settings`, `*, bus: EventBus`, optional `mcp_server_path: Path | None` (defaults to `Path(r"C:\Users\Admin\tradingview-mcp-jackson")` from settings or hardcoded). `_symbol_map = {'ES': 'CME_MINI:ES1!', 'MES': 'CME_MINI:MES1!'}`. `async def fetch_bars(symbol, timeframe, start, end)`: maps the symbol; raises `ValueError` on `SPY`; opens `stdio_client(StdioServerParameters(command='node', args=[str(self._mcp_server_path / 'dist' / 'server.js')]))` (or whatever the actual `tradingview-mcp-jackson` server's entry point is — check `00-02-SUMMARY.md` and the `tv_mcp_smoke.py` spike for the exact spawn args); wraps in `ClientSession`; awaits `session.initialize()` with `asyncio.wait_for(timeout=15.0)`; on TimeoutError publishes `DegradedStateEvent` to bus and raises `DataSourceUnavailable("mcp initialize timed out")`; calls `session.call_tool("tv_health_check", {})` and asserts `result.content[0].text` parses to `{"api_available": True, ...}` JSON (or whatever the exact response shape is per Phase 0's `tv-mcp-transcript.log`); on `api_available=False` publishes `DegradedStateEvent` and raises `DataSourceUnavailable`; calls `session.call_tool("data_get_ohlcv", {"symbol": tv_symbol, "timeframe": timeframe, "count": ..., "end": end.isoformat()})`; parses the response into a DataFrame with columns `[symbol, timeframe, ts_utc, open, high, low, close, volume, provider]` (note: provider="tradingview_mcp"); `ts_utc` is constructed from the response time field — TV `data_get_ohlcv` typically returns Unix epoch ms or ISO 8601; whichever it is, convert to tz-aware UTC. Implement `subscribe_bars` as an `async def` returning an `AsyncIterator[Bar]` that polls every `{1m:60s, 5m:300s, 15m:900s}[tf]` and yields completed bars; on disconnect publishes `DegradedStateEvent` and breaks (does NOT raise). Write `tests/test_tradingview_source.py` using `pytest-asyncio` + `unittest.mock` to mock `mcp.client.stdio.stdio_client` and `mcp.ClientSession` — DO NOT spawn an actual subprocess in tests; mock the `call_tool` responses. Tests cover: (a) happy path with mocked `tv_health_check` returning `api_available=True` and `data_get_ohlcv` returning 30 sample bars → fetch_bars returns a 30-row DataFrame with correct columns; (b) `tv_health_check` returns `api_available=False` → adapter publishes DegradedStateEvent to a stub bus (use the real `EventBus` from Plan 03; subscribe in the test; assert the event appears) AND raises `DataSourceUnavailable`; (c) `session.initialize` raises `asyncio.TimeoutError` → adapter publishes DegradedStateEvent + raises `DataSourceUnavailable`; (d) `fetch_bars('SPY', '1m', ...)` raises `ValueError`; (e) symbol `'ES'` maps to `'CME_MINI:ES1!'` in the captured tool-call args. The TradingViewDataSource live integration test (per VALIDATION.md §"Manual-Only Verifications") is NOT required here — only the mocked unit tests. If the exact `data_get_ohlcv` response schema is ambiguous, infer from `00-02-SUMMARY.md` mentions of "300 bars returned" and write the adapter against a documented contract; if the mock test reveals the real shape differs, fix and document in SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_tradingview_source.py -q</automated>
  </verify>
  <done>
    test_tradingview_source passes including all 5 cases; `grep -n "DegradedStateEvent" packages/trading-core/src/trading_core/data/tradingview.py` matches at least 2 occurrences (publish on health-check fail + publish on init timeout); `grep -n "tv_health_check\|api_available" packages/trading-core/src/trading_core/data/tradingview.py` matches both tool name and the field check; `grep -n "CME_MINI:ES1\!" packages/trading-core/src/trading_core/data/tradingview.py` matches; running `uv run python -c "from trading_core.data.tradingview import TradingViewDataSource; print(TradingViewDataSource.name)"` prints `tradingview_mcp`; running `uv run mypy packages/trading-core/src/trading_core/data/twelvedata.py packages/trading-core/src/trading_core/data/tradingview.py --strict --follow-imports=silent` exits 0 (MD-01 static check — both adapters mypy-validate against the DataSource Protocol).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Twelve Data API → adapter | Untrusted JSON response; rate-limit signal in headers |
| .env API key → outgoing HTTPS request URL | API key value embedded in query string; MUST be redacted in logs |
| MCP stdio subprocess → adapter | tradingview-mcp-jackson is operator-managed local subprocess; stderr inaccessible (Pitfall 9) |
| DuckDB upsert → bars table | Composite PK conflict resolution — silent-fail risk if INSERT OR REPLACE form used (Pitfall 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | TWELVEDATA_API_KEY in structlog audit log | mitigate | `_redact_url` substitutes the `apikey=` value with `<TWELVEDATA_API_KEY>` before logging; test (d) in Task 3 asserts the raw key never appears in captured logs |
| T-01-04-02 | Tampering | DuckDB INSERT OR REPLACE silent-fail | mitigate | Use explicit `ON CONFLICT ... DO UPDATE SET col = EXCLUDED.col` (Pitfall 2); idempotency test in Task 1 asserts zero net rows + byte-identical content on re-run |
| T-01-04-03 | Information Disclosure | DuckDB file written to a path containing the API key (e.g., if a careless override of duckdb_path uses env interpolation) | mitigate | `Settings.duckdb_path` defaults to `data/duckdb/trading.duckdb` (no env interpolation); no code path joins the API key into a filesystem path |
| T-01-04-04 | DoS | Twelve Data 429 storm | mitigate | Adapter raises `RateLimited` on first 429 (no retry); 9s pacing default prevents burst |
| T-01-04-05 | Tampering | TV CDP disconnect treated as success | mitigate | tv_health_check `api_available == True` gate BEFORE `data_get_ohlcv`; DegradedStateEvent published on every failure mode |
| T-01-04-06 | Information Disclosure | TV MCP CDP target leakage in audit logs (Phase 6 inherits) | mitigate | Phase 1 baseline: TV adapter logs do not include the CDP port or process IDs; only symbol/timeframe/timestamps; documented for Phase 6 to extend |
| T-01-04-07 | Tampering | data_hash non-determinism across pyarrow patch versions | mitigate | `compression=None, use_dictionary=False, write_statistics=False` flags (Pattern 7); pyarrow pinned to `>=17.0,<18.0` in pyproject.toml |
</threat_model>

<verification>
After all 4 tasks complete:
1. `uv run pytest packages/trading-core/tests/test_duckdb_store.py packages/trading-core/tests/test_runs.py packages/trading-core/tests/test_twelvedata_source.py packages/trading-core/tests/test_tradingview_source.py -q` — all pass
2. `uv run mypy packages/trading-core/src/` exits 0 (MD-01 — both adapters mypy-validate against DataSource)
3. `grep -rn 'INSERT OR REPLACE' packages/trading-core/` returns zero matches (Pitfall 2 anti-pattern excluded)
4. `grep -rn 'from twelvedata' packages/trading-core/` returns zero matches (Pitfall 6 — official SDK not used)
5. Twelve Data adapter log entries (captured via structlog.testing) never contain the literal fake API key in any test case
6. DuckDB idempotency test green: re-running upsert_bars on same df produces zero net row change
7. `adr_hash()` round-trips against `.planning/decisions/0001-data-provider.md`
</verification>

<success_criteria>
Plan 01-04 is complete when:
- DuckDB schema + ON CONFLICT upserts + Hive Parquet partitioning all work; idempotency proven (MD-04)
- `runs` table writer captures the full 10-field row including `adr_hash` (FND-08)
- `data_hash` recipe is deterministic across re-runs
- TwelveDataSource implements `DataSource`, reads rate-limit headers, redacts the API key in logs, raises `RateLimited` on 429 (MD-03)
- TradingViewDataSource implements `DataSource`, calls `tv_health_check` before `data_get_ohlcv`, publishes `DegradedStateEvent` on disconnect (MD-02)
- Both adapters mypy-validate against the Protocol (MD-01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-in/01-04-SUMMARY.md` documenting: the exact `tradingview-mcp-jackson` server spawn command line discovered (entry point + args), the exact `data_get_ohlcv` response shape used to decode bars, any deviations from RESEARCH.md Pattern 6 SQL (e.g., DuckDB 1.x API quirks), the mypy result for `packages/trading-core/src/`, and the data_hash hex for the synthetic 390-row SPY day fixture (locks the reproducibility baseline for Phase 3 CI).
</output>
