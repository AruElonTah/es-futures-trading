---
phase: 01-foundation-data-in
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05-PLAN.md
files_modified:
  - packages/api/src/api/__init__.py
  - packages/api/src/api/app.py
  - packages/api/tests/test_health.py
  - apps/web/app/page.tsx
  - apps/web/README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - FND-01
  - FND-02
must_haves:
  truths:
    - "`packages/api/src/api/app.py` defines `app = FastAPI(...)` with a single `GET /health` endpoint returning `{\"status\": \"ok\"}`"
    - "`uv run python -c \"from api.app import app; print(type(app).__name__)\"` prints `FastAPI`"
    - "`uv run pytest packages/api/tests/test_health.py -q` passes (TestClient-based health check)"
    - "`pnpm --filter web build` succeeds and `pnpm --filter web exec tsc --noEmit` exits 0 — final acceptance gate"
    - "`apps/web/app/page.tsx` renders the Phase 1 placeholder (no real chart yet) per D-01"
    - "Full acceptance smoke passes: `uv sync && uv run pytest && pnpm --filter web build && pnpm --filter web exec tsc --noEmit && uv run pre-commit run --all-files`"
    - "ROADMAP.md updated with the final plan list + objectives; STATE.md advanced to Phase 1 in-progress"
  artifacts:
    - path: "packages/api/src/api/app.py"
      provides: "FastAPI app shell with GET /health (Phase 3 expands to /bars, /backtests, WS /stream)"
      contains: "GET /health"
    - path: "packages/api/tests/test_health.py"
      provides: "TestClient health-check integration test"
      contains: "response.status_code == 200"
    - path: "apps/web/app/page.tsx"
      provides: "Next.js 16.2 placeholder page (per D-01; Phase 3 replaces with /dashboard)"
      contains: "ES Futures Trading System"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 progress updated with completed plans"
      contains: "Phase 1: Foundation"
  key_links:
    - from: "packages/api/src/api/app.py"
      to: "trading_core.config.Settings"
      via: "FastAPI imports Settings to verify the import graph + provides the foundation for Phase 3 to wire endpoints"
      pattern: "from trading_core"
    - from: "apps/web/app/page.tsx"
      to: "packages/api"
      via: "(no real call yet — placeholder only; Phase 3 wires fetch to /bars)"
      pattern: "ES Futures Trading System"
---

<objective>
Land the FastAPI shell + finalize the Next.js placeholder page + run the full Phase 1 acceptance smoke. This is the workspace-contract closure: every workspace member is importable, build, and verifiable end-to-end. Update ROADMAP + STATE with the completed plan list.

Purpose: Final closure on D-01 (runnable Next.js stub) and D-02 (3-package Python split, all importable), and completes FND-01 (uv workspace + `packages/{trading-core,api,tv-bridge}` + `apps/web/` importable) and FND-02 (uv.lock pin verification).

Output: The phase verifier can run a single command sequence and prove every ROADMAP Phase 1 success criterion is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-in/01-CONTEXT.md
@.planning/phases/01-foundation-data-in/01-RESEARCH.md
@.planning/phases/01-foundation-data-in/01-VALIDATION.md
@.planning/phases/01-01-SUMMARY.md
@.planning/phases/01-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 ships only the FastAPI shell — real endpoints land in Phase 3. -->

FastAPI app shell (packages/api/src/api/app.py):
  - app = FastAPI(title="ES Futures Trading System API", version="0.1.0")
  - @app.get("/health") def health() -> dict: returns {"status": "ok", "service": "es-api", "version": app.version}
  - module-level `Settings()` import to prove the trading-core dep wires correctly

TestClient pattern (packages/api/tests/test_health.py):
  - from fastapi.testclient import TestClient
  - client = TestClient(app)
  - response = client.get("/health")
  - assert response.status_code == 200
  - assert response.json()["status"] == "ok"

Next.js stub final form (apps/web/app/page.tsx):
  - Server component (default export, no "use client")
  - Renders the placeholder text from RESEARCH.md lines 1349–1364
  - Uses Tailwind v3 utility classes (font-mono bg-black text-gray-200)
</interfaces>

<canonical_skeletons>
RESEARCH.md verbatim code (line refs):
- apps/web/app/page.tsx — lines 1349–1364
- FastAPI shell shape — D-02 says "api ships an empty app = FastAPI() + a single GET /health endpoint to prove the import graph works. Real endpoints land Phase 3."
- ROADMAP §"Phase 1 success criteria" #1 and #4 are the closure targets
</canonical_skeletons>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI app shell + GET /health + TestClient integration test</name>
  <files>packages/api/src/api/__init__.py, packages/api/src/api/app.py, packages/api/tests/test_health.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Architectural Responsibility Map line 97 (api/ ships an empty app + GET /health; real endpoints Phase 3)
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Recommended Project Structure lines 319–322 (api package layout)
    - packages/trading-core/src/trading_core/config.py (Plan 02) — Settings import target
    - packages/api/pyproject.toml (Plan 01) — already declares fastapi 0.136 + uvicorn 0.32 + trading-core via workspace
  </read_first>
  <behavior>
    - `from api.app import app` succeeds; `type(app).__name__ == 'FastAPI'`
    - `TestClient(app).get('/health').status_code == 200`
    - Response JSON: `{"status": "ok", "service": "es-api", "version": "0.1.0"}`
    - The module imports `from trading_core.config import Settings` and constructs `Settings()` at module top — proves the workspace dep wires correctly (FND-01 success criterion #1)
    - Running `uv run uvicorn api.app:app --host 127.0.0.1 --port 0 --workers 1` succeeds (the test does NOT need to actually bind a port — TestClient is enough; this verify line is documented as a manual sanity step in the README)
  </behavior>
  <action>
    Author `packages/api/src/api/__init__.py` with: `from .app import app  # re-export so `uvicorn api:app` works` (the `[project.scripts]` mechanism is NOT needed Phase 1; uvicorn discovery via dotted module is). Author `packages/api/src/api/app.py` keeping it short — module-level FastAPI instantiation: `app = FastAPI(title="ES Futures Trading System API", version="0.1.0")`; import `from trading_core.config import Settings` at module top and assign a module-level `_settings = Settings()` (proves the import graph); register a single endpoint `@app.get("/health") def health(): return {"status": "ok", "service": "es-api", "version": app.version}`. Do NOT add any other endpoints — Phase 3 owns `/bars`, `/backtests`, `WS /stream`, `/positions`, etc. (UI-01 surface). Write `packages/api/tests/test_health.py` using `fastapi.testclient.TestClient` (which is a thin wrapper around httpx) — `client = TestClient(app); response = client.get("/health"); assert response.status_code == 200; assert response.json() == {"status": "ok", "service": "es-api", "version": "0.1.0"}`. Replace the trivial `test_import.py` stub Plan 01 left in `packages/api/tests/` — actually, keep `test_import.py` AND add `test_health.py` (the import test still proves importability after dependency churn). Note: the `packages/api/tests/conftest.py` Plan 01 created should declare `pytest_plugins = ["trading_core.tests.conftest"]` per CONTEXT.md D-04 fixture re-export — Plan 02 Task 1 may have already done this; verify and update if not.
  </action>
  <verify>
    <automated>uv run pytest packages/api/tests/test_health.py -q; uv run python -c "from api.app import app; print(type(app).__name__, [r.path for r in app.routes if hasattr(r, 'path')])"</automated>
  </verify>
  <done>
    test_health passes; `uv run python -c "from api.app import app; print(type(app).__name__)"` prints `FastAPI`; the same command lists `/health` in the routes; `grep -nE "from trading_core" packages/api/src/api/app.py` matches (workspace dep wiring proven); the file does NOT register any endpoint OTHER than `/health` (Phase 3 owns the rest — `grep -cE "@app\.(get|post|put|delete|websocket)" packages/api/src/api/app.py` returns exactly `1`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize apps/web/app/page.tsx placeholder + apps/web/README.md (Tailwind v3 note)</name>
  <files>apps/web/app/page.tsx, apps/web/README.md</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §"apps/web/ — Next.js stub setup" (lines 1340–1384) — verbatim page.tsx body
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Open Question O-1 — Tailwind v3 vs v4 choice and the requirement to document it in apps/web/README.md
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §D-01 — runnable Next.js 16.2 stub with App Router, React 19, TypeScript 5.x
    - .planning/phases/01-01-SUMMARY.md — Plan 01 may have already authored a placeholder; verify and adjust
  </read_first>
  <action>
    Replace `apps/web/app/page.tsx` with the verbatim placeholder body from RESEARCH.md lines 1351–1363 — a single default-export server component rendering:
    - `<main>` with classes `flex min-h-screen items-center justify-center bg-black font-mono text-gray-200`
    - inside, a div with `max-w-xl space-y-2 p-8`
    - `<h1 className="text-2xl font-bold">ES Futures Trading System</h1>`
    - `<p className="text-sm text-gray-400">Phase 1: Foundation scaffold. Real charts ship in Phase 3.</p>`
    - `<p className="text-xs text-gray-600">Next.js 16.2 · React 19 · TypeScript 5</p>`
    No `"use client"` directive (it's a server component). No event handlers. No fetch calls.
    Create or update `apps/web/README.md` with: (a) project header noting this is the Phase 1 stub; (b) explicit note that Tailwind v3 is pinned per RESEARCH.md Open Question O-1 with a 1-sentence rationale (v4 still maturing; v3 is more stable in 2026; revisit at Phase 7 polish); (c) dev commands `pnpm --filter web dev`, `pnpm --filter web build`, `pnpm --filter web exec tsc --noEmit`; (d) one line noting Phase 3 will add the real /dashboard with Lightweight Charts.
    Do NOT touch `package.json` (Plan 01 owned that), `tailwind.config.ts`, or `postcss.config.mjs` here — only the page + README.
  </action>
  <verify>
    <automated>pnpm --filter web build; pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>
    `pnpm --filter web build` exits 0; `pnpm --filter web exec tsc --noEmit` exits 0; `grep -n "ES Futures Trading System" apps/web/app/page.tsx` matches; `grep -nE "Tailwind\s*v?3" apps/web/README.md` matches (Tailwind v3 documented); `grep -n "use client" apps/web/app/page.tsx` returns ZERO matches (server component); the rendered HTML output (from `apps/web/.next/server/app/page.html` if produced, or the build log) contains the literal text `Phase 1: Foundation scaffold`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full Phase 1 acceptance smoke + ROADMAP/STATE updates</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 1: Foundation + Data In" (lines 56–77) — Goal, Success Criteria, Notes, Plans placeholder
    - .planning/STATE.md current contents — `current_position` + `progress` block
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §"Required gate before phase close" (lines 1632–1644) — the 9-point gate checklist
    - .planning/phases/01-foundation-data-in/01-VALIDATION.md §"Validation Sign-Off" — final checklist for the plan-checker
  </read_first>
  <behavior>
    - Full acceptance smoke (from repo root): `uv sync && uv run pytest -q && pnpm --filter web build && pnpm --filter web exec tsc --noEmit && uv run pre-commit run --all-files` exits 0 for all five commands
    - ROADMAP.md `Phase 1` section shows `Plans: 6/TBD plans` → `Plans: 6/6 plans complete` once all SUMMARY files exist, OR `0/6 plans complete` if executor has not started (planner sets it to `6` total, status `Not started → In progress`)
    - ROADMAP.md Plans list under Phase 1 enumerates the 6 plans (`01-01-PLAN.md` through `01-06-PLAN.md`) with one-line objectives
    - STATE.md `Current Position` advances to `Phase: 1` and `Status: Phase 1 plans authored, ready for execution`
    - STATE.md `Recent decisions` mentions the 6-plan / 3-wave structure
  </behavior>
  <action>
    Update `.planning/ROADMAP.md`: in the `### Phase 1: Foundation + Data In` block, replace `**Plans:** 0/TBD plans` (or whatever the placeholder reads) with `**Plans:** 6 plans` and below it write the bullet list:
    - `- [ ] 01-01-PLAN.md — Workspace scaffold + uv/pnpm install + Next.js stub (Wave 1)`
    - `- [ ] 01-02-PLAN.md — Domain models + 4 Protocols + Settings + structlog (Wave 2)`
    - `- [ ] 01-03-PLAN.md — Calendars (RTH/rollover/gap) + EventBus + DST fixtures (Wave 2)`
    - `- [ ] 01-04-PLAN.md — Storage (DuckDB + Parquet + runs) + Twelve Data + TradingView adapters (Wave 2)`
    - `- [ ] 01-05-PLAN.md — seed_bars.py CLI + pre-commit hooks + .gitleaks.toml (Wave 3)`
    - `- [ ] 01-06-PLAN.md — FastAPI shell + apps/web finalize + Phase 1 acceptance smoke (Wave 4)`
    Update the `## Progress` table — Phase 1 row should show `Plans Complete: 0/6` and `Status: Not started` (execute-phase will flip it as plans complete). Keep all other phase entries unchanged.
    Update `.planning/STATE.md`: frontmatter `current_position` to `Phase: 1`, `stopped_at: "Phase 1 plans authored, ready for execution"`, `last_activity: "2026-05-14 — Phase 1 planning complete (6 plans, 3-wave structure)"`. The Accumulated Context > Decisions section gets one new bullet: `- Phase 1 plan: 6 plans across 3 waves (1 scaffold → 3 parallel core/calendars/storage → 1 CLI+hooks → 1 closure). See .planning/phases/01-foundation-data-in/01-*-PLAN.md.`
    Then run the FULL acceptance smoke from repo root (use the exact command chain in `<behavior>`). All five commands MUST exit 0. If `pre-commit run --all-files` fires on any pre-existing file (e.g., a stray naive datetime in a `.planning/` doc), fix the underlying issue — do NOT silence the hook. The smoke must be reproducible by the phase verifier without any prior cleanup. Do NOT run `seed_bars.py` against the live Twelve Data API here — that consumes operator credits; the integration test in Plan 05 already proves the pipeline via respx mocks.
  </action>
  <verify>
    <automated>uv sync; uv run pytest -q; pnpm --filter web build; pnpm --filter web exec tsc --noEmit; uv run pre-commit run --all-files</automated>
  </verify>
  <done>
    All five acceptance smoke commands exit 0; ROADMAP.md shows `**Plans:** 6 plans` for Phase 1 with the 6-item bullet list; STATE.md `Current Position` reads `Phase: 1`; STATE.md `last_updated` ISO is fresh; `grep -nE "01-0[1-6]-PLAN\.md" .planning/ROADMAP.md` matches all 6 plan filenames; `git status` shows ROADMAP.md + STATE.md as modified (and any test/fix files as expected by plans 01–05) but no spurious untracked files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI shell → trading_core import graph | Proves Phase 3 will be able to import from trading_core into the api package without circular-import issues |
| Next.js stub → no external API | Phase 1 page makes no fetch calls; Phase 3 will wire to /bars |
| Acceptance smoke → operator's local repo state | Reproducible without prior cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | FastAPI shell exposing endpoints beyond /health by accident | mitigate | `grep -c "@app\.(get|post|put|delete|websocket)" packages/api/src/api/app.py` must equal 1 (Task 1 done-condition) |
| T-01-06-02 | Information Disclosure | /health endpoint leaking Settings values | mitigate | Endpoint returns only literal status/service/version; no env-derived fields |
| T-01-06-03 | Tampering | Acceptance smoke silenced by skipping pre-commit | mitigate | Task 3 explicitly runs `pre-commit run --all-files`; --no-verify not permitted per CLAUDE.md GSD workflow |
| T-01-06-04 | DoS | Background `pnpm build` consuming the operator's machine | accept | Phase 1 stub is tiny; build time < 30s on Windows |
</threat_model>

<verification>
After all 3 tasks complete (Phase 1 closure):
1. `uv sync` from a clean clone produces `.venv/` with all required pins (FND-01, FND-02)
2. `uv run pytest -q` collects and passes every test from Plans 01–06 (3 trivial imports + ~25–35 real tests)
3. `pnpm --filter web build` exits 0; `pnpm --filter web exec tsc --noEmit` exits 0
4. `uv run pre-commit run --all-files` exits 0
5. FastAPI `/health` endpoint reachable via TestClient
6. ROADMAP.md + STATE.md reflect the 6-plan structure and Phase 1 in-progress status
7. The verifier can spot-check every ROADMAP Phase 1 success criterion (1, 2, 3, 4) against existing artifacts:
   - #1: `cat uv.lock | grep -E "(vectorbt|pandas|fastapi|pydantic|duckdb|structlog|httpx|pytest|hypothesis|respx|freezegun)"` matches all 11 pins
   - #2: documented via Plan 05's `test_seed_bars_e2e.py` (no live network run required for verification)
   - #3: documented via Plan 03's DST fixtures + Plan 05's pre-commit hook tests
   - #4: documented via Plan 04's `test_runs.py` + Plan 02's `test_instruments.py` (instruments.py is the only file with tick_value Decimals)
</verification>

<success_criteria>
Plan 01-06 (and Phase 1 overall) is complete when:
- FastAPI shell at packages/api/src/api/app.py exposes /health and imports trading_core (proves FND-01 import graph)
- apps/web/app/page.tsx renders the placeholder; pnpm build + tsc both succeed on Windows
- Full acceptance smoke (uv sync → pytest → pnpm build → pnpm tsc → pre-commit run) exits 0
- ROADMAP.md lists the 6 plans with one-line objectives; STATE.md current position = Phase 1
- No stray endpoints in the FastAPI shell beyond /health
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-in/01-06-SUMMARY.md` documenting: total Phase 1 wall-clock duration (sum across all 6 SUMMARYs), the FastAPI startup smoke result (uvicorn dry-run if attempted), the final acceptance-smoke command output, and an explicit statement that all 18 phase REQ-IDs (FND-01..09 + MD-01..09) are addressed — with which plan implemented each. This becomes the input to `/gsd-verify-work` Phase 1 closure.
</output>
