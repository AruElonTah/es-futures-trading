---
phase: 01-foundation-data-in
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02-PLAN.md
  - 01-03-PLAN.md
  - 01-04-PLAN.md
files_modified:
  - scripts/seed_bars.py
  - scripts/hooks/no_naive_tz.py
  - .pre-commit-config.yaml
  - .gitleaks.toml
  - packages/trading-core/tests/integration/__init__.py
  - packages/trading-core/tests/integration/test_seed_bars_e2e.py
  - packages/trading-core/tests/integration/test_pre_commit_hooks.py
  - packages/trading-core/tests/fixtures/bad_naive_datetime.py
  - packages/trading-core/tests/fixtures/bad_api_key.py
autonomous: true
requirements:
  - FND-04
  - FND-05
  - MD-09
must_haves:
  truths:
    - "`python scripts/seed_bars.py --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03 --provider twelvedata` exits 0, populates `bars`, writes a `runs` row with all 10 fields populated"
    - "Running the same command a second time produces ZERO new bar rows and zero data divergence (idempotency)"
    - "`bar_gaps` is populated if any intra-RTH bars are missing from the provider response"
    - "`rollover_seam` column is True on 3rd-Friday-of-quarter test bars; the seed pipeline calls RolloverDetector.annotate before upsert"
    - "`runs.adr_hash` matches `sha256(.planning/decisions/0001-data-provider.md)` byte-for-byte"
    - "Pre-commit hook `gitleaks` REJECTS a commit containing a fake API key string (FND-04)"
    - "Pre-commit hook `no-naive-tz` REJECTS a commit containing `datetime.now()` without a `tz=` kwarg (FND-05)"
    - "`.gitleaks.toml` allowlist permits the `<TWELVEDATA_API_KEY>` redaction sentinel — gitleaks does NOT fire on the Phase 0 spike artifact"
  artifacts:
    - path: "scripts/seed_bars.py"
      provides: "CLI command for backfilling bars to DuckDB + Parquet through configured DataSource; idempotent"
      contains: "asyncio.run(main"
    - path: "scripts/hooks/no_naive_tz.py"
      provides: "AST-based pre-commit hook forbidding datetime.now()/utcnow() without tz kwarg"
      contains: "ast.walk"
    - path: ".pre-commit-config.yaml"
      provides: "gitleaks + no-naive-tz hook configuration"
      contains: "no-naive-tz"
    - path: ".gitleaks.toml"
      provides: "Gitleaks allowlist for the <TWELVEDATA_API_KEY> sentinel"
      contains: "<TWELVEDATA_API_KEY>"
  key_links:
    - from: "scripts/seed_bars.py"
      to: "trading_core.storage.duckdb_store.DuckDBStore"
      via: "composes the upsert+gap+rollover+runs pipeline"
      pattern: "DuckDBStore"
    - from: "scripts/seed_bars.py"
      to: "trading_core.data.twelvedata.TwelveDataSource"
      via: "PROVIDERS registry maps --provider arg to adapter class"
      pattern: "PROVIDERS"
    - from: ".pre-commit-config.yaml"
      to: "scripts/hooks/no_naive_tz.py"
      via: "local hook entry calls the AST scanner"
      pattern: "no-naive-tz"
    - from: ".pre-commit-config.yaml"
      to: ".gitleaks.toml"
      via: "gitleaks hook reads custom config + allowlist"
      pattern: "gitleaks"
---

<objective>
Close the data-ingest loop. Compose Plan 02 (Settings/logging/Bar/Protocol) + Plan 03 (RthFilter/Rollover/GapDetector/EventBus) + Plan 04 (DuckDBStore/runs/adapters) into a working `seed_bars.py` CLI, AND ship the two pre-commit hooks (no-naive-tz + gitleaks) plus `.gitleaks.toml` allowlist that gate every future commit.

Purpose: Implements MD-09 (`seed_bars.py` CLI backfill), FND-04 (gitleaks pre-commit), FND-05 (UTC discipline via no-naive-tz hook).

Output: The ROADMAP Phase 1 success criterion #2 is testable end-to-end — running the seed command produces a populated `bars` + `bar_gaps` + `runs` triple, and idempotent re-run produces zero new rows. Success criterion #3 (pre-commit rejects naive datetime + fake API key) is satisfied by the two hooks landing here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-in/01-CONTEXT.md
@.planning/phases/01-foundation-data-in/01-RESEARCH.md
@.planning/phases/01-foundation-data-in/01-VALIDATION.md
@.planning/phases/01-02-SUMMARY.md
@.planning/phases/01-03-SUMMARY.md
@.planning/phases/01-04-SUMMARY.md
@.planning/decisions/0001-data-provider.md
@.planning/research/spike-0/spy-bar-budget.md
@CLAUDE.md

<interfaces>
<!-- Composed surface — all imports are from prior plans -->

From Plan 02:
  - trading_core.config.Settings
  - trading_core.logging.setup_logging, get_logger, correlation_id
  - trading_core.data.protocols.DataSource, DataSourceUnavailable, RateLimited, GapDetected
  - trading_core.data.models.Bar
  - trading_core.events.models (TOPIC_DEGRADED_STATE constants)

From Plan 03:
  - trading_core.calendars.rth.RthFilter, RolloverDetector, expected_rth_timestamps, find_gaps
  - trading_core.events.bus.EventBus

From Plan 04:
  - trading_core.storage.duckdb_store.DuckDBStore
  - trading_core.storage.runs.new_run_id, git_sha, adr_hash, param_hash, data_hash
  - trading_core.data.twelvedata.TwelveDataSource
  - trading_core.data.tradingview.TradingViewDataSource

seed_bars.py CLI public surface:
  python scripts/seed_bars.py --symbol {ES,MES,SPY} --tf {1m,5m,15m} --from <iso-date> --to <iso-date> [--provider {twelvedata,tradingview}] [--seed <int>]
  Exit codes: 0 = ok, 1 = failed, 2 = partial (some gaps but data otherwise loaded)

Pre-commit hook CLI shapes:
  - scripts/hooks/no_naive_tz.py <files...>  # exits 1 if any file has datetime.now()/utcnow() without tz= kwarg
  - gitleaks (vendored binary via pre-commit framework): scans staged content against .gitleaks.toml rules
</interfaces>

<canonical_skeletons>
RESEARCH.md verbatim code (line refs):
- seed_bars.py top-level shape — lines 849–933
- no_naive_tz.py AST scanner — lines 1037–1077
- .pre-commit-config.yaml — lines 1204–1218
- .gitleaks.toml — lines 1223–1233
Use these verbatim as the starting point; the seed_bars.py pipeline composes all prior plans' classes — do not invent new abstractions here.
</canonical_skeletons>

<pitfall_callouts>
- RESEARCH.md §Pitfall 5 (lines 1012–1017): `seed_bars.py` MUST reconfigure sys.stdout/stderr to UTF-8 at script entry. The script line 862–864 in RESEARCH.md is the canonical pattern. This is belt-and-braces with the `setup_logging` reconfigure from Plan 02 — keep both because the script may print before logging is configured.
- RESEARCH.md §Pitfall 7 (lines 1025–1029): gitleaks WILL false-positive on the literal `<TWELVEDATA_API_KEY>` sentinel string committed in `.planning/research/spike-0/twelvedata-probe.json` AND in `.env.example` unless `.gitleaks.toml` allowlists it.
- RESEARCH.md §Pitfall 8 (lines 1031–1078): the no-naive-tz hook MUST be AST-based, NOT regex-based. Regex matches comments and docstrings producing useless false positives.
- The CLI integration test runs against the live Twelve Data API → DO NOT include it in the default test suite (it consumes API credits + needs a network). Mark it `@pytest.mark.live` and skip by default. Use respx-mocked tests for the test_seed_bars_e2e suite.
</pitfall_callouts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AST-based no-naive-tz pre-commit hook + .pre-commit-config.yaml + .gitleaks.toml + reject-fixtures</name>
  <files>scripts/hooks/no_naive_tz.py, .pre-commit-config.yaml, .gitleaks.toml, packages/trading-core/tests/integration/__init__.py, packages/trading-core/tests/integration/test_pre_commit_hooks.py, packages/trading-core/tests/fixtures/bad_naive_datetime.py, packages/trading-core/tests/fixtures/bad_api_key.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pitfall 8 (lines 1031–1078) — AST scanner reference
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §".pre-commit-config.yaml" (lines 1201–1218)
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §".gitleaks.toml" (lines 1220–1233)
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pitfall 7 — sentinel allowlist rationale
    - .planning/REQUIREMENTS.md §FND-04 (gitleaks), §FND-05 (no-naive-tz lint)
  </read_first>
  <behavior>
    - `python scripts/hooks/no_naive_tz.py path/to/clean.py` exits 0 (clean file)
    - `python scripts/hooks/no_naive_tz.py packages/trading-core/tests/fixtures/bad_naive_datetime.py` exits 1 AND prints a line `<path>:<lineno>: datetime.now() without tz= produces naive timestamp`
    - File `bad_naive_datetime.py` contains exactly: `from datetime import datetime\ndef bad(): return datetime.now()\ndef also_bad(): return datetime.utcnow()` — both must be flagged
    - `from datetime import datetime; datetime.now(tz=timezone.utc)` does NOT trigger
    - `# comment about datetime.now()` does NOT trigger (regex would; AST does not)
    - `"docstring mentioning datetime.now()"` does NOT trigger
    - `pre-commit run no-naive-tz --files packages/trading-core/tests/fixtures/bad_naive_datetime.py` exits 1
    - `pre-commit run gitleaks --files packages/trading-core/tests/fixtures/bad_api_key.py` exits 1 (the fixture contains a synthetic-but-realistic API key string — e.g., a 32-char hex)
    - `pre-commit run gitleaks --all-files` exits 0 against the existing repo (allowlist for `<TWELVEDATA_API_KEY>` sentinel in `.env.example` + `.planning/research/spike-0/twelvedata-probe.json` works)
  </behavior>
  <action>
    Author `scripts/hooks/no_naive_tz.py` verbatim from RESEARCH.md Pitfall 8 lines 1037–1077. Use `ast.parse(path.read_text(encoding="utf-8"))`; walk for `ast.Call` nodes; match `func.attr in ("now", "utcnow")` AND `func.value` is `ast.Name(id="datetime")`; flag `utcnow()` unconditionally (deprecated in 3.12); flag `now()` only when there are no positional args AND no `tz=` kwarg. Add a `__main__` block that takes file paths from argv and prints `path:lineno: message` for each violation; exits 1 if any violation, 0 if clean. Author `.pre-commit-config.yaml` verbatim from RESEARCH.md lines 1204–1218: gitleaks repo `https://github.com/gitleaks/gitleaks` rev `v8.24.2` + local hook for no-naive-tz with `entry: python scripts/hooks/no_naive_tz.py`, `language: python`, `types: [python]`, `require_serial: true`. Author `.gitleaks.toml` verbatim from RESEARCH.md lines 1223–1233: `[[allowlists]]` block with `regexes = ['<TWELVEDATA_API_KEY>']` and `paths = ['.env.example', '.planning/research/spike-0/twelvedata-probe.json']`. Author the two test fixture files: `bad_naive_datetime.py` (the literal-must-trigger content from `<behavior>`) and `bad_api_key.py` (contains a fake key like `TWELVEDATA_API_KEY=abc123def456ghi789jkl012mno345pq` — a 32-char string that matches gitleaks's generic-api-key regex). Write `tests/integration/test_pre_commit_hooks.py` running each hook via `subprocess.run`: (a) no-naive-tz on the bad fixture exits 1; (b) no-naive-tz on a clean fixture exits 0; (c) running `pre-commit run gitleaks --files <bad_api_key path>` exits non-zero (gitleaks rejects); (d) running `pre-commit run gitleaks --all-files` exits 0 (allowlist works against existing spike-0 artifact). Note: `pre-commit` must be initialized — Task 1 also runs `uv run pre-commit install` once to install the .git/hooks/pre-commit shim. Use `monkeypatch.setenv("PRE_COMMIT_HOME", str(tmp_path))` in the test to isolate the pre-commit cache. If subprocess invocation of `pre-commit` is too slow for the test suite, an acceptable fallback is calling `no_naive_tz.py`'s `main(argv)` function directly in-process for parts (a) + (b) — but (c) and (d) MUST go through `pre-commit run` since they exercise the gitleaks integration. Mark test_pre_commit_hooks tests with `@pytest.mark.integration` if they exceed 5s wall-clock; the default `pytest -q` from repo root should still run them.
  </action>
  <verify>
    <automated>uv run python scripts/hooks/no_naive_tz.py packages/trading-core/tests/fixtures/bad_naive_datetime.py; if [ $? -eq 1 ]; then echo "hook correctly rejected"; fi; uv run pytest packages/trading-core/tests/integration/test_pre_commit_hooks.py -q</automated>
  </verify>
  <done>
    All test_pre_commit_hooks tests pass; `uv run python scripts/hooks/no_naive_tz.py packages/trading-core/tests/fixtures/bad_naive_datetime.py` exits 1 with the expected message; `uv run pre-commit run --all-files` exits 0 against the current repo (allowlist works); `grep -n "<TWELVEDATA_API_KEY>" .gitleaks.toml` matches; `grep -n "no-naive-tz" .pre-commit-config.yaml` matches; `grep -nE "rev:\s*v8\.24\.2" .pre-commit-config.yaml` matches (gitleaks version pinned per RESEARCH.md Standard Stack).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed_bars.py CLI composition + integration test (respx-mocked end-to-end)</name>
  <files>scripts/seed_bars.py, packages/trading-core/tests/integration/test_seed_bars_e2e.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pattern 8 (lines 849–940) — seed_bars.py top-level shape
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §Claude's Discretion paragraph 3 — CLI ergonomics (`scripts/seed_bars.py --symbol SPY --tf 1m --from 2024-01-01 --to 2024-02-01 [--provider twelvedata]`)
    - .planning/REQUIREMENTS.md §MD-09 (CLI backfills history through configured DataSource), §FND-08 (runs row with hashes)
    - All prior plan SUMMARYs for the composed classes' public surfaces
  </read_first>
  <behavior>
    - CLI arg parse: `--symbol` choices=[ES,MES,SPY]; `--tf` choices=[1m,5m,15m]; `--from`/`--to` parsed as tz-aware UTC datetimes (interpret ISO 8601 date strings as 00:00 UTC); `--provider` optional choices=[twelvedata,tradingview], defaults to Settings.default_provider; `--seed` int default 42
    - sys.stdout/stderr reconfigured to UTF-8 errors=replace as the FIRST script action (Pitfall 5)
    - `setup_logging(settings.audit_log_dir)` called immediately after
    - `run_id = new_run_id()`; logger bound with run_id, symbol, tf
    - Pipeline order: fetch_bars (from selected adapter) → RthFilter.filter → RolloverDetector.annotate → DuckDBStore.upsert_bars → find_gaps + DuckDBStore.upsert_gaps → DuckDBStore.write_run with status=ok|failed|partial
    - On adapter exception: status='failed', notes=`f"{type(exc).__name__}: {exc}"`, the runs row is still written, exit code 1
    - On unexpected exception inside pipeline: same — finally-block guarantees the runs row gets written
    - Idempotency: running the SAME command twice in succession results in 2 runs rows but ZERO net new bars rows; the data_hash in the second runs row equals the data_hash in the first
    - exit 0 on success; exit 1 on failure; exit 2 if status='partial' (bars loaded but len(gaps) > 0 — design decision per CLI ergonomics)
    - `runs.adr_hash == sha256(.planning/decisions/0001-data-provider.md bytes)` for every run
  </behavior>
  <action>
    Author `scripts/seed_bars.py` verbatim from RESEARCH.md Pattern 8 lines 851–933, with these refinements: (a) `PROVIDERS = {"twelvedata": TwelveDataSource, "tradingview": TradingViewDataSource}`; (b) the script's FIRST action is the stdout/stderr UTF-8 reconfigure (Pitfall 5); (c) BOTH adapters require Settings; TradingViewDataSource also needs an EventBus — construct an `EventBus()` instance and pass it; (d) the `--from`/`--to` parser produces tz-aware UTC: `lambda s: datetime.fromisoformat(s).replace(tzinfo=timezone.utc)`; (e) inside the try block, status starts as "ok" — if `len(gaps) > 0` flip to "partial" before the finally block; (f) `await source.aclose()` if the adapter has one (TwelveDataSource opens an httpx.AsyncClient); (g) exit codes: 0 / 1 / 2 per `<behavior>`. The runs row writer composes ALL plan-04 helpers — `new_run_id()`, `git_sha()`, `adr_hash()`, `param_hash(vars(args))`, `data_hash(df)` (or empty string if df is None on failure). Write `tests/integration/test_seed_bars_e2e.py` using `respx` to mock the Twelve Data `/time_series` endpoint with a deterministic 390-bar response for SPY 1m on 2024-01-02. Use a `tmp_path / "test.duckdb"` for the DuckDBStore so the test does not touch the operator's real DB. Invoke `seed_bars.py` via `subprocess.run([sys.executable, "scripts/seed_bars.py", "--symbol", "SPY", "--tf", "1m", "--from", "2024-01-02", "--to", "2024-01-03", "--provider", "twelvedata"])` with env `DUCKDB_PATH=<tmp_path>` (extend Settings to accept this override, OR use a `--duckdb-path` CLI flag). Tests cover: (a) happy path exits 0; bars table has 390 rows; runs table has 1 row with status='ok' AND `adr_hash` matching `sha256(.planning/decisions/0001-data-provider.md)`; (b) running it again exits 0 (or 2 if partial); bars table STILL has 390 rows; runs table has 2 rows; the second runs row's `data_hash` == first row's `data_hash`; (c) mock `respx` to drop 5 minutes from the response → re-run; `bar_gaps` has 5 rows; the CLI exits 2 (partial); (d) mock `respx` to return HTTP 429 → CLI exits 1; runs row has status='failed' and notes containing 'RateLimited'. Tests must run in <60s wall-clock; mock `asyncio.sleep` to 0 so the 9s pacing doesn't slow the suite. Document the `--duckdb-path` flag (or env override mechanism) in seed_bars.py's `--help`.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/integration/test_seed_bars_e2e.py -q</automated>
  </verify>
  <done>
    test_seed_bars_e2e passes including all 4 cases (happy path, idempotent re-run, partial-with-gaps, adapter failure); `grep -n "sys.stdout.reconfigure" scripts/seed_bars.py` matches at the top of the file (Pitfall 5); `grep -n "PROVIDERS" scripts/seed_bars.py` matches with both `twelvedata` and `tradingview` keys; `grep -n "adr_hash\|param_hash\|data_hash\|new_run_id" scripts/seed_bars.py` matches all four hash helpers; running `uv run python scripts/seed_bars.py --help` exits 0 and prints usage including `--symbol`, `--tf`, `--from`, `--to`, `--provider`, `--seed` (and `--duckdb-path` if that override mechanism was chosen).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → asyncio pipeline | argparse + Pydantic Settings constrain inputs; `--from`/`--to` parsing must produce tz-aware UTC |
| Pre-commit hook → staged content | gitleaks scans staged blobs; must NOT exfiltrate content; runs locally |
| seed_bars.py stdout → operator terminal | UTF-8 reconfigure required for Windows piped-stdout safety |
| Pipeline failure path → runs table | Even on failure, runs row MUST be written (audit trail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | API key in committed file | mitigate | gitleaks pre-commit hook (FND-04) configured with .gitleaks.toml; test fixture `bad_api_key.py` proves it rejects synthetic keys (Task 1) |
| T-01-05-02 | Tampering | Naive datetime entering production code | mitigate | AST-based no-naive-tz hook (FND-05); test fixture `bad_naive_datetime.py` proves rejection (Task 1) |
| T-01-05-03 | Information Disclosure | Phase 0 sentinel triggering gitleaks (false positive) | mitigate | .gitleaks.toml allowlist for `<TWELVEDATA_API_KEY>` + .env.example/twelvedata-probe.json paths (RESEARCH.md Pitfall 7) |
| T-01-05-04 | DoS | Pipeline exception leaves runs row missing → audit chain broken | mitigate | finally-block guarantees DuckDBStore.write_run is called on every code path; test (d) in Task 2 asserts a failed run still writes a status='failed' row |
| T-01-05-05 | Tampering | Pre-commit hook bypassed via `git commit --no-verify` | accept | Documented operator-discipline issue; v2 may add a server-side CI mirror; out of scope for Phase 1 |
| T-01-05-06 | Information Disclosure | Audit log written under a path containing the API key | mitigate | Settings.audit_log_dir defaults to `data/logs/audit` (no env interpolation); inherited mitigation from Plan 04 T-01-04-03 |
</threat_model>

<verification>
After both tasks complete:
1. `uv run pytest packages/trading-core/tests/integration/ -q` — both test files pass
2. `uv run pre-commit run --all-files` exits 0 against the current repo state
3. `uv run pre-commit run no-naive-tz --files packages/trading-core/tests/fixtures/bad_naive_datetime.py` exits 1
4. `uv run pre-commit run gitleaks --files packages/trading-core/tests/fixtures/bad_api_key.py` exits non-zero
5. `uv run python scripts/seed_bars.py --help` exits 0 and shows all required flags
6. ROADMAP Phase 1 success criterion #2 (seed_bars idempotent + bar_gaps populated + rollover_seam) is testable end-to-end via the integration test
7. ROADMAP Phase 1 success criterion #3 (pre-commit rejects naive + fake key) is satisfied
</verification>

<success_criteria>
Plan 01-05 is complete when:
- `seed_bars.py` composes all prior plans' modules into a working CLI (MD-09)
- Idempotent re-run produces zero new bar rows and identical data_hash (ROADMAP #2)
- `runs` row written on every CLI exit (success / failure / partial) with all 10 fields including adr_hash (FND-08, completes the row infra)
- Pre-commit `no-naive-tz` hook rejects `datetime.now()` without tz (FND-05)
- Pre-commit `gitleaks` hook rejects synthetic API key strings AND allowlists the `<TWELVEDATA_API_KEY>` sentinel (FND-04)
- Pipeline test suite runs in <60s wall-clock with no network calls
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-in/01-05-SUMMARY.md` documenting: any pre-commit framework quirks discovered on Windows (path-with-space behavior, gitleaks binary fetch), the exact exit-code mapping decided for partial vs failed (status='partial' → exit 2 OR exit 0 — pick one and document the rationale), and the captured stdout from a happy-path seed_bars run (showing the structlog JSON output via the console renderer).
</output>
