---
phase: 01-foundation-data-in
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - packages/trading-core/src/trading_core/calendars/__init__.py
  - packages/trading-core/src/trading_core/calendars/rth.py
  - packages/trading-core/src/trading_core/events/bus.py
  - packages/trading-core/tests/conftest.py
  - packages/trading-core/tests/fixtures/__init__.py
  - packages/trading-core/tests/fixtures/dst_bars.py
  - packages/trading-core/tests/test_rth_filter.py
  - packages/trading-core/tests/test_rollover_detector.py
  - packages/trading-core/tests/test_gap_detector.py
  - packages/trading-core/tests/test_event_bus.py
autonomous: true
requirements:
  - FND-07
  - MD-05
  - MD-07
  - MD-08
must_haves:
  truths:
    - "`RthFilter.filter(df, symbol='SPY')` excludes every timestamp outside 09:30–16:00 ET for trading days; includes none on weekends; honors CME_Equity half-days (early close)"
    - "DST spring-forward 2026-03-08: RTH window begins at 13:30 UTC (= 09:30 EDT); 390 1m bars expected"
    - "DST fall-back 2026-11-01: RTH window begins at 13:30 UTC (= 09:30 EDT until shift; window after 2 AM ET is at 14:30 UTC = 09:30 EST); 390 bars expected"
    - "CME half-day 2024-11-29 (Black Friday): RthFilter honors early 13:00 ET close"
    - "`RolloverDetector.is_rollover_seam(ts)` returns True on the 3rd Friday of Mar/Jun/Sep/Dec and the trading days before/after; False otherwise"
    - "`GapDetector` produces a list of expected-but-missing ts_utc values inside the RTH window"
    - "`EventBus` supports concurrent subscribers, FIFO per topic, async publish + subscribe"
  artifacts:
    - path: "packages/trading-core/src/trading_core/calendars/rth.py"
      provides: "RthFilter (hybrid CME_Equity calendar + manual 9:30–16:00 ET window), RolloverDetector, GapDetector, helpers trading_days/rth_window_utc/is_rth/expected_rth_timestamps"
      contains: "def is_rth"
    - path: "packages/trading-core/src/trading_core/events/bus.py"
      provides: "EventBus (asyncio.Queue-per-topic pub/sub, FIFO per topic, no backpressure for v1)"
      contains: "class EventBus"
    - path: "packages/trading-core/tests/fixtures/dst_bars.py"
      provides: "Synthetic DST-transition fixtures (2026-03-08 spring forward, 2026-11-01 fall back) + CME half-day 2024-11-29 + rollover-seam 2026-03-20"
  key_links:
    - from: "packages/trading-core/src/trading_core/calendars/rth.py"
      to: "pandas_market_calendars"
      via: "mcal.get_calendar('CME_Equity').schedule(...) — trading-day determination + half-day reads"
      pattern: "mcal\\.get_calendar"
    - from: "packages/trading-core/src/trading_core/calendars/rth.py"
      to: "trading_core.instruments"
      via: "from trading_core.instruments import get — for calendar_name + rth_open_et + rth_close_et"
      pattern: "from trading_core.instruments"
    - from: "packages/trading-core/src/trading_core/events/bus.py"
      to: "asyncio.Queue"
      via: "per-topic queues; FIFO ordering"
      pattern: "asyncio\\.Queue"
---

<objective>
Ship the calendar + RTH window discipline that every Phase 1 ingestion path enforces, plus the asyncio EventBus skeleton Phase 3 will wire. RTH is the load-bearing invariant — get this wrong and every backtest is silently corrupted. This plan delivers Pattern 3 (hybrid CME_Equity filter), Pattern 4 (rollover detector), and Pattern 5 (hand-rolled EventBus) per RESEARCH.md, plus the DST-transition + half-day test fixtures the verification gate demands.

Purpose: Implements MD-05 (CME equity-index calendar including half-days), MD-07 (bar-gap detector), MD-08 (rollover-seam detector), FND-07 (EventBus typed topics; deterministic event ordering).

Output: Plan 04 can construct an `RthFilter(symbol='SPY')`, call `.filter(df)`, and trust the result; Plan 05 can route `BarReceived` events through the EventBus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-in/01-CONTEXT.md
@.planning/phases/01-foundation-data-in/01-RESEARCH.md
@.planning/phases/01-foundation-data-in/01-VALIDATION.md
@.planning/phases/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Public API of trading_core.calendars.rth — Plan 04 and Plan 05 will import these. -->

RthFilter public API:
  - class RthFilter
    - def __init__(self, *, half_day_close_et_default: str | None = None)
    - def filter(self, df: pd.DataFrame, *, symbol: str) -> pd.DataFrame
      (Input: tz-aware UTC DatetimeIndex on df. Output: same DataFrame restricted to rows where is_rth(ts, symbol) is True.)
    - def find_gaps(self, df: pd.DataFrame, symbol: str, timeframe: str, start: datetime, end: datetime) -> list[datetime]
      (Returns the timestamps in expected_rth_timestamps(symbol, timeframe, start, end) that are not present in df.index. List sorted ascending.)

RolloverDetector public API:
  - class RolloverDetector
    - def annotate(self, df: pd.DataFrame) -> pd.DataFrame
      (Adds/overwrites a `rollover_seam: bool` column based on is_rollover_seam(ts_utc) for each index ts; returns the same df.)
    - @staticmethod
    - def is_rollover_seam(ts_utc: datetime) -> bool

Helper functions (module-level, exported):
  - def trading_days(calendar_name: str, start: datetime, end: datetime) -> pd.DatetimeIndex
  - def rth_window_utc(calendar_name: str, trading_day, open_et: str, close_et: str) -> tuple[datetime, datetime]
  - def is_rth(ts_utc: datetime, *, instrument_symbol: str) -> bool
  - def expected_rth_timestamps(symbol: str, timeframe: str, start: datetime, end: datetime) -> pd.DatetimeIndex
  - def third_friday(year: int, month: int) -> date
  - def is_rollover_seam(ts_utc: datetime) -> bool

EventBus public API:
  - class EventBus
    - async def publish(self, topic: str, event: Event) -> None
    - @asynccontextmanager
    - async def subscribe(self, topic: str) -> AsyncIterator[Subscription]
  - class Subscription
    - async def __aiter__(self) -> AsyncIterator[Event]
</interfaces>

<canonical_skeletons>
RESEARCH.md verbatim code (line refs):
- RthFilter + helpers (Pattern 3) — lines 519–604
- RolloverDetector (Pattern 4) — lines 608–635
- EventBus (Pattern 5) — lines 639–688
Use these verbatim as the starting point; if a pandas_market_calendars 5.3.x API surface differs, fix the call site and note the deviation in SUMMARY.
</canonical_skeletons>

<pitfall_callout>
RESEARCH.md §Pitfall 1 (CME_Equity 23-hour session, NOT 9:30–16:00 ET): a naive `mcal.get_calendar("CME_Equity").schedule(...)` returns market_open=18:00 ET previous day, market_close=17:00 ET. The hybrid pattern here uses CME_Equity ONLY for trading-day determination and half-day close detection — the actual RTH window comes from `instruments.py` (`rth_open_et='09:30'`, `rth_close_et='16:00'`). Test fixture: 1m bar at `2024-06-12 13:30 UTC` (= 09:30 ET) MUST appear in RTH; bar at `2024-06-12 02:00 UTC` (= 22:00 ET prev day) MUST NOT.

RESEARCH.md §Pitfall 3 (DST transitions): all bars stored in UTC; ET is a derived view. RTH on 2026-03-08 starts at `13:30 UTC` (= 09:30 EDT) — there are no missing bars even though wall-clock ET jumped 02:00→03:00. RTH on 2026-11-01 starts at `13:30 UTC` (= 09:30 EDT) and ends at `20:00 UTC` (= 16:00 EDT) — DST is still in effect through Nov 1; Nov 2 is the actual fall-back day in 2026 (DST ends 2026-11-01 02:00 ET → wait — confirm via `zoneinfo` before locking the test: in 2026 DST in the US ends Sunday Nov 1 at 02:00 local). The test must call `zoneinfo.ZoneInfo("America/New_York")` to compute the correct UTC offset for each transition day and assert the resulting 390-bar count.
</pitfall_callout>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RthFilter + helpers (hybrid CME_Equity + manual 9:30–16:00 ET window) + DST fixtures</name>
  <files>packages/trading-core/src/trading_core/calendars/__init__.py, packages/trading-core/src/trading_core/calendars/rth.py, packages/trading-core/tests/conftest.py, packages/trading-core/tests/fixtures/__init__.py, packages/trading-core/tests/fixtures/dst_bars.py, packages/trading-core/tests/test_rth_filter.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pattern 3 (lines 519–604) — full rth.py body
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pitfall 1 (lines 985–991) + §Pitfall 3 (lines 999–1005) — the two RTH bugs this module must NOT have
    - .planning/phases/01-foundation-data-in/01-VALIDATION.md §"Required Test Fixtures (load-bearing)" — fixture names + purposes
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §canonical_refs DST dates 2026-03-08 + 2026-11-01
    - .planning/REQUIREMENTS.md §MD-05 (RTH session filter applies CME equity-index calendar including half-days and exchange holidays)
  </read_first>
  <behavior>
    - `is_rth(datetime(2024,6,12,13,30,tzinfo=UTC), instrument_symbol='SPY')` returns True (= 09:30 ET on a regular trading day)
    - `is_rth(datetime(2024,6,12,20,0,tzinfo=UTC), instrument_symbol='SPY')` returns False (= 16:00 ET; close is exclusive)
    - `is_rth(datetime(2024,6,12,2,0,tzinfo=UTC), instrument_symbol='SPY')` returns False (= 22:00 ET previous day — ETH, must be excluded)
    - `is_rth(datetime(2024,7,4,14,30,tzinfo=UTC), instrument_symbol='SPY')` returns False (NYSE July 4 holiday)
    - `is_rth(datetime(2024,11,29,18,30,tzinfo=UTC), instrument_symbol='SPY')` returns False if 13:30 ET is past the half-day close (Black Friday early-close 13:00 ET — schedule shows market_close=13:00; 13:30 ET = 18:30 UTC is past close)
    - `is_rth(datetime(2024,11,29,15,0,tzinfo=UTC), instrument_symbol='SPY')` returns True (10:00 ET, before the early 13:00 ET close)
    - `expected_rth_timestamps('SPY', '1m', date_2024_06_12, date_2024_06_13)` returns exactly 390 UTC timestamps (1 trading day × 390 RTH minutes)
    - `expected_rth_timestamps('SPY', '1m', 2026-03-08, 2026-03-09)` returns 390 timestamps starting at `2026-03-09 13:30 UTC` (Monday after spring forward; Sunday Mar 8 is not a trading day for NYSE)
    - `RthFilter().filter(df_with_eth_bars, symbol='SPY')` removes the ETH rows
    - `RthFilter().find_gaps(df_with_one_missing_bar, 'SPY', '1m', start, end)` returns a list of length 1 containing the missing UTC timestamp
    - Raises `ValueError` if input `ts.tzinfo is None`
  </behavior>
  <action>
    Create `packages/trading-core/src/trading_core/calendars/__init__.py` re-exporting the public API (`RthFilter`, `RolloverDetector`, `is_rth`, `is_rollover_seam`, `trading_days`, `rth_window_utc`, `expected_rth_timestamps`). Author `calendars/rth.py` per RESEARCH.md Pattern 3 lines 519–604 — module-level `ET = ZoneInfo("America/New_York")` and `UTC = ZoneInfo("UTC")`; `trading_days(calendar_name, start, end)` uses `mcal.get_calendar(calendar_name).schedule(start_date=start.date(), end_date=end.date())`; `rth_window_utc(calendar_name, trading_day, open_et, close_et)` constructs naive datetimes from open_et/close_et strings, replaces tzinfo with ET, converts to UTC, AND queries the calendar's schedule for that single day to read `market_close` and clamp the cash close to the schedule's market_close (early-close half-days win); `is_rth(ts_utc, instrument_symbol)` reads inst from `trading_core.instruments`, validates tz-aware input, computes the trading-day membership and the RTH bounds; `expected_rth_timestamps(symbol, timeframe, start, end)` builds the full RTH index using `pd.date_range(o_utc, c_utc, freq={'1m':'1min','5m':'5min','15m':'15min'}[tf], inclusive='left')` per trading day, then concatenates. Add an `RthFilter` thin class wrapping these with `.filter(df, *, symbol)` returning the rows whose index is in RTH and `.find_gaps(df, symbol, tf, start, end)` returning sorted missing timestamps. Author `tests/fixtures/dst_bars.py` exposing factory functions: `make_dst_spring_forward_2026_03_09_bars()` (returns a 390-row DataFrame with synthetic OHLCV at every UTC minute from 13:30 to 19:59 inclusive on 2026-03-09, Monday after DST starts), `make_dst_fall_back_2026_11_02_bars()` (Monday after DST ends — Nov 1 2026 is the DST end Sunday, so use Nov 2 as the trading day), `make_cme_half_day_2024_11_29_bars()` (Black Friday early-close, fewer than 390 bars expected per `expected_rth_timestamps`), `make_synthetic_spy_day_bars(date)` (generic 390-row factory). Author `tests/test_rth_filter.py` covering every behavior bullet — use `freezegun` to anchor dates and `zoneinfo.ZoneInfo` to compute UTC offsets exactly. Cross-check the Nov 1 vs Nov 2 question by computing `ZoneInfo("America/New_York").utcoffset(datetime(2026,11,1,18,0,tzinfo=ZoneInfo("America/New_York")))` in the test and asserting the bar count comes out correctly for whichever day the schedule reports as a trading day. The DST tests MUST cover both `2026-03-08` (spring) and `2026-11-01` (fall) per ROADMAP success criterion #3 — assert the trading-day decisions correctly (Sunday Mar 8 not a trading day; spring forward effect tested on Monday Mar 9; similar for Nov 1/2). Extend `packages/trading-core/tests/conftest.py` with fixtures `dst_spring_forward_2026_03_09` / `dst_fall_back_2026_11_02` / `cme_half_day_thanksgiving_2024_11_29` / `synthetic_spy_day` re-exporting the dst_bars.py factories. Do NOT touch `events/bus.py` (Task 3) or any RolloverDetector code (Task 2).
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_rth_filter.py -q</automated>
  </verify>
  <done>
    All test_rth_filter tests pass; `grep -n "mcal.get_calendar" packages/trading-core/src/trading_core/calendars/rth.py` matches at least twice (`trading_days` + `rth_window_utc`); `grep -n "ZoneInfo" packages/trading-core/src/trading_core/calendars/rth.py` matches; running `uv run python -c "from trading_core.calendars import is_rth, expected_rth_timestamps; from datetime import datetime; from zoneinfo import ZoneInfo; print(is_rth(datetime(2024,6,12,13,30,tzinfo=ZoneInfo('UTC')), instrument_symbol='SPY'))"` prints True.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RolloverDetector (3rd-Friday-of-quarter) + GapDetector wired into RthFilter</name>
  <files>packages/trading-core/src/trading_core/calendars/rth.py, packages/trading-core/tests/test_rollover_detector.py, packages/trading-core/tests/test_gap_detector.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pattern 4 (lines 608–637) — RolloverDetector body
    - .planning/phases/01-foundation-data-in/01-CONTEXT.md §"DuckDB schema + seed_bars.py ergonomics + runs-table scope" — rollover detected via 3rd-Friday-of-quarter, NOT volume-jump heuristic
    - .planning/REQUIREMENTS.md §MD-07 (gap detector writes to bar_gaps), §MD-08 (rollover-seam detector)
    - The rth.py file authored in Task 1 (extend, do not rewrite)
  </read_first>
  <behavior>
    - `third_friday(2026, 3) == date(2026, 3, 20)`; `third_friday(2026, 6) == date(2026, 6, 19)`; `third_friday(2026, 9) == date(2026, 9, 18)`; `third_friday(2026, 12) == date(2026, 12, 18)`
    - `is_rollover_seam(datetime(2026,3,20,14,30,tzinfo=UTC))` returns True (3rd Friday of March)
    - `is_rollover_seam(datetime(2026,3,19,14,30,tzinfo=UTC))` returns True (Thursday before)
    - `is_rollover_seam(datetime(2026,3,23,14,30,tzinfo=UTC))` returns True (Monday after — abs((d - tf).days) <= 1)
    - `is_rollover_seam(datetime(2026,3,17,14,30,tzinfo=UTC))` returns False (Tuesday, > 1 day from Friday Mar 20)
    - `is_rollover_seam(datetime(2026,1,16,14,30,tzinfo=UTC))` returns False (3rd Friday of January — not a quarterly contract month)
    - `RolloverDetector().annotate(df)` adds/overwrites `rollover_seam` column matching `is_rollover_seam(ts)` for every row
    - `is_rollover_seam(datetime(2026,3,20,14,30))` (naive) raises `ValueError`
    - `GapDetector` integration (already on RthFilter): `find_gaps(df_missing_one_bar, 'SPY', '1m', start_2024_06_12, end_2024_06_13)` returns a 1-element list with the missing UTC timestamp; empty list when df is complete
  </behavior>
  <action>
    Extend `calendars/rth.py` with `third_friday(year, month)` (uses `calendar.Calendar().itermonthdates` per RESEARCH.md Pattern 4) and `is_rollover_seam(ts_utc)` per lines 614–634. Author `class RolloverDetector` with `annotate(df: pd.DataFrame) -> pd.DataFrame` that adds/overwrites a `rollover_seam: bool` column by applying `is_rollover_seam` to each timestamp in `df.index` (or to `df['ts_utc']` if `ts_utc` is a column rather than index — accept both shapes; document the expectation that Plan 04's adapter output uses `ts_utc` as the index). The GapDetector function `find_gaps` already exists on `RthFilter` from Task 1 — add additional polish here (e.g., output dataframe shape suitable for `DuckDBStore.upsert_gaps` consumption: a DataFrame with columns `[symbol, timeframe, ts_utc]` for Plan 04 to accept). Write `tests/test_rollover_detector.py` covering every behavior bullet using freezegun-anchored dates. Write `tests/test_gap_detector.py` building two synthetic 1m-day DataFrames — one complete (390 rows) → `find_gaps` returns `[]`; one missing 5 random RTH minutes → `find_gaps` returns those 5 timestamps sorted ascending; one with bars OUTSIDE RTH that look like gaps in ETH → `find_gaps` returns `[]` (we only flag missing RTH bars). All test inputs use tz-aware UTC timestamps.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_rollover_detector.py packages/trading-core/tests/test_gap_detector.py -q</automated>
  </verify>
  <done>
    test_rollover_detector + test_gap_detector pass; `grep -n "third_friday\|is_rollover_seam" packages/trading-core/src/trading_core/calendars/rth.py` matches at least 2 occurrences each; running `uv run python -c "from trading_core.calendars import is_rollover_seam; from datetime import datetime; from zoneinfo import ZoneInfo; print(is_rollover_seam(datetime(2026,3,20,14,30,tzinfo=ZoneInfo('UTC'))))"` prints True.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EventBus (asyncio in-process pub/sub) + FIFO + topic constants smoke</name>
  <files>packages/trading-core/src/trading_core/events/bus.py, packages/trading-core/tests/test_event_bus.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §Pattern 5 (lines 639–697) — full bus.py body + topic constants list
    - .planning/phases/01-foundation-data-in/01-RESEARCH.md §"broadcaster REJECTED" — alternatives-considered (do NOT add broadcaster as a dep)
    - .planning/REQUIREMENTS.md §FND-07 (asyncio in-process pub/sub with deterministic ordering)
    - packages/trading-core/src/trading_core/events/models.py (authored in Plan 02 Task 2) for Event + BarReceived + DegradedStateEvent + TOPIC_* constants
  </read_first>
  <behavior>
    - `EventBus.publish('bars', BarReceived(...))` delivers the event to every active subscriber on 'bars'
    - Two subscribers on the same topic both receive every published event, in publish order (FIFO per topic)
    - A subscriber on a different topic does NOT receive the event
    - `async with bus.subscribe('bars') as sub:` then `async for ev in sub:` yields events; exiting the context manager removes the subscription queue
    - Multiple publishers concurrently calling `publish` on the same topic produce a deterministic FIFO order under the asyncio event loop (the bus uses an internal lock to serialize the subscriber-list snapshot)
    - Late subscribers do NOT receive events published before they subscribed (best-effort live; no replay buffer in v1)
    - No backpressure: queues are unbounded for v1 (RESEARCH.md Pattern 5)
  </behavior>
  <action>
    Author `packages/trading-core/src/trading_core/events/bus.py` verbatim from RESEARCH.md Pattern 5 lines 645–688: imports `asyncio`, `defaultdict`, `asynccontextmanager`, `AsyncIterator`; `class EventBus` with `_subscribers: dict[Topic, list[asyncio.Queue[Event]]]` + `_lock: asyncio.Lock`; `async def publish(self, topic, event)` snapshots queues under lock then awaits `q.put(event)` for each; `@asynccontextmanager async def subscribe(self, topic)` creates a new queue, registers it, yields a `Subscription`, removes on exit; `class Subscription` with `async def __aiter__(self)` looping `yield await self._q.get()`. Update `events/__init__.py` to re-export `EventBus` and `Subscription` alongside the models from Plan 02. Write `tests/test_event_bus.py` covering every behavior using `pytest-asyncio` (asyncio_mode=auto is set in root pyproject.toml from Plan 01). Tests must include: (a) single publish, single subscriber receives; (b) two subscribers on same topic both receive (use `asyncio.gather` to start both subscribers concurrently); (c) subscriber on different topic does NOT receive; (d) publish-publish-subscribe sequence shows the late subscriber receives ZERO events (no replay buffer); (e) FIFO order — publish events 1..10 on one topic, subscriber pulls them in order. Use `BarReceived` from `trading_core.events.models` as the test event payload.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_event_bus.py -q</automated>
  </verify>
  <done>
    test_event_bus passes; `grep -n "asyncio.Queue\|asynccontextmanager" packages/trading-core/src/trading_core/events/bus.py` matches both; `grep -rn "broadcaster" packages/trading-core/` returns ZERO matches (anti-dep gate); `uv run python -c "from trading_core.events.bus import EventBus, Subscription; print('OK')"` prints OK; `uv run python -c "from trading_core.events import EventBus, BarReceived, TOPIC_BARS; print('OK')"` prints OK (the public re-exports work).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTH filter → downstream backtest | Wrong RTH window = silent backtest corruption (RESEARCH.md Pitfall 1) |
| pandas_market_calendars schedule output → RTH window construction | Half-day reads must be correct or early-close days produce wrong gap counts |
| EventBus → bar / signal / fill consumers (Phase 3+) | Event ordering must be FIFO per topic; out-of-order = silent strategy bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | RTH window construction using Globex 23-hour schedule | mitigate | Hybrid filter pattern — CME_Equity schedule used only for trading-day determination + half-day reads; cash-session times read from instruments.py (RESEARCH.md Pitfall 1) |
| T-01-03-02 | Tampering | DST transition producing duplicate/missing bars | mitigate | All timestamps in UTC; ET derived via zoneinfo; DST test fixtures for 2026-03-08 + 2026-11-01 lock the expected 390-bar count (RESEARCH.md Pitfall 3) |
| T-01-03-03 | Tampering | Naive datetime entering is_rth/is_rollover_seam | mitigate | Both functions raise `ValueError` on `ts.tzinfo is None`; Plan 02's pre-commit no-naive-tz hook adds belt-and-braces enforcement |
| T-01-03-04 | DoS | Unbounded EventBus queue under runaway producer | accept | Phase 1 ships unbounded queues per RESEARCH.md Security Domain (single-operator low rate); documented in bus.py docstring; Phase 5/7 may add bounded queues if needed |
| T-01-03-05 | Information Disclosure | Event payload containing API keys | accept | No event type in Phase 1 carries secrets; Bar/DegradedStateEvent contain only ts/price/source name |
</threat_model>

<verification>
After all 3 tasks complete:
1. `uv run pytest packages/trading-core/tests/test_rth_filter.py packages/trading-core/tests/test_rollover_detector.py packages/trading-core/tests/test_gap_detector.py packages/trading-core/tests/test_event_bus.py -q` — all pass
2. DST tests for 2026-03-08 (spring forward) and 2026-11-01 (fall back) both green (ROADMAP success criterion #3)
3. CME half-day test (2024-11-29 Black Friday early-close at 13:00 ET) honored — RthFilter returns < 390 bars for that day
4. `grep -rn "broadcaster" packages/trading-core/` returns zero matches
5. `is_rth` / `is_rollover_seam` both reject naive timestamps with `ValueError`
6. `RthFilter` and `EventBus` are importable via documented public paths
</verification>

<success_criteria>
Plan 01-03 is complete when:
- RthFilter applies CME_Equity calendar (trading-day determination + half-day reads) + manual 9:30–16:00 ET window from instruments.py (MD-05)
- DST test fixtures for 2026-03-08 and 2026-11-01 pass (ROADMAP success criterion #3)
- RolloverDetector flags 3rd-Friday-of-quarter and adjacent trading days (MD-08)
- GapDetector returns sorted-ascending missing RTH timestamps (MD-07)
- EventBus delivers events in FIFO order per topic to all live subscribers (FND-07)
- No naive datetimes anywhere in this module's code
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-in/01-03-SUMMARY.md` documenting: any pandas_market_calendars 5.3.x API quirks discovered (e.g., schedule columns named `market_close` vs `MarketClose`), the exact UTC offset behavior of `ZoneInfo("America/New_York")` for the 2026-03-08 and 2026-11-01 fixture days, and a one-line confirmation that `RthFilter().filter(eth_df)` correctly returns empty when fed only ETH bars.
</output>
