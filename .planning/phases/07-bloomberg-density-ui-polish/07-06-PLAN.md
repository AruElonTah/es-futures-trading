---
phase: "07"
plan: "06"
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - .planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md
autonomous: false
requirements:
  - UI-02
  - UI-03
  - UI-06
  - UI-07
  - SP-06

must_haves:
  truths:
    - "All 4 panes visible at /dashboard at 1440px+ width with correct labels"
    - "Drag-resize handles function correctly for both vertical and horizontal splits"
    - "localStorage layout persists across page reloads"
    - "/dashboard/blotter redirects to /dashboard"
    - "WS ConnectionStatus indicator transitions on uvicorn kill and recover"
    - "Params saved transient text appears in green for ~2s after Save & Hot-reload"
    - "Run Backtest button disables during polling and re-enables on completion"
    - "Playwright E2E test ws-reconnect.spec.ts passes with live servers"
  artifacts:
    - path: ".planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md"
      provides: "Completed UAT record with all 8 tests marked passed/failed"
      contains: "passed: 8"
  key_links:
    - from: "uvicorn (port 8000)"
      to: "Next.js dev server (port 3000)"
      via: "API_BASE env var or default localhost:8000"
      pattern: "localhost:8000"
    - from: "Playwright test"
      to: "live servers"
      via: "pnpm --filter web test:e2e"
      pattern: "ws-reconnect.spec.ts"
---

<objective>
Execute the 8 human UAT tests that were auto-approved (bypassed) during Plan 07-04's blocking checkpoint.

Purpose: Plan 07-04 declared `gate="blocking"` on its human-verify checkpoint, but `workflow.auto_advance=true` caused it to be skipped without actual human confirmation. The 07-VERIFICATION.md scored 18/18 on static analysis but marked status: human_needed. These 8 tests require a running browser and live servers — they cannot be confirmed programmatically.

Output:
- 07-HUMAN-UAT.md updated with actual pass/fail results for all 8 tests
- Phase 7 fully signed off after all 8 pass (or issues documented for gap closure)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md
@.planning/phases/07-bloomberg-density-ui-polish/07-VERIFICATION.md
@.planning/phases/07-bloomberg-density-ui-polish/07-04-SUMMARY.md
@config/strategies/orb.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start servers (uvicorn + Next.js)</name>
  <files>.planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md</files>

  <read_first>
    - apps/api/src/api/app.py (or packages/api/src/api/app.py) — confirm uvicorn entry point
    - apps/web/package.json — confirm "dev" script and port (should be 3000)
    - config/strategies/orb.yaml — confirm ORB params exist for UAT test 6 (opening_range_minutes, atr_stop_mult, etc.)
  </read_first>

  <action>
    Start the two required servers in separate terminals. Both must be running before any UI tests proceed.

    Terminal 1 — FastAPI backend:
    ```
    cd "C:\Users\Admin\Desktop\Day Trading"
    uv run uvicorn api.app:app --reload --port 8000
    ```
    Wait for: "Application startup complete." in the uvicorn log.

    Terminal 2 — Next.js frontend:
    ```
    cd "C:\Users\Admin\Desktop\Day Trading"
    pnpm --filter web dev
    ```
    Wait for: "Ready in" or "Local: http://localhost:3000" in the Next.js output.

    Smoke-check both servers are up:
    - curl http://localhost:8000/health should return {"status":"ok"} or similar
    - curl http://localhost:3000 should return HTML (200)

    Update the "## Current Test" section of 07-HUMAN-UAT.md to: "Servers started — running UAT"
  </action>

  <verify>
    <automated>curl -s http://localhost:8000/health || echo "Backend not responding — start uvicorn first"</automated>
  </verify>

  <acceptance_criteria>
    - uvicorn responds on port 8000 (GET /health returns 200 or GET / returns some response)
    - Next.js dev server responds on port 3000
    - No import errors in the FastAPI startup log
    - config/strategies/orb.yaml exists with at least one param (confirmed above) for Test 6
  </acceptance_criteria>

  <done>Both servers running. Terminal 1: uvicorn on :8000. Terminal 2: Next.js on :3000. Smoke-check passed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 1: Four panes visible at /dashboard (1440px+ width)</name>

  <read_first>
    - .planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md — read Test 1 expected result
    - apps/web/app/dashboard/page.tsx — confirm 4 PaneContainer labels: "CHART", "BLOTTER", "HISTORY", "CONTROLS"
  </read_first>

  <what-built>
    Task 1 above started uvicorn on :8000 and Next.js on :3000.

    The 4-pane TerminalLayout was built in Plans 07-02 through 07-04:
    - Left pane: CHART (candlestick chart via Lightweight Charts)
    - Right top: BLOTTER (positions table or empty state)
    - Right middle: HISTORY (trade table or empty state)
    - Right bottom: CONTROLS (strategy list)

    Each pane has a 28px title bar (PaneContainer.tsx) with its label.
    Layout uses react-resizable-panels with a vertical split (left/right) and horizontal splits in the right column.
  </what-built>

  <action>
    Open a browser window and size it to at least 1440px wide (full-screen on a 1080p+ monitor is sufficient).
    Navigate to: http://localhost:3000/dashboard

    Verify:
    1. All 4 pane title bars are visible with their labels: CHART (left), BLOTTER (right-top), HISTORY (right-middle), CONTROLS (right-bottom)
    2. Each title bar is approximately 28px tall with the label text visible
    3. The chart pane takes the left ~60-65% of the viewport width
    4. The right column has 3 panes stacked vertically

    If the page shows a loading spinner, wait up to 10 seconds for it to resolve.
    If any pane shows an error boundary or blank white space, note which pane.

    Update 07-HUMAN-UAT.md Test 1 result with "passed" or "failed: [describe issue]"
  </action>

  <how-to-verify>
    1. Navigate to http://localhost:3000/dashboard in a browser at 1440px+ width
    2. Visually confirm: CHART label (left pane), BLOTTER label (right top), HISTORY label (right middle), CONTROLS label (right bottom)
    3. Each label should be in a 28px dark header strip at the top of its pane
    4. Expected: all 4 labels visible, no blank panes, no JS error in the browser console (F12)
  </how-to-verify>

  <acceptance_criteria>
    - All 4 pane labels are visible: CHART, BLOTTER, HISTORY, CONTROLS
    - No pane shows a React error boundary (red "Something went wrong" overlay)
    - Browser console (F12 → Console) shows no uncaught exceptions at page load
    - The page did not redirect away from /dashboard
  </acceptance_criteria>

  <resume-signal>Type "passed" if all 4 panes are visible, or "failed: [describe what's wrong]"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 2: Drag-resize handles work</name>

  <read_first>
    - apps/web/app/dashboard/page.tsx — look for PanelGroup and Panel components from react-resizable-panels; note the handle IDs and defaultSize values
  </read_first>

  <what-built>
    react-resizable-panels provides PanelResizeHandle components between panels. Plan 07-02 wired a 4px vertical handle between the chart pane and the right column, and 3 horizontal handles between the right-column panes. The handle renders as a thin draggable strip.
  </what-built>

  <action>
    With /dashboard open at 1440px+ width:

    Test A — Vertical handle (chart vs right column):
    1. Hover over the 4px divider line between the CHART pane and the right column
    2. Cursor should change to a resize cursor (ew-resize or col-resize)
    3. Click and drag left or right by approximately 50–100px
    4. Release. Both panes should resize — chart pane shrinks/grows, right column shrinks/grows.

    Test B — Horizontal handles (between right-column panes):
    1. Hover over the 4px divider between BLOTTER and HISTORY (right column, upper gap)
    2. Cursor should change to a resize cursor (ns-resize or row-resize)
    3. Drag up or down by approximately 30–50px
    4. Release. BLOTTER and HISTORY should resize.
    5. Repeat for the divider between HISTORY and CONTROLS.

    If a handle does not respond to drag, note which handle.
    Update 07-HUMAN-UAT.md Test 2 result.
  </action>

  <how-to-verify>
    1. Drag the vertical divider between CHART and right column — both panes resize
    2. Drag the horizontal divider between BLOTTER and HISTORY — both panes resize
    3. Drag the horizontal divider between HISTORY and CONTROLS — both panes resize
    4. No pane collapses to zero width/height unintentionally
    5. No page scroll or layout break occurs during drag
  </how-to-verify>

  <acceptance_criteria>
    - Vertical handle between CHART and right column is draggable and resizes both panes
    - At least 2 of the 3 horizontal handles in the right column are draggable
    - No pane collapses entirely when dragging
    - Page layout remains stable after release (no overflow or scroll artifacts)
  </acceptance_criteria>

  <resume-signal>Type "passed" if resize handles work, or "failed: [describe which handle failed]"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 3: localStorage layout persistence across reloads</name>

  <read_first>
    - apps/web/app/dashboard/page.tsx — look for the localStorage keys "es-terminal-layout-h" and "es-terminal-layout-v" and the loadSizes / saveSizes functions
  </read_first>

  <what-built>
    Plan 07-02 added onLayout callbacks to PanelGroup components that write the panel sizes array to localStorage under keys "es-terminal-layout-h" (horizontal/vertical split) and "es-terminal-layout-v" (right column splits). The loadSizes function reads these back at mount time with an SSR guard (typeof window === 'undefined') added by review fix CR-04.
  </what-built>

  <action>
    With /dashboard open:

    1. Drag the vertical handle to a noticeably different position (e.g., make the chart pane take ~40% width instead of default ~65%)
    2. Open browser DevTools (F12) → Application tab → Local Storage → http://localhost:3000
    3. Verify keys "es-terminal-layout-h" and "es-terminal-layout-v" exist with array values like [40, 60] or [33, 33, 34]
    4. Hard reload the page: Ctrl+Shift+R (bypasses browser cache) or close and reopen the tab
    5. After reload, verify the layout is restored to the resized positions — NOT back to the default sizes

    If the keys are missing from localStorage, note that.
    If the layout resets to default on reload, note that.
    Update 07-HUMAN-UAT.md Test 3 result.
  </action>

  <how-to-verify>
    1. Resize panes, then open DevTools Application → Local Storage → check "es-terminal-layout-h" and "es-terminal-layout-v" keys exist
    2. Hard-reload (Ctrl+Shift+R)
    3. Panes should appear at the same sizes as before reload
    4. The default layout is roughly 65/35 horizontal; if your resized layout was 40/60, it should remain 40/60 after reload
  </how-to-verify>

  <acceptance_criteria>
    - "es-terminal-layout-h" key exists in localStorage after dragging
    - "es-terminal-layout-v" key exists in localStorage after dragging
    - After hard reload, panel sizes are restored to the pre-reload positions (not the default sizes)
    - No JavaScript error logged about "localStorage is not defined" (the SSR guard from CR-04 prevents this)
  </acceptance_criteria>

  <resume-signal>Type "passed" if layout persists across reload, or "failed: [describe what happened]"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 4: /dashboard/blotter redirect</name>

  <read_first>
    - apps/web/next.config.ts — look for the "async redirects()" function with source: '/dashboard/blotter'
  </read_first>

  <what-built>
    Plan 07-02 added a permanent redirect in next.config.ts from /dashboard/blotter to /dashboard. The VERIFICATION.md confirmed this is present in code. The test verifies the live Next.js server actually serves this redirect.

    Note: The VERIFICATION.md expected "301" but the next.config.ts may produce 308 (Permanent Redirect for non-GET safe methods) — both are acceptable permanent redirects.
  </what-built>

  <action>
    Test via browser:
    1. In the browser address bar, type: http://localhost:3000/dashboard/blotter
    2. Press Enter
    3. Observe: the browser should navigate to http://localhost:3000/dashboard (the address bar should change)

    Optionally verify via curl for the exact HTTP status code:
    curl -v http://localhost:3000/dashboard/blotter 2>&1 | grep -E "Location:|< HTTP"

    Expected: Location header points to /dashboard, HTTP status is 301 or 308.
    Update 07-HUMAN-UAT.md Test 4 result.
  </action>

  <how-to-verify>
    1. Navigate to http://localhost:3000/dashboard/blotter in the browser
    2. Browser address bar should change to http://localhost:3000/dashboard
    3. The BLOTTER pane should be visible as part of the /dashboard 4-pane layout
    4. Optional: curl -v http://localhost:3000/dashboard/blotter shows "Location: /dashboard" header with 301 or 308 status
  </how-to-verify>

  <acceptance_criteria>
    - Navigating to /dashboard/blotter ends up at /dashboard (address bar shows /dashboard)
    - The browser does NOT show a 404, 500, or empty page
    - curl confirms the redirect is permanent (301 or 308) with Location: /dashboard or http://localhost:3000/dashboard
  </acceptance_criteria>

  <resume-signal>Type "passed" if redirect works, or "failed: [describe what happened — 404? no redirect?]"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 5: WS reconnect visible in ConnectionStatus indicator</name>

  <read_first>
    - apps/web/hooks/useStream.ts — note MAX_BACKOFF_MS = 30_000 and the first backoff is Math.min(2^1 * 1000, 30000) = 2s; with jitter the first reconnect attempt fires within ~1–3s
    - apps/web/components/ — look for ConnectionStatus component that reads from useWsStore
  </read_first>

  <what-built>
    Plan 07-02 hardened useStream.ts with exponential backoff (MAX_BACKOFF_MS = 30_000), jitter, and seq gap detection. The ConnectionStatus component in the dashboard header reads WS connection state and changes color: green (connected), yellow (connecting/degraded), red (disconnected for > threshold).
  </what-built>

  <action>
    With /dashboard open and both servers running:

    1. Observe the ConnectionStatus indicator in the dashboard header — it should be green (connected to WS)
    2. In Terminal 1, kill uvicorn: press Ctrl+C
    3. Watch the ConnectionStatus indicator in the browser:
       - It should transition from green to yellow or red within ~2–5 seconds
    4. In Terminal 1, restart uvicorn:
       uv run uvicorn api.app:app --reload --port 8000
    5. Watch the ConnectionStatus indicator:
       - Within ~5–10 seconds (first backoff attempt at ~2s + reconnect time), it should return to green
       - The useStream hook attempts reconnect at: 2s, 4s, 8s, 16s, 30s (capped) with jitter

    If the indicator stays green after killing uvicorn (never shows disconnected), that is a bug.
    If the indicator never returns to green after restart, that is a bug.
    Update 07-HUMAN-UAT.md Test 5 result.
  </action>

  <how-to-verify>
    1. Baseline: ConnectionStatus is green while uvicorn is running
    2. Kill uvicorn (Ctrl+C in Terminal 1)
    3. Within 5 seconds: indicator changes to yellow or red
    4. Restart uvicorn
    5. Within 10 seconds: indicator returns to green
    6. The transition sequence proves exponential backoff reconnect is live
  </how-to-verify>

  <acceptance_criteria>
    - ConnectionStatus indicator is visibly green (connected) while uvicorn runs
    - Indicator visibly changes to yellow or red within 5s of killing uvicorn
    - After restarting uvicorn, indicator returns to green within 10s (first backoff ~2s + reconnect overhead)
    - No page reload required for the reconnect — it happens automatically
  </acceptance_criteria>

  <resume-signal>Type "passed" if the indicator transitions correctly, or "failed: [describe — stayed green? never recovered?]"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 6: Strategy controls — "Params saved — engine reloading" transient text</name>

  <read_first>
    - apps/web/components/StrategyControlsPane.tsx — look for setSaveSuccess(true), the 2000ms setTimeout, and the green text rendered when saveSuccess === true
    - config/strategies/orb.yaml — the orb-v1 strategy has params: opening_range_minutes (30), atr_stop_mult (2.0), ema_period (20), min_range_ticks (2), r_target (3.0), atr_period (14)
  </read_first>

  <what-built>
    Plan 07-04 built StrategyControlsPane with a ParamForm. When the user clicks "Save & Hot-reload":
    - PUT /strategies/{id}/params is called with the form values
    - On 200 response: setSaveSuccess(true) triggers rendering "Params saved — engine reloading" in color #4ade80 (bright green)
    - A setTimeout of 2000ms calls setSaveSuccess(false) to hide the text
  </what-built>

  <action>
    With /dashboard open and uvicorn running on :8000:

    1. In the CONTROLS pane (bottom right), locate the strategy list. The "orb-v1" strategy should be listed.
    2. Click the row (or the expand chevron) to expand the param form for orb-v1.
    3. Locate the "Opening Range (min)" field (or any param field). Change its value slightly — e.g., change 30 to 31.
    4. Click the "Save & Hot-reload" button.
    5. Observe:
       a. A green confirmation text "Params saved — engine reloading" should appear
       b. It should disappear after approximately 2 seconds
       c. The field should not show an error (a valid integer like 31 passes Pydantic validation)
    6. If uvicorn is not running, the button will hang or show an error. Restart uvicorn if needed.

    Optional: change the value back to 30 after the test to restore defaults.
    Update 07-HUMAN-UAT.md Test 6 result.
  </action>

  <how-to-verify>
    1. Expand the orb-v1 param form in CONTROLS pane
    2. Change opening_range_minutes from 30 to 31
    3. Click "Save & Hot-reload"
    4. Within 1–2s: green text "Params saved — engine reloading" appears below the button
    5. After ~2s: text disappears automatically
    6. No red error border appears on the field (31 is a valid integer)
  </how-to-verify>

  <acceptance_criteria>
    - "Params saved — engine reloading" text appears after clicking Save & Hot-reload with a valid param value
    - Text is rendered in green (approximately #4ade80)
    - Text disappears after approximately 2 seconds
    - No inline field error shown for a valid integer value
    - PUT /strategies/orb-v1/params request returns 200 (visible in browser Network tab if desired)
  </acceptance_criteria>

  <resume-signal>Type "passed" if the transient confirmation text appeared and disappeared, or "failed: [describe — no text? error shown? request failed?]"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT Test 7: Run Backtest button disables/re-enables</name>

  <read_first>
    - apps/web/components/StrategyControlsPane.tsx — look for isRunning state, aria-disabled={isRunning}, the POST /backtests/run call, and useStrategyRun(runId) polling with refetchInterval
    - packages/api/src/api/routes/strategies.py — _run_backtest_task is a stub that sleeps 2s then writes status='complete'; this means the polling cycle completes in ~4–6s total
  </read_first>

  <what-built>
    Plan 07-04 built the Run Backtest button flow:
    - Click → POST /backtests/run → 202 response with {run_id}
    - Button label changes to "Running…", aria-disabled=true, cursor: not-allowed, color: #555555
    - useStrategyRun(runId) polls GET /backtests/{run_id} every 2s
    - When status == 'complete' or 'failed': invalidateQueries(['backtests']), setRunId(null), button re-enables
    
    Note: _run_backtest_task is a stub (2s sleep, no real BacktestEngine — Phase 8 deferred). The button should still cycle through Running…→complete within ~4–6s.
  </what-built>

  <action>
    With /dashboard open, uvicorn running on :8000, CONTROLS pane visible:

    1. In the orb-v1 row (expand if collapsed), locate the "Run Backtest" button at the bottom of the expanded section.
    2. Click "Run Backtest".
    3. Observe:
       a. Button label should change to "Running…"
       b. Button should appear visually disabled (grayed out, cursor changes to not-allowed when hovered)
       c. Wait approximately 4–8 seconds (the stub sleeps 2s then marks complete; polling fires every 2s)
       d. Button should return to "Run Backtest" label and become enabled again
    4. If the button never re-enables, open browser Network tab and check if GET /backtests/{run_id} is polling and what status it returns.

    Update 07-HUMAN-UAT.md Test 7 result.
  </action>

  <how-to-verify>
    1. Click "Run Backtest" — label immediately changes to "Running…"
    2. Button is visually disabled (grayed, not-allowed cursor on hover)
    3. After ~4–8s: button returns to "Run Backtest" and is clickable again
    4. Browser Network tab: POST /backtests/run returns 202; subsequent GET /backtests/{id} returns status='complete' after ~2s
  </how-to-verify>

  <acceptance_criteria>
    - Button label changes to "Running…" immediately after click
    - Button is disabled (aria-disabled=true) while running — clicking again does nothing
    - Button returns to "Run Backtest" label and becomes enabled after polling detects status='complete' or 'failed'
    - No JS error thrown in browser console during the polling cycle
    - Total cycle (click → re-enable) completes in under 15 seconds
  </acceptance_criteria>

  <resume-signal>Type "passed" if the button disabled/re-enabled correctly, or "failed: [describe — stayed disabled? never changed label?]"</resume-signal>
</task>

<task type="auto">
  <name>Task 8: Run Playwright E2E test (ws-reconnect.spec.ts)</name>
  <files>.planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md</files>

  <read_first>
    - apps/web/e2e/ws-reconnect.spec.ts — read the full test to understand: it navigates to /dashboard, waits for pane labels, uses routeWebSocket to simulate a drop after 500ms, waits for reconnect, asserts pane labels still visible and GET /positions responds 200 or 404
    - apps/web/e2e/playwright.config.ts — confirm webServer config: uvicorn on 8000, Next.js on 3000; confirm testDir points to e2e/
  </read_first>

  <action>
    With uvicorn and Next.js already running on :8000 and :3000 (from Task 1):

    Run the Playwright E2E test suite:
    ```
    cd "C:\Users\Admin\Desktop\Day Trading"
    pnpm --filter web test:e2e
    ```

    The test uses test.slow() which extends the default timeout. Expected total runtime: 20–40s.

    Capture the output. Expected passing result:
    ```
    Running 1 test using 1 worker
    ✓ WS reconnects after a simulated drop
    1 passed (Xs)
    ```

    If the test fails, capture the error message. Common failure modes:
    - "localhost:3000 not available" — Next.js not running; start it
    - "localhost:8000 not available" — uvicorn not running; start it
    - "Timeout waiting for BLOTTER" — the pane label is different from expected; note the actual label
    - "Route is already handled" — Playwright browser version mismatch; run: npx playwright install

    Update 07-HUMAN-UAT.md Test 8 result with the actual pass/fail and output excerpt.
  </action>

  <verify>
    <automated>cd "C:\Users\Admin\Desktop\Day Trading" && pnpm --filter web test:e2e 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - pnpm --filter web test:e2e exits with code 0 (all tests pass)
    - Output shows "1 passed" for ws-reconnect.spec.ts
    - If Playwright browsers are not installed, "npx playwright install" resolves it
    - A non-zero exit code with a meaningful error message (not a compilation error) is also acceptable — document the failure and update UAT accordingly
  </acceptance_criteria>

  <done>Playwright E2E test ran. Result (pass or documented failure) recorded in 07-HUMAN-UAT.md Test 8.</done>
</task>

<task type="auto">
  <name>Task 9: Update 07-HUMAN-UAT.md with final results</name>
  <files>.planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md</files>

  <read_first>
    - .planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md — read the current state; identify which tests have been filled in with results from the checkpoint tasks above
  </read_first>

  <action>
    Update 07-HUMAN-UAT.md to reflect the results from all 8 tests completed above:

    1. Set the frontmatter "status" to "complete" (or "partial" if any test failed)
    2. Set "updated" to today's date: 2026-05-20
    3. For each test (1–8), update the "result" field:
       - Tests 1–7: copy the human-approved result from the checkpoint tasks
       - Test 8: copy the Playwright output result from Task 8
    4. Update the Summary section:
       - "total: 8"
       - "passed: N" (count of passed tests)
       - "issues: N" (count of tests that failed or have issues)
       - "pending: 0"
    5. If any test failed, add a "## Gaps" section at the bottom listing each issue with:
       - Test number and name
       - What was observed
       - Suggested next step (e.g., "open an issue", "create a fix plan")

    If all 8 tests passed, add a final line:
    "Phase 7 fully signed off — all 8 human UAT tests passed (2026-05-20)."
  </action>

  <verify>
    <automated>cd "C:\Users\Admin\Desktop\Day Trading" &amp;&amp; grep "pending: 0" .planning/phases/07-bloomberg-density-ui-polish/07-HUMAN-UAT.md</automated>
  </verify>

  <acceptance_criteria>
    - 07-HUMAN-UAT.md has "pending: 0" in the Summary section (all tests resolved)
    - Each test has a concrete result (not "[pending]")
    - Frontmatter "status" is either "complete" or "partial" (not "partial" with no explanation)
    - If any test failed, the Gaps section explains what happened
  </acceptance_criteria>

  <done>07-HUMAN-UAT.md updated with all 8 test results. Phase 7 human UAT complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js dev server (port 3000) | User browser sending requests to local dev server |
| Next.js → FastAPI (port 8000) | Frontend API calls to local uvicorn backend |
| Playwright → local servers | Test automation accessing localhost endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Denial of Service | uvicorn kill/restart in Test 5 | accept | Paper-only local system; no production impact |
| T-07-06-02 | Tampering | orb.yaml param change in Test 6 | accept | Test changes opening_range_minutes 30→31; restore to 30 after test |
</threat_model>

<verification>
Phase 7 human UAT is complete when:
1. 07-HUMAN-UAT.md shows "pending: 0" and "status: complete" (or "partial" with documented issues)
2. All 8 test results are filled in with concrete outcomes
3. If any test failed, a gap closure plan or issue is documented

UAT checklist:
- [ ] Test 1: 4 panes visible — human approved
- [ ] Test 2: Drag-resize handles — human approved
- [ ] Test 3: localStorage persistence — human approved
- [ ] Test 4: /dashboard/blotter redirect — human approved
- [ ] Test 5: WS reconnect indicator — human approved
- [ ] Test 6: Params saved transient text — human approved
- [ ] Test 7: Run Backtest disable/re-enable — human approved
- [ ] Test 8: Playwright E2E — automated result recorded
</verification>

<success_criteria>
- All 8 UAT tests resolved (passed or documented failure with next steps)
- 07-HUMAN-UAT.md updated: pending: 0, status: complete or partial
- Phase 7 fully signed off if all 8 pass
- If any test fails, gap closure is documented for the next iteration
</success_criteria>

<output>
After completion, create `.planning/phases/07-bloomberg-density-ui-polish/07-06-SUMMARY.md`
</output>
