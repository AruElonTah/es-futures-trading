---
phase: "07"
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - apps/web/components/BlotterPane.tsx
  - apps/web/components/TradeHistoryPane.tsx
  - apps/web/components/Chart.tsx
  - apps/web/app/dashboard/blotter/page.tsx
  - apps/web/app/dashboard/page.tsx
  - apps/web/__tests__/TradeHistoryPane.test.ts
autonomous: true
requirements:
  - UI-03
  - UI-06

must_haves:
  truths:
    - "BlotterPane renders positions table + F/K/P controls using ConfirmationDialog and HelpOverlay from components/"
    - "apps/web/app/dashboard/blotter/page.tsx is deleted; its directory no longer exists"
    - "TradeHistoryPane renders closed-trade table with 9 columns plus equity+DD chart"
    - "Clicking a trade row calls setFocusedBarTs(trade.entry_ts_utc)"
    - "Chart.tsx has chartRef stored in a ref and a third useEffect watching focusedBarTs"
    - "TradeHistoryPane.test.ts tests are green (not failing stubs)"
    - "dashboard/page.tsx renders BlotterPane and TradeHistoryPane in right-column panes"
  artifacts:
    - path: "apps/web/components/BlotterPane.tsx"
      provides: "Blotter pane with positions + F/K/P dialogs"
      contains: "BlotterPane"
    - path: "apps/web/components/TradeHistoryPane.tsx"
      provides: "Trade table + equity+DD chart"
      contains: "TradeHistoryPane"
    - path: "apps/web/components/Chart.tsx"
      provides: "chartRef + focusedBarTs scroll effect"
      contains: "chartRef"
    - path: "apps/web/__tests__/TradeHistoryPane.test.ts"
      provides: "Green Vitest tests for TradeHistoryPane"
  key_links:
    - from: "apps/web/components/TradeHistoryPane.tsx"
      to: "apps/web/store/ws.ts setFocusedBarTs"
      via: "useWsStore selector"
      pattern: "setFocusedBarTs"
    - from: "apps/web/components/Chart.tsx"
      to: "apps/web/store/ws.ts focusedBarTs"
      via: "useWsStore selector + useEffect dep"
      pattern: "focusedBarTs"
    - from: "apps/web/app/dashboard/page.tsx"
      to: "apps/web/components/BlotterPane.tsx"
      via: "import + Panel child"
      pattern: "BlotterPane"
---

<objective>
BlotterPane migration + TradeHistoryPane + Chart focusedBarTs + delete retired blotter route.

Purpose: Deliver the two right-column panes that show operational data: the blotter (migrated from the retired /dashboard/blotter page) and the trade history with equity+DD chart. After this plan, the /dashboard route is a fully functional 3-of-4-pane terminal (StrategyControlsPane still a placeholder until Plan 04).

Output: BlotterPane, TradeHistoryPane (with DD histogram), Chart.tsx updated, blotter/ directory deleted, TradeHistoryPane tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-bloomberg-density-ui-polish/07-CONTEXT.md
@.planning/phases/07-bloomberg-density-ui-polish/07-UI-SPEC.md
@.planning/phases/07-bloomberg-density-ui-polish/07-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/web/app/dashboard/blotter/page.tsx (source of truth for BlotterPane — read before writing):
  - Full BlotterPage component: positions table columns, F/K/P button styles, ConfirmationDialog usage, HelpOverlay usage, engine state badge, useHotkeys() call, useQuery positions polling
  - useStream() is called in dashboard/page.tsx parent; BlotterPane must NOT call useStream() again
  - Position type shape (from the file or from api.ts)

From apps/web/components/EquityCurve.tsx (source of truth for equity chart lifecycle pattern):
  - createChart + LineSeries lifecycle effect pattern
  - ResizeObserver pattern for responsive chart sizing
  - etTimeFormatter + etTickFormatter (import path)

From apps/web/lib/api.ts:
  - TradeRow: { side, entry, exit, gross_pnl, mae, mfe, slippage_ticks, size, entry_ts_utc, exit_reason, ... }
  - EquityPoint: { ts_utc, equity, drawdown }
  - BacktestRow type

From apps/web/hooks/useBacktests.ts (already updated in Plan 02):
  - useBacktests() for latestRunId
  - useEquityTrades(runId) for TradeRow[]
  - useEquityCurve(runId) for EquityPoint[]

From apps/web/components/Chart.tsx (to modify):
  - seriesRef: useRef<ISeriesApi<"Candlestick"> | null>(null) — add chartRef alongside this
  - Effect 1: chart lifecycle (add chartRef.current = chart; clear in cleanup)
  - Effect 2: overlay/trades data update (leave untouched)
  - Add Effect 3: focusedBarTs scroll watcher

From apps/web/store/ws.ts (already updated in Plan 02):
  - useWsStore(s => s.focusedBarTs): string | null
  - useWsStore(s => s.setFocusedBarTs): (ts: string | null) => void

HistogramSeries usage (lightweight-charts v5.2.0):
  - import { createChart, LineSeries, HistogramSeries, type IChartApi, type Time } from 'lightweight-charts'
  - chart.addSeries(HistogramSeries, { color: 'rgba(239, 68, 68, 0.5)', priceScaleId: 'right', title: 'DD $' })
  - Per-data-point color also set in setData() map to avoid Pitfall 7
  - Values negated: value: -Math.abs(point.drawdown)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlotterPane + delete blotter directory + wire into dashboard</name>
  <files>
    apps/web/components/BlotterPane.tsx
    apps/web/app/dashboard/blotter/page.tsx
    apps/web/app/dashboard/page.tsx
  </files>
  <read_first>
    apps/web/app/dashboard/blotter/page.tsx
    apps/web/app/dashboard/page.tsx
    apps/web/components/ConfirmationDialog.tsx
    apps/web/components/HelpOverlay.tsx
  </read_first>
  <action>
    Read the full blotter/page.tsx first. It contains the complete BlotterPage implementation.

    Create apps/web/components/BlotterPane.tsx: This is a migration of the BlotterPage component body into a reusable pane component. Key rules:
    - Add 'use client' as first line
    - Import ConfirmationDialog from @/components/ConfirmationDialog (the extracted file from Plan 02)
    - Import HelpOverlay from @/components/HelpOverlay (the extracted file from Plan 02)
    - Do NOT call useStream() inside BlotterPane — the hook is called once in dashboard/page.tsx. BlotterPane reads positions and engineState from useWsStore.
    - Copy the full positions table (columns: Symbol, Side, Qty, Avg Fill, Mark, Unrealized P&L, Stop Dist $, Stop Dist ticks, Target Dist, Time In) with all the color logic and tabular-nums styling.
    - Copy the F/K/P action handler callbacks (handleFlatten, handleKill, handlePause) and the ConfirmationDialog open state.
    - Copy the controls row at the bottom (Flatten button in #ef4444, Kill button in #dc2626, Pause button state-conditional).
    - Copy the hotkey setup via useHotkeys({ onFlatten: handleFlatten, onKill: handleKill, onPause: handlePause, onHelp: () => setHelpOpen(true) }).
    - Include the 1s polling fallback query for /positions (for when WS is disconnected): useQuery with queryKey: ['positions'], refetchInterval: 1000.
    - Preserve the EngineStateBadge display inside the component body (NOT in PaneContainer title bar — the parent passes it via rightSlot in dashboard/page.tsx).
    - Empty state: when positions.length === 0, show "No open positions" heading + body text from 07-UI-SPEC.md §Empty States.
    - Error state: when positions fetch fails, show the error text from 07-UI-SPEC.md §Error States.

    DELETION: After BlotterPane.tsx is complete and verified to compile (tsc --noEmit), delete apps/web/app/dashboard/blotter/page.tsx AND the apps/web/app/dashboard/blotter/ directory. On Windows PowerShell use: Remove-Item -Recurse -Force "apps/web/app/dashboard/blotter" from the repo root. The next.config.ts redirect (added in Plan 02) handles any incoming /dashboard/blotter requests.

    dashboard/page.tsx: Replace the BLOTTER placeholder pane with BlotterPane. Import BlotterPane from @/components/BlotterPane. Update the PaneContainer label="BLOTTER" to include rightSlot with AuthorTVAlertButton (import from @/components/AuthorTVAlertButton). Pass latestRunId to TradeHistoryPane placeholder if available.

    Preserve the HISTORY and CONTROLS placeholder divs — they get filled in Task 2 and Plan 04.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    BlotterPane.tsx exists in components/ with positions table, F/K/P controls, hotkeys, ConfirmationDialog + HelpOverlay integration, empty/error states. apps/web/app/dashboard/blotter/ directory does not exist (deleted). dashboard/page.tsx imports BlotterPane and renders it in the BLOTTER Panel slot with AuthorTVAlertButton in rightSlot. TypeScript compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TradeHistoryPane + Chart.tsx focusedBarTs + green tests</name>
  <files>
    apps/web/components/TradeHistoryPane.tsx
    apps/web/components/Chart.tsx
    apps/web/__tests__/TradeHistoryPane.test.ts
    apps/web/app/dashboard/page.tsx
  </files>
  <read_first>
    apps/web/components/EquityCurve.tsx
    apps/web/components/Chart.tsx
    apps/web/hooks/useBacktests.ts
    apps/web/lib/api.ts
    apps/web/__tests__/TradeHistoryPane.test.ts
  </read_first>
  <behavior>
    - Test: TradeHistoryPane renders a table row for each trade in the mocked TradeRow array
    - Test: Clicking a trade row calls setFocusedBarTs with the trade's entry_ts_utc value
    - Test: When no backtest runs exist, renders empty state "No backtest results" text
    - Test: HistogramSeries is created (mock lightweight-charts to verify addSeries is called with HistogramSeries)
    - Test: DD values in setData are negated (-Math.abs(point.drawdown))
  </behavior>
  <action>
    Read EquityCurve.tsx fully first — TradeHistoryPane's equity chart lifecycle mirrors it closely.

    Create apps/web/components/TradeHistoryPane.tsx:
    - 'use client'
    - Props: { runId: string | null } — the run_id of the last backtest to display
    - Use useEquityTrades(runId) for TradeRow[], useEquityCurve(runId) for EquityPoint[], useBacktests() for latestRunId if runId prop is null
    - Two-section vertical layout: trade table (flex: 1 1 0, overflow: auto) above equity chart (height: 160px, flexShrink: 0)
    - Trade table: semantic <table><thead><tbody> with columns per 07-UI-SPEC.md §TradeHistoryPane. Columns: SIDE, ENTRY, EXIT, GROSS P&L, SLIPPAGE $, MAE, MFE, HOLD, REASON. Column headers 11px #888888. All numeric cells: className="tabular-nums". SIDE colored #4ade80 for long, #f87171 for short. GROSS P&L colored positive/negative. SLIPPAGE $ derived: trade.slippage_ticks * (pointValue / 4) * trade.size where pointValue = 50 for ES/12.5 for MES — hardcode 50 for now (D-13 says derive from instruments.py but the client doesn't call that endpoint; use point_value from a known constant). HOLD time: format as HH:MM from hold_bars * 60 seconds (or from entry/exit timestamps if available). REASON: tp/sl/eod/manual as-is.
    - Row click: onClick={() => setFocusedBarTs(trade.entry_ts_utc)}. Trade rows get cursor: pointer. Selected row (matching focusedBarTs) gets border: 1px solid #4a90d9, backgroundColor: #1a1a2e.
    - Row aria: role="button", tabIndex={0}, onKeyDown enter handler.
    - Empty state: if no runId, show "No backtest results" + body text. If runId exists but zero trades: "No trades" + body text. Per 07-UI-SPEC §Empty States.
    - Backtest-running state: if a runId is being polled (status=pending), show spinner row with "Running backtest…" in #eab308. role="status" aria-live="polite".
    - Equity chart useEffect: Follow EquityCurve.tsx lifecycle pattern exactly (two-effect split: one for chart lifecycle, one for data). ADD chartRef to store the chart instance. Add LineSeries (color #4ade80, lineWidth 1) and HistogramSeries (color rgba(239,68,68,0.5), priceScaleId 'right'). Map EquityPoint[] to time/value for LineSeries (value = equity - startEquity). Map EquityPoint[] to time/value/color for HistogramSeries (value = -Math.abs(drawdown), color = 'rgba(239, 68, 68, 0.5)' per-bar per Pitfall 7). Chart container: aria-label="Equity curve and drawdown chart".

    Chart.tsx modifications:
    - Read Chart.tsx fully first. Add: const chartRef = useRef<IChartApi | null>(null) after seriesRef.
    - In Effect 1 (chart lifecycle), after the line that sets seriesRef.current, add: chartRef.current = chart. In cleanup, before chart.remove(), add: chartRef.current = null.
    - Add new import useWsStore from @/store/ws (if not already there).
    - Add at component top (after seriesRef): const focusedBarTs = useWsStore((s) => s.focusedBarTs); const setFocusedBarTs = useWsStore((s) => s.setFocusedBarTs).
    - Add Effect 3 (separate useEffect) watching [focusedBarTs, bars, setFocusedBarTs]: if !focusedBarTs || !chartRef.current return. Find target bar index in sorted bars array by matching unix timestamp. If found: chartRef.current.timeScale().scrollToPosition(idx - Math.floor(sorted.length * 0.3), false). Then: setFocusedBarTs(null). Follow exact pattern from 07-PATTERNS.md §Chart.tsx.
    - Import IChartApi type from lightweight-charts (already there or add to existing import).

    TradeHistoryPane.test.ts: Replace the two failing stubs with real tests. Use vi.mock('lightweight-charts') to mock createChart, LineSeries, HistogramSeries. Use @testing-library/react to render TradeHistoryPane with mock data. Test: table renders correct row count, row click calls setFocusedBarTs, HistogramSeries addSeries call verified, negated DD values in setData call.

    dashboard/page.tsx: Replace the HISTORY placeholder with <TradeHistoryPane runId={latestRunId ?? null} />. Import TradeHistoryPane. The latestRunId comes from useBacktests() hook already called in the dashboard.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    TradeHistoryPane.tsx exists with 9-column trade table, equity+DD chart, click-to-scroll via setFocusedBarTs, empty states, spinner state, aria attributes. Chart.tsx has chartRef, Effect 3 watching focusedBarTs. TradeHistoryPane.test.ts all tests green. dashboard/page.tsx renders TradeHistoryPane in HISTORY pane. pnpm test -- --run: all tests pass (or only ws-reconnect E2E stub fails as expected).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→client TradeRow | Trade data fetched from DuckDB; client renders it — no user input in this plan |
| client→DOM | focusedBarTs is an ISO timestamp string from trade data; not from user text input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | TradeHistoryPane API errors | mitigate | Error states show generic "Is the API running?" message per 07-UI-SPEC §Error States — no stack traces rendered |
| T-07-03-02 | Tampering | focusedBarTs atom | accept | atom set from trade.entry_ts_utc (server-sourced ISO string); no user-typed input; no code execution risk |
| T-07-03-03 | Denial | chart.remove() double-call | mitigate | chartRef.current = null before chart.remove() in cleanup + null guard in Effect 3 prevents double-remove (Pitfall 2 from RESEARCH.md) |
</threat_model>

<verification>
Full frontend unit test suite passes:
  pnpm --filter web test -- --run 2>&1 | tail -5

TypeScript compiles clean:
  pnpm --filter web exec tsc --noEmit 2>&1 | head -10

Blotter directory is gone:
  Test-Path "apps/web/app/dashboard/blotter"
  (should return False)

Backend tests still pass:
  uv run pytest packages/ -x -q 2>&1 | tail -5
</verification>

<success_criteria>
- BlotterPane.tsx exists in apps/web/components/ with full positions table, F/K/P controls, ConfirmationDialog, HelpOverlay
- apps/web/app/dashboard/blotter/ directory does not exist
- TradeHistoryPane.tsx renders 9-column trade table + 160px equity+DD chart
- HistogramSeries used with color 'rgba(239, 68, 68, 0.5)', per-bar color set, values negated
- Clicking trade row calls setFocusedBarTs(trade.entry_ts_utc)
- Chart.tsx has chartRef useRef, Effect 3 watching focusedBarTs, calls scrollToPosition
- TradeHistoryPane.test.ts all tests green
- dashboard/page.tsx: BLOTTER pane = BlotterPane, HISTORY pane = TradeHistoryPane
- pnpm --filter web test -- --run passes (all tests)
- pnpm --filter web exec tsc --noEmit: no errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-bloomberg-density-ui-polish/07-03-SUMMARY.md`
</output>
