---
phase: 06-tradingview-mcp-bridge
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - packages/tv-bridge/src/tv_bridge/cleanup.py
  - packages/tv-bridge/src/tv_bridge/__init__.py
  - packages/tv-bridge/src/tv_bridge/bridge.py
  - packages/tv-bridge/tests/test_overlay_registry.py
  - packages/tv-bridge/tests/integration/test_tv_failure_isolation.py
  - packages/api/src/api/app.py
  - apps/web/components/AuthorTVAlertButton.tsx
  - apps/web/app/dashboard/blotter/page.tsx
  - .planning/phases/06-tradingview-mcp-bridge/06-04-CHECKPOINT-NOTES.md
autonomous: false
requirements: [TV-02, TV-03, TV-06, TV-07]
tags: [tv-bridge, cleanup, frontend, integration-test, checkpoint]

must_haves:
  truths:
    - "A daily nightly_cleanup task runs at 03:00 ET and marks tv_overlays rows with trading_date < (today - 5 trading days) as deleted_at = now()"
    - "Each cleaned-up row triggers an attempt to remove the shape from the live TV chart via draw_remove_one MCP call (best effort; failure tolerated)"
    - "An integration test kills the TVBridge MCP session mid-execution and confirms the trading pipeline (Signal → Risk → Fill → audit_log) continues uninterrupted with zero skipped signals"
    - "The /dashboard/blotter page exposes an 'Author TV Alert' button that calls POST /tv/alerts with the active strategy's current params and shows a success toast with the returned tv_alert_id"
    - "Human-verify checkpoint confirms: (a) entry arrow + stop line + target line + ORB box appear on TV Desktop within 2s of a synthetic Signal published to the bus, (b) POST /tv/focus visibly moves the chart within 15s, (c) the Author TV Alert button creates a TV alert visible in the TV Desktop alerts panel"
  artifacts:
    - path: "packages/tv-bridge/src/tv_bridge/cleanup.py"
      provides: "nightly_cleanup() coroutine + NightlyCleanupScheduler wrapper"
      exports: ["nightly_cleanup", "NightlyCleanupScheduler"]
    - path: "packages/tv-bridge/tests/integration/test_tv_failure_isolation.py"
      provides: "End-to-end failure-isolation test (Signal → Risk → audit_log continues when TVBridge session dies)"
      contains: "test_pipeline_continues_when_tv_killed"
    - path: "apps/web/components/AuthorTVAlertButton.tsx"
      provides: "React client component that POSTs to /tv/alerts and renders a toast"
      contains: "AuthorTVAlertButton"
    - path: "packages/api/src/api/app.py"
      provides: "Lifespan launches NightlyCleanupScheduler as app.state.cleanup_task"
      contains: "cleanup_task"
  key_links:
    - from: "FastAPI lifespan"
      to: "NightlyCleanupScheduler -> nightly_cleanup() -> store.list_overlays_older_than + draw_remove_one + store.mark_tv_overlay_deleted"
      via: "asyncio.create_task at 03:00 ET daily"
      pattern: "cleanup_task"
    - from: "AuthorTVAlertButton onClick"
      to: "POST /tv/alerts"
      via: "fetch(API_BASE + '/tv/alerts', {method:'POST', body: JSON.stringify({...})})"
      pattern: "tv/alerts"
    - from: "test_pipeline_continues_when_tv_killed"
      to: "bridge.stop() mid-signal; risk_manager + audit_log keep firing"
      via: "asyncio.create_task fan-out on bus; bridge errors caught inside _safe_draw_signal"
      pattern: "test_pipeline_continues_when_tv_killed"
---

<objective>
Close Phase 6 with the four remaining vertical-slice pieces:
1. **Nightly overlay cleanup (TV-03):** Daily scheduled task that retires overlays older than 5 trading days from both DuckDB (mark deleted_at) and the live TV chart (best-effort draw_remove_one).
2. **Failure isolation integration test (TV-06, TV-01):** End-to-end test that publishes Signals while the TVBridge MCP session is forcibly torn down and asserts the trading pipeline emits no errors and skips zero signals.
3. **Minimal frontend "Author TV Alert" button (TV-07):** A `<AuthorTVAlertButton>` React component placed in the blotter header that POSTs to /tv/alerts and surfaces the returned tv_alert_id in a toast. Full Strategy Controls panel integration is deferred to Phase 7 per the roadmap.
4. **Human-verify checkpoint:** Operator confirms that drawing-on-fill, focus-on-date, and alert-authoring all work against a live TV Desktop before the phase exits.

Purpose: This is the closeout slice — it proves the Phase 6 success criteria as observable behavior (not just unit-test green), wires in the only remaining cron-style scheduler, and exposes the alert authoring flow to the UI so the operator can use it during Phase 7 polish.

Output: One new tv-bridge module (`cleanup.py`), one new frontend component (`AuthorTVAlertButton.tsx`), a fully passing integration test (no more xfail/skip in 06-VALIDATION.md), and a checkpoint-notes file describing the visual verification steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
@.planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md
@.planning/phases/06-tradingview-mcp-bridge/06-VALIDATION.md
@.planning/phases/06-tradingview-mcp-bridge/06-02-PLAN.md
@.planning/phases/06-tradingview-mcp-bridge/06-03-PLAN.md
@.planning/phases/06-tradingview-mcp-bridge/06-03-SUMMARY.md
@packages/tv-bridge/src/tv_bridge/bridge.py
@packages/tv-bridge/src/tv_bridge/reconciliation.py
@packages/tv-bridge/src/tv_bridge/__init__.py
@packages/tv-bridge/tests/integration/test_tv_failure_isolation.py
@packages/tv-bridge/tests/test_overlay_registry.py
@packages/trading-core/src/trading_core/execution/eod_scheduler.py
@packages/api/src/api/app.py
@apps/web/app/dashboard/blotter/page.tsx
@apps/web/lib/api.ts

<interfaces>
<!-- Contracts the executor needs. -->

DuckDBStore (Plan 01):
- list_overlays_older_than(trading_date: date) -> list[tuple[str, str]]  # returns [(overlay_id, shape_id), ...]
- mark_tv_overlay_deleted(*, overlay_id: str) -> None
- count_active_overlays() -> int
- write_audit_event(...) -> None

TVBridge (Plan 02):
- bridge.call_tool(tool, args) -> dict | None  (never raises)
- bridge.is_connected (property)
- bridge.start(), bridge.stop()

EodScheduler (existing):
- EodScheduler(*, on_flatten: Callable[[], Coroutine], close_time_et: str, lead_seconds: int = 0, tz: str)
- For NightlyCleanupScheduler use close_time_et="03:00", lead_seconds=0, tz="America/New_York"

pandas_market_calendars (in uv.lock):
- mcal.get_calendar("CME_Equity").schedule(start_date, end_date)
- Used to compute "5 trading days ago" (skipping weekends + holidays)

apps/web/lib/api.ts (existing):
- export const API_BASE = process.env.NEXT_PUBLIC_API_BASE || 'http://localhost:8000'
- export const apiUrl = (path: string) => `${API_BASE}${path}`

apps/web/app/dashboard/blotter/page.tsx (existing):
- 'use client' React component; uses useStream, useHotkeys, useWsStore, ETClock, ConnectionStatus
- has a header area where the new button should be placed alongside existing controls
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nightly_cleanup + NightlyCleanupScheduler + lifespan wiring + integration test (06-04-01, 06-04-02)</name>
  <files>
    packages/tv-bridge/src/tv_bridge/cleanup.py,
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/test_overlay_registry.py,
    packages/tv-bridge/tests/integration/test_tv_failure_isolation.py,
    packages/api/src/api/app.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/execution/eod_scheduler.py,
    packages/tv-bridge/src/tv_bridge/bridge.py,
    packages/tv-bridge/src/tv_bridge/reconciliation.py,
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/test_overlay_registry.py,
    packages/tv-bridge/tests/integration/test_tv_failure_isolation.py,
    packages/api/src/api/app.py,
    .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
  </read_first>
  <behavior>
    - test_overlay_registry.py::test_nightly_cleanup — pre-populate tv_overlays with 5 rows (trading_dates spanning today, today-3, today-7, today-10, today-30); call `await nightly_cleanup(bridge=mock_bridge, store=in_memory_store, today=date.today())` (where retention_trading_days=5); assert: rows with trading_date < (today - 5 trading days) have non-null deleted_at; recent rows are untouched; count_active_overlays returns the correct remaining count; for each cleaned row, mock_bridge.call_tool was invoked once with tool="draw_remove_one" and args={"entity_id": <shape_id>}
    - test_overlay_registry.py::test_nightly_cleanup_tolerates_mcp_failure — mock_bridge.call_tool returns None (simulating MCP failure); cleanup still marks rows as deleted_at and writes an audit_log row with topic='cleanup_partial' for each failed draw_remove_one
    - test_tv_failure_isolation.py::test_pipeline_continues_when_tv_killed — real EventBus, real DuckDBStore in-memory, mock RiskManager + Executor (or real if simple enough); start TVBridge with a mocked stdio_client that succeeds initially, then raises ConnectionError mid-stream; publish 10 Signal events on TOPIC_SIGNALS; assert: bridge._session becomes None within 2s, all 10 Signals flow through the bus (risk_manager.check called 10 times), the audit_log has at least 10 rows from risk decisions, and at most a small number (<= 10) of `tv_draw_*` rows from the bridge's error handlers — but ZERO skipped or rejected signals on the trading-pipeline side
  </behavior>
  <action>
    Create `packages/tv-bridge/src/tv_bridge/cleanup.py`:
      - `from __future__ import annotations`
      - Imports: `asyncio`, `json`, `from collections.abc import Callable, Coroutine`, `from datetime import date, datetime, timedelta, timezone`, `from typing import Any`, `import pandas_market_calendars as mcal`, `from trading_core.logging import get_logger`, `from trading_core.storage.duckdb_store import DuckDBStore`, `from trading_core.storage.runs import new_run_id`, `from trading_core.execution.eod_scheduler import EodScheduler`
      - `_log = get_logger(__name__)`
      - Constants:
          DEFAULT_RETENTION_TRADING_DAYS = 5
          CLEANUP_FIRE_TIME_ET = "03:00"   # Off-hours — TV Desktop typically not in active use
      - `def _trading_days_ago(today: date, n: int) -> date:` — body:
          cal = mcal.get_calendar("CME_Equity")
          # Look back enough calendar days to guarantee n trading days
          schedule = cal.schedule(start_date=today - timedelta(days=n * 2 + 10), end_date=today)
          trading_days = [d.date() for d in schedule.index]
          if len(trading_days) <= n:
              return today - timedelta(days=n * 2)   # Conservative fallback
          return trading_days[-(n + 1)]              # n-th trading day before today
      - `async def nightly_cleanup(*, bridge: Any | None, store: DuckDBStore, today: date | None = None, retention_trading_days: int = DEFAULT_RETENTION_TRADING_DAYS) -> int:` — body:
          today = today or date.today()
          cutoff = _trading_days_ago(today, retention_trading_days)
          rows = store.list_overlays_older_than(cutoff)
          removed = 0
          for overlay_id, shape_id in rows:
              # Best-effort remove from TV chart; tolerate failure
              if bridge is not None:
                  result = await bridge.call_tool("draw_remove_one", {"entity_id": shape_id})
                  if result is None:
                      store.write_audit_event(event_id=new_run_id(), ts_utc=datetime.now(timezone.utc), topic="cleanup_partial", entity_id=overlay_id, reason_code="draw_remove_failed", payload_json=json.dumps({"shape_id": shape_id}))
              # Always mark deleted_at — forensic history preserved (deleted_at != NULL)
              store.mark_tv_overlay_deleted(overlay_id=overlay_id)
              removed += 1
          # Single summary audit row
          store.write_audit_event(event_id=new_run_id(), ts_utc=datetime.now(timezone.utc), topic="cleanup_completed", entity_id=today.isoformat(), reason_code="nightly_cleanup", payload_json=json.dumps({"removed": removed, "cutoff": cutoff.isoformat()}))
          return removed
      - `class NightlyCleanupScheduler:`
          def __init__(self, *, on_cleanup: Callable[[], Coroutine[Any, Any, None]], fire_time_et: str = CLEANUP_FIRE_TIME_ET):
              self._scheduler = EodScheduler(on_flatten=on_cleanup, close_time_et=fire_time_et, lead_seconds=0, tz="America/New_York")
          async def run(self) -> None:
              await self._scheduler.run()

    Update `packages/tv-bridge/src/tv_bridge/__init__.py`:
      - `from .cleanup import nightly_cleanup, NightlyCleanupScheduler`
      - extend `__all__`

    Wire into `packages/api/src/api/app.py` lifespan. Insert AFTER the ReconciliationScheduler block (added by Plan 03) and BEFORE the `yield`:
      - `from tv_bridge import NightlyCleanupScheduler, nightly_cleanup`
      - `async def _do_cleanup() -> None:`
          await nightly_cleanup(bridge=app.state.tv_bridge, store=app.state.store)
      - `_cleanup_scheduler = NightlyCleanupScheduler(on_cleanup=_do_cleanup)`
      - `app.state.cleanup_task = asyncio.create_task(_cleanup_scheduler.run(), name="nightly_cleanup_scheduler")`

    Add to shutdown block (BEFORE the existing recon_task cancellation):
      - `app.state.cleanup_task.cancel()`
      - `try: await app.state.cleanup_task` / `except asyncio.CancelledError: pass`

    Convert `test_overlay_registry.py::test_nightly_cleanup` from xfail to real test per behavior. Add `test_nightly_cleanup_tolerates_mcp_failure` as a new test in the same file.

    Convert `test_tv_failure_isolation.py::test_pipeline_continues_when_tv_killed` from skip to real test. The test setup is the most involved; structure:
      - Set up a real EventBus and an in-memory DuckDBStore
      - Build a minimal mock_risk_manager that records every Signal it receives and writes an audit_log row with topic='risk_decision', reason_code='pass'
      - Build a minimal subscriber that consumes Signals from the bus, calls mock_risk_manager.check, and writes the audit_log row
      - Construct a TVBridge with a mock stdio_client; the mock yields a session that succeeds for ~3 events then raises ConnectionError on the 4th call_tool
      - Subscribe a Signal-producer task that publishes 10 Signal objects (using the existing Signal model — read packages/trading-core/src/trading_core/strategy/signal.py first) at 50ms intervals
      - After all 10 are published, await one event-loop tick + 100ms grace
      - Assertions:
          - mock_risk_manager.received_count == 10  (NO skipped signals)
          - len(store.query("SELECT * FROM audit_log WHERE topic='risk_decision'")) >= 10  (all decisions persisted)
          - bridge._session is None or bridge.is_connected is False (the bridge knows it's degraded)
          - The test completes in < 5 seconds (no deadlock)
      - This test goes in `packages/tv-bridge/tests/integration/` and uses `@pytest.mark.asyncio` (asyncio_mode="auto" from pyproject.toml means the marker is implicit).
      - BLOCKER 5 fix — DO NOT use `@pytest.mark.timeout(10)`. The project does not depend on `pytest-timeout` (it is not listed in CLAUDE.md's testing stack and is not in uv.lock). Instead, enforce the 10-second deadline INSIDE the async test body using `asyncio.wait_for`. Pattern:
        ```
        async def test_pipeline_continues_when_tv_killed():
            pipeline_ran_without_skip = asyncio.Event()
            # ... set up bridge + bus + risk_manager subscriber that calls pipeline_ran_without_skip.set() once 10 signals have been processed
            try:
                await asyncio.wait_for(pipeline_ran_without_skip.wait(), timeout=10.0)
            except asyncio.TimeoutError:
                pytest.fail("Pipeline did not complete 10 signals within 10s — TV failure isolation broken")
        ```
      - DO NOT add `pytest-timeout` to pyproject.toml. The asyncio.wait_for pattern provides equivalent guarantees using only the standard library + pytest-asyncio (already in the stack).

    Threat mitigations applied here:
      - T-06-04-01 (DoS via slow draw_remove_one): each call uses bridge.call_tool which already has a 12s timeout (from Plan 02). Cleanup runs through 200 overlays × 12s worst-case = 2400s — acceptable because the scheduler fires nightly off-hours and there's no other task contending.
      - T-06-04-02 (Information disclosure via cleanup audit rows): payload_json only contains overlay_id, shape_id, and counts — no price or strategy data.
  </action>
  <verify>
    <automated>uv run pytest packages/tv-bridge/tests/test_overlay_registry.py::test_nightly_cleanup packages/tv-bridge/tests/test_overlay_registry.py::test_nightly_cleanup_tolerates_mcp_failure packages/tv-bridge/tests/integration/test_tv_failure_isolation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/tv-bridge/src/tv_bridge/cleanup.py` exists with `nightly_cleanup` and `NightlyCleanupScheduler`
    - `grep -c "async def nightly_cleanup" packages/tv-bridge/src/tv_bridge/cleanup.py` returns 1
    - `grep -c "class NightlyCleanupScheduler" packages/tv-bridge/src/tv_bridge/cleanup.py` returns 1
    - `grep -c "DEFAULT_RETENTION_TRADING_DAYS = 5" packages/tv-bridge/src/tv_bridge/cleanup.py` returns 1
    - `grep -c "draw_remove_one" packages/tv-bridge/src/tv_bridge/cleanup.py` returns at least 1
    - `grep -c "from .cleanup import" packages/tv-bridge/src/tv_bridge/__init__.py` returns 1
    - `grep -c "app.state.cleanup_task" packages/api/src/api/app.py` returns at least 2 (create + cancel)
    - `uv run pytest packages/tv-bridge/tests/test_overlay_registry.py::test_nightly_cleanup -x -q` exits 0 (previously xfail-strict)
    - `uv run pytest packages/tv-bridge/tests/test_overlay_registry.py::test_nightly_cleanup_tolerates_mcp_failure -x -q` exits 0
    - `uv run pytest packages/tv-bridge/tests/integration/test_tv_failure_isolation.py -x -q` exits 0 (previously skipped) AND completes in < 10 seconds (enforced by `asyncio.wait_for(..., timeout=10.0)` inside the test body — BLOCKER 5 fix)
    - BLOCKER 5 fix: `grep -c "pytest.mark.timeout" packages/tv-bridge/tests/integration/test_tv_failure_isolation.py` returns 0 (pytest-timeout marker NOT used)
    - BLOCKER 5 fix: `grep -c "asyncio.wait_for" packages/tv-bridge/tests/integration/test_tv_failure_isolation.py` returns at least 1 (timeout enforced via stdlib)
    - BLOCKER 5 fix: `grep -c "pytest-timeout" pyproject.toml` returns 0 (dependency NOT added)
    - `uv run pytest packages/ -q` exits 0 with zero remaining xfail/skip markers in Phase 6 test files (06-VALIDATION.md "Status" column now ✅ for every row)
  </acceptance_criteria>
  <done>
    Nightly cleanup runs at 03:00 ET, drops shapes older than 5 trading days from DuckDB + best-effort from the live chart; the failure-isolation integration test proves the trading pipeline survives a mid-session TV crash with zero skipped signals; every Phase 6 task in 06-VALIDATION.md is now green.
  </done>
</task>

<task type="auto">
  <name>Task 2: AuthorTVAlertButton frontend component + blotter integration (TV-07)</name>
  <files>
    apps/web/components/AuthorTVAlertButton.tsx,
    apps/web/app/dashboard/blotter/page.tsx
  </files>
  <read_first>
    apps/web/app/dashboard/blotter/page.tsx,
    apps/web/lib/api.ts,
    apps/web/components/DegradationBanner.tsx,
    apps/web/CLAUDE.md,
    apps/web/AGENTS.md
  </read_first>
  <action>
    Read `apps/web/AGENTS.md` and `apps/web/node_modules/next/dist/docs/` for any Next.js 16.2-specific patterns before writing this component. The codebase uses 'use client', native fetch, no socket.io, and avoids React wrappers.

    Create `apps/web/components/AuthorTVAlertButton.tsx`:
      - First line: `'use client'`
      - Imports: `import { useState } from 'react'`, `import { API_BASE } from '@/lib/api'`
      - TypeScript interface:
          interface AuthorTVAlertButtonProps {
            strategyId: string
            condition: string
            message: string
          }
      - Component body:
          export default function AuthorTVAlertButton({ strategyId, condition, message }: AuthorTVAlertButtonProps) {
            const [busy, setBusy] = useState(false)
            const [toast, setToast] = useState<string | null>(null)
            const onClick = async () => {
              setBusy(true)
              setToast(null)
              try {
                const res = await fetch(`${API_BASE}/tv/alerts`, {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify({ strategy_id: strategyId, condition, message }),
                })
                if (!res.ok) {
                  const errText = await res.text()
                  setToast(`Alert failed: ${res.status} ${errText.slice(0, 120)}`)
                  return
                }
                const data: { alert_id: string; tv_alert_id: string } = await res.json()
                setToast(`TV alert created: ${data.tv_alert_id}`)
              } catch (e) {
                setToast(`Network error: ${String(e).slice(0, 120)}`)
              } finally {
                setBusy(false)
                // Auto-dismiss toast after 6s
                setTimeout(() => setToast(null), 6000)
              }
            }
            return (
              <div className="inline-flex items-center gap-2">
                <button
                  type="button"
                  onClick={onClick}
                  disabled={busy}
                  className="px-2 py-1 text-xs font-mono border border-gray-600 hover:bg-gray-800 disabled:opacity-50"
                  aria-label="Author TradingView alert for current strategy"
                >
                  {busy ? 'Authoring…' : 'Author TV Alert'}
                </button>
                {toast && (
                  <span className="text-xs font-mono text-amber-300" role="status">{toast}</span>
                )}
              </div>
            )
          }

    Wire the button into `apps/web/app/dashboard/blotter/page.tsx`:
      - Add the import at the top of the file alongside existing imports: `import AuthorTVAlertButton from '@/components/AuthorTVAlertButton'`
      - Locate the header bar where ETClock and ConnectionStatus are rendered (search for `<ETClock` to find the section)
      - Add a `<AuthorTVAlertButton>` instance next to ConnectionStatus. For Phase 6 the strategy params come from a hardcoded ORB default — pass `strategyId="orb"`, `condition="ORB long entry threshold"`, `message="ORB strategy alert"`. Phase 7's Strategy Controls panel (UI-07) replaces these with live values.
      - WARNING 4 fix — REQUIRED comment for traceability: add the inline JSX comment `{/* TODO(Phase 7 UI-07): replace hardcoded condition/message with live strategy registry values */}` immediately above the `<AuthorTVAlertButton ...>` usage in `apps/web/app/dashboard/blotter/page.tsx`. Additionally inside `apps/web/components/AuthorTVAlertButton.tsx`, add a JSDoc-style comment above the component declaration: `// TODO(Phase 7 UI-07): condition + message will be sourced from the live strategy registry; Phase 6 wires hardcoded ORB defaults from the call site.`
      - The button placement is in a flex container `<div className="flex items-center gap-3">` (use the existing classNames pattern in the file for consistency).

    Sanitization / threat mitigations:
      - T-06-04-03 (XSS via toast text): React's default JSX escaping protects against script injection in `errText` or `data.tv_alert_id`. `errText.slice(0, 120)` and `String(e).slice(0, 120)` cap message length to prevent runaway UI strings.
      - T-06-04-04 (Tampering via strategyId/condition/message props): the API layer already enforces Pydantic max_length validators (Plan 02); frontend props are best-effort caps.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && pnpm build --no-lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/AuthorTVAlertButton.tsx` exists
    - `grep -c "'use client'" apps/web/components/AuthorTVAlertButton.tsx` returns 1 (must be the first line, not a string elsewhere)
    - `grep -c "fetch(.*\\${API_BASE}/tv/alerts" apps/web/components/AuthorTVAlertButton.tsx` returns at least 1
    - `grep -c "export default function AuthorTVAlertButton" apps/web/components/AuthorTVAlertButton.tsx` returns 1
    - `grep -c "AuthorTVAlertButton" apps/web/app/dashboard/blotter/page.tsx` returns at least 2 (import + usage)
    - `cd apps/web && pnpm tsc --noEmit` exits 0 with zero type errors
    - `cd apps/web && pnpm build --no-lint` exits 0 (full Next.js build green)
    - Button has `aria-label` attribute (accessibility): `grep -c "aria-label=" apps/web/components/AuthorTVAlertButton.tsx` returns 1
    - Toast has `role="status"` so screen readers announce it: `grep -c "role=\"status\"" apps/web/components/AuthorTVAlertButton.tsx` returns 1
    - WARNING 4 fix: `grep -c "TODO(Phase 7 UI-07)" apps/web/app/dashboard/blotter/page.tsx` returns at least 1 (traceability marker for Phase 7 wiring)
    - WARNING 4 fix: `grep -c "TODO(Phase 7 UI-07)" apps/web/components/AuthorTVAlertButton.tsx` returns at least 1 (component-internal marker)
  </acceptance_criteria>
  <done>
    AuthorTVAlertButton is a self-contained client component rendered in the blotter header; it POSTs to /tv/alerts and shows a 6-second toast with the returned tv_alert_id or an error; TypeScript build is clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify TV Desktop integration (drawing, focus, alert authoring)</name>
  <what-built>
    Phase 6 is feature-complete: TVBridge supervisor + bus subscribers + draw_shape orchestration with 200-cap; POST /tv/focus, POST /tv/alerts, DELETE /tv/alerts/{id}; TVReplayDataSource + reconciliation; nightly cleanup; AuthorTVAlertButton frontend. The unit + integration tests prove the contracts; this checkpoint confirms the visual behavior the unit tests cannot assert.
  </what-built>
  <how-to-verify>
    PREREQUISITES:
      1. TradingView Desktop is running with a chart open (any symbol — the test will retarget it).
      2. The `tradingview-mcp-jackson` Node server can be reached (verify by inspecting `C:\Users\Admin\tradingview-mcp-jackson\src\server.js` exists).
      3. `.env` has TWELVEDATA_API_KEY set (optional — reconciliation will skip cleanly if missing).
      4. Start the FastAPI server: `uv run uvicorn api.app:app --host 127.0.0.1 --port 8000`. Watch the logs — you should see `tv_bridge.connected` within ~30s of startup.
      5. Start the Next.js dev server: `cd apps/web && pnpm dev`.

    STEP 1 — POST /tv/focus drives the chart:
      Run from a separate terminal:
      ```
      curl -X POST -H "Content-Type: application/json" -d "{\"symbol\":\"ES\",\"date\":\"2024-06-12\"}" http://127.0.0.1:8000/tv/focus
      ```
      Expected: HTTP response is `{"status":"accepted","symbol":"ES","date":"2024-06-12"}` within < 1 second.
      Visual: Within ~15 seconds, TV Desktop chart switches to `CME_MINI:ES1!` 1m and scrolls to 2024-06-12.

    STEP 2 — Drawing on synthetic Signal:
      A. Open the existing dashboard at http://localhost:3000/dashboard
      B. Trigger a backtest (use the Run Backtest CLI in another terminal):
         `uv run python scripts/run_backtest.py --strategy orb --symbol SPY --tf 1m --from 2024-06-12 --to 2024-06-13 --config config/strategies/orb.yaml`
      C. If the backtest produces a Signal, it is published on the bus. The TV bridge subscribes to it.
      Visual: Within 2 seconds of any emitted signal, on the TV Desktop ES chart, you should see:
        - One horizontal line (entry arrow) at the entry price, green for long / red for short
        - One dashed horizontal line at the stop price (red)
        - One dashed horizontal line at the target price (blue)
        - A yellow rectangle drawn around the 09:30–09:45 ET opening range bars
      NOTE: If the chart is not on ES (because STEP 1's focus changed it), the shapes will land at numerically correct prices but visually off-chart for SPY. To verify the shapes, open TV's drawing list panel or call:
        ```
        curl http://127.0.0.1:8000/tv/status
        ```

    STEP 3 — POST /tv/focus is non-blocking even when chart is cold:
      Restart TV Desktop (close & re-open) so the next chart_set_symbol is "cold". Then call:
      ```
      curl -w "Time: %{time_total}s\n" -X POST -H "Content-Type: application/json" -d "{\"symbol\":\"MES\",\"date\":\"2024-08-15\"}" http://127.0.0.1:8000/tv/focus
      ```
      Expected: `Time:` reported by curl is < 0.5 seconds (the HTTP response is fire-and-forget). The actual chart update may take up to 15s.

    STEP 4 — Author TV Alert button:
      A. Open http://localhost:3000/dashboard/blotter
      B. In the header, locate the "Author TV Alert" button.
      C. Click it.
      Visual:
        - Button becomes "Authoring…" briefly.
        - A toast appears next to the button reading `TV alert created: <some-id>`.
        - Open TV Desktop's Alerts panel (top toolbar). You should see a new alert.
      D. Verify the alert is registered in DuckDB:
         `uv run python -c "import duckdb; conn = duckdb.connect('data/duckdb/trading.duckdb', read_only=True); print(conn.execute('SELECT alert_id, tv_alert_id, condition FROM tv_alerts WHERE deleted_at IS NULL').fetchall())"`
         Expected: at least one row, tv_alert_id matches what the toast displayed.

    STEP 5 — TV crash isolation:
      A. With the backtest CLI still running (or after triggering a fresh one), forcibly kill TV Desktop (Task Manager → End Task).
      B. Watch the FastAPI logs.
      Expected:
        - `tv_bridge.reconnecting attempt=0 backoff=1` then 2, 4, 8 — exponential backoff.
        - The Next.js dashboard's degradation banner appears (Phase 3 already wired this to TOPIC_DEGRADED_STATE).
        - The backtest CLI continues without printing any TV-related errors. Signals + fills + audit_log all keep working.
      C. Restart TV Desktop.
      Expected: Within ~30s, FastAPI logs show `tv_bridge.connected` and the degradation banner clears.

    STEP 6 — Reconciliation smoke (optional; only meaningful at end-of-RTH):
      If running this checkpoint near 16:10 ET on a trading day with both TV and Twelve Data live, watch for the `reconciliation_scheduler.fired` log around 16:10. Otherwise verify the scheduler is registered:
      `uv run python -c "import asyncio; print(asyncio.all_tasks())"` — not directly inspectable from outside the FastAPI process; skip if uncomfortable.

    RECORD RESULTS in `.planning/phases/06-tradingview-mcp-bridge/06-04-CHECKPOINT-NOTES.md`. Use this template:
      ```
      # Phase 6 Plan 04 — Human Verify Checkpoint Notes

      **Verified:** YYYY-MM-DD by <operator>
      **TV Desktop version:** <version>
      **MCP server commit:** <git rev-parse HEAD at C:/Users/Admin/tradingview-mcp-jackson>

      ## Step 1 — POST /tv/focus
      - HTTP response time: __ ms
      - Chart visual update time: __ s
      - Result: PASS / FAIL — <notes>

      ## Step 2 — Drawing on Signal
      - Entry arrow appeared: YES / NO
      - Stop line appeared: YES / NO
      - Target line appeared: YES / NO
      - ORB box appeared at 09:30–09:45 ET: YES / NO
      - Latency from signal publish → shapes visible: __ s
      - Result: PASS / FAIL

      ## Step 3 — POST /tv/focus cold
      - HTTP response time: __ ms (< 500ms required)
      - Result: PASS / FAIL

      ## Step 4 — Author TV Alert
      - Toast appeared with tv_alert_id: YES / NO
      - DB row present: YES / NO
      - TV Desktop alerts panel shows the new alert: YES / NO
      - Result: PASS / FAIL

      ## Step 5 — TV crash isolation
      - Pipeline continued during TV down: YES / NO
      - Degradation banner appeared: YES / NO
      - Auto-reconnect on TV restart: YES / NO
      - Result: PASS / FAIL

      ## Step 6 — Reconciliation (optional)
      - Skipped / Verified at __

      ## Issues found
      <bullets>

      ## Sign-off
      All required steps (1, 2, 3, 4, 5) PASS — Phase 6 ready for verification.
      ```
  </how-to-verify>
  <resume-signal>
    Type `approved` after STEP 1, 2, 3, 4, 5 all PASS and the notes file is committed. If any step FAILS, type `failed: <step>: <description>` so the orchestrator can open a gap-closure plan.
  </resume-signal>
  <acceptance_criteria>
    - `.planning/phases/06-tradingview-mcp-bridge/06-04-CHECKPOINT-NOTES.md` exists and contains the template above filled in
    - All of Steps 1–5 marked PASS
    - File is committed (will be included in the phase-completion git commit)
  </acceptance_criteria>
  <done>
    Operator has personally confirmed: drawing-on-signal works on the live chart, /tv/focus does drive the chart, alert authoring round-trips through TV Desktop, and a forced TV crash does not disrupt the trading pipeline. Checkpoint notes file committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend POST → /tv/alerts | Browser → FastAPI; body validated by Pydantic (already from Plan 02), additionally `strategyId`/`condition`/`message` come from React component props (developer-controlled in Phase 6, user-controlled in Phase 7) |
| Cleanup scheduler → live TV MCP | Once per day; draw_remove_one calls are best-effort, never block the cleanup if they fail |
| Integration test → real EventBus | The failure-isolation test exercises real asyncio coroutines, not mocks at the EventBus layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Denial of Service | nightly_cleanup iterating thousands of shapes | mitigate | Each call_tool has a 12s timeout (inherited from Plan 02); the cleanup runs at 03:00 ET off-hours; with the 200-shape cap and 5-day retention, max ~1000 shapes accumulate; worst-case run time is bounded |
| T-06-04-02 | Information Disclosure | cleanup audit rows | accept | Only overlay_id, shape_id, counts in payload_json — no price or strategy data |
| T-06-04-03 | XSS via toast text | AuthorTVAlertButton toast rendering | mitigate | React JSX escapes by default; `slice(0, 120)` caps lengths; no `dangerouslySetInnerHTML` used |
| T-06-04-04 | Tampering via frontend props | AuthorTVAlertButton props | mitigate | API layer (Plan 02) enforces max_length on every string field; client-side caps are defense in depth |
| T-06-04-05 | Denial of Service | TV Desktop force-kill during integration test | mitigate | This is the exact threat we test for — the bridge isolates failures via `_safe_draw_signal` (Plan 02) and the integration test in Task 1 proves it |
| T-06-04-06 | Repudiation | Cleanup partial failures invisible | mitigate | Every failed draw_remove_one writes a `topic='cleanup_partial'` audit row; the summary `cleanup_completed` row records the total |
</threat_model>

<verification>
- All Task 1 acceptance criteria pass; the failure-isolation integration test runs in < 10s.
- All Task 2 acceptance criteria pass; `pnpm tsc --noEmit` and `pnpm build --no-lint` both succeed.
- Task 3 checkpoint notes file exists with all 5 required steps marked PASS.
- Full suite: `uv run pytest packages/ -q` exits 0 with ZERO xfailed and ZERO skipped tests across all Phase 6 test files (06-VALIDATION.md is fully ✅).
- Frontend: `cd apps/web && pnpm test 2>/dev/null` is acceptable if no frontend test runner is configured; otherwise it must pass.
</verification>

<success_criteria>
1. `uv run pytest packages/tv-bridge/ packages/api/tests/test_tv_routes.py packages/trading-core/tests/storage/ packages/trading-core/tests/test_protocols.py -q` exits 0 with all Phase 6 task IDs in 06-VALIDATION.md showing ✅ status.
2. `uv run pytest packages/tv-bridge/tests/integration/ -q` exits 0 within 10 seconds.
3. `cd apps/web && pnpm tsc --noEmit && pnpm build --no-lint` exits 0.
4. `.planning/phases/06-tradingview-mcp-bridge/06-04-CHECKPOINT-NOTES.md` exists with all 5 required steps marked PASS.
5. `uv run pytest packages/ -q` exits 0 (no regressions in any earlier phase).
6. All 8 Phase 6 requirement IDs (TV-01, TV-02, TV-03, TV-04, TV-05, TV-06, TV-07, MD-10) have at least one implementing task across Plans 01–04 (cross-check in 06-04-SUMMARY.md).
</success_criteria>

<output>
After completion, create `.planning/phases/06-tradingview-mcp-bridge/06-04-SUMMARY.md` describing:
- The nightly_cleanup schedule (03:00 ET, retention 5 trading days, best-effort draw_remove_one)
- The integration test methodology and the observed behavior under forced disconnect
- The AuthorTVAlertButton placement + props (current hardcoded values + the Phase 7 hookup point)
- The full checkpoint sign-off summary (which of Steps 1–5 passed, any issues observed)
- The final Phase 6 traceability matrix mapping each REQ-ID to the plan + test that proves it
</output>
