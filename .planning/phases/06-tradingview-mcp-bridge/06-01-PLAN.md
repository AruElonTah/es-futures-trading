---
phase: 06-tradingview-mcp-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/trading-core/src/trading_core/storage/schema.sql
  - packages/trading-core/src/trading_core/storage/duckdb_store.py
  - packages/trading-core/tests/storage/test_schema.py
  - packages/trading-core/tests/storage/test_duckdb_store.py
  - packages/tv-bridge/src/tv_bridge/__init__.py
  - packages/tv-bridge/src/tv_bridge/bridge.py
  - packages/tv-bridge/tests/conftest.py
  - packages/tv-bridge/tests/test_bridge.py
  - packages/tv-bridge/tests/test_overlay_registry.py
  - packages/tv-bridge/tests/test_replay_source.py
  - packages/tv-bridge/tests/test_reconciliation.py
  - packages/tv-bridge/tests/integration/test_tv_failure_isolation.py
  - packages/api/tests/test_tv_routes.py
autonomous: true
requirements: [TV-01, TV-02, TV-07]
tags: [tv-bridge, mcp, duckdb, schema]
user_setup:
  - service: tradingview_mcp
    why: "Wave 0 task 4 needs a live draw_shape call against TV Desktop to capture the entity_id field name"
    env_vars: []
    dashboard_config:
      - task: "TradingView Desktop must be running with chart open to CME_MINI:ES1! (or any visible chart)"
        location: "Local TV Desktop app"
      - task: "tradingview-mcp-jackson Node server reachable at C:\\Users\\Admin\\tradingview-mcp-jackson"
        location: "Local filesystem"

must_haves:
  truths:
    - "DuckDB schema has tv_overlays and tv_alerts tables after ensure_schema()"
    - "DuckDBStore exposes write_tv_overlay, write_tv_alert, mark_tv_alert_deleted, count_active_overlays, get_tv_alert_tv_id methods"
    - "`from tv_bridge import TVBridge` succeeds; TVBridge.__init__(store, bus, settings) constructs without spawning a subprocess"
    - "TVBridge.start() and TVBridge.stop() are awaitable; call_tool() returns None when no session is active"
    - "All Wave 0 test stub files exist with the test names from 06-VALIDATION.md (each test currently xfails or skips, no orphan ImportErrors)"
    - "draw_shape entity_id field name is documented in 06-RESEARCH.md (Open Question 1 resolved before Plan 02 starts)"
  artifacts:
    - path: "packages/trading-core/src/trading_core/storage/schema.sql"
      provides: "tv_overlays + tv_alerts DDL"
      contains: "CREATE TABLE IF NOT EXISTS tv_overlays"
    - path: "packages/trading-core/src/trading_core/storage/duckdb_store.py"
      provides: "TV writer methods + count_active_overlays read"
      exports: ["write_tv_overlay", "write_tv_alert", "mark_tv_alert_deleted", "count_active_overlays", "get_tv_alert_tv_id"]
    - path: "packages/tv-bridge/src/tv_bridge/bridge.py"
      provides: "TVBridge class skeleton (constructor, start, stop, call_tool wrapper)"
      contains: "class TVBridge"
    - path: "packages/tv-bridge/src/tv_bridge/__init__.py"
      provides: "Public TVBridge symbol"
      exports: ["TVBridge"]
    - path: "packages/tv-bridge/tests/conftest.py"
      provides: "Shared fixtures: in-memory DuckDBStore, mock ClientSession, mock EventBus"
    - path: "packages/tv-bridge/tests/test_bridge.py"
      provides: "Wave 0 stubs: test_bridge_importable + xfail stubs for test_reconnect, test_draw_on_signal, test_draw_timeout_nonblocking"
    - path: "packages/tv-bridge/tests/test_overlay_registry.py"
      provides: "Wave 0 stubs: test_write_overlay, test_cap_enforcement, test_nightly_cleanup"
    - path: "packages/tv-bridge/tests/test_replay_source.py"
      provides: "Wave 0 stubs: test_fetch_bars"
    - path: "packages/tv-bridge/tests/test_reconciliation.py"
      provides: "Wave 0 stubs: test_price_divergence, test_audit_log_write"
    - path: "packages/tv-bridge/tests/integration/test_tv_failure_isolation.py"
      provides: "Wave 0 stub: skipped until Plan 04 (marked with pytest.mark.skip)"
    - path: "packages/api/tests/test_tv_routes.py"
      provides: "Wave 0 stubs: test_tv_focus, test_create_delete_alert"
  key_links:
    - from: "DuckDBStore.ensure_schema()"
      to: "schema.sql tv_overlays/tv_alerts DDL"
      via: "file read at module-init time"
      pattern: "CREATE TABLE IF NOT EXISTS tv_(overlays|alerts)"
    - from: "packages/tv-bridge/src/tv_bridge/__init__.py"
      to: "TVBridge in bridge.py"
      via: "re-export"
      pattern: "from .bridge import TVBridge"
    - from: "TVBridge.__init__"
      to: "DuckDBStore + EventBus references (NEVER opens its own connection)"
      via: "constructor injection"
      pattern: "def __init__\\(self, store: DuckDBStore, bus: EventBus"
---

<objective>
Deliver the Wave 1 foundation slice for Phase 6 (TradingView MCP Bridge): the persistence layer (tv_overlays + tv_alerts DuckDB tables and matching DuckDBStore writer methods), the TVBridge class skeleton (importable, instantiable, with start/stop/call_tool no-op stubs that downstream waves complete), and the full Wave 0 test scaffolding so every Phase 6 task ID in 06-VALIDATION.md has a destination test file before any production logic is written.

Purpose: Establish schema + skeleton first so Wave 2 can fill in the supervisor loop and bus-subscriber logic without the executor scavenger-hunting for storage signatures or test conventions. This is the thinnest end-to-end slice that makes "a TV bridge object exists and DuckDB can persist its overlays" observably true.

Output: Updated `schema.sql`, new `DuckDBStore` methods, importable `TVBridge` class with sync constructor (no MCP subprocess spawn), 6 new test files with stubbed-but-named test functions matching the 06-VALIDATION.md task IDs, and a one-off Wave 0 verification of the `draw_shape` MCP response shape (Open Question 1 from RESEARCH.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
@.planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md
@.planning/phases/06-tradingview-mcp-bridge/06-VALIDATION.md
@packages/trading-core/src/trading_core/storage/schema.sql
@packages/trading-core/src/trading_core/storage/duckdb_store.py
@packages/trading-core/src/trading_core/data/tradingview.py
@packages/tv-bridge/src/tv_bridge/__init__.py
@packages/tv-bridge/tests/conftest.py
@packages/tv-bridge/tests/test_import.py

<interfaces>
<!-- Key contracts and call signatures the executor needs. Extracted from codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From packages/trading-core/src/trading_core/storage/duckdb_store.py:
- DuckDBStore is constructed as DuckDBStore(db_path) and exposes ensure_schema() which reads schema.sql verbatim
- Existing writer signature pattern (keyword-only): write_audit_event(*, event_id: str, ts_utc: datetime, topic: str, entity_id: str, reason_code: str, payload_json: str) -> None
- Module-level SQL constants follow naming pattern WRITE_*_SQL (UPPER_SNAKE)
- _LockedConn proxy automatically serializes execute() calls — no extra locking needed in new methods
- new_run_id() returns a uuid7 string and is imported lazily inside writer methods to avoid cycles (see write_engine_state)
- _ET = ZoneInfo("America/New_York") is already defined at module level

From packages/trading-core/src/trading_core/events/__init__.py:
- EventBus, TOPIC_SIGNALS, TOPIC_FILLS, TOPIC_DEGRADED_STATE, DegradedStateEvent are re-exported

From packages/trading-core/src/trading_core/config.py:
- Settings is the pydantic-settings class loaded from .env + config/*.yaml

From packages/trading-core/src/trading_core/data/tradingview.py:
- _DEFAULT_MCP_SERVER_PATH = Path(r"C:\Users\Admin\tradingview-mcp-jackson")
- _SYMBOL_MAP = {"ES": "CME_MINI:ES1!", "MES": "CME_MINI:MES1!"}
- _server_params() returns StdioServerParameters(command="node", args=[str(server_js)], env=None) where server_js = mcp_server_path / "src" / "server.js"
- _parse_tool_payload(result) handles result.content[0].text -> json.loads -> dict | {"_raw": text} | {"_value": payload}
- _INIT_TIMEOUT_SECONDS = 15.0
- _publish_degraded(reason) publishes a DegradedStateEvent with source=self.name on the bus
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tv_overlays + tv_alerts DDL to schema.sql and DuckDBStore writer methods (06-01-01, 06-01-02)</name>
  <files>
    packages/trading-core/src/trading_core/storage/schema.sql,
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    packages/trading-core/tests/storage/test_schema.py,
    packages/trading-core/tests/storage/test_duckdb_store.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/storage/schema.sql,
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    .planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md,
    .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
  </read_first>
  <behavior>
    - test_schema.py::test_tv_overlays_table_exists — after ensure_schema(), `SELECT * FROM tv_overlays LIMIT 0` succeeds and returns the column list: overlay_id, strategy_id, signal_id, shape_kind, shape_id, trading_date, created_at, deleted_at
    - test_schema.py::test_tv_alerts_table_exists — after ensure_schema(), `SELECT * FROM tv_alerts LIMIT 0` succeeds and returns: alert_id, strategy_id, tv_alert_id, condition, created_at, deleted_at
    - test_duckdb_store.py::test_write_tv_overlay_inserts_row — calling store.write_tv_overlay(overlay_id=uuid7, strategy_id="orb", signal_id="sig1", shape_kind="entry_arrow", shape_id="shape_abc", trading_date=date(2026,5,19)) results in `SELECT COUNT(*) FROM tv_overlays WHERE overlay_id = ?` == 1
    - test_duckdb_store.py::test_count_active_overlays_excludes_deleted — after writing 3 overlays and marking 1 with deleted_at, count_active_overlays() returns 2
    - test_duckdb_store.py::test_write_tv_alert_and_get_tv_id — write_tv_alert(alert_id, strategy_id, tv_alert_id, condition) followed by get_tv_alert_tv_id(alert_id) returns the tv_alert_id; mark_tv_alert_deleted(alert_id) sets deleted_at and subsequent get_tv_alert_tv_id of the same alert_id still returns the tv_alert_id (we keep history; the API layer reads deleted_at separately if needed)
  </behavior>
  <action>
    Append to `packages/trading-core/src/trading_core/storage/schema.sql` (after the existing Phase 5 engine_state block) two new `CREATE TABLE IF NOT EXISTS` blocks for `tv_overlays` and `tv_alerts` exactly matching the column list in 06-RESEARCH.md §tv_overlays Schema and 06-PATTERNS.md schema.sql section. Columns for `tv_overlays`: overlay_id VARCHAR PRIMARY KEY, strategy_id VARCHAR NOT NULL, signal_id VARCHAR NOT NULL, shape_kind VARCHAR NOT NULL, shape_id VARCHAR NOT NULL, trading_date DATE NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, deleted_at TIMESTAMPTZ. Columns for `tv_alerts`: alert_id VARCHAR PRIMARY KEY, strategy_id VARCHAR NOT NULL, tv_alert_id VARCHAR NOT NULL, condition VARCHAR NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, deleted_at TIMESTAMPTZ. Use the same comment style and quoted `IF NOT EXISTS` pattern as the existing audit_log / engine_state blocks.

    In `packages/trading-core/src/trading_core/storage/duckdb_store.py`, add module-level SQL constants `WRITE_TV_OVERLAY_SQL`, `WRITE_TV_ALERT_SQL`, `MARK_TV_ALERT_DELETED_SQL`, `COUNT_ACTIVE_OVERLAYS_SQL`, `GET_TV_ALERT_TV_ID_SQL`, and `LIST_OVERLAYS_OLDER_THAN_SQL` near the existing `WRITE_AUDIT_EVENT_SQL` block (lines ~167–172). Statement bodies:
      - WRITE_TV_OVERLAY_SQL: `INSERT INTO tv_overlays (overlay_id, strategy_id, signal_id, shape_kind, shape_id, trading_date) VALUES (?, ?, ?, ?, ?, ?);`
      - WRITE_TV_ALERT_SQL: `INSERT INTO tv_alerts (alert_id, strategy_id, tv_alert_id, condition) VALUES (?, ?, ?, ?);`
      - MARK_TV_ALERT_DELETED_SQL: `UPDATE tv_alerts SET deleted_at = now() WHERE alert_id = ?;`
      - COUNT_ACTIVE_OVERLAYS_SQL: `SELECT COUNT(*) FROM tv_overlays WHERE deleted_at IS NULL;`
      - GET_TV_ALERT_TV_ID_SQL: `SELECT tv_alert_id FROM tv_alerts WHERE alert_id = ?;`
      - LIST_OVERLAYS_OLDER_THAN_SQL: `SELECT overlay_id, shape_id FROM tv_overlays WHERE deleted_at IS NULL AND trading_date < ?;`

    Add five new methods to `DuckDBStore` using the keyword-only signature pattern already used by `write_audit_event` (no positional kwargs, just `self` then `*`). Order them after `write_engine_state` (around line 687):
      - `write_tv_overlay(*, overlay_id: str, strategy_id: str, signal_id: str, shape_kind: str, shape_id: str, trading_date: date) -> None` — single execute with parameter list; no return value
      - `write_tv_alert(*, alert_id: str, strategy_id: str, tv_alert_id: str, condition: str) -> None`
      - `mark_tv_alert_deleted(*, alert_id: str) -> None`
      - `mark_tv_overlay_deleted(*, overlay_id: str) -> None` — uses `UPDATE tv_overlays SET deleted_at = now() WHERE overlay_id = ?;` (inline SQL since this is only used by Plan 04's cleanup task)
      - `count_active_overlays() -> int` — runs `COUNT_ACTIVE_OVERLAYS_SQL`, fetchone()[0]
      - `get_tv_alert_tv_id(alert_id: str) -> str | None` — runs `GET_TV_ALERT_TV_ID_SQL`, returns row[0] or None
      - `list_overlays_older_than(trading_date: date) -> list[tuple[str, str]]` — runs `LIST_OVERLAYS_OLDER_THAN_SQL`, returns fetchall() of (overlay_id, shape_id) tuples for Plan 04

    Add `from datetime import date` to the existing datetime import line (already imports datetime, timezone — extend to include `date`).

    Create `packages/trading-core/tests/storage/test_schema.py` (if missing) with the two table-existence tests above. Create / extend `packages/trading-core/tests/storage/test_duckdb_store.py` with the four `test_*tv_*` tests above. Use the existing in-memory DuckDBStore fixture pattern from Phase 4/5 tests (`DuckDBStore(":memory:")` then `store.ensure_schema()`). Each new test imports `from trading_core.storage.runs import new_run_id` for PK values.

    Do NOT add `tv_overlays` / `tv_alerts` to any existing query method. The cleanup query (Plan 04) and read paths (Plan 02) use the new methods directly.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/storage/test_schema.py -k tv_ -x -q AND uv run pytest packages/trading-core/tests/storage/test_duckdb_store.py -k tv_ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS tv_overlays" packages/trading-core/src/trading_core/storage/schema.sql` returns 1
    - `grep -c "CREATE TABLE IF NOT EXISTS tv_alerts" packages/trading-core/src/trading_core/storage/schema.sql` returns 1
    - `grep -c "def write_tv_overlay" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns 1
    - `grep -c "def write_tv_alert" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns 1
    - `grep -c "def count_active_overlays" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns 1
    - `grep -c "def get_tv_alert_tv_id" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns 1
    - `grep -c "def mark_tv_alert_deleted" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns 1
    - `grep -c "def list_overlays_older_than" packages/trading-core/src/trading_core/storage/duckdb_store.py` returns 1
    - `uv run pytest packages/trading-core/tests/storage/ -k tv_ -x -q` exits 0 with at least 6 passing tests
    - No existing test in `packages/trading-core/tests/storage/` regresses (`uv run pytest packages/trading-core/tests/storage/ -x -q` exits 0)
  </acceptance_criteria>
  <done>
    schema.sql contains both new CREATE TABLE blocks; DuckDBStore exposes 7 new TV-related methods; tests covering insert, count-active, mark-deleted, and get-tv-alert-id all pass; no Phase 1–5 storage tests regress.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TVBridge class skeleton + tv_bridge package wiring (06-01-03)</name>
  <files>
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/src/tv_bridge/bridge.py,
    packages/tv-bridge/tests/conftest.py,
    packages/tv-bridge/tests/test_bridge.py
  </files>
  <read_first>
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/conftest.py,
    packages/tv-bridge/tests/test_import.py,
    packages/trading-core/src/trading_core/data/tradingview.py,
    .planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md,
    .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
  </read_first>
  <behavior>
    - test_bridge.py::test_bridge_importable — `from tv_bridge import TVBridge` succeeds; `TVBridge(store=store, bus=bus, settings=settings)` constructs without exception and does NOT spawn a subprocess (verify by inspecting that `bridge._session is None` immediately after construction)
    - test_bridge.py::test_call_tool_returns_none_when_no_session — `await bridge.call_tool("any_tool", {})` returns None and emits a structlog warning when `_session is None` (no exception raised)
    - test_bridge.py::test_start_creates_supervisor_task — calling `await bridge.start()` sets `bridge._supervisor_task` to a non-None asyncio.Task; calling `await bridge.stop()` cancels the task cleanly with no exception
    - test_bridge.py::test_reconnect (XFAIL stub) — marked `@pytest.mark.xfail(reason="implemented in Plan 02")` so Wave 2 can flip it to a real test
    - test_bridge.py::test_draw_on_signal (XFAIL stub) — marked xfail with same reason
    - test_bridge.py::test_draw_timeout_nonblocking (XFAIL stub) — marked xfail with same reason
  </behavior>
  <action>
    Create `packages/tv-bridge/src/tv_bridge/bridge.py` with a `TVBridge` class skeleton. Required imports (top of file, after `from __future__ import annotations`): `asyncio`, `io`, `from datetime import datetime, timezone`, `from pathlib import Path`, `from typing import Any`, `structlog`, `from mcp import ClientSession, StdioServerParameters`, `from mcp.client.stdio import stdio_client`, `from trading_core.config import Settings`, `from trading_core.events import EventBus, TOPIC_DEGRADED_STATE, DegradedStateEvent`, `from trading_core.storage.duckdb_store import DuckDBStore`. Module-level `_log = structlog.get_logger(__name__)`.

    Module-level constants (mirror tradingview.py):
      - `_DEFAULT_MCP_SERVER_PATH = Path(r"C:\Users\Admin\tradingview-mcp-jackson")`
      - `_SYMBOL_MAP: dict[str, str] = {"ES": "CME_MINI:ES1!", "MES": "CME_MINI:MES1!"}`
      - `_INIT_TIMEOUT_SECONDS = 15.0`
      - `_TOOL_TIMEOUT_SECONDS = 12.0`
      - `_SYMBOL_ALLOWLIST: frozenset[str] = frozenset({"ES", "MES", "SPY"})` — used by Plan 02's focus() validation (declare here so the constant is one-place-of-truth)

    `TVBridge` class with:
      - class attribute `name = "tv_bridge"`
      - class attribute `_BACKOFF_SECONDS = [1, 2, 4, 8, 16, 30]`
      - `__init__(self, *, store: DuckDBStore, bus: EventBus, settings: Settings, mcp_server_path: Path | None = None)` — stores references on self; initializes `self._session: ClientSession | None = None`, `self._session_lock: asyncio.Lock = asyncio.Lock()`, `self._draw_semaphore: asyncio.Semaphore = asyncio.Semaphore(3)`, `self._stderr_capture: io.StringIO = io.StringIO()`, `self._supervisor_task: asyncio.Task | None = None`, `self._mcp_server_path = mcp_server_path or _DEFAULT_MCP_SERVER_PATH`. Does NOT call `stdio_client` or `subprocess.Popen` — construction is pure attribute setup.
      - `is_connected` property returning `self._session is not None`
      - `_server_params(self) -> StdioServerParameters` — returns `StdioServerParameters(command="node", args=[str(self._mcp_server_path / "src" / "server.js")], env=None)` (verbatim copy from tradingview.py)
      - `@staticmethod _parse_tool_payload(result: object) -> dict[str, Any]` — exact copy of the helper from tradingview.py lines 155–169 (see PATTERNS.md Pattern 4)
      - `async def _publish_degraded(self, reason: str) -> None` — exact copy of the helper from tradingview.py lines 138–153, but with `source="tv_bridge"`
      - `async def call_tool(self, tool: str, args: dict[str, Any]) -> dict[str, Any] | None` — body matches PATTERNS.md "safe_call_tool" snippet: acquire `_session_lock`, copy `_session` into a local, release; if local is None log warning `tv_bridge.no_session` and return None; otherwise wrap `session.call_tool(tool, args)` in `asyncio.wait_for(..., timeout=_TOOL_TIMEOUT_SECONDS)`, return `self._parse_tool_payload(result)`; on `asyncio.TimeoutError` log `tv_bridge.tool_timeout` and return None; on any other Exception log `tv_bridge.tool_error` and return None. NEVER re-raises.
      - `async def _supervisor_loop(self) -> None` — STUB IMPLEMENTATION for Wave 1: body is just `# Wave 2 (Plan 02) implements the supervisor loop; this stub keeps start()/stop() safe.\n        try:\n            await asyncio.Future()\n        except asyncio.CancelledError:\n            return`. Plan 02 will replace this body with the full reconnect loop.
      - `async def start(self) -> None` — creates and stores the supervisor task: `self._supervisor_task = asyncio.create_task(self._supervisor_loop(), name="tv_bridge.supervisor")`
      - `async def stop(self) -> None` — if `_supervisor_task is not None`, cancel it and await (suppressing CancelledError); set `_session = None`

    Update `packages/tv-bridge/src/tv_bridge/__init__.py` to `from .bridge import TVBridge\n\n__all__ = ["TVBridge"]\n` (replace the placeholder docstring-only file).

    Extend `packages/tv-bridge/tests/conftest.py` with shared fixtures:
      - `@pytest.fixture def in_memory_store()` — yields a `DuckDBStore(":memory:")` after calling `ensure_schema()`; closes on teardown
      - `@pytest.fixture def mock_bus()` — returns a minimal stub object with `async def publish(self, topic, event)` recording calls into a list `mock_bus.published_events`
      - `@pytest.fixture def mock_settings()` — returns a real `Settings()` constructed from defaults (matches Phase 1 test pattern)
      - `@pytest.fixture def mock_mcp_session()` — returns a `unittest.mock.AsyncMock` configured so `call_tool(...)` returns an object with `.content[0].text = '{}'` by default; tests can override per-test
      - `@pytest.fixture def bridge(in_memory_store, mock_bus, mock_settings)` — returns `TVBridge(store=in_memory_store, bus=mock_bus, settings=mock_settings)` (constructed but NOT started)

    Create `packages/tv-bridge/tests/test_bridge.py` with the four real tests (test_bridge_importable, test_call_tool_returns_none_when_no_session, test_start_creates_supervisor_task) and three xfail-marked stubs (test_reconnect, test_draw_on_signal, test_draw_timeout_nonblocking). Each xfail uses `@pytest.mark.xfail(reason="implemented in Plan 02", strict=True)` and a body of `pytest.fail("Plan 02 implements")` — strict ensures Plan 02's executor cannot accidentally skip the conversion.

    Delete `packages/tv-bridge/tests/test_import.py` only if its single test is preserved as `test_bridge_importable` (verify the assertion is equivalent — the existing test just imports tv_bridge; the new test additionally constructs TVBridge).
  </action>
  <verify>
    <automated>uv run pytest packages/tv-bridge/tests/test_bridge.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/tv-bridge/src/tv_bridge/bridge.py` exists and `grep -c "class TVBridge" packages/tv-bridge/src/tv_bridge/bridge.py` returns 1
    - `grep -c "from .bridge import TVBridge" packages/tv-bridge/src/tv_bridge/__init__.py` returns 1
    - `uv run python -c "from tv_bridge import TVBridge; print(TVBridge.__name__)"` prints `TVBridge` and exits 0
    - `uv run pytest packages/tv-bridge/tests/test_bridge.py::test_bridge_importable -x -q` exits 0
    - `uv run pytest packages/tv-bridge/tests/test_bridge.py::test_call_tool_returns_none_when_no_session -x -q` exits 0
    - `uv run pytest packages/tv-bridge/tests/test_bridge.py::test_start_creates_supervisor_task -x -q` exits 0
    - `uv run pytest packages/tv-bridge/tests/test_bridge.py -k xfail -q` reports 3 xfailed tests
    - `grep -c "_SYMBOL_ALLOWLIST" packages/tv-bridge/src/tv_bridge/bridge.py` returns at least 1
    - Construction does NOT spawn a Node subprocess: assert by reading `bridge._session is None` and `bridge._supervisor_task is None` immediately after `__init__` (test_bridge_importable enforces)
  </acceptance_criteria>
  <done>
    TVBridge importable, instantiable, and the supervisor/start/stop/call_tool API is in place as stubs ready for Plan 02 to flesh out. All real Wave 1 tests pass; Plan 02's tests sit in the file as strict xfails.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolding for Plans 02/03/04 + draw_shape entity_id Wave 0 verification (06-01-04)</name>
  <files>
    packages/tv-bridge/tests/test_overlay_registry.py,
    packages/tv-bridge/tests/test_replay_source.py,
    packages/tv-bridge/tests/test_reconciliation.py,
    packages/tv-bridge/tests/integration/test_tv_failure_isolation.py,
    packages/tv-bridge/tests/integration/__init__.py,
    packages/api/tests/test_tv_routes.py,
    .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
  </files>
  <read_first>
    .planning/phases/06-tradingview-mcp-bridge/06-VALIDATION.md,
    .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md,
    packages/tv-bridge/tests/conftest.py,
    packages/api/tests/test_tv_routes.py
  </read_first>
  <action>
    Create five new test files containing strict-xfail stubs. Each stub uses `@pytest.mark.xfail(reason="implemented in Plan XX", strict=True)` so a later plan that fills in the implementation flips the test result. Stub body is `pytest.fail("Plan XX implements")`. The exact test-name list MUST come from 06-VALIDATION.md (Per-Task Verification Map) — the executor cross-references task IDs 06-02-* through 06-04-* and creates one test function per row.

    - `packages/tv-bridge/tests/test_overlay_registry.py` (3 stubs from 06-02-04, 06-02-05, 06-04-01):
      - `def test_write_overlay(in_memory_store)` — xfail, reason "implemented in Plan 02"
      - `def test_cap_enforcement(in_memory_store, mock_mcp_session)` — xfail, reason "implemented in Plan 02"
      - `def test_nightly_cleanup(in_memory_store)` — xfail, reason "implemented in Plan 04"

    - `packages/tv-bridge/tests/test_replay_source.py` (1 stub from 06-03-02):
      - `async def test_fetch_bars(mock_mcp_session)` — xfail, reason "implemented in Plan 03"

    - `packages/tv-bridge/tests/test_reconciliation.py` (2 stubs from 06-03-03, 06-03-04):
      - `async def test_price_divergence(in_memory_store)` — xfail, reason "implemented in Plan 03"
      - `async def test_audit_log_write(in_memory_store)` — xfail, reason "implemented in Plan 03"

    - `packages/tv-bridge/tests/integration/__init__.py` (empty file so pytest collects the directory)
    - `packages/tv-bridge/tests/integration/test_tv_failure_isolation.py` (1 stub from 06-04-02):
      - `@pytest.mark.skip(reason="implemented in Plan 04 — requires full TVBridge supervisor + pipeline")`
      - `async def test_pipeline_continues_when_tv_killed()` — body: `pass`

    - `packages/api/tests/test_tv_routes.py` (2 stubs from 06-02-06, 06-02-07):
      - `def test_tv_focus(client)` — xfail, reason "implemented in Plan 02"
      - `def test_create_delete_alert(client)` — xfail, reason "implemented in Plan 02"
      Where `client` is a fixture; create a minimal one inline as `@pytest.fixture def client() -> TestClient: return TestClient(app)` if not already in a parent conftest (check `packages/api/tests/conftest.py` first — if it provides `client`, use that fixture by name).

    Also add a section to `.planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md` titled `## Wave 0 Verification: draw_shape entity_id` immediately above the existing `## Sources` heading. Content:
      - One paragraph: "Wave 0 verified the draw_shape response shape against a live TradingView Desktop session. Test invocation: `mcp__tradingview__draw_shape({shape: 'horizontal_line', price: <visible-chart-price>, color: '#888888', text: 'phase6 wave0 probe', line_style: 'dashed', line_width: 1})`."
      - Subsection "Result": fill in the literal JSON snippet returned by the tool. The key field name (entity_id / shape_id / id) MUST be quoted verbatim. If the test cannot run (TV not reachable), record that and mark Open Question 1 as deferred to Plan 02's first task.
      - Subsection "Decision": one line such as `tv_overlays.shape_id is populated from response["entity_id"]` (or whatever the actual field name is).
      - REQUIRED grep gate: Within this section, add a single line in the exact form `entity_id_field: <name>` (e.g., `entity_id_field: entity_id`). This line is the binding handoff to Plan 02 — Plan 02 reads this exact field name. The acceptance criterion below uses `grep "entity_id_field:" 06-RESEARCH.md | grep -v TODO` to enforce that the value is not the literal placeholder `TODO` before this task can be marked done. If TV is truly unreachable, resolve the field name from the MCP server source (`C:\Users\Admin\tradingview-mcp-jackson\src\tools\draw.js` or similar) and record `entity_id_field: <name-from-source>` plus a note explaining the source-read fallback.

    To perform the verification, prefer calling the live TV MCP tool via the existing chat-time MCP if available. Otherwise run a one-off Python snippet `uv run python -c "import asyncio; from mcp.client.stdio import stdio_client; from mcp import ClientSession, StdioServerParameters; ..."` that connects, calls `draw_shape`, prints the raw text. Save the raw transcript under `.planning/research/spike-6/draw_shape_response.json` for forensic record.
  </action>
  <verify>
    <automated>uv run pytest packages/tv-bridge/tests/ packages/api/tests/test_tv_routes.py -q --no-header 2>&1 | grep -E "xfailed|skipped|passed"</automated>
  </verify>
  <acceptance_criteria>
    - All five new test files exist and collect under pytest without errors (`uv run pytest packages/tv-bridge/tests/ packages/api/tests/test_tv_routes.py --collect-only -q` exits 0)
    - Test count by file: test_overlay_registry.py has exactly 3 test functions; test_replay_source.py has exactly 1; test_reconciliation.py has exactly 2; test_tv_failure_isolation.py has exactly 1; test_tv_routes.py has exactly 2 NEW xfail tests (any prior real tests are preserved)
    - Every xfail uses `strict=True`: `grep -c "strict=True" packages/tv-bridge/tests/test_overlay_registry.py packages/tv-bridge/tests/test_replay_source.py packages/tv-bridge/tests/test_reconciliation.py packages/api/tests/test_tv_routes.py | awk -F: '{s+=$2}END{print s}'` is >= 9
    - 06-RESEARCH.md contains a new heading: `grep -c "## Wave 0 Verification: draw_shape entity_id" .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md` returns 1
    - Either: (a) `.planning/research/spike-6/draw_shape_response.json` exists containing a JSON object captured from a live MCP call, OR (b) the field name was resolved from the MCP server source code (record path + rev in 06-RESEARCH.md)
    - BINDING gate (BLOCKER 3 fix): `grep "entity_id_field:" .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md | grep -v "TODO"` returns a non-empty line. The literal `entity_id_field: TODO` is NOT acceptable — this command must filter it out and still return at least one matching line. This is the structural proof that Plan 02 can consume a resolved field name.
    - `uv run pytest packages/tv-bridge/tests/ -q` reports 0 errors, 0 failures (xfails/skips are accepted outcomes)
  </acceptance_criteria>
  <done>
    Every Phase 6 task ID in 06-VALIDATION.md has a destination test function in the correct file, marked xfail-strict so Wave 2/3/4 plans flip them to green incrementally; draw_shape entity_id field name is documented (or explicitly deferred).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | schema.sql is read verbatim by DuckDBStore.ensure_schema(); a malicious edit could inject arbitrary DDL. Mitigated by file living inside the trading-core package (no external-write surface). |
| TVBridge construction → DuckDBStore | TVBridge receives a DuckDBStore reference via constructor (no second connection). Mitigation: type annotation + single-writer convention documented at module top of duckdb_store.py. |
| Wave 0 MCP probe → live TV Desktop | Task 3 calls draw_shape once on a live chart. Mitigation: probe uses an obvious "phase6 wave0 probe" text so the operator can see + manually delete the shape if desired. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | schema.sql DDL | accept | File lives inside the package and is checked into git; any unauthorized edit would be visible in PR diff. No runtime mitigation needed. |
| T-06-01-02 | Information Disclosure | _stderr_capture (StringIO) inside TVBridge | mitigate | StringIO is in-memory only; never written to disk in Wave 1. Plan 02 must NOT write captured stderr to audit_log without sanitization. Documented in TVBridge docstring. |
| T-06-01-03 | Denial of Service | Wave 0 draw_shape probe leaves a shape on the user's chart | accept | Single probe shape, "phase6 wave0 probe" text, operator can delete manually. 200-shape cap (Plan 02) prevents recurrence. |
| T-06-01-04 | Tampering | New DuckDBStore methods accept SQL-parameterized inputs (no string interpolation) | mitigate | All new methods use `?` parameter binding via `_conn.execute(SQL, [args])`. Acceptance criterion verifies `grep` confirms no f-string SQL construction in the new methods. |
| T-06-01-05 | Repudiation | TVBridge constructed but never started leaves no audit trail | accept | Wave 1 establishes only the skeleton; audit-log writes for MCP errors land in Plan 02. |
</threat_model>

<verification>
- All Task 1 acceptance criteria pass: schema.sql contains both new tables; DuckDBStore has 7 new TV methods; 6+ new storage tests pass.
- All Task 2 acceptance criteria pass: `from tv_bridge import TVBridge` works; the 3 real tests pass; 3 xfail stubs are recognized.
- All Task 3 acceptance criteria pass: 5 new test files exist with the exact test counts above; draw_shape entity_id documented in 06-RESEARCH.md (or explicitly deferred).
- Full Phase 6 test sweep `uv run pytest packages/tv-bridge/ packages/api/tests/test_tv_routes.py packages/trading-core/tests/storage/ -q` exits 0 (xfails/skips are not failures).
- No prior phase tests regress: `uv run pytest packages/ -q` exits 0.
</verification>

<success_criteria>
1. `uv run python -c "from tv_bridge import TVBridge"` exits 0.
2. `uv run pytest packages/trading-core/tests/storage/test_schema.py -k tv_ -x -q` exits 0 with at least 2 passing tests.
3. `uv run pytest packages/trading-core/tests/storage/test_duckdb_store.py -k tv_ -x -q` exits 0 with at least 4 passing tests.
4. `uv run pytest packages/tv-bridge/tests/test_bridge.py -x -q` exits 0 with 3 passing + 3 xfailed tests.
5. `uv run pytest packages/tv-bridge/tests/ packages/api/tests/test_tv_routes.py --collect-only -q` exits 0 — every Phase 6 task ID in 06-VALIDATION.md has a collectible destination.
6. 06-RESEARCH.md `## Wave 0 Verification: draw_shape entity_id` section is present; either contains the live response or explicitly defers Open Question 1 to Plan 02 Task 1.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tradingview-mcp-bridge/06-01-SUMMARY.md` describing:
- The exact set of DuckDBStore methods added (signatures + SQL constant names)
- The TVBridge surface area now in place (constructor params, public methods, stubs)
- The draw_shape entity_id field name (or the reason it was deferred and what Plan 02 must verify before proceeding)
- The test-file inventory with xfail/skip counts (so Plan 02/03/04 know exactly which tests to flip)
</output>
