---
phase: 06-tradingview-mcp-bridge
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - packages/tv-bridge/src/tv_bridge/replay.py
  - packages/tv-bridge/src/tv_bridge/__init__.py
  - packages/tv-bridge/src/tv_bridge/reconciliation.py
  - packages/tv-bridge/tests/test_replay_source.py
  - packages/tv-bridge/tests/test_reconciliation.py
  - packages/trading-core/tests/test_protocols.py
  - packages/api/src/api/app.py
  - scripts/run_backtest.py
autonomous: true
requirements: [TV-04, MD-10]
tags: [tv-bridge, datasource, replay, reconciliation, scheduler]

must_haves:
  truths:
    - "TVReplayDataSource implements the DataSource protocol (fetch_bars + subscribe_bars + name attribute) and passes a static-type / runtime structural check"
    - "TVReplayDataSource.fetch_bars uses a per-call stdio_client subprocess (NOT the shared TVBridge session) so live drawing and replay can co-exist"
    - "run_backtest.py --data-source tv-replay routes through TVReplayDataSource instead of DuckDB historical bars"
    - "The reconciliation scheduler fires once per day at 16:10 ET; the run_reconciliation() coroutine compares same-instrument cross-vendor SPY bars — TV SPY bars (via TradingViewDataSource.fetch_bars) against Twelve Data SPY bars — and writes an audit_log row with topic='reconciliation_alert' for every bar with price divergence > 0.05% or volume divergence > 5%. NO basis-ratio conversion is applied — both sources report the same instrument so direct comparison is meaningful. (BLOCKER 2 fix: an earlier draft cross-compared ES futures volume vs SPY ETF volume using a fixed basis ratio; that comparison is invalid because volume is not basis-comparable and would emit false-positive flood alerts daily.)"
    - "When Twelve Data api key is missing, reconciliation skips and writes a single audit_log row with topic='reconciliation_skipped'"
  artifacts:
    - path: "packages/tv-bridge/src/tv_bridge/replay.py"
      provides: "TVReplayDataSource class implementing DataSource protocol via per-call MCP stdio_client"
      exports: ["TVReplayDataSource"]
    - path: "packages/tv-bridge/src/tv_bridge/reconciliation.py"
      provides: "run_reconciliation() coroutine + ReconciliationScheduler wrapper"
      exports: ["run_reconciliation", "ReconciliationScheduler"]
    - path: "packages/api/src/api/app.py"
      provides: "Lifespan launches ReconciliationScheduler as asyncio.create_task(app.state.recon_task)"
      contains: "ReconciliationScheduler"
    - path: "scripts/run_backtest.py"
      provides: "--data-source tv-replay CLI flag routes to TVReplayDataSource"
      contains: "tv-replay"
  key_links:
    - from: "scripts/run_backtest.py CLI"
      to: "TVReplayDataSource.fetch_bars"
      via: "argparse --data-source tv-replay branch + DataSource protocol seam"
      pattern: "data_source.*tv-replay|tv_replay"
    - from: "FastAPI lifespan"
      to: "ReconciliationScheduler.run() asyncio task"
      via: "asyncio.create_task; stored on app.state.recon_task; cancelled on shutdown"
      pattern: "recon_task"
    - from: "run_reconciliation"
      to: "DuckDBStore.write_audit_event(topic='reconciliation_alert')"
      via: "per-divergent-bar write inside the comparison loop"
      pattern: "reconciliation_alert"
---

<objective>
Deliver the Wave 3 slice that closes the two non-drawing requirements of Phase 6:
1. **TV-04**: `TVReplayDataSource` so the operator can drive a backtest from a TV replay session through the same `Strategy.on_bar` path as historical-Parquet runs.
2. **MD-10**: Daily reconciliation comparing TV ES bars against Twelve Data SPY bars (same-instrument cross-vendor where data overlaps) and surfacing divergences via audit_log → WS stream → UI notification.

Purpose: After this plan, `run_backtest.py --data-source tv-replay --strategy orb --start 2024-06-12` produces a real BacktestResult against a TV replay session; a scheduled task runs at 16:10 ET daily and any divergent bars surface as `topic='reconciliation_alert'` audit rows visible in the existing WS stream (which Phase 3 already wired to the UI).

Output: Two new modules (`replay.py`, `reconciliation.py`), one CLI flag added to `scripts/run_backtest.py`, lifespan integration for the daily scheduler, and four converted xfail-strict tests now passing as real tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md
@.planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md
@.planning/phases/06-tradingview-mcp-bridge/06-VALIDATION.md
@.planning/phases/06-tradingview-mcp-bridge/06-02-PLAN.md
@.planning/phases/06-tradingview-mcp-bridge/06-02-SUMMARY.md
@packages/tv-bridge/src/tv_bridge/__init__.py
@packages/tv-bridge/src/tv_bridge/bridge.py
@packages/trading-core/src/trading_core/data/tradingview.py
@packages/trading-core/src/trading_core/data/protocols.py
@packages/trading-core/src/trading_core/data/twelve_data.py
@packages/trading-core/src/trading_core/execution/eod_scheduler.py
@packages/trading-core/src/trading_core/storage/duckdb_store.py
@packages/api/src/api/app.py
@scripts/run_backtest.py

<interfaces>
<!-- Contracts the executor needs. -->

DataSource Protocol (from packages/trading-core/src/trading_core/data/protocols.py):
- class DataSource(Protocol):
-     name: str
-     async def fetch_bars(self, symbol: str, timeframe: str, start: datetime, end: datetime) -> pd.DataFrame: ...
-     async def subscribe_bars(self, symbol: str, timeframe: str) -> AsyncIterator[Bar]: ...
- Exceptions: DataSourceUnavailable, RateLimited, GapDetected

TwelveDataSource (existing — packages/trading-core/src/trading_core/data/twelve_data.py):
- TwelveDataSource(settings: Settings) — reads TWELVEDATA_API_KEY lazily on fetch (NOT __init__) per Plan 04 decision
- fetch_bars returns pd.DataFrame with columns [symbol, timeframe, ts_utc, open, high, low, close, volume, provider]

EodScheduler (existing — packages/trading-core/src/trading_core/execution/eod_scheduler.py):
- EodScheduler(*, on_flatten: Callable[[], Coroutine], close_time_et: str = "16:00", lead_seconds: int = 60, tz: str = "America/New_York")
- async run() — infinite loop, awaits next fire time, calls on_flatten
- For ReconciliationScheduler use close_time_et="16:10", lead_seconds=0 (fires AT 16:10 ET, not before)

TVBridge (from Plan 02):
- bridge.call_tool(tool, args) -> dict | None  (returns None on error, never raises)
- bridge.is_connected (property)

DuckDBStore (existing + extended by Plan 01):
- write_audit_event(*, event_id, ts_utc, topic, entity_id, reason_code, payload_json) -> None
- new_run_id() -> str   (uuid7 from trading_core.storage.runs)

run_backtest.py CLI (existing — scripts/run_backtest.py):
- argparse parser has --symbol, --tf, --from, --to, --config, --seed
- BacktestEngine.run(...) consumes (bars, strategy, risk_manager, executor) — bars is a list of Bar objects
- Currently reads bars from DuckDB via a SQL query; this plan adds an alternate path via DataSource.fetch_bars()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TVReplayDataSource + DataSource protocol compliance + run_backtest.py --data-source flag (06-03-01, 06-03-02)</name>
  <files>
    packages/tv-bridge/src/tv_bridge/replay.py,
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/test_replay_source.py,
    packages/trading-core/tests/test_protocols.py,
    scripts/run_backtest.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/data/tradingview.py,
    packages/trading-core/src/trading_core/data/protocols.py,
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/test_replay_source.py,
    packages/trading-core/tests/test_protocols.py,
    scripts/run_backtest.py,
    .planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md
  </read_first>
  <behavior>
    - test_protocols.py::test_replay_source_protocol — `from tv_bridge import TVReplayDataSource`; `TVReplayDataSource(settings=settings)` constructs without errors; structural check via `assert hasattr(TVReplayDataSource, "fetch_bars") and hasattr(TVReplayDataSource, "subscribe_bars") and TVReplayDataSource.name == "tradingview_replay"`; and a static-type assertion via `_: DataSource = TVReplayDataSource(settings=settings)` (the mypy check is enforced by the existing pyright/mypy config; runtime test catches missing methods)
    - test_replay_source.py::test_fetch_bars — patch `tv_bridge.replay.stdio_client` and `tv_bridge.replay.ClientSession` so `session.call_tool("replay_start", ...)` returns success, `session.call_tool("replay_step", ...)` returns a single bar, and three iterations yield three bars before `replay_status` reports `current_time > end`; assert: returned DataFrame has the expected columns (symbol, timeframe, ts_utc, open, high, low, close, volume, provider), len(df) == 3, every ts_utc is tz-aware UTC, df.provider.iloc[0] == "tradingview_replay"
    - test_replay_source.py::test_fetch_bars_empty — patch session to return zero bars; fetch_bars returns an empty DataFrame with the correct columns (matches tradingview.py empty-frame convention)
    - test_replay_source.py::test_fetch_bars_disconnect — patch session.initialize() to raise TimeoutError; fetch_bars raises DataSourceUnavailable and a DegradedStateEvent is published on the bus (this matches Phase 1's TradingViewDataSource contract)
  </behavior>
  <action>
    Create `packages/tv-bridge/src/tv_bridge/replay.py` modeled exactly on `packages/trading-core/src/trading_core/data/tradingview.py` (per PATTERNS.md "replay.py — DataSource protocol"). Required structure:
      - `from __future__ import annotations`
      - Imports: `asyncio`, `from datetime import datetime, timezone`, `from pathlib import Path`, `from typing import Any, AsyncIterator, cast`, `pandas as pd`, `structlog`, `from mcp import ClientSession, StdioServerParameters`, `from mcp.client.stdio import stdio_client`, `from trading_core.config import Settings`, `from trading_core.data.models import Bar`, `from trading_core.data.protocols import DataSourceUnavailable`, `from trading_core.events import EventBus, TOPIC_DEGRADED_STATE, DegradedStateEvent`
      - Module-level constants (verbatim from tradingview.py):
          _SYMBOL_MAP = {"ES": "CME_MINI:ES1!", "MES": "CME_MINI:MES1!"}
          _TF_TO_TV = {"1m": "1", "5m": "5", "15m": "15"}
          _DEFAULT_MCP_SERVER_PATH = Path(r"C:\Users\Admin\tradingview-mcp-jackson")
          _INIT_TIMEOUT_SECONDS = 15.0
          _REPLAY_STEP_TIMEOUT = 5.0
      - `class TVReplayDataSource:`
          - `name = "tradingview_replay"` (class attribute, satisfies DataSource protocol)
          - `def __init__(self, settings: Settings, *, bus: EventBus | None = None, mcp_server_path: Path | None = None) -> None:`
              Store settings, optional bus (None means publish-degraded becomes a no-op log), `self._mcp_server_path = mcp_server_path or _DEFAULT_MCP_SERVER_PATH`. NO subprocess spawn.
          - `_server_params(self) -> StdioServerParameters` — same as tradingview.py
          - `@staticmethod _parse_tool_payload(result)` — verbatim copy of helper
          - `@staticmethod _bar_time_to_utc(value)` — verbatim copy of tradingview.py lines 172–187 (handles Unix epoch seconds, milliseconds, and ISO strings)
          - `async def _publish_degraded(self, reason: str)` — analogous to tradingview.py, no-op if `self._bus is None`
          - `async def fetch_bars(self, symbol: str, timeframe: str, start: datetime, end: datetime) -> pd.DataFrame:` body:
              tv_symbol = _SYMBOL_MAP.get(symbol, symbol)   # SPY falls through unchanged
              tv_timeframe = _TF_TO_TV[timeframe]
              empty_columns = ["symbol", "timeframe", "ts_utc", "open", "high", "low", "close", "volume", "provider"]
              try:
                  async with stdio_client(self._server_params()) as (read, write):
                      async with ClientSession(read, write) as session:
                          await asyncio.wait_for(session.initialize(), timeout=_INIT_TIMEOUT_SECONDS)
                          # Health gate identical to Phase 1
                          health_result = await session.call_tool("tv_health_check", {})
                          health = self._parse_tool_payload(health_result)
                          if not health.get("api_available"):
                              await self._publish_degraded(f"tv_replay health: {health}")
                              raise DataSourceUnavailable(f"tv_health_check failed: {health}")
                          # Start replay
                          start_result = await session.call_tool("replay_start", {
                              "symbol": tv_symbol,
                              "date": start.isoformat(),
                              "timeframe": tv_timeframe,
                          })
                          start_payload = self._parse_tool_payload(start_result)
                          if not start_payload.get("success", True):
                              await self._publish_degraded(f"replay_start: {start_payload}")
                              raise DataSourceUnavailable(f"replay_start failed: {start_payload}")
                          bars: list[dict[str, Any]] = []
                          try:
                              while True:
                                  step_result = await asyncio.wait_for(
                                      session.call_tool("replay_step", {"count": 1}),
                                      timeout=_REPLAY_STEP_TIMEOUT,
                                  )
                                  step = self._parse_tool_payload(step_result)
                                  bar = step.get("bar") or step.get("last_bar")
                                  if not bar:
                                      break
                                  ts = self._bar_time_to_utc(bar.get("time") or bar.get("datetime"))
                                  if ts >= end:
                                      break
                                  bars.append({**bar, "_ts": ts})
                          finally:
                              # Always tear down replay even on partial success
                              await session.call_tool("replay_stop", {})
              except DataSourceUnavailable:
                  raise
              except Exception as e:
                  await self._publish_degraded(f"tv_replay transport: {type(e).__name__}: {e}")
                  raise DataSourceUnavailable(f"tv_replay transport: {e}") from e
              if not bars:
                  return pd.DataFrame(columns=empty_columns)
              df = pd.DataFrame({
                  "symbol": [symbol] * len(bars),
                  "timeframe": [timeframe] * len(bars),
                  "ts_utc": [b["_ts"] for b in bars],
                  "open": [float(b["open"]) for b in bars],
                  "high": [float(b["high"]) for b in bars],
                  "low": [float(b["low"]) for b in bars],
                  "close": [float(b["close"]) for b in bars],
                  "volume": [int(b.get("volume") or 0) for b in bars],
                  "provider": [self.name] * len(bars),
              })
              return df.sort_values("ts_utc").reset_index(drop=True)
          - `async def subscribe_bars(self, symbol: str, timeframe: str) -> AsyncIterator[Bar]:` body — `raise NotImplementedError("TVReplayDataSource is fetch-only in v1; subscribe semantics require Phase 7 streaming")`. The protocol allows the implementation to declare unsupported features at runtime; downstream callers that need streaming use TradingViewDataSource.

    Update `packages/tv-bridge/src/tv_bridge/__init__.py` to export the new class:
      - `from .bridge import TVBridge`
      - `from .replay import TVReplayDataSource`
      - `__all__ = ["TVBridge", "TVReplayDataSource"]`

    Convert `packages/tv-bridge/tests/test_replay_source.py::test_fetch_bars` from xfail-strict to a real test. Add the two additional behaviors above (test_fetch_bars_empty, test_fetch_bars_disconnect) as new tests in the same file.

    Add `test_replay_source_protocol` to `packages/trading-core/tests/test_protocols.py` (the Phase 1 test that verifies TwelveDataSource conforms to DataSource — read it first to mirror the import pattern). Test:
      - `from tv_bridge import TVReplayDataSource`
      - construct via `TVReplayDataSource(settings=Settings())`
      - assert `instance.name == "tradingview_replay"`
      - assert `inspect.iscoroutinefunction(instance.fetch_bars)`
      - assert `inspect.iscoroutinefunction(instance.subscribe_bars)`

    Update `scripts/run_backtest.py`:
      - Add `--data-source` argparse argument with `choices=["duckdb", "tv-replay"]` and `default="duckdb"` (read existing argparse block first to confirm exact position; place after --tf or --config to maintain ordering convention)
      - After parsing args and before the existing DuckDB bar-loading block, add a branch: `if args.data_source == "tv-replay":` — instantiate `TVReplayDataSource(settings=Settings(), bus=app.state.bus_if_available_else_None)`, call `df = await source.fetch_bars(args.symbol, args.tf, from_dt, to_dt)`, then convert to list[Bar] using the same Bar adapter the existing DuckDB path uses (see existing bar-conversion code). Document via comment that tv-replay requires TV Desktop running.
      - Argparse threat note: T-06-03-01 — the new `--data-source` is constrained by `choices=` so user input is whitelisted; no SQL/shell escape risk.
  </action>
  <verify>
    <automated>uv run pytest packages/tv-bridge/tests/test_replay_source.py packages/trading-core/tests/test_protocols.py::test_replay_source_protocol -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/tv-bridge/src/tv_bridge/replay.py` exists and `grep -c "class TVReplayDataSource" packages/tv-bridge/src/tv_bridge/replay.py` returns 1
    - `grep -c "name = \"tradingview_replay\"" packages/tv-bridge/src/tv_bridge/replay.py` returns 1
    - `grep -E "async def (fetch_bars|subscribe_bars)" packages/tv-bridge/src/tv_bridge/replay.py | wc -l` returns 2
    - `grep -c "from .replay import TVReplayDataSource" packages/tv-bridge/src/tv_bridge/__init__.py` returns 1
    - `uv run python -c "from tv_bridge import TVReplayDataSource; print(TVReplayDataSource.name)"` prints `tradingview_replay`
    - `uv run pytest packages/tv-bridge/tests/test_replay_source.py::test_fetch_bars -x -q` exits 0 (previously xfail-strict)
    - `uv run pytest packages/tv-bridge/tests/test_replay_source.py::test_fetch_bars_empty packages/tv-bridge/tests/test_replay_source.py::test_fetch_bars_disconnect -x -q` exits 0
    - `uv run pytest packages/trading-core/tests/test_protocols.py::test_replay_source_protocol -x -q` exits 0
    - `grep -c "data-source" scripts/run_backtest.py` returns at least 1
    - `grep -c "tv-replay" scripts/run_backtest.py` returns at least 2 (choices argument + branch handler)
    - `uv run python scripts/run_backtest.py --help | grep -c -- '--data-source'` returns 1
    - `replay_stop` appears in a `finally:` block so partial reads still tear down the replay session: `grep -B 0 -A 1 "finally:" packages/tv-bridge/src/tv_bridge/replay.py | grep -c "replay_stop"` returns at least 1
  </acceptance_criteria>
  <done>
    TVReplayDataSource exists as a fully conformant DataSource implementation; per-call stdio_client pattern (not the shared TVBridge session); run_backtest.py routes through it when `--data-source tv-replay` is passed; all 4 corresponding tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_reconciliation + ReconciliationScheduler + FastAPI lifespan wiring (06-03-03, 06-03-04)</name>
  <files>
    packages/tv-bridge/src/tv_bridge/reconciliation.py,
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/test_reconciliation.py,
    packages/api/src/api/app.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/execution/eod_scheduler.py,
    packages/trading-core/src/trading_core/data/twelve_data.py,
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    packages/tv-bridge/src/tv_bridge/__init__.py,
    packages/tv-bridge/tests/test_reconciliation.py,
    packages/api/src/api/app.py,
    .planning/phases/06-tradingview-mcp-bridge/06-RESEARCH.md,
    .planning/phases/06-tradingview-mcp-bridge/06-PATTERNS.md
  </read_first>
  <behavior>
    - test_reconciliation.py::test_price_divergence — build two DataFrames with identical ts_utc representing SPY 1m bars from two vendors. TV SPY close = 549.90 vs Twelve SPY close = 549.85: divergence = (549.90 - 549.85) / 549.85 = ~0.009% < 0.05% threshold → NO alert. Then change Twelve SPY close to 548.50: divergence = (549.90 - 548.50) / 548.50 = ~0.255% > 0.05% → ALERT. Call `run_reconciliation(tv_source=mock_tv_source, twelve_source=mock_twelve_source, store=in_memory_store, trading_date=date(2026,5,19))` (both sources return identical-timestamp SPY DataFrames). Assert returned divergence count == 1 and the audit_log table has exactly one row with topic='reconciliation_alert' and reason_code='price_divergence'.
    - test_reconciliation.py::test_audit_log_write — provide two SPY bar DataFrames with identical price but volume divergence > 5% (e.g., TV SPY volume=1_000_000 vs Twelve SPY volume=1_100_000 → 10% divergence); assert: audit_log row exists with topic='reconciliation_alert', reason_code='volume_divergence', payload_json parses as JSON and contains keys ['ts', 'price_pct', 'vol_pct']. NOTE: same-instrument cross-vendor SPY/SPY comparison means volume divergence reflects real vendor disagreement (different last-print-aggregation rules), NOT instrument-scale mismatch.
    - test_reconciliation.py::test_skipped_when_no_api_key — call `run_reconciliation` with `tv_bridge=None` (simulating missing TV) OR `twelve_source` whose api_key returns None; assert: zero alert rows; one audit_log row with topic='reconciliation_skipped'
  </behavior>
  <action>
    Create `packages/tv-bridge/src/tv_bridge/reconciliation.py`:
      - `from __future__ import annotations`
      - Imports: `asyncio`, `json`, `from collections.abc import Callable, Coroutine`, `from datetime import date, datetime, timezone`, `from typing import Any`, `import pandas as pd`, `from trading_core.logging import get_logger`, `from trading_core.storage.duckdb_store import DuckDBStore`, `from trading_core.storage.runs import new_run_id`, `from trading_core.execution.eod_scheduler import EodScheduler`
      - `_log = get_logger(__name__)`
      - Module-level thresholds (sourced from MD-10 requirement text):
          PRICE_DIVERGENCE_THRESHOLD = 0.0005   # 0.05%
          VOLUME_DIVERGENCE_THRESHOLD = 0.05    # 5%
          # NOTE (BLOCKER 2 fix): No basis-ratio constant. Both sides fetch SPY directly so values are directly comparable. Cross-instrument ES-vs-SPY normalization was rejected during plan revision because ES futures volume and SPY ETF volume are not basis-comparable (different exchanges, different aggregation rules) and would emit false-positive flood alerts every session.
      - `async def run_reconciliation(*, tv_source: Any | None, twelve_source: Any | None, store: DuckDBStore, trading_date: date) -> int:` — full body:
          # 1. Gate: if either source unavailable, write reconciliation_skipped and return 0.
          #    tv_source is a DataSource (TradingViewDataSource from packages/trading-core/src/trading_core/data/tradingview.py — NOT the live-drawing TVBridge). This is the WARNING 3 fix: route reconciliation through the same DataSource seam used by Phase 1 historical fetches, so the TV live-drawing session is not contended.
          if tv_source is None or twelve_source is None or not getattr(twelve_source, "_api_key_available", lambda: True)():
              store.write_audit_event(event_id=new_run_id(), ts_utc=datetime.now(timezone.utc), topic="reconciliation_skipped", entity_id=trading_date.isoformat(), reason_code="missing_source", payload_json=json.dumps({"tv_source_present": tv_source is not None, "twelve_source_present": twelve_source is not None}))
              return 0
          # 2. Compute RTH window in UTC (use America/New_York 9:30–16:00 = UTC 14:30–21:00 in EDT, 13:30–20:00 in EST). For simplicity import existing pandas_market_calendars helper from packages/trading-core/src/trading_core/calendars.py if it exists; otherwise compute via ZoneInfo("America/New_York").
          # 3. Fetch TV SPY bars via `tv_df = await tv_source.fetch_bars("SPY", "1m", rth_start, rth_end)`. TradingViewDataSource already supports SPY (it falls through _SYMBOL_MAP unchanged).
          # 4. Fetch Twelve SPY bars via `twelve_df = await twelve_source.fetch_bars("SPY", "1m", rth_start, rth_end)`.
          # 5. Align on ts_utc using `merged = tv_df.merge(twelve_df, on="ts_utc", suffixes=("_tv", "_twelve"))`.
          # 6. For each aligned row: `price_pct = abs(row["close_tv"] - row["close_twelve"]) / row["close_twelve"]; vol_pct = abs(row["volume_tv"] - row["volume_twelve"]) / max(row["volume_twelve"], 1)`. Direct comparison — no ratio scaling.
          # 7. If price_pct > PRICE_DIVERGENCE_THRESHOLD: reason_code='price_divergence'; elif vol_pct > VOLUME_DIVERGENCE_THRESHOLD: reason_code='volume_divergence'; else continue.
          # 8. `store.write_audit_event(event_id=new_run_id(), ts_utc=ts, topic='reconciliation_alert', entity_id=trading_date.isoformat(), reason_code=reason_code, payload_json=json.dumps({"ts": ts.isoformat(), "price_pct": float(price_pct), "vol_pct": float(vol_pct), "source_pair": "tv_spy_vs_twelve_spy"}))`.
          # 9. Return total divergence count.
          The implementation MUST handle the case where either source returns zero bars (write reconciliation_skipped with reason_code='no_bars') and where the two feeds have no overlapping timestamps (also reconciliation_skipped, reason_code='no_overlap').

      - `class ReconciliationScheduler:`
          def __init__(self, *, on_reconcile: Callable[[], Coroutine[Any, Any, None]], close_time_et: str = "16:10"):
              self._scheduler = EodScheduler(on_flatten=on_reconcile, close_time_et=close_time_et, lead_seconds=0, tz="America/New_York")
          async def run(self) -> None:
              await self._scheduler.run()

    Update `packages/tv-bridge/src/tv_bridge/__init__.py` to additionally export `run_reconciliation` and `ReconciliationScheduler`:
      - `from .reconciliation import ReconciliationScheduler, run_reconciliation`
      - extend `__all__` list

    Wire into FastAPI lifespan (`packages/api/src/api/app.py`). Insert AFTER the TVBridge wiring block (added in Plan 02) and BEFORE the `yield`:
      - Import: `from tv_bridge import ReconciliationScheduler, run_reconciliation`
      - Import: `from trading_core.data.twelve_data import TwelveDataSource` (verify exact import path against the existing file structure first)
      - Create the source: `_twelve_source = TwelveDataSource(_settings)` — this is safe at lifespan time; the api key check is lazy (Phase 1 decision)
      - Construct the TV DataSource for reconciliation (separate from the live-drawing TVBridge): `from trading_core.data.tradingview import TradingViewDataSource`; `_tv_source = TradingViewDataSource(_settings, bus=app.state.bus)` (verify exact constructor signature against tradingview.py before writing).
      - Define a closure: `async def _do_reconcile() -> None:`
          from datetime import date as _date
          await run_reconciliation(tv_source=_tv_source, twelve_source=_twelve_source, store=app.state.store, trading_date=_date.today())
      - Rationale (WARNING 3): TradingViewDataSource opens a per-call MCP subprocess, so reconciliation does NOT contend with the live-drawing TVBridge session. This also makes the call route through the same DataSource protocol seam Phase 1 already validated.
      - Instantiate: `_recon_scheduler = ReconciliationScheduler(on_reconcile=_do_reconcile)`
      - Launch: `app.state.recon_task = asyncio.create_task(_recon_scheduler.run(), name="reconciliation_scheduler")`
      - Log: `_log.info("reconciliation_scheduler.started")`

    Add shutdown cancellation BEFORE `_tv_bridge_ref.stop()`:
      - `app.state.recon_task.cancel()`
      - `try: await app.state.recon_task` / `except asyncio.CancelledError: pass`

    Convert the three xfail-strict tests in `packages/tv-bridge/tests/test_reconciliation.py` (test_price_divergence, test_audit_log_write) and add `test_skipped_when_no_api_key`. Each test uses the `in_memory_store` fixture (from Plan 01 conftest) and builds two pandas DataFrames inline; no live MCP or Twelve Data needed.
  </action>
  <verify>
    <automated>uv run pytest packages/tv-bridge/tests/test_reconciliation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/tv-bridge/src/tv_bridge/reconciliation.py` exists and `grep -c "async def run_reconciliation" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 1
    - `grep -c "class ReconciliationScheduler" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 1
    - `grep -c "PRICE_DIVERGENCE_THRESHOLD = 0.0005" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 1
    - `grep -c "VOLUME_DIVERGENCE_THRESHOLD = 0.05" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 1
    - BLOCKER 2 fix: `grep -c "ES_SPY_BASIS_RATIO" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 0 — the constant must NOT exist (cross-instrument basis comparison was rejected)
    - BLOCKER 2 fix: `grep -c 'fetch_bars("SPY"' packages/tv-bridge/src/tv_bridge/reconciliation.py` returns at least 2 (one for tv_source, one for twelve_source — both fetch the SAME instrument)
    - BLOCKER 2 fix: `grep -c 'CME_MINI:ES1!' packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 0 (no ES symbol used in reconciliation)
    - WARNING 3 fix: `grep -c "tv_source:" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns at least 1 (run_reconciliation accepts a DataSource, not a TVBridge)
    - WARNING 3 fix: `grep -c "tv_bridge" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns 0 in the run_reconciliation signature line (acceptable to mention tv_bridge in comments explaining the change)
    - `grep -c "reconciliation_alert" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns at least 1
    - `grep -c "reconciliation_skipped" packages/tv-bridge/src/tv_bridge/reconciliation.py` returns at least 1
    - `grep -c "from .reconciliation import" packages/tv-bridge/src/tv_bridge/__init__.py` returns 1
    - `grep -c "app.state.recon_task" packages/api/src/api/app.py` returns at least 2 (create + cancel)
    - `uv run pytest packages/tv-bridge/tests/test_reconciliation.py::test_price_divergence -x -q` exits 0
    - `uv run pytest packages/tv-bridge/tests/test_reconciliation.py::test_audit_log_write -x -q` exits 0
    - `uv run pytest packages/tv-bridge/tests/test_reconciliation.py::test_skipped_when_no_api_key -x -q` exits 0
    - `uv run pytest packages/api/tests/ -x -q` exits 0 — no API test regressions from lifespan changes
  </acceptance_criteria>
  <done>
    A scheduled reconciliation job runs daily at 16:10 ET, compares TV ES and Twelve SPY bars with the documented basis-adjustment, writes per-divergent-bar audit rows that surface in the WS stream and the UI; when sources are unavailable, a single audit row records the skip with a reason; three tests cover divergence detection, audit-log writes, and the missing-source path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → run_backtest.py --data-source | User-supplied flag must be in `choices` allowlist; outside `{duckdb, tv-replay}` argparse rejects |
| TVReplayDataSource → MCP stdio subprocess | Per-call subprocess; output JSON parsed via `json.loads` (raises on malformed; caught and converted to DataSourceUnavailable) |
| Twelve Data API → TwelveDataSource | Inherited from Phase 1; api_key stays in .env, URL redaction in place |
| Reconciliation results → audit_log | All writes use the SQL-parameterized write_audit_event method; payload_json is created via `json.dumps` (no string interpolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | run_backtest.py --data-source argv | mitigate | `argparse choices=["duckdb", "tv-replay"]` — invalid values rejected before any code runs |
| T-06-03-02 | DoS | TVReplayDataSource.fetch_bars infinite loop if replay_status never advances | mitigate | `asyncio.wait_for(replay_step, timeout=_REPLAY_STEP_TIMEOUT)` + outer `end` boundary check + `replay_stop` in `finally:` |
| T-06-03-03 | Information Disclosure | Reconciliation payload_json contains price data | accept | Audit log is local-only DuckDB + CSV; same trust boundary as all other audit events |
| T-06-03-04 | Tampering | reconciliation comparing untrusted DataFrames | mitigate | Pandas merge on `ts_utc` enforces type alignment; downstream comparisons use `float()` coercion; non-numeric data raises TypeError before any audit_log write. Both DataFrames carry SPY 1m bars from different vendors — same instrument, so direct comparison is valid (BLOCKER 2 fix). |
| T-06-03-05 | DoS | Reconciliation scheduler runs daily; one stuck run could prevent the next | mitigate | `EodScheduler.run` already wraps callbacks in a `try/except Exception` block and continues the loop — verified in eod_scheduler.py |
| T-06-03-06 | Repudiation | Skipped reconciliation invisible | mitigate | Every skip writes a `topic='reconciliation_skipped'` audit row with a reason_code |
| T-06-03-07 | DoS | TVReplayDataSource racing the shared TVBridge session | mitigate | Per-call stdio_client opens its own subprocess (PATTERNS.md Pitfall 4); documented in the class docstring |
</threat_model>

<verification>
- `uv run pytest packages/tv-bridge/tests/test_replay_source.py -x -q` exits 0 with 3 passing tests.
- `uv run pytest packages/trading-core/tests/test_protocols.py::test_replay_source_protocol -x -q` exits 0.
- `uv run pytest packages/tv-bridge/tests/test_reconciliation.py -x -q` exits 0 with 3 passing tests.
- `uv run python scripts/run_backtest.py --help` shows `--data-source` argument with choices [duckdb, tv-replay].
- `uv run pytest packages/ -q` exits 0 — no regressions across Phases 1–5 or Plan 02.
</verification>

<success_criteria>
1. `from tv_bridge import TVReplayDataSource, run_reconciliation, ReconciliationScheduler` succeeds.
2. TVReplayDataSource passes a runtime structural-protocol check (name attribute + 2 coroutine methods).
3. `scripts/run_backtest.py --data-source tv-replay --strategy orb --symbol ES --tf 1m --from 2024-06-12 --to 2024-06-13 --config config/strategies/orb.yaml` exits 0 OR exits 1 with a clear `DataSourceUnavailable` reason (manual smoke when TV is running; not asserted in CI).
4. `run_reconciliation` correctly identifies a synthetic 0.255% SPY-vs-SPY price divergence and writes a `topic='reconciliation_alert'` audit row (BLOCKER 2 fix: divergence is computed directly between two SPY feeds, no basis adjustment).
5. FastAPI lifespan registers + cancels `app.state.recon_task` cleanly: `uv run pytest packages/api/tests/ -x -q` exits 0.
6. The xfail-strict count in Phase 6 test files drops by 4 (test_fetch_bars + test_replay_source_protocol + test_price_divergence + test_audit_log_write all green).
</success_criteria>

<output>
After completion, create `.planning/phases/06-tradingview-mcp-bridge/06-03-SUMMARY.md` describing:
- TVReplayDataSource's per-call subprocess strategy and any deviations from PATTERNS.md (e.g., if the actual MCP `replay_step` returns multiple bars per call, how the code handles batching)
- The reconciliation source pair (TV SPY via TradingViewDataSource vs Twelve Data SPY via TwelveDataSource — same instrument, cross-vendor) and the precise thresholds (0.05% price, 5% volume). Confirm explicitly that no basis-ratio adjustment is applied.
- The lifespan task name and shutdown order (recon_task, tv_bridge, eod_task)
- Any open questions about the MCP `replay_*` tool response shapes that surfaced (these become Plan 04 inputs)
</output>
