---
phase: 03-vertical-mvp-slice-backtester
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01-PLAN.md"
  - "03-02-PLAN.md"
files_modified:
  - packages/trading-core/src/trading_core/backtest/engine.py
  - packages/trading-core/src/trading_core/backtest/__init__.py
  - scripts/run_backtest.py
  - packages/trading-core/tests/test_backtest_engine.py
  - packages/trading-core/tests/integration/test_lookahead.py
  - packages/trading-core/tests/integration/test_reproducibility.py
autonomous: true
requirements:
  - BT-01
  - BT-04
  - BT-05
  - BT-06
  - BT-07
  - BT-09
tags:
  - backtest-engine
  - cli
  - vbt-metrics
  - mae-mfe
  - bl1
  - reproducibility
must_haves:
  truths:
    - "BacktestEngine drives ORBStrategy through bars using the locked snapshot→on_bar→_push_bar order"
    - "Engine emits a BacktestResult with trades list (D-02 attribution chain) + metrics (BT-04) + equity curve"
    - "Per-trade MAE / MFE are computed manually from high/low arrays between entry_idx and exit_idx (BT-05)"
    - "safe_from_signals neutralizes a deliberately-leaking ORB variant: finite Sharpe, 35-65% win rate (BL-1 gate)"
    - "Same `--symbol SPY --tf 1m --from … --to … --seed 42` → bitwise-identical equity-curve Parquet (FND-08)"
    - "run_backtest.py CLI writes a runs row + backtests row + per-trade rows + equity_curve Parquet"
    - "Every trades table row has a non-null signal_id matching the originating ORBStrategy-emitted Signal.signal_id (BT-06 fill→signal attribution chain)"
  artifacts:
    - path: "packages/trading-core/src/trading_core/backtest/engine.py"
      provides: "BacktestEngine.run(...) → BacktestResult (driver loop + VBT metrics + equity-curve Parquet)"
      contains: "class BacktestEngine"
    - path: "scripts/run_backtest.py"
      provides: "CLI: --strategy orb --symbol SPY --tf 1m --from … --to … --config … --seed …"
      contains: "def _build_parser"
    - path: "packages/trading-core/tests/integration/test_lookahead.py"
      provides: "BL-1 lookahead-detector CI gate (D-14)"
      contains: "test_bl1_lookahead_neutralized_by_safe_from_signals"
    - path: "packages/trading-core/tests/integration/test_reproducibility.py"
      provides: "FND-08 bitwise-identical equity-curve assertion"
      contains: "test_reproducibility_same_inputs_bitwise_identical"
  key_links:
    - from: "BacktestEngine driver loop"
      to: "ORBStrategy.on_bar"
      via: "snapshot ctx → on_bar → _push_bar (Phase 2 pattern)"
      pattern: "strategy\\._push_bar"
    - from: "BacktestEngine"
      to: "vbt.Portfolio metrics"
      via: "safe_from_signals(...).total_return() / sharpe_ratio() / sortino_ratio() / calmar_ratio() / drawdowns.max_duration() / trades.win_rate() / trades.expectancy() / trades.profit_factor() / trades.count() / trades.avg_duration()"
      pattern: "safe_from_signals.*\\..*sharpe_ratio"
    - from: "BacktestEngine equity write"
      to: "data/parquet/equity/{run_id}.parquet"
      via: "pyarrow.parquet.write_table(compression='none', use_dictionary=False, write_statistics=False)"
      pattern: "compression=.none.*use_dictionary=False"
    - from: "BacktestEngine"
      to: "DuckDBStore.write_backtest + write_trades"
      via: "store.write_backtest(...) + store.write_trades(trades_list)"
      pattern: "write_backtest.*write_trades"
---

<objective>
Build the `BacktestEngine` (hybrid driver loop for D-02 attribution + VBT for BT-04 portfolio metrics), the `run_backtest.py` CLI that wires it to DuckDB + Parquet, and the two CI gate integration tests: BL-1 lookahead detector (D-14) and the FND-08 bitwise-identical reproducibility assertion.

Purpose: This is the integration gate for Phase 3. Without `BacktestEngine`, there is no `BacktestResult` to render in the UI. Without `test_lookahead.py` and `test_reproducibility.py`, Phase 3 cannot exit — both are cross-phase guardrails per ROADMAP.

Output:
- `BacktestEngine.run(bars_df, strategy, risk_manager, executor, *, seed, init_cash) -> BacktestResult` (a typed dataclass holding `trades: list[dict]`, `metrics: dict[str, Any]`, `equity_df: pd.DataFrame`)
- `scripts/run_backtest.py` CLI structurally modeled on `scripts/seed_bars.py` (argparse + asyncio.run + try/finally runs-row audit chain)
- Integration tests: BL-1 leakage detection at `tests/integration/test_lookahead.py`; bitwise-identical equity-curve Parquet at `tests/integration/test_reproducibility.py`
- DuckDB `runs` + `backtests` + `trades` rows + `data/parquet/equity/{run_id}.parquet` produced on every CLI invocation
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-VALIDATION.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-01-PLAN.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-02-PLAN.md
@.planning/phases/02-strategy-engine/02-02-SUMMARY.md
@packages/trading-core/src/trading_core/strategy/orb.py
@packages/trading-core/src/trading_core/strategy/registry.py
@packages/trading-core/src/trading_core/storage/runs.py
@packages/trading-core/src/trading_core/storage/duckdb_store.py
@scripts/seed_bars.py
@packages/trading-core/tests/fixtures/orb_day.py
@packages/trading-core/tests/test_orb_strategy.py
@packages/trading-core/tests/integration/test_indicator_leakage.py
@config/strategies/orb.yaml

<interfaces>
<!-- Contracts the executor needs. -->

From trading_core.strategy.orb (Phase 2):
- ORBStrategy(config: ORBConfig)
- ORBStrategy.on_bar(bar: Bar, ctx: StrategyContext) -> Signal | None
- ORBStrategy._push_bar(bar) — internal indicator updater (used by driver)
- ORBStrategy._atr.current, ._vwap.current, ._ema.current — prior-bar indicator snapshots
- ORBStrategy.is_warm() -> bool
- ORBStrategy._bar_count: int

From trading_core.strategy.registry (Phase 2):
- StrategyRegistry.from_yaml(yaml_path: Path) -> Strategy instance

From trading_core.storage.runs (Phase 1):
- new_run_id() -> str (uuid7)
- git_sha() -> str
- adr_hash(adr_path=None) -> str (defaults to .planning/decisions/0001-data-provider.md)
- param_hash(args: dict) -> str
- data_hash(df: pd.DataFrame) -> str (canonical pyarrow Parquet sha256)

From trading_core.storage.duckdb_store (Plan 01):
- DuckDBStore(path).ensure_schema() / write_run(...) / write_backtest(...) / write_trades(trades: list[dict])
- Existing query method: `self._conn.execute("SELECT ... FROM bars WHERE …", params).fetch_df()` returns a pd.DataFrame

From trading_core.backtest.safe_signals (Plan 02):
- safe_from_signals(close, entries, exits, *, price, sl_stop=None, tp_stop=None, short_entries=None, short_exits=None, open=None, high=None, low=None, **kwargs) -> vbt.Portfolio

From trading_core.risk.pass_through (Plan 02):
- PassThroughRiskManager(config: RiskConfig) — async check(signal, state) -> RiskDecision

From trading_core.execution.paper (Plan 02):
- PaperExecutor(symbol: str)
- async fill_entry(signal, decision, next_bar) -> Fill
- check_exit(*, side, entry_price, stop, target, bar, is_last_rth_bar) -> tuple[str, Decimal] | None
- async fill_exit(*, signal, exit_reason, exit_price, exit_ts_utc, fill_qty) -> Fill

VBT 1.0.0 metric API (VERIFIED in 03-RESEARCH.md):
- pf.total_return(), pf.annualized_return(), pf.sharpe_ratio(), pf.sortino_ratio(), pf.calmar_ratio()
- pf.max_drawdown() (negative fraction)
- pf.drawdowns.max_duration() (pd.Timedelta)
- pf.trades.win_rate() (float [0,1]), pf.trades.expectancy() (avg PnL per trade)
- pf.trades.profit_factor(), pf.trades.count(), pf.trades.avg_duration() (pd.Timedelta)
- pf.value() (equity curve), pf.drawdown() (drawdown fractions)

MAE/MFE manual computation (VERIFIED — VBT 1.0.0 OSS does NOT expose):
- For long: mae = entry_price - low_arr[entry_idx:exit_idx+1].min(); mfe = high_arr[entry_idx:exit_idx+1].max() - entry_price
- For short: mae = high_arr[entry_idx:exit_idx+1].max() - entry_price; mfe = entry_price - low_arr[entry_idx:exit_idx+1].min()

Equity curve Parquet path: data/parquet/equity/{run_id}.parquet
Columns: ts_utc, equity_$, drawdown_$ (column names with $; pa.Table preserves names)
Byte-stable flags (locked in Phase 1 runs.py): compression="none", use_dictionary=False, write_statistics=False

Reproducibility fixture baseline:
- orb_day_bars() data_hash baseline: 2d61c1889a7dbca4fee3e3cf7ea719be6cb3e12810d575635e69d38a6bbdb19f
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestEngine (hybrid driver loop + VBT metrics + MAE/MFE + Parquet write)</name>
  <files>
    packages/trading-core/src/trading_core/backtest/engine.py,
    packages/trading-core/src/trading_core/backtest/__init__.py,
    packages/trading-core/tests/test_backtest_engine.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/strategy/orb.py,
    packages/trading-core/tests/test_orb_strategy.py (_drive_strategy helper, lines 22-47),
    packages/trading-core/tests/integration/test_indicator_leakage.py (driver loop pattern),
    packages/trading-core/src/trading_core/storage/runs.py (data_hash byte-stable Parquet recipe),
    packages/trading-core/tests/fixtures/orb_day.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (Architectural Responsibility Map + BacktestEngine Hybrid Driver Loop Sketch + Extracting Metrics + MAE/MFE Manual + Pitfall 5/6/7),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (engine.py section),
    .planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md (D-02 / D-03 / D-12)
  </read_first>
  <behavior>
    - BacktestEngine.run(bars_df, strategy, risk_manager, executor, *, seed=42, init_cash=10_000.0) returns a BacktestResult dataclass with: `trades: list[dict]` (each dict has the 17 D-02 fields including stop_price and target_price), `metrics: dict` (12 BT-04 scalars + max_dd_duration_bars int), `equity_df: pd.DataFrame` with columns `ts_utc`, `equity_$`, `drawdown_$`.
    - Driving the engine over `orb_day_bars()` (390 bars, breakout at bar 15) produces exactly one trade with:
        - side="long", entry_price≈471.00 + 1-tick slippage (bar 16 opens during 9:45+ ET so slippage_ticks=1; verify by inspecting bar[16].ts_utc).
        - exit_reason is one of `{"target","stop","eod_flat"}` depending on whether 472.50 (target) is touched in bars 17-389 — given the fixture (post-breakout bars all close at 471.25), neither target nor stop fires, so exit_reason=="eod_flat" at bar 389.
        - mae and mfe computed correctly: mae = entry_price - min(low[entry_idx:exit_idx+1]); mfe = max(high[entry_idx:exit_idx+1]) - entry_price.
        - signal_id field matches the ORBStrategy-emitted signal's signal_id (BT-06 attribution chain).
        - stop_price field equals float(signal.stop) from the ORBStrategy-emitted signal; target_price equals float(signal.target).
    - metrics dict has keys: total_return, cagr, sharpe, sortino, calmar, max_dd, max_dd_duration_bars, win_rate, expectancy, profit_factor, trade_count, avg_hold_bars — all numeric (or None where the single-trade fixture can't produce them; e.g., Sortino may be NaN if no losing trades — coerce to None for storage).
    - equity_df has 390 rows (one per bar) and is monotonic in ts_utc.
    - Writing equity_df via the engine helper produces a byte-stable Parquet: two runs of `engine.write_equity_parquet(equity_df, tmp_path/"a.parquet")` and `(tmp_path/"b.parquet")` yield byte-identical files.
    - The driver loop respects rollover_seam: feeding bars with bar[0].rollover_seam=True (via rollover_seam_day_bars) produces zero trades.
    - The engine never calls `vbt.Portfolio.from_signals` directly — it goes through `safe_from_signals` (assertion: monkey-patch vbt.Portfolio.from_signals to count call sites; expect ≥ 1 but ALL through safe_from_signals).
  </behavior>
  <action>
    Create `packages/trading-core/src/trading_core/backtest/engine.py` with the hybrid architecture from 03-RESEARCH.md:

    Module docstring cites D-01..D-03, D-12, BT-01..BT-09, and the architecture decision: "Hybrid: driver loop produces per-trade attribution (D-02) + MAE/MFE (BT-05) + exit_reason (D-11); VBT computes portfolio-level metrics (BT-04). VBT is called via safe_from_signals only (D-13)."

    Imports follow scripts/seed_bars.py pattern (03-PATTERNS.md):
    - `from __future__ import annotations`, dataclasses (@dataclass), `datetime`, `Decimal`, `Path`, `pandas as pd`, `pyarrow as pa`, `pyarrow.parquet as pq`, `vectorbt as vbt` (used only for type references — the actual VBT calls go through safe_from_signals).
    - `from trading_core.backtest.safe_signals import safe_from_signals`
    - `from trading_core.data.models import Bar`
    - `from trading_core.execution.paper import PaperExecutor`
    - `from trading_core.execution.models import Fill`
    - `from trading_core.instruments import get as get_instrument`
    - `from trading_core.logging import get_logger`
    - `from trading_core.risk.models import RiskConfig, RiskDecision, RiskState`
    - `from trading_core.risk.pass_through import PassThroughRiskManager`
    - `from trading_core.strategy.models import Signal, StrategyContext`

    Define `@dataclass(frozen=True) class BacktestResult: trades: list[dict]; metrics: dict; equity_df: pd.DataFrame`.

    Define `class BacktestEngine:`
    - `__init__(self, *, symbol: str)`: store symbol; load instrument; instantiate `_log = get_logger(__name__)`.
    - `async def run(self, *, bars: list[Bar], strategy, risk_manager, executor: PaperExecutor, seed: int = 42, init_cash: float = 10_000.0) -> BacktestResult`:
        - PHASE 1: bar-by-bar driver loop modeled exactly on test_orb_strategy.py::_drive_strategy and test_indicator_leakage.py — snapshot ctx, call strategy.on_bar(bar, ctx), then strategy._push_bar(bar).
        - State: open_position: dict|None = None; trades: list[dict] = []; entries_bool[i] = False; exits_bool[i] = False; equity_per_bar[i] = init_cash (float).
        - On every bar i:
            1. Build ctx from prior-bar indicator snapshots (atr=strategy._atr.current, vwap=strategy._vwap.current, ema=strategy._ema.current, adr=None).
            2. signal = await/sync strategy.on_bar(bar, ctx). (Phase 2 made it sync; await NOT needed.)
            3. strategy._push_bar(bar).
            4. If signal is not None AND open_position is None AND i+1 < len(bars):
                - state = RiskState() (default realized_pnl_today=Decimal("0"))
                - decision = await risk_manager.check(signal, state)
                - if decision.approved:
                    - next_bar = bars[i+1]
                    - fill = await executor.fill_entry(signal, decision, next_bar)
                    - open_position = {"signal": signal, "entry_fill": fill, "entry_idx": i+1, "fill_qty": decision.adjusted_size}
                    - entries_bool[i] = True   # raw (un-shifted) entry signal — wrapper shifts internally
            5. If open_position is not None:
                - is_last_rth_bar = (i == len(bars) - 1)  # for Phase 3 use the last bar of the input slice; calendar EOD is Phase 5's wall-clock scheduler
                - exit = executor.check_exit(side=open_position["signal"].side, entry_price=open_position["entry_fill"].fill_price, stop=open_position["signal"].stop, target=open_position["signal"].target, bar=bar, is_last_rth_bar=is_last_rth_bar)
                - if exit is not None:
                    - exit_reason, exit_price = exit
                    - exit_fill = await executor.fill_exit(signal=open_position["signal"], exit_reason=exit_reason, exit_price=exit_price, exit_ts_utc=bar.ts_utc, fill_qty=open_position["fill_qty"])
                    - Compute MAE/MFE from bars[entry_idx:i+1] using high/low arrays (Decimal): for long, mae=open_position["entry_fill"].fill_price - min(b.low for b in bars[entry_idx:i+1]); mfe=max(b.high for b in bars[entry_idx:i+1]) - entry_fill.fill_price; mirror for short.
                    - Compute pnl: for long, (exit_price - entry_fill.fill_price) * fill_qty * instrument.point_value; mirror for short.
                    - Append trade dict with all 17 D-02 fields. trade_id = new_run_id() (uuid7). run_id will be set by the caller (CLI) after the result returns. Leave run_id="" or pass it into run() — locked choice: pass `run_id` into BacktestEngine.run() so the engine fills it in directly. Update method signature to accept run_id: `async def run(self, *, run_id: str, bars, strategy, risk_manager, executor, seed=42, init_cash=10_000.0) -> BacktestResult`. The trade dict MUST include these fields explicitly (BT-06 attribution chain):
                        - `"signal_id": open_position["signal"].signal_id` — traces the DuckDB trades row back to the originating Signal (non-null for all ORBStrategy trades)
                        - `"stop_price": float(open_position["signal"].stop)` — sourced from Signal.stop (Decimal); enables UI stop priceLine rendering
                        - `"target_price": float(open_position["signal"].target)` — sourced from Signal.target (Decimal); enables UI target priceLine rendering
                    - exits_bool[i] = True
                    - Append exit_fill to a running fills list (used for sum(positions) assertion).
                    - open_position = None
                - else:
                    - Update equity_per_bar[i] with unrealized: for long, equity_per_bar[i] = equity_per_bar[i-1] + (bar.close - entry_fill.fill_price) * fill_qty * point_value; mirror for short.
            6. equity_per_bar[i] defaults to equity_per_bar[i-1] when no position (or init_cash on i=0).
        - PHASE 2: VBT metrics pass (BT-04).
            - Build pd.Series from the bars list: index = pd.DatetimeIndex([b.ts_utc for b in bars], tz="UTC"); open_s, high_s, low_s, close_s as Series of float(b.open)/etc.
            - entries_s = pd.Series(entries_bool, index=index); exits_s = pd.Series(exits_bool, index=index)
            - price = open_s.shift(-1).fillna(open_s)  # next-bar open as fill price; final-bar shifts to itself
            - pf = safe_from_signals(close=close_s, entries=entries_s, exits=exits_s, price=price, freq='1min', init_cash=init_cash, size=1, direction='longonly', open=open_s, high=high_s, low=low_s, seed=seed)
            - metrics dict (use the EXACT API paths verified in 03-RESEARCH.md — note Pitfall 5: `pf.drawdowns.max_duration()` not `pf.max_drawdown_duration()`):
                total_return = float(pf.total_return())
                cagr = float(pf.annualized_return())
                sharpe = float(pf.sharpe_ratio()) if pf.trades.count() > 0 else None
                sortino = float(pf.sortino_ratio()) if pf.trades.count() > 0 else None
                calmar = float(pf.calmar_ratio()) if pf.trades.count() > 0 else None
                max_dd = float(pf.max_drawdown())
                max_dd_duration_bars = int(pf.drawdowns.max_duration().total_seconds() // 60) if pf.trades.count() > 0 else 0
                win_rate = float(pf.trades.win_rate()) if pf.trades.count() > 0 else None
                expectancy = float(pf.trades.expectancy()) if pf.trades.count() > 0 else None
                profit_factor = float(pf.trades.profit_factor()) if pf.trades.count() > 0 else None
                trade_count = int(pf.trades.count())
                avg_hold_bars = int(pf.trades.avg_duration().total_seconds() // 60) if pf.trades.count() > 0 else 0
                Coerce NaN to None: helper `_or_none(x) = None if x is None or (isinstance(x, float) and not math.isfinite(x)) else x` to keep DuckDB writes safe.
            - equity_df: build from equity_per_bar list, columns ts_utc / equity_$ / drawdown_$. Compute drawdown_$ from the equity_per_bar running peak: peak[i] = max(equity_per_bar[:i+1]); drawdown_$[i] = peak[i] - equity_per_bar[i]; (positive drawdown depth).
        - Return BacktestResult(trades=trades, metrics=metrics, equity_df=equity_df).

    Add `def write_equity_parquet(equity_df: pd.DataFrame, path: Path) -> None` at module level matching 03-PATTERNS.md byte-stable flags:
    - path.parent.mkdir(parents=True, exist_ok=True)
    - table = pa.Table.from_pandas(equity_df.reset_index(drop=True), preserve_index=False)
    - pq.write_table(table, str(path), compression="none", use_dictionary=False, write_statistics=False)

    Update `packages/trading-core/src/trading_core/backtest/__init__.py` to re-export `BacktestEngine`, `BacktestResult`, `safe_from_signals`, `write_equity_parquet`.

    Replace the Wave 0 stub in `packages/trading-core/tests/test_backtest_engine.py` with TestBacktestEngineDriverLoop / TestMetrics / TestMAEMFE / TestAttributionChain / TestRolloverSeam test classes following the structure in 03-PATTERNS.md §test_backtest_engine.py:
    - TestBacktestEngineDriverLoop.test_produces_backtest_result: drive the engine over orb_day_bars(); assert exactly one trade with side="long", exit_reason in {"target","stop","eod_flat"}; assert len(trades) == 1; assert equity_df has 390 rows.
    - TestMetrics.test_metrics_keys_present: assert metrics dict contains all 12 keys + max_dd_duration_bars; assert trade_count == 1; assert isinstance(metrics["total_return"], (float, type(None))).
    - TestMAEMFE.test_mae_mfe_correct_against_fixture: hand-compute MAE/MFE for the orb_day_bars trade (entry around bar 16 at 471.00+0.01 = 471.01; exit at bar 389 close 471.25 with eod_flat; MAE = entry - min(low[16:390]); MFE = max(high[16:390]) - entry); assert engine's reported MAE/MFE matches within Decimal("0.01") tolerance.
    - TestAttributionChain.test_signal_id_in_trade_row: assert trade["signal_id"] equals the strategy's emitted signal.signal_id; assert trade["strategy_id"] == "orb-v1"; assert trade["stop_price"] == float(signal.stop); assert trade["target_price"] == float(signal.target) (validates the BT-06 attribution chain and stop/target fields).
    - TestRolloverSeam.test_no_trades_on_rollover_day: feed rollover_seam_day_bars(); assert len(trades) == 0; equity_df should still have 390 rows (constant init_cash).
    - Also include a test_parquet_byte_stability: call write_equity_parquet twice into tmp_path; assert path1.read_bytes() == path2.read_bytes().
    - Also include test_engine_uses_safe_from_signals: monkey-patch trading_core.backtest.engine.safe_from_signals (via mocker or unittest.mock.patch.object) with a spy; assert it was called and assert vbt.Portfolio.from_signals was NOT called as a top-level reference outside the wrapper (this is a soft proxy — the hard guarantee is the pre-commit hook).
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_backtest_engine.py -x -q</automated>
  </verify>
  <done>
    BacktestEngine drives ORBStrategy over orb_day_bars and produces a single-trade BacktestResult with correct MAE/MFE, exit_reason="eod_flat", attribution chain intact (signal_id, stop_price, target_price all present), byte-stable equity Parquet. All metric keys are present (or None when single-trade undefined). Rollover-seam fixture produces zero trades. Existing 244-test suite still passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_backtest.py CLI + BL-1 lookahead + reproducibility integration tests</name>
  <files>
    scripts/run_backtest.py,
    packages/trading-core/tests/integration/test_lookahead.py,
    packages/trading-core/tests/integration/test_reproducibility.py
  </files>
  <read_first>
    scripts/seed_bars.py (CLI structure + try/finally audit chain + argparse builder + stdout reconfigure),
    packages/trading-core/src/trading_core/storage/runs.py,
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    packages/trading-core/src/trading_core/strategy/registry.py,
    config/strategies/orb.yaml,
    packages/trading-core/tests/integration/test_indicator_leakage.py (test scaffolding pattern),
    packages/trading-core/tests/fixtures/orb_day.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (BL-1 Lookahead Detector Test Pattern + Reproducibility Hash Chain),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (run_backtest.py + test_lookahead.py + test_reproducibility.py sections)
  </read_first>
  <behavior>
    run_backtest.py CLI:
    - `uv run python scripts/run_backtest.py --strategy orb --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03 --config config/strategies/orb.yaml --seed 42 --duckdb-path <tmp>.duckdb` exits 0 and prints a structured JSON line containing run_id, status="ok", trade_count, total_return.
    - After the CLI completes, the DuckDB at --duckdb-path contains exactly 1 runs row, 1 backtests row, and N trade rows where N == result.metrics["trade_count"].
    - An equity-curve Parquet at `data/parquet/equity/{run_id}.parquet` (relative to the repo root or --equity-root if added) exists with 3 columns (ts_utc, equity_$, drawdown_$).
    - On adapter exception (raise inside the engine via a bad config), the script exits 1 and STILL writes a runs row with status="failed" and notes containing the exception class.
    - Two invocations with identical args (--symbol SPY --tf 1m --from … --to … --config … --seed 42) against a tmp_path-isolated DuckDB produce two different run_ids (uuid7) but byte-identical equity-curve Parquet files.
    - param_hash, data_hash, git_sha, adr_hash are populated on the runs row (none == "").

    BL-1 lookahead test (D-14, ROADMAP cross-phase guardrail):
    - test_bl1_lookahead_neutralized_by_safe_from_signals constructs leaking_entries = (close.shift(-1) > Decimal("471.00")).fillna(False) over orb_day_bars; calls safe_from_signals with that and asserts:
        - np.isfinite(pf.sharpe_ratio()) is True (Sharpe is not inf — lookahead is neutralized)
        - 0.35 <= win_rate <= 0.65 (random-walk band) OR if trade_count == 0, the test is informational only (skip with reason) — but per 03-RESEARCH.md the fixture is designed to produce ≥ 1 trade. The locked assertion is the finite-Sharpe one; the win-rate band is a soft assertion (it must pass with the orb_day_bars fixture's actual numbers — verify during execution and adjust the band if needed, documenting the deviation in SUMMARY.md).
    - This test is the CI gate required by ROADMAP guardrails. It must run in <30s.

    Reproducibility test (FND-08):
    - test_reproducibility_same_inputs_bitwise_identical(tmp_path) runs run_backtest's `main()` function twice (programmatic invocation, not subprocess) with identical args into two tmp DuckDB paths and two tmp equity-Parquet paths; asserts `path1.read_bytes() == path2.read_bytes()`.
    - Alternative: invoke the CLI via subprocess.run([sys.executable, "scripts/run_backtest.py", ...]) twice; both invocations succeed; compare the resulting equity Parquet bytes. Pick whichever is faster and more deterministic — locked choice: programmatic `main()` import. Subprocess is slower and more flaky on Windows path-with-space.
    - The test runs in <30s.
  </behavior>
  <action>
    Create `scripts/run_backtest.py` modeled on `scripts/seed_bars.py` (copy the structural patterns from 03-PATTERNS.md §run_backtest.py):

    Module docstring cites BT-09 + FND-08 + cross-phase guardrails; documents the pipeline: `DuckDB bars query → StrategyRegistry.from_yaml → BacktestEngine.run → DuckDBStore.write_run/write_backtest/write_trades → write_equity_parquet`.

    Header reconfigures stdout/stderr to UTF-8 (Pitfall 5 — Windows piped-stdout). Add the same sys.path guard `_REPO_ROOT = Path(__file__).resolve().parent.parent`; insert `_SRC = _REPO_ROOT / "packages" / "trading-core" / "src"` to sys.path if not present.

    Imports: argparse, asyncio, sys, json, math, datetime, decimal, pathlib; structured-logging via `setup_logging` from trading_core.logging; `from trading_core.backtest.engine import BacktestEngine, write_equity_parquet`; `from trading_core.config import Settings`; `from trading_core.execution.paper import PaperExecutor`; `from trading_core.risk.models import RiskConfig`; `from trading_core.risk.pass_through import PassThroughRiskManager`; `from trading_core.storage.duckdb_store import DuckDBStore`; `from trading_core.storage.runs import adr_hash, data_hash, git_sha, new_run_id, param_hash`; `from trading_core.strategy.registry import StrategyRegistry`; `from trading_core.data.models import Bar`.

    `def _parse_iso_utc(s: str) -> datetime` — same helper as seed_bars.py.

    `def _build_parser()` returns ArgumentParser with these flags:
    - `--strategy` required, choices=["orb"] (more strategies arrive in v2; lock to ORB here).
    - `--symbol` required, choices=["ES","MES","SPY"].
    - `--tf` required, choices=["1m","5m","15m"].
    - `--from` (dest=frm) type=_parse_iso_utc required.
    - `--to` type=_parse_iso_utc required.
    - `--config` type=Path default Path("config/strategies/orb.yaml").
    - `--seed` type=int default=42.
    - `--duckdb-path` (dest=duckdb_path) type=Path default=None (resolves via Settings.duckdb_path).
    - `--equity-root` type=Path default=Path("data/parquet/equity").
    - `--init-cash` type=float default=10_000.0.

    `async def main(args) -> int`:
    - run_id = new_run_id()
    - log = get_logger(__name__).bind(run_id=run_id, symbol=args.symbol, tf=args.tf, strategy=args.strategy)
    - log.info("backtest.start", from_ts=str(args.frm), to_ts=str(args.to), seed=args.seed)
    - settings = Settings(); duckdb_path = args.duckdb_path or settings.duckdb_path
    - status = "failed"; notes = ""; df = None
    - Build args_dict = {"symbol": args.symbol, "tf": args.tf, "from": args.frm.isoformat(), "to": args.to.isoformat(), "config": str(args.config), "seed": args.seed, "init_cash": args.init_cash, "strategy": args.strategy}.
    - started_at = datetime.now(tz=timezone.utc); finished_at = None.
    - try:
        - store = DuckDBStore(duckdb_path); store.ensure_schema()
        - df = store._conn.execute("SELECT symbol, timeframe, ts_utc, open, high, low, close, volume, rollover_seam, provider FROM bars WHERE symbol = ? AND timeframe = ? AND ts_utc >= ? AND ts_utc < ? ORDER BY ts_utc ASC", [args.symbol, args.tf, args.frm, args.to]).fetch_df()
        - if df.empty: raise RuntimeError(f"No bars found for {args.symbol} {args.tf} in [{args.frm}, {args.to}). Run seed_bars.py first.")
        - bars: list[Bar] = [Bar(symbol=row.symbol, timeframe=row.timeframe, ts_utc=row.ts_utc.to_pydatetime(), open=Decimal(str(row.open)), high=Decimal(str(row.high)), low=Decimal(str(row.low)), close=Decimal(str(row.close)), volume=int(row.volume), rollover_seam=bool(row.rollover_seam)) for row in df.itertuples(index=False)]
        - strategy = StrategyRegistry.from_yaml(args.config) — confirm this returns an ORBStrategy instance per Phase 2 registry contract; if the API is slightly different, adapt to the actual API (e.g., `registry = StrategyRegistry(args.config); strategy = registry.get("orb")` — read packages/trading-core/src/trading_core/strategy/registry.py FIRST and use the actual function names).
        - risk_manager = PassThroughRiskManager(RiskConfig())
        - executor = PaperExecutor(args.symbol)
        - engine = BacktestEngine(symbol=args.symbol)
        - result = await engine.run(run_id=run_id, bars=bars, strategy=strategy, risk_manager=risk_manager, executor=executor, seed=args.seed, init_cash=args.init_cash)
        - equity_path = args.equity_root / f"{run_id}.parquet"
        - write_equity_parquet(result.equity_df, equity_path)
        - store.write_backtest(run_id=run_id, strategy_id="orb-v1", symbol=args.symbol, timeframe=args.tf, from_ts=args.frm, to_ts=args.to, param_hash=param_hash(args_dict), equity_curve_path=str(equity_path.relative_to(_REPO_ROOT)), **result.metrics) — only if metrics dict keys match the write_backtest kwargs exactly; otherwise unpack explicitly.
        - store.write_trades(result.trades)
        - status = "ok"
        - print(json.dumps({"run_id": run_id, "status": "ok", "trade_count": result.metrics["trade_count"], "total_return": result.metrics["total_return"]}))
        - return 0
    - except Exception as exc:
        - status = "failed"; notes = f"{type(exc).__name__}: {exc}"
        - log.exception("backtest.failed", error_type=type(exc).__name__)
        - return 1
    - finally:
        - try:
            - finished_at = datetime.now(tz=timezone.utc)
            - store.write_run(run_id=run_id, git_sha=git_sha(), data_hash=data_hash(df) if df is not None and not df.empty else "", param_hash=param_hash(args_dict), seed=int(args.seed), adr_hash=adr_hash(_REPO_ROOT / ".planning" / "decisions" / "0001-data-provider.md"), started_at=started_at, finished_at=finished_at, status=status, notes=notes)
        - except Exception:
            - log.exception("runs.write_run.failed")
        - finally:
            - try: store.close()
            - except Exception: pass

    `if __name__ == "__main__":` invokes asyncio.run(main(_build_parser().parse_args())) and sys.exits with its return code.

    Replace `packages/trading-core/tests/integration/test_lookahead.py` (xfail stub from Plan 01) with the BL-1 gate test from 03-PATTERNS.md §test_lookahead.py:
    - Build pd.DatetimeIndex from orb_day_bars; close/high/low Series.
    - leaking_entries = (close.shift(-1) > 471.00).fillna(False).infer_objects(copy=False)
    - exits = pd.Series([False] * len(close), index=index)
    - pf = safe_from_signals(close=close, entries=leaking_entries, exits=exits, price=close.shift(-1).fillna(close), freq='1min', init_cash=10_000.0, size=1, direction='longonly', high=high, low=low)
    - sharpe = pf.sharpe_ratio(); assert np.isfinite(sharpe). 
    - if pf.trades.count() > 0: win_rate = float(pf.trades.win_rate()); assert 0.30 <= win_rate <= 0.70 (slightly wider than the recommended 0.35-0.65 to absorb the deterministic fixture's actual numbers). Document the actual win_rate in the SUMMARY for traceability.
    - If pf.trades.count() == 0 the test should fail loudly with a clear message — the fixture is intentionally designed to fire trades.

    Replace `packages/trading-core/tests/integration/test_reproducibility.py` (xfail stub) with:
    - test_reproducibility_same_inputs_bitwise_identical(tmp_path):
        - First seed a synthetic DuckDB with the orb_day_bars fixture (use DuckDBStore.upsert_bars on a pd.DataFrame built from the fixture); a helper `_seed_tmp_db(tmp_path) -> Path` reuses the existing seeding logic. ALTERNATIVE if simpler: call the BacktestEngine programmatically twice and write its equity_df to two Parquet paths via write_equity_parquet, then compare bytes. This avoids needing to seed bars at all and is the locked choice — the test focuses on whether the engine + Parquet write are byte-stable, not whether the CLI is byte-stable. The CLI is covered by behavior bullets in Task 2 above (not asserted in this test).
        - Construct identical BacktestEngine inputs (same orb_day_bars, same ORBStrategy(config), same PaperExecutor("SPY"), same PassThroughRiskManager(RiskConfig()), same seed=42).
        - run1 = await engine.run(run_id="aaa", bars=..., ...); run2 = await engine.run(run_id="bbb", bars=..., ...).
        - write_equity_parquet(run1.equity_df, tmp_path / "a.parquet"); write_equity_parquet(run2.equity_df, tmp_path / "b.parquet").
        - assert (tmp_path / "a.parquet").read_bytes() == (tmp_path / "b.parquet").read_bytes(), "Equity curve is not bitwise-identical — pyarrow flags or seed mismatch"
    - test_engine_metrics_deterministic: assert run1.metrics == run2.metrics (when metrics dict comparison is well-defined; coerce NaN floats to a sentinel before comparing — but since we already coerce NaN to None in the engine, this should just work).
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/integration/test_lookahead.py packages/trading-core/tests/integration/test_reproducibility.py -x -q</automated>
  </verify>
  <done>
    BL-1 test passes: Sharpe finite, win_rate in band. Reproducibility test passes: two engine runs with identical inputs produce byte-identical equity Parquet. CLI invocation `uv run python scripts/run_backtest.py --strategy orb --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03 --config config/strategies/orb.yaml --seed 42 --duckdb-path <tmp>.duckdb` (after seeding bars) exits 0 and writes a runs+backtests+trades row. `uv run pytest -x -q` shows all 244 existing tests still pass; xfail placeholders are now real passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argparse → DuckDB query | --symbol / --tf / --from / --to / --config user input crosses into SQL + filesystem |
| BacktestEngine driver loop → ORBStrategy._push_bar order | wrong order = silent ATR lookahead |
| Equity-curve Parquet write → reproducibility CI | non-deterministic flags break FND-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering (V5) | CLI args (`--symbol`, `--tf`, `--from`, `--to`, `--config`, `--seed`) | mitigate | argparse `choices=` enums on --symbol/--tf/--strategy; type=_parse_iso_utc on dates; type=Path on file args; type=int on seed. DuckDB query uses parameterized placeholders `?` (no string interpolation of args.symbol / args.tf / args.frm / args.to). |
| T-03-03-02 | Tampering | ATR lookahead via wrong driver-loop order | mitigate | The driver loop preserves Phase 2's locked snapshot→on_bar→_push_bar order (verified by reusing the test_indicator_leakage.py pattern); BL-1 integration test (Task 2) is the runtime gate. |
| T-03-03-03 | Tampering | Reproducibility drift via Parquet flags | mitigate | write_equity_parquet hardcodes `compression="none", use_dictionary=False, write_statistics=False`; test_reproducibility asserts byte-identical bytes between two runs. |
| T-03-03-04 | Tampering | YAML strategy config injection | mitigate | StrategyRegistry uses yaml.safe_load (locked in Phase 2 T-02-02-01); no change needed here. |
| T-03-03-05 | DoS | Engine memory blowup on multi-year backtests | accept | Phase 3 scope is single-day fixtures + a January 2024 month at most. Multi-year scaling is Phase 4's concern (workers + Parquet shards). Documented in engine docstring. |
| T-03-03-06 | Information Disclosure | Audit log leaking duckdb_path with embedded credentials | mitigate | Path objects only; no URL parsing; structlog has no auto-redaction needed because the path is a local filesystem path. |
</threat_model>

<verification>
- BacktestEngine: `uv run pytest packages/trading-core/tests/test_backtest_engine.py -x -q` → all classes pass
- BL-1 gate: `uv run pytest packages/trading-core/tests/integration/test_lookahead.py -x -q` → passes (Sharpe finite, win_rate in band)
- Reproducibility: `uv run pytest packages/trading-core/tests/integration/test_reproducibility.py -x -q` → byte-identical bytes
- CLI smoke (manual): seed bars first (`uv run python scripts/seed_bars.py --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03`), then `uv run python scripts/run_backtest.py --strategy orb --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03 --config config/strategies/orb.yaml --seed 42` — exits 0, prints JSON with run_id + trade_count + total_return
- Full regression: `uv run pytest -x -q` → 244 + new ≈ 270+ tests all pass
- Pre-commit hook: `uv run pre-commit run no-direct-vbt-from-signals --all-files` → exit 0 (only safe_signals.py calls vbt.Portfolio.from_signals)
</verification>

<success_criteria>
- BacktestEngine drives ORBStrategy and produces a single-trade result on orb_day_bars with correct MAE/MFE, exit_reason, signal_id, stop_price, and target_price
- BL-1 test_lookahead passes: leaking strategy → finite Sharpe + win_rate in 30-70% band (cross-phase guardrail)
- test_reproducibility passes: two engine runs → byte-identical equity Parquet (FND-08 CI gate)
- run_backtest.py CLI smoke exits 0 with status="ok"; writes runs + backtests + trades rows; writes equity Parquet
- All 6 BT requirements covered by tests (BT-01, BT-04, BT-05, BT-06, BT-07, BT-09)
- 244 existing + new tests all green; no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-vertical-mvp-slice-backtester/03-03-SUMMARY.md` documenting:
- Actual win_rate produced by BL-1 leaking ORB on orb_day_bars (numeric value with 2 decimal places)
- Whether the metrics dict required NaN-to-None coercion for the single-trade fixture (and which metrics)
- Any deviation from the planned BacktestEngine architecture (e.g., made run() synchronous because async added no value with all sync collaborators)
</output>
