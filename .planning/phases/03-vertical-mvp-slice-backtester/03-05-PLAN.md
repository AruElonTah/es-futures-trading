---
phase: 03-vertical-mvp-slice-backtester
plan: "05"
type: execute
wave: 5
depends_on:
  - "03-04-PLAN.md"
files_modified:
  - apps/web/app/dashboard/page.tsx
  - apps/web/app/layout.tsx
  - apps/web/components/Chart.tsx
  - apps/web/components/EquityCurve.tsx
  - apps/web/components/ETClock.tsx
  - apps/web/components/ConnectionStatus.tsx
  - apps/web/components/DegradationBanner.tsx
  - apps/web/components/QueryProvider.tsx
  - apps/web/hooks/useBars.ts
  - apps/web/hooks/useBacktests.ts
  - apps/web/hooks/useStream.ts
  - apps/web/store/ws.ts
  - apps/web/lib/api.ts
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
autonomous: false
requirements:
  - UI-01
  - UI-04
  - UI-08
tags:
  - nextjs
  - lightweight-charts
  - dashboard
  - websocket
  - zustand
  - tanstack-query
  - wave-5
user_setup:
  - service: backend
    why: "Visual verification of the dashboard requires uvicorn + next dev both running"
    dashboard_config:
      - task: "Start the FastAPI backend"
        location: "Repo root: `uv run uvicorn api:app --host 127.0.0.1 --port 8000`"
      - task: "Start the Next.js dev server"
        location: "Repo root: `pnpm --filter web dev` (or `cd apps/web && pnpm dev`)"
      - task: "Open the dashboard in a browser"
        location: "http://localhost:3000/dashboard"
must_haves:
  truths:
    - "Loading /dashboard renders ES/SPY candlesticks from GET /bars without any uncaught JS error"
    - "Time axis displays America/New_York hours (tickMarkFormatter) — no browser-timezone drift"
    - "ORB box overlay (high + low priceLines) renders for the visible RTH day when at least one bar in the opening-range window is present"
    - "Entry arrow marker + stop priceLine + target priceLine render for the most recent backtest's first trade (when a backtests row exists and GET /backtests/{run_id}/trades returns rows with stop_price and target_price)"
    - "Equity-curve pane (bottom ~30%) renders the equity_$ line from the most recent backtest (loaded via GET /backtests/{run_id}/equity JSON endpoint)"
    - "Header shows an ET clock that ticks every second using Intl.DateTimeFormat({timeZone: 'America/New_York'})"
    - "Connection-status indicator is green when WS is connected AND last bar ≤ 10s ago; yellow when last bar > 10s; red when WS disconnected OR last bar > 30s"
    - "Degradation banner appears when the WS client receives a `{type: 'degraded_state'}` envelope; the banner disappears when a subsequent non-degraded event arrives or after a manual dismiss"
    - "Two-pane layout uses fixed 70/30 percent heights — chart on top, equity on bottom (D-08); drag/resize is explicitly Phase 7"
    - "All chart components use `'use client'` and mount lightweight-charts inside a useEffect with chart.remove() cleanup"
    - "At least one trade's entry arrow + stop price line + target price line are visible on the chart after running a backtest and loading /dashboard"
    - "createSeriesMarkers is imported from 'lightweight-charts' (v5 API); series.setMarkers() is NEVER called (removed in v5 — Pitfall 2)"
  artifacts:
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "/dashboard route — two-pane layout with Chart + EquityCurve + ET clock + connection status + degradation banner"
      contains: "DashboardPage"
    - path: "apps/web/components/Chart.tsx"
      provides: "lightweight-charts v5 candlestick chart with ORB priceLines + entry markers + stop/target priceLines"
      contains: "createChart"
    - path: "apps/web/components/EquityCurve.tsx"
      provides: "lightweight-charts v5 LineSeries showing equity_$ over time"
      contains: "LineSeries"
    - path: "apps/web/components/ETClock.tsx"
      provides: "Header clock ticking every 1s in America/New_York"
      contains: "America/New_York"
    - path: "apps/web/components/ConnectionStatus.tsx"
      provides: "Color-coded WS + bar-staleness indicator (green/yellow/red per D-08)"
      contains: "lastBarAt"
    - path: "apps/web/components/DegradationBanner.tsx"
      provides: "Top banner shown when degraded_state event is received (UI-08)"
      contains: "degraded"
    - path: "apps/web/hooks/useBars.ts"
      provides: "TanStack Query v5 hook for GET /bars"
      contains: "useQuery"
    - path: "apps/web/hooks/useStream.ts"
      provides: "Native WebSocket subscription with JSON envelope routing (D-05); updates Zustand WS store"
      contains: "new WebSocket"
    - path: "apps/web/store/ws.ts"
      provides: "Zustand store for connected/lastBarAt/degraded state"
      contains: "create"
  key_links:
    - from: "Chart.tsx createSeriesMarkers"
      to: "lightweight-charts v5 named export"
      via: "import { createSeriesMarkers } from 'lightweight-charts'"
      pattern: "createSeriesMarkers"
    - from: "Chart.tsx tickMarkFormatter"
      to: "Intl.DateTimeFormat({timeZone:'America/New_York'})"
      via: "format Unix-seconds time as ET hh:mm"
      pattern: "America/New_York"
    - from: "useBars hook"
      to: "GET /bars endpoint"
      via: "fetch('http://localhost:8000/bars?symbol=...&tf=...&limit=...')"
      pattern: "fetch.*api/bars|/bars\\?"
    - from: "useStream hook"
      to: "WS /stream endpoint"
      via: "new WebSocket('ws://localhost:8000/stream'); onmessage parses {type,payload}"
      pattern: "ws://.*stream"
    - from: "ConnectionStatus color logic"
      to: "Zustand ws store"
      via: "useWsStore((s) => ({connected: s.connected, lastBarAt: s.lastBarAt}))"
      pattern: "lastBarAt"
---

<objective>
Ship the Phase-3 minimal Next.js `/dashboard` page (UI-01 + UI-04 + UI-08) that loads ES/SPY candles from the FastAPI backend, renders the ORB box overlay + entry/stop/target markers, the equity curve from the most-recent backtest, an ET clock, a connection-status indicator with bar-staleness coloring, and a degradation banner driven by the `degraded_state` WS event.

Purpose: This is the operator's first end-to-end view. After this lands, the user can:
1. Run `scripts/run_backtest.py` (Plan 03 CLI).
2. Visit `localhost:3000/dashboard`.
3. See the candles, ORB box, entry arrow, equity curve, and ET clock — proving the architecture closes end-to-end.

Output:
- `/dashboard` page with two-pane layout (D-08: chart 70%, equity 30%).
- `Chart.tsx`, `EquityCurve.tsx`, `ETClock.tsx`, `ConnectionStatus.tsx`, `DegradationBanner.tsx` components.
- `useBars`, `useBacktests`, `useStream` hooks.
- Zustand `ws` store for WS connection state + bar-staleness.
- Tailwind dark monospace theme applied to the dashboard route.
- A human-verify checkpoint at the end so the operator confirms visual correctness before the phase is closed.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-04-PLAN.md
@apps/web/CLAUDE.md
@apps/web/AGENTS.md
@apps/web/app/layout.tsx
@apps/web/app/page.tsx
@apps/web/package.json
@apps/web/next.config.ts
@apps/web/tsconfig.json
@apps/web/app/globals.css

<interfaces>
<!-- Contracts the executor needs. Lifted from research + installed package shapes. -->

apps/web/CLAUDE.md → apps/web/AGENTS.md WARNING:
- "This is NOT the Next.js you know" — Next.js 16.2 + React 19 have breaking changes.
- "Read the relevant guide in `node_modules/next/dist/docs/` before writing any code."
- This means: do NOT assume App Router patterns from training data. Read `apps/web/node_modules/next/dist/docs/` for the current API before writing route handlers, client components, or layout files.

lightweight-charts 5.2.0 (VERIFIED in 03-RESEARCH.md from installed typings.d.ts):
- Markers API CHANGED in v5: `series.setMarkers(...)` was REMOVED. Use the plugin API:
  `import { createSeriesMarkers } from 'lightweight-charts';`
  `const markersPlugin = createSeriesMarkers(candleSeries, []);`
  `markersPlugin.setMarkers(markersArray);  // update`
- `createChart(container, options)` returns IChartApi
- `chart.addSeries(CandlestickSeries, opts)` returns ISeriesApi (Candlestick)
- `chart.addSeries(LineSeries, opts, paneIndex)` returns ISeriesApi (Line) — paneIndex is the 3rd arg; index=1 creates a second pane
- `series.setData(data)` where data is `{time:number,open,high,low,close}[]` for candles or `{time:number,value:number}[]` for lines; `time` is Unix seconds UTC
- `series.createPriceLine({price,color,lineWidth,lineStyle,axisLabelVisible,title})` returns IPriceLine
- `series.removePriceLine(line)` to clear
- `chart.remove()` for cleanup
- Time formatting: pass options to `createChart({localization:{timeFormatter},timeScale:{tickMarkFormatter,timeVisible,secondsVisible}})`. Both formatters receive Unix seconds.

FastAPI backend (Plan 04):
- `GET http://localhost:8000/bars?symbol=SPY&tf=1m&limit=390` returns list of `{ts_utc: ISO string, open, high, low, close: number, volume: int, rollover_seam: bool}`
- `GET http://localhost:8000/backtests` returns list of `{run_id, ..., equity_curve_path: string, ...}` sorted by created_at DESC
- `WS ws://localhost:8000/stream` emits JSON messages `{type: string, payload: object}` per D-05
- 7 topic types: `bars`, `signals`, `risk_decisions`, `fills`, `positions`, `equity`, `degraded_state`

CORS / cross-origin note:
- Next dev runs on :3000, FastAPI on :8000. Either configure FastAPI CORS or proxy through Next. Locked choice for Phase 3: ADD CORS middleware to api/app.py allowing `http://localhost:3000` (single-operator localhost — minimal risk). The CORS middleware addition is a Task in this plan.

@tanstack/react-query v5 (installed):
- Wrap the app in QueryClientProvider at the layout/page level (client component).
- `useQuery({queryKey, queryFn, staleTime})` is the standard hook.

Zustand v5 (installed):
- `import { create } from 'zustand'`
- `const useStore = create((set) => ({...}))`

Equity curve loading constraint:
- Equity curve is stored as a Parquet file at `data/parquet/equity/{run_id}.parquet` (Plan 03).
- The browser CANNOT parse Parquet without an additional JS dependency (e.g., parquetjs / hyparquet) which is OUT OF SCOPE for Phase 3.
- Locked choice: ADD a Phase-3 `GET /backtests/{run_id}/equity` endpoint that reads the Parquet via DuckDB inside FastAPI and returns the curve as JSON `[{ts_utc, equity, drawdown}, ...]`. This endpoint is a small addition to Plan 04's surface — implement it as Task 1 of this plan to avoid blocking on Plan 04 revisions.
- Phase 7 may switch to a streaming Parquet response when performance demands it; Phase 3 sticks with JSON.

Note on stop_price / target_price columns:
- These columns are defined in the `trades` DDL in 03-01-PLAN.md (as nullable DOUBLE) and populated by BacktestEngine in 03-03-PLAN.md (sourced from Signal.stop and Signal.target). By the time Wave 5 runs, the columns already exist in schema.sql — no ALTER TABLE is needed here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET /backtests/{run_id}/equity + GET /backtests/{run_id}/trades endpoints + CORS middleware to FastAPI</name>
  <files>
    packages/api/src/api/routes/backtests.py,
    packages/api/src/api/app.py,
    packages/api/tests/test_routes.py
  </files>
  <read_first>
    packages/api/src/api/routes/backtests.py (Plan 04 output),
    packages/api/src/api/app.py (Plan 04 output),
    packages/api/tests/test_routes.py (Plan 04 output),
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (Equity Curve Parquet write — byte-stable flags reminder)
  </read_first>
  <behavior>
    GET /backtests/{run_id}/equity:
    - `GET /backtests/<run_id>/equity` returns HTTP 200 with `application/json` and a body `[{ts_utc, equity, drawdown}, ...]`. Order is ts_utc ASC.
    - The route queries `backtests` for the row matching `run_id`, reads `equity_curve_path` (relative to the repo root), and uses DuckDB's native Parquet reader to fetch the rows.
    - Path is resolved relative to the repo root. The resolved path must be inside `data/parquet/equity/` — reject any path that escapes the directory (path traversal guard).
    - Missing run_id returns HTTP 404 with body `{"detail":"backtest not found"}`.
    - Missing Parquet file returns HTTP 404 with body `{"detail":"equity curve not found"}` (the DB row exists but the file is gone).
    - Path traversal attempt (`equity_curve_path` outside `data/parquet/equity/`) returns HTTP 403 with `{"detail":"forbidden equity path"}`.

    GET /backtests/{run_id}/trades:
    - Returns HTTP 200 with a list of trade rows from the `trades` table for the given run_id, ordered by entry_ts_utc ASC.
    - Each row includes: trade_id, run_id, signal_id, strategy_id, side, entry_price, exit_price, exit_reason, entry_ts_utc, exit_ts_utc, pnl, size, slippage_ticks, mae, mfe, stop_price, target_price. TIMESTAMPTZ fields are returned as ISO 8601 strings; numeric fields as float/int.
    - stop_price and target_price are nullable (may be None for strategies that do not emit stop/target); the frontend must handle null values gracefully.
    - Missing run_id returns HTTP 404 with body `{"detail":"backtest not found"}`.

    CORS:
    - The FastAPI app installs `CORSMiddleware` with `allow_origins=["http://localhost:3000"]`, `allow_methods=["GET"]`, `allow_headers=["*"]`, `allow_credentials=False`. Inserted in app.py BEFORE `include_router` calls.
    - A request to `GET /bars` with `Origin: http://localhost:3000` returns the standard `Access-Control-Allow-Origin: http://localhost:3000` response header.
    - A request from a different origin (e.g., `Origin: http://evil.example.com`) does NOT receive the allow header.
  </behavior>
  <action>
    Edit `packages/api/src/api/routes/backtests.py`:
    - Add imports: `from pathlib import Path`; `from fastapi import HTTPException`.
    - Add a module-level constant `_EQUITY_ROOT = (Path(__file__).resolve().parents[4] / "data" / "parquet" / "equity").resolve()` — four parents up from `packages/api/src/api/routes/backtests.py` reaches the repo root. Verify with `assert _EQUITY_ROOT.name == "equity"` at module import time (raises early if the path math is wrong on a refactor).
    - Add a second route: `@router.get("/backtests/{run_id}/equity")` returning `list[dict]`. Parameters: `run_id: str` (path param — FastAPI auto-validates as str), `store: Annotated[DuckDBStore, Depends(get_store)]`.
    - Body steps:
        1. First query: `SELECT equity_curve_path FROM backtests WHERE run_id = ?` with `[run_id]`. If `fetchone() is None`: raise `HTTPException(status_code=404, detail="backtest not found")`.
        2. Resolve `equity_curve_path` against the repo root: `abs_path = (Path(__file__).resolve().parents[4] / equity_curve_path).resolve()`. Path-traversal guard via `try: abs_path.relative_to(_EQUITY_ROOT); except ValueError: raise HTTPException(status_code=403, detail="forbidden equity path")`.
        3. If `not abs_path.exists()`: raise `HTTPException(status_code=404, detail="equity curve not found")`.
        4. Second query: `SELECT ts_utc, "equity_$" AS equity, "drawdown_$" AS drawdown FROM read_parquet(?) ORDER BY ts_utc ASC` with `[str(abs_path)]`. Iterate rows; coerce ts_utc to ISO 8601, equity / drawdown to `float()`. Return the list.

    - Add a third route: `@router.get("/backtests/{run_id}/trades")` returning `list[dict]`. Parameters: `run_id: str` (path), `store: Annotated[DuckDBStore, Depends(get_store)]`. Body:
        1. Query `SELECT COUNT(*) FROM backtests WHERE run_id = ?` with `[run_id]`. If count == 0: raise `HTTPException(status_code=404, detail="backtest not found")`.
        2. Query `SELECT trade_id, run_id, signal_id, strategy_id, side, entry_price, exit_price, exit_reason, entry_ts_utc, exit_ts_utc, pnl, size, slippage_ticks, mae, mfe, stop_price, target_price FROM trades WHERE run_id = ? ORDER BY entry_ts_utc ASC` with `[run_id]`. Note: stop_price and target_price are already present in the trades DDL from 03-01-PLAN.md — no schema migration required.
        3. Iterate rows; coerce TIMESTAMPTZ fields to ISO 8601 strings, numeric fields to float/int. Return the list.

    Edit `packages/api/src/api/app.py`:
    - Add `from fastapi.middleware.cors import CORSMiddleware` import.
    - Inside the module body, BEFORE `app.include_router(...)` calls, insert:
        `app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:3000"], allow_methods=["GET"], allow_headers=["*"], allow_credentials=False)`.

    Edit `packages/api/tests/test_routes.py`:
    - Add a `TestEquityRoute` class with the 5 behavior cases above. Build a tmp DuckDB seeded with one backtests row + an actual byte-stable equity Parquet at `tmp_path/"data/parquet/equity/{run_id}.parquet"` (use `write_equity_parquet` from `trading_core.backtest.engine`). For the path-traversal case, manually update the backtests row's `equity_curve_path` to `"../../etc/passwd"` and assert HTTP 403.
    - Add a `TestCORS` test that issues `client.get("/bars?symbol=SPY&tf=1m&limit=1", headers={"Origin": "http://localhost:3000"})` and asserts `response.headers.get("access-control-allow-origin") == "http://localhost:3000"`.
    - Add a `TestCORSRejected` test from a non-allowed origin and assert the allow header is absent.
    - Add a `TestTradesRoute` class testing the `GET /backtests/{run_id}/trades` endpoint. Build a tmp DuckDB seeded with one backtests row + one trades row (via store.write_trades([...]) with stop_price and target_price populated). Assert the response is 200, body is a list with the expected fields (entry_ts_utc, entry_price, exit_price, exit_reason, side, stop_price, target_price). Assert that an unknown run_id returns 404. Assert stop_price/target_price are present as float or None.

    No changes to the test_routes helper `_make_test_app` other than ensuring CORS middleware is wired (it is automatically since the helper mirrors app.py setup).
  </action>
  <verify>
    <automated>uv run pytest packages/api/tests/test_routes.py -x -q -k "Equity or CORS or test_get_backtests or test_get_bars or Trades"</automated>
  </verify>
  <done>
- `GET /backtests/{run_id}/equity` returns the JSON equity curve from the run's Parquet; path traversal is blocked; CORS allows localhost:3000 only
- `GET /backtests/{run_id}/trades` returns trade rows with entry_ts_utc, entry_price, stop_price, target_price, side, exit_reason; missing run_id returns 404
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Next.js dashboard — hooks, store, chart components, /dashboard page</name>
  <files>
    apps/web/lib/api.ts,
    apps/web/store/ws.ts,
    apps/web/hooks/useBars.ts,
    apps/web/hooks/useBacktests.ts,
    apps/web/hooks/useStream.ts,
    apps/web/components/QueryProvider.tsx,
    apps/web/components/Chart.tsx,
    apps/web/components/EquityCurve.tsx,
    apps/web/components/ETClock.tsx,
    apps/web/components/ConnectionStatus.tsx,
    apps/web/components/DegradationBanner.tsx,
    apps/web/app/dashboard/page.tsx,
    apps/web/app/layout.tsx,
    apps/web/next.config.ts,
    apps/web/tsconfig.json
  </files>
  <read_first>
    apps/web/CLAUDE.md,
    apps/web/AGENTS.md,
    apps/web/node_modules/next/dist/docs/ (the App Router + Client Component + layout guides — APIs may differ from training data; read before writing),
    apps/web/app/layout.tsx,
    apps/web/app/page.tsx,
    apps/web/app/globals.css,
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/node_modules/lightweight-charts/dist/typings.d.ts (for the createChart / createSeriesMarkers / CandlestickSeries / LineSeries / IPriceLine surfaces — installed; the v5 API is verified in 03-RESEARCH.md),
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (lightweight-charts v5.2.0 Verified API + ET Clock + Connection Status sections + Native WebSocket Client Pattern + TanStack Query v5 Data Fetching Pattern),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (dashboard/page.tsx + Chart.tsx + EquityCurve.tsx + useWebSocket.ts + useBars.ts sections),
    .planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md (D-07, D-08, D-09)
  </read_first>
  <behavior>
    - `/dashboard` route loads, renders SPY 1m candlesticks fetched from `GET /bars`, and shows an ORB box overlay derived from the first opening-range window in the visible RTH session.
    - Below the candle pane (≈30% page height) renders an equity-curve line series populated from `GET /backtests/{run_id}/equity` (using the most-recent run).
    - Header shows: title, ET clock ticking each second (America/New_York), connection-status indicator (green/yellow/red), and a placeholder "Run Backtest" button (disabled — Phase 7 wires it).
    - Connection-status indicator color is computed from the Zustand store: green when connected AND last bar ≤ 10s ago; yellow when last bar > 10s; red when WS disconnected OR last bar > 30s.
    - When a `{type: 'degraded_state'}` envelope arrives via WS, a top banner renders with `Degraded: {source} — {reason}`; a "Dismiss" button clears it.
    - `pnpm tsc --noEmit` exits 0; `pnpm next build` succeeds with no TypeScript or build errors.
    - No `setMarkers` method is ever called on a series object — only `createSeriesMarkers(series, [])` plugin instances. Verified by a Python pathlib scan over `apps/web/` returning zero hits for `.setMarkers(` where the caller is a series (not a markersPlugin).
  </behavior>
  <action>
    Path alias:
    - tsconfig.json paths field maps `@/*` to `./*` so all the components/hooks/store imports use `@/components/Chart`, `@/hooks/useBars`, etc. If not already present, add `"baseUrl": "."` and `"paths": {"@/*": ["./*"]}` to the compilerOptions.

    apps/web/lib/api.ts:
    - Exports `const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? "http://localhost:8000"` and `const WS_BASE = process.env.NEXT_PUBLIC_WS_BASE ?? "ws://localhost:8000"`.
    - Exports a typed `BarRow` interface mirroring `GET /bars` row shape (ts_utc:string, open/high/low/close:number, volume:number, rollover_seam:boolean) and a `BacktestRow` interface mirroring `GET /backtests`.

    apps/web/store/ws.ts (Zustand):
    - State: `connected: boolean` (initial false), `lastBarAt: number | null` (initial null), `degraded: { source: string; reason: string } | null` (initial null).
    - Actions: `setConnected(v: boolean)`, `setLastBarAt(ts: number)`, `setDegraded(value)`, `clearDegraded()`.
    - Selector `selectStatusColor(state)` returning `'green' | 'yellow' | 'red'`:
        - `!state.connected` returns 'red'
        - `state.lastBarAt == null` returns 'red' (no bars yet — locked v1 behavior)
        - `Date.now() - state.lastBarAt > 30_000` returns 'red'
        - `Date.now() - state.lastBarAt > 10_000` returns 'yellow'
        - else returns 'green'
    - Memoize selector via zustand's `subscribeWithSelector` middleware or `useShallow` — pick whichever is idiomatic for zustand v5.

    apps/web/hooks/useBars.ts:
    - `'use client'` directive at top.
    - `export function useBars(symbol: string, tf: string, limit = 390)` returns `useQuery` with queryKey `['bars', symbol, tf, limit]` and queryFn that fetches `${API_BASE}/bars?symbol=${symbol}&tf=${tf}&limit=${limit}` and throws on `!res.ok`. staleTime 60_000.

    apps/web/hooks/useBacktests.ts:
    - Two hooks: `useBacktests()` (list) and `useEquityCurve(runId: string | null)` with `enabled: runId != null`. The list query hits `GET /backtests`; the equity hook hits `GET /backtests/{run_id}/equity`.

    apps/web/hooks/useStream.ts:
    - `'use client'`. `export function useStream()` builds `new WebSocket(`${WS_BASE}/stream`)` inside a useEffect with `[]` deps; handlers update the zustand store:
        - `ws.onopen` calls `setConnected(true)`
        - `ws.onclose` calls `setConnected(false)`
        - `ws.onerror` calls `setConnected(false)`
        - `ws.onmessage` parses `JSON.parse(event.data)` as `{type, payload}` and switches on `type`: `'bars'` calls `setLastBarAt(Date.now())`; `'degraded_state'` calls `setDegraded({source: payload.source, reason: payload.reason})`. Other types are ignored in Phase 3 (TODO comment: bars-cache update via queryClient.setQueryData lands in Phase 7).
    - Cleanup: `return () => ws.close()`.

    apps/web/components/QueryProvider.tsx:
    - `'use client'`. Exports a default component that wraps children in `<QueryClientProvider client={queryClient}>` where `queryClient` is constructed inside a `useState(() => new QueryClient(...))` to ensure a single client per browser session (documented Next.js + React Query v5 pattern).

    apps/web/components/Chart.tsx:
    - `'use client'`. Props: `{bars: BarRow[], orbHigh?: number, orbLow?: number, entry?: {ts_utc:string, price:number, side:'long'|'short'}, stop?: number, target?: number}`.
    - Inside useEffect: call `createChart(containerRef.current, {layout:{background:{color:'#000'}, textColor:'#d1d4dc'}, localization:{timeFormatter: <ET formatter>}, timeScale:{tickMarkFormatter: <ET formatter>, timeVisible:true, secondsVisible:false}})`.
    - Both formatters convert Unix seconds to America/New_York display using `Intl.DateTimeFormat({timeZone:'America/New_York', hour:'2-digit', minute:'2-digit', month:'short', day:'numeric'})` (timeFormatter) and `Intl.DateTimeFormat({timeZone:'America/New_York', hour:'2-digit', minute:'2-digit'})` (tickMarkFormatter).
    - `const candleSeries = chart.addSeries(CandlestickSeries, {upColor:'#26a69a', downColor:'#ef5350', borderVisible:false, wickUpColor:'#26a69a', wickDownColor:'#ef5350'})`.
    - Convert each BarRow to `{time: Math.floor(new Date(bar.ts_utc).getTime() / 1000), open, high, low, close}`. Call `candleSeries.setData(data)`.
    - ORB lines: when `orbHigh != null`, call `candleSeries.createPriceLine({price: orbHigh, color: '#ffeb3b', lineWidth: 1, lineStyle: 2 /* Dashed */, axisLabelVisible: true, title: 'ORB High'})`. Same for `orbLow`. Track both in a ref so they can be removed/updated on prop changes (re-run the effect with `[bars, orbHigh, orbLow, ...]` deps OR use a sub-effect for the lines).
    - Stop / target lines: similar createPriceLine with red (`#ef5350`) for stop and green (`#26a69a`) for target.
    - Entry marker: `import { createSeriesMarkers } from 'lightweight-charts'` and `const markersPlugin = createSeriesMarkers(candleSeries, [])`. When an entry exists, call `markersPlugin.setMarkers([{ time, position: side==='long' ? 'belowBar' : 'aboveBar', color: side==='long' ? '#26a69a' : '#ef5350', shape: side==='long' ? 'arrowUp' : 'arrowDown', text: 'Entry' }])`.
    - Cleanup: `return () => chart.remove()`.
    - NOTE D-09 superseded: series.setMarkers() was removed in v5.2.0 per RESEARCH.md Pitfall 2; createSeriesMarkers(series, markers) is the correct v5 API (named import from 'lightweight-charts'). Any use of candleSeries.setMarkers() is a compile-time and runtime error in v5.2.0.
    - CRITICAL: do NOT call `candleSeries.setMarkers(...)` anywhere. The v4 API is gone. Pitfall 2 in 03-RESEARCH.md.

    apps/web/components/EquityCurve.tsx:
    - `'use client'`. Props: `{points: Array<{ts_utc:string, equity:number, drawdown:number}>}`.
    - Standalone chart (own createChart call); uses LineSeries on pane 0 for equity_$. Optional second LineSeries for drawdown_$ in light red.
    - Same ET-aware time formatters.
    - Cleanup with chart.remove().

    apps/web/components/ETClock.tsx:
    - `'use client'`. Renders a `<span>` with current time in America/New_York, hh:mm:ss 24h. Uses `useEffect` + `setInterval(tick, 1000)` and `Intl.DateTimeFormat({timeZone:'America/New_York', hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false})`.

    apps/web/components/ConnectionStatus.tsx:
    - `'use client'`. Reads zustand store. Re-computes the color on a 1Hz timer (staleness depends on wall clock — `useEffect` + `setInterval(forceUpdate, 1000)` is the simplest way). Renders a colored circle + a tiny text label ("LIVE" / "STALE Ns" / "OFFLINE").

    apps/web/components/DegradationBanner.tsx:
    - `'use client'`. Reads `degraded` from the store. When non-null renders a yellow-orange top banner with `Degraded: {source} — {reason}` and a small "Dismiss" button calling `clearDegraded()`. Hidden when degraded is null.

    apps/web/app/dashboard/page.tsx:
    - `'use client'`. Uses `useStream()` to mount the WS subscription (no deps). Uses `useBars('SPY','1m',390)` for the chart data. Uses `useBacktests()` to find the most-recent run; takes its `run_id` and feeds `useEquityCurve(run_id)` to get the curve points.
    - ORB box: derive CLIENT-SIDE from the bars data — identify the first opening_range_minutes (15) 1m bars after 9:30 ET in the displayed window, compute high/low. Entry markers + stop/target priceLines: after loading the most-recent backtests row, fetch `GET /backtests/{run_id}/trades` via `useEquityTrades(run_id)` hook (add to useBacktests.ts). For each trade in the response, render: (a) entry marker via `createSeriesMarkers(candleSeries, [{time: Math.floor(new Date(t.entry_ts_utc).getTime()/1000), position: t.side==='long' ? 'belowBar' : 'aboveBar', color: t.side==='long' ? '#26a69a' : '#ef5350', shape: t.side==='long' ? 'arrowUp' : 'arrowDown', text: 'Entry'}])` — re-call `markersPlugin.setMarkers(allMarkers)` after building the array; (b) stop price line via `candleSeries.createPriceLine({price: t.stop_price, color: '#ef4444', lineWidth: 1, lineStyle: LineStyle.Dashed, axisLabelVisible: true, title: 'Stop'})` — only when `t.stop_price != null` (import `LineStyle` from 'lightweight-charts'); (c) target price line via `candleSeries.createPriceLine({price: t.target_price, color: '#26a69a', lineWidth: 1, lineStyle: LineStyle.Dashed, axisLabelVisible: true, title: 'Target'})` — only when `t.target_price != null`. Store all priceLine refs in a ref array and call `candleSeries.removePriceLine(line)` on each during useEffect cleanup or on trades-prop change.
    - Layout (D-08): top-level `<div style={{display:'flex', flexDirection:'column', height:'100vh', backgroundColor:'#000', color:'#d1d4dc'}}>` containing:
        - `<DegradationBanner />` (renders nothing when degraded is null — layout unaffected)
        - `<header style={{height:'48px', display:'flex', alignItems:'center', gap:'16px', padding:'0 16px', borderBottom:'1px solid #222'}}>` with title + ETClock + ConnectionStatus + a disabled placeholder "Run Backtest" button
        - `<div style={{flex:'0 0 70%'}}><Chart bars={bars ?? []} orbHigh={...} orbLow={...} /></div>`
        - `<div style={{flex:'0 0 30%'}}><EquityCurve points={equity ?? []} /></div>`
    - Tailwind class `font-mono` already applied at the layout body — keep.

    apps/web/app/layout.tsx:
    - Wrap `{children}` in `<QueryProvider>{children}</QueryProvider>` so any client component under the dashboard can use `useQuery`.

    apps/web/next.config.ts:
    - No changes expected; the v5 library is plain ESM. Document any required experimental flag in SUMMARY.md if you discover one.

    apps/web/tsconfig.json:
    - Confirm/add `"baseUrl": "."` and `"paths": {"@/*": ["./*"]}` so the `@/components/...` imports work.

    AGENTS.md compliance:
    - Before writing any of the above, the executor MUST read `apps/web/node_modules/next/dist/docs/` for the current App Router + Client Component conventions. Document in 03-05-SUMMARY.md which Next.js docs were consulted (file paths) and any deviation from training-data assumptions.
  </action>
  <verify>
    <automated>pnpm --filter web tsc --noEmit && pnpm --filter web build && uv run python -c "import sys; from pathlib import Path; hits=[str(f) for f in Path('apps/web/components').rglob('*.tsx') if '.setMarkers(' in f.read_text()]; sys.exit(0) if not hits else (print('FAIL: setMarkers found in series context:', hits) or sys.exit(1))"</automated>
  </verify>
  <done>
    `pnpm tsc --noEmit` exits 0. `pnpm next build` succeeds (no TypeScript or build errors). All new component / hook / store files are created. The `/dashboard` route resolves and SSR-prerenders the shell (the chart canvas renders client-side after hydration). No `setMarkers` calls on series objects. No console errors during `pnpm next build`. `GET /backtests/{run_id}/trades` returns trade rows with entry_ts_utc, entry_price, stop_price, target_price, side, exit_reason fields. Entry arrow + stop line + target line render on the chart when trades data is available.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + Functional Verification of /dashboard (UI-04 + UI-08)</name>
  <files>
    apps/web/app/dashboard/page.tsx,
    apps/web/components/Chart.tsx,
    apps/web/components/EquityCurve.tsx,
    apps/web/components/ETClock.tsx,
    apps/web/components/ConnectionStatus.tsx,
    apps/web/components/DegradationBanner.tsx
  </files>
  <read_first>
    .planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md (D-07, D-08, D-09 for the expected visual contract)
  </read_first>
  <action>
    This is a manual human-verify checkpoint. The executor MUST NOT proceed past this task without operator approval. Steps below are operator-driven; the executor's job is to spin up the three processes and present the URL.

    1. From the repo root, in one terminal, seed bars and run a backtest:
       - `uv run python scripts/seed_bars.py --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03`
       - `uv run python scripts/run_backtest.py --strategy orb --symbol SPY --tf 1m --from 2024-01-02 --to 2024-01-03 --config config/strategies/orb.yaml --seed 42`
       Confirm both exit 0 and the second prints a JSON line with `"status":"ok"`.
    2. In a second terminal, start the FastAPI backend: `uv run uvicorn api:app --host 127.0.0.1 --port 8000`. Confirm uvicorn logs "Application startup complete".
    3. In a third terminal, start the Next.js dev server: `cd apps/web && pnpm dev`. Confirm Next prints "Ready - started server on 0.0.0.0:3000".
    4. Open http://localhost:3000/dashboard in a desktop browser (Chrome/Edge/Firefox — NOT mobile).
    5. Operator visually verifies each of the following:
       - Chart loads with SPY 1m candlesticks (about 390 bars for the 2024-01-02 RTH session). Candles are colored green up / red down on a black background.
       - X-axis time labels show ET hours (09:30, 10:00, ..., 15:30). The crosshair label also reads in ET when hovered.
       - An ORB high + ORB low horizontal dashed-yellow line appears at the price of the first 15 1m bars' high/low (derived client-side).
       - Below the candle pane (about 30% of the page height) is the equity curve: a single blue line moving over time.
       - Header shows: a title; an ET clock that ticks every 1 second in 24h format (e.g. 14:32:05); a colored circle (green/yellow/red) labeled "LIVE" / "STALE Ns" / "OFFLINE".
       - The connection-status indicator starts green within about 1s of page load (WS connected to ws://localhost:8000/stream).
    6. Operator stops the FastAPI backend (Ctrl-C uvicorn). Within 30 seconds, the indicator transitions to red and reads "OFFLINE".
    7. Operator restarts uvicorn. Within a few seconds the indicator returns to green.
    8. Degradation banner: With both servers running, publish a synthetic degraded_state event via the EventBus. Easiest path: open a Python REPL with the bus in scope and call `await bus.publish(TOPIC_DEGRADED_STATE, DegradedStateEvent(...))` — or run a one-shot script `scripts/dev/emit_degraded.py` (executor may create this as a helper). The banner appears at the top of the page with the message and a Dismiss button. Dismissing clears the banner.
    9. Operator confirms no browser console errors (red entries in DevTools Console) during steps 5-8. Yellow warnings are acceptable; surface any unexpected red errors in SUMMARY.md.
  </action>
  <verify>
    <automated>echo "manual-verify checkpoint — executor presents URL http://localhost:3000/dashboard and waits for operator approval"</automated>
  </verify>
  <done>
    Operator types "approved" after confirming all 9 visual checks pass. If any check fails, the executor opens a follow-up task in the next plan revision (or fixes within this task if the gap is trivial) and the checkpoint is re-run. The phase cannot exit until this checkpoint is approved.
  </done>
  <resume-signal>
    Type "approved" if all visual checks pass; otherwise describe what is broken (which step failed and the actual vs expected behavior). Optionally attach a screenshot path.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to FastAPI HTTP | unauthenticated localhost cross-origin (CORS) |
| Browser to FastAPI WebSocket | unauthenticated localhost stream |
| FastAPI /backtests/{run_id}/equity to filesystem | run_id is path-controlled by URL; raw equity_curve_path comes from DB row |
| Browser-side Chart.tsx to external strings | bar JSON could contain unexpected fields (defense by Pydantic on server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering (V5) | GET /backtests/{run_id}/equity path traversal | mitigate | resolve(equity_curve_path) and require resolved path is_relative_to(_EQUITY_ROOT); reject with HTTP 403 otherwise. Test_routes covers the traversal case. |
| T-03-05-02 | Tampering | Browser CORS bypass via reflected origin | mitigate | CORSMiddleware uses explicit `allow_origins=["http://localhost:3000"]`, NOT `["*"]` and not regex-reflected. allow_credentials=False so cookies cannot be exfiltrated. Test asserts non-localhost origins do not get the allow header. |
| T-03-05-03 | DoS | WS reconnect storm (browser repeatedly hits a dead server) | accept | Phase 3 has no exponential backoff (Phase 7 SP-06 adds it); single operator, single tab — acceptable. Document in SUMMARY.md. |
| T-03-05-04 | Information Disclosure | DegradationBanner reveals internal infra (source field e.g. "tradingview_mcp") | accept | Single-operator localhost — no external attacker model. |
| T-03-05-05 | Tampering | Browser-side Chart.tsx parses BarRow timestamps in browser TZ | mitigate | `new Date(bar.ts_utc).getTime()` returns a Unix-ms agnostic to browser TZ; the display TZ is forced to America/New_York via Intl.DateTimeFormat in both timeFormatter and tickMarkFormatter. Manual checkpoint validates the TZ display. |
| T-03-05-06 | Tampering | series.setMarkers() v4 API used by mistake | mitigate | Mandatory read of `apps/web/AGENTS.md` warning + 03-RESEARCH.md Pitfall 2 before coding; the createSeriesMarkers import is explicit and visible in code review. |
</threat_model>

<verification>
- API: `uv run pytest packages/api/tests/test_routes.py -x -q` shows all (Plan 04 cases + new equity-route + trades-route + CORS cases) pass
- Frontend type-check: `pnpm --filter web tsc --noEmit` exits 0
- Frontend build: `pnpm --filter web build` succeeds
- Manual checkpoint (Task 3): all visual / functional bullets approved
- Regression: `uv run pytest -x -q` shows 280+ tests pass
</verification>

<success_criteria>
- `GET /backtests/{run_id}/equity` returns the JSON equity curve from the run's Parquet; path traversal is blocked; CORS allows localhost:3000 only
- `GET /backtests/{run_id}/trades` returns trade rows with entry_ts_utc, entry_price, stop_price, target_price, side, exit_reason; missing run_id returns 404
- `/dashboard` renders SPY candlesticks with ET timezone display
- ORB box overlay (yellow dashed priceLines) appears on the chart
- Equity-curve pane renders below the chart (30% height)
- Header shows the ET clock, the WS connection status (green/yellow/red), and the degradation banner appears when a `degraded_state` event is received
- All chart components import `createSeriesMarkers` (v5 API); no `series.setMarkers` calls exist (verified by Python pathlib scan over apps/web/components/ returning zero hits)
- `pnpm --filter web tsc --noEmit` + `pnpm --filter web build` succeed
- Manual checkpoint (Task 3) is approved by the operator
- All existing 280+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-vertical-mvp-slice-backtester/03-05-SUMMARY.md` documenting:
- Which Next.js docs files were consulted (apps/web/node_modules/next/dist/docs/...) and any API differences from training data
- Confirm entry/stop/target markers render correctly for the most recent backtest's trades (entry arrow + stop priceLine + target priceLine per BLOCKER 5 requirement — SC#5 of ROADMAP Phase 3)
- The actual color/timing values used for the connection-status indicator (the spec is green ≤10s / yellow >10s / red >30s OR disconnected)
- Whether `series.setMarkers` was caught by any automated check (it should not appear anywhere in the apps/web/components/ tree)
</output>
