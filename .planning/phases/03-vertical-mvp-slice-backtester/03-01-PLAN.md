---
phase: 03-vertical-mvp-slice-backtester
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/trading-core/src/trading_core/risk/models.py
  - packages/trading-core/src/trading_core/execution/models.py
  - packages/trading-core/src/trading_core/storage/schema.sql
  - packages/trading-core/src/trading_core/storage/duckdb_store.py
  - .pre-commit-config.yaml
  - scripts/hooks/no_direct_vbt.py
  - packages/trading-core/tests/test_models_phase3_fields.py
  - packages/trading-core/tests/test_duckdb_backtests_trades.py
  - packages/trading-core/tests/test_safe_signals.py
  - packages/trading-core/tests/test_backtest_engine.py
  - packages/trading-core/tests/test_paper_executor.py
  - packages/trading-core/tests/integration/test_lookahead.py
  - packages/trading-core/tests/integration/test_reproducibility.py
  - packages/api/tests/test_routes.py
  - packages/api/tests/test_ws_stream.py
autonomous: true
requirements:
  - BT-06
tags:
  - foundation
  - models
  - duckdb
  - pre-commit
  - wave-0
must_haves:
  truths:
    - "Fill, RiskDecision, RiskState, RiskConfig carry exactly the D-10 minimal fields"
    - "DuckDB has backtests + trades tables with the D-01 / D-02 columns"
    - "DuckDBStore exposes write_backtest() + write_trades() methods"
    - "Pre-commit blocks any direct vbt.Portfolio.from_signals() call outside safe_signals.py"
    - "Wave 0 test files exist (empty/xfail stubs) so executors can fill them in subsequent plans"
  artifacts:
    - path: "packages/trading-core/src/trading_core/risk/models.py"
      provides: "RiskDecision/RiskState/RiskConfig with D-10 fields"
      contains: "class RiskDecision"
    - path: "packages/trading-core/src/trading_core/execution/models.py"
      provides: "Fill with D-10 fields"
      contains: "class Fill"
    - path: "packages/trading-core/src/trading_core/storage/schema.sql"
      provides: "backtests + trades DDL"
      contains: "CREATE TABLE IF NOT EXISTS backtests"
    - path: "packages/trading-core/src/trading_core/storage/duckdb_store.py"
      provides: "write_backtest + write_trades methods"
      contains: "def write_backtest"
    - path: "scripts/hooks/no_direct_vbt.py"
      provides: "Pre-commit guard against direct vbt.Portfolio.from_signals"
      contains: "vbt.Portfolio.from_signals"
    - path: ".pre-commit-config.yaml"
      provides: "no-direct-vbt-from-signals hook registered"
      contains: "no-direct-vbt-from-signals"
  key_links:
    - from: "Fill.exit_reason"
      to: "schema.sql trades.exit_reason"
      via: "Literal[target,stop,eod_flat,manual] mirrored in CHECK constraint or doc"
      pattern: "exit_reason.*target.*stop.*eod_flat.*manual"
    - from: "DuckDBStore.write_trades"
      to: "trades table"
      via: "executemany(WRITE_TRADE_SQL, rows)"
      pattern: "executemany.*WRITE_TRADE_SQL"
    - from: ".pre-commit-config.yaml no-direct-vbt-from-signals hook"
      to: "scripts/hooks/no_direct_vbt.py"
      via: "entry: python scripts/hooks/no_direct_vbt.py"
      pattern: "no_direct_vbt\\.py"
---

<objective>
Lay the Wave 1 foundation for the Phase 3 backtester: fill in the D-10 minimal fields on the existing Pydantic stubs, extend the DuckDB schema with the `backtests` + `trades` tables and writer methods, install the `no-direct-vbt-from-signals` pre-commit hook, and create empty Wave 0 test files so downstream plans inherit a clean test surface.

Purpose: Every other Wave depends on these data-shape decisions. Locking models + schema in Wave 1 eliminates churn in Waves 2-5 (no "we have to re-migrate the trades table" surprises). The pre-commit hook is installed here, BEFORE `safe_from_signals` lands in Plan 02, so the guard is active the instant the wrapper exists.

Output:
- Phase-3-minimal D-10 fields on `Fill`, `RiskDecision`, `RiskState`, `RiskConfig`
- DuckDB `backtests` + `trades` tables (D-01, D-02) with `write_backtest()` + `write_trades()` writers
- `no-direct-vbt-from-signals` hook + `scripts/hooks/no_direct_vbt.py` Python script
- Wave 0 empty test files at every path listed in `03-VALIDATION.md` so subsequent plans can append tests
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-VALIDATION.md
@.planning/phases/02-strategy-engine/02-01-SUMMARY.md
@packages/trading-core/src/trading_core/risk/models.py
@packages/trading-core/src/trading_core/execution/models.py
@packages/trading-core/src/trading_core/strategy/models.py
@packages/trading-core/src/trading_core/storage/schema.sql
@packages/trading-core/src/trading_core/storage/duckdb_store.py
@.pre-commit-config.yaml
@scripts/hooks/no_naive_tz.py

<interfaces>
<!-- Key contracts the executor needs. Lifted verbatim from existing files. -->

From packages/trading-core/src/trading_core/strategy/models.py:
- Signal (frozen, extra="forbid"): strategy_id:str, strategy_version:str, ts_utc:AwareDatetime, side:Literal["long","short"], entry:Decimal(gt=0), stop:Decimal(gt=0), target:Decimal(gt=0), size_hint:Decimal(gt=0), signal_id:str (uuid4)
- StrategyContext (frozen, extra="forbid"): rollover_seam:bool, warmup_complete:bool, bar_index:int, ts_utc:AwareDatetime, atr/session_vwap/ema/adr:Decimal|None
- must_be_utc validator: rejects offset != 0

From packages/trading-core/src/trading_core/risk/models.py (current stub):
- class RiskConfig(BaseModel) — empty stub, ConfigDict(extra="forbid")
- class RiskState(BaseModel) — empty stub, ConfigDict(extra="forbid")
- class RiskDecision(BaseModel) — empty stub, ConfigDict(extra="forbid")

From packages/trading-core/src/trading_core/execution/models.py (current stub):
- class Fill(BaseModel) — empty stub, ConfigDict(extra="forbid")

From packages/trading-core/src/trading_core/storage/duckdb_store.py:
- UPSERT_BAR_SQL / UPSERT_GAP_SQL / WRITE_RUN_SQL — module-level SQL constants
- class DuckDBStore: __init__(db_path), ensure_schema(), upsert_bars(df, provider), upsert_gaps(...), write_run(...), write_parquet_partition(...), close(), __enter__/__exit__
- Schema loaded verbatim from `schema.sql` next to the module

From .pre-commit-config.yaml (current):
- repos: gitleaks (v8.24.2) + local "no-naive-tz" hook (python scripts/hooks/no_naive_tz.py)
- exclude pattern uses `(?x)^( ... )$` form

From scripts/hooks/no_naive_tz.py:
- CLI shape: `python scripts/hooks/no_naive_tz.py <file1> <file2> ...`
- Exits 0 if clean, exits 1 (and prints `<path>:<lineno>: <message>`) on violation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill D-10 minimal fields on Fill / RiskDecision / RiskState / RiskConfig</name>
  <files>
    packages/trading-core/src/trading_core/risk/models.py,
    packages/trading-core/src/trading_core/execution/models.py,
    packages/trading-core/tests/test_models_phase3_fields.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/risk/models.py,
    packages/trading-core/src/trading_core/execution/models.py,
    packages/trading-core/src/trading_core/strategy/models.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md (D-10 + D-11),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (execution/models.py + risk/models.py sections)
  </read_first>
  <behavior>
    - Construct Fill(signal_id="s1", fill_price=Decimal("471.50"), fill_qty=1, side="long", slippage_ticks=1, ts_utc=datetime(2024,1,2,14,30,tzinfo=UTC), exit_reason="target") succeeds.
    - Fill with side="diagonal" raises pydantic.ValidationError (Literal check).
    - Fill with exit_reason="liquidated" raises ValidationError (Literal four-value check).
    - Fill with ts_utc naive datetime raises ValidationError ("must be tz-aware" / AwareDatetime).
    - Fill with ts_utc in America/New_York offset raises ValidationError (must_be_utc validator).
    - Fill with fill_price=Decimal("0") raises ValidationError (gt=0).
    - Fill with fill_qty=0 raises ValidationError (gt=0).
    - Fill is frozen — assigning to .fill_price raises pydantic.ValidationError or FrozenInstanceError.
    - Fill rejects extra fields (extra="forbid").
    - RiskDecision(approved=True, reason="pass_through", adjusted_size=1) succeeds; adjusted_size=-1 currently allowed (no gt constraint per D-10 spec — confirm test does NOT assert that).
    - RiskState() defaults realized_pnl_today to Decimal("0").
    - RiskConfig() defaults max_contracts to 1.
    - RiskConfig(max_contracts=2) succeeds.
  </behavior>
  <action>
    Extend the existing stubs in risk/models.py and execution/models.py with exactly the D-10 / D-11 fields and nothing more. Do NOT introduce Phase 5 fields (HWM, daily_dd_limit, account_equity, commission_$).

    In `packages/trading-core/src/trading_core/execution/models.py`:
    - Add imports: `from decimal import Decimal`, `from typing import Literal`, `from pydantic import AwareDatetime, Field, field_validator` (extend existing BaseModel + ConfigDict import).
    - Update class Fill to include fields, in this declaration order: signal_id:str; fill_price:Decimal=Field(gt=Decimal("0")); fill_qty:int=Field(gt=0); side:Literal["long","short"]; slippage_ticks:int=Field(ge=0); ts_utc:AwareDatetime; exit_reason:Literal["target","stop","eod_flat","manual"].
    - Set model_config = ConfigDict(frozen=True, extra="forbid").
    - Add the must_be_utc field_validator copied from strategy/models.py Signal (same body — reject offset is None or offset.total_seconds() != 0).
    - Update the module docstring to reference D-10 and D-11 and remove the "Phase 5 fills in" wording for these specific fields.

    In `packages/trading-core/src/trading_core/risk/models.py`:
    - Add imports: `from decimal import Decimal` (extend existing pydantic import).
    - Update class RiskConfig: `max_contracts: int = 1` (D-10). Keep ConfigDict(extra="forbid") — do NOT make this frozen so Phase 5 can extend cleanly.
    - Update class RiskState: `realized_pnl_today: Decimal = Decimal("0")` (D-10). Keep ConfigDict(extra="forbid").
    - Update class RiskDecision: required fields `approved:bool`, `reason:str`, `adjusted_size:int` (no defaults — caller MUST supply). Keep ConfigDict(extra="forbid").
    - Update the module docstring to mark these as D-10 minimal fields and reference Phase 5 for the rest.

    Create `packages/trading-core/tests/test_models_phase3_fields.py` covering every behavior bullet above. Use direct `import datetime` for the UTC datetime fixture; do NOT pull in the orb_day_bars fixture. Each test is a top-level function (no class needed — match the test_strategy_models.py style established in Phase 2).
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_models_phase3_fields.py -x -q</automated>
  </verify>
  <done>
    All Fill / RiskDecision / RiskState / RiskConfig field tests pass. `from trading_core.execution.models import Fill` and `from trading_core.risk.models import RiskDecision, RiskState, RiskConfig` succeed with the D-10 fields. Existing Phase 1/2 tests still pass (`uv run pytest -x -q` green).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend schema.sql + DuckDBStore with backtests / trades tables and writers</name>
  <files>
    packages/trading-core/src/trading_core/storage/schema.sql,
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    packages/trading-core/tests/test_duckdb_backtests_trades.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/storage/schema.sql,
    packages/trading-core/src/trading_core/storage/duckdb_store.py,
    packages/trading-core/tests/test_duckdb_store.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md (D-01, D-02),
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (DuckDB Schema Extension section),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (DuckDBStore section)
  </read_first>
  <behavior>
    - After DuckDBStore(tmp_db).ensure_schema(), tables `backtests` and `trades` exist (query `information_schema.tables` or `SELECT * FROM duckdb_tables()`).
    - write_backtest with all 20 fields (run_id, strategy_id, symbol, timeframe, from_ts, to_ts, param_hash, equity_curve_path, total_return, cagr, sharpe, sortino, calmar, max_dd, max_dd_duration_bars, win_rate, expectancy, profit_factor, trade_count, avg_hold_bars) inserts a single row; `SELECT * FROM backtests WHERE run_id=?` returns it.
    - write_trades with a list of two trade dicts (with all 17 D-02 fields, including stop_price and target_price) inserts two rows; `SELECT COUNT(*) FROM trades WHERE run_id=?` returns 2.
    - Calling write_trades with an empty list is a no-op (zero rows inserted).
    - A trade row's exit_reason value is one of `target|stop|eod_flat|manual` (D-11) — pass `target` in the test.
    - Re-running ensure_schema on an already-populated DB does NOT drop data (idempotent).
    - SQL injection attempt: passing a malicious strategy_id like `"foo'; DROP TABLE backtests; --"` into write_backtest does NOT drop the table (parameterized queries — Threat V5 mitigation).
  </behavior>
  <action>
    Append the D-01 / D-02 DDL to `packages/trading-core/src/trading_core/storage/schema.sql` exactly as specified in 03-RESEARCH.md §"DuckDB Schema Extension":

    - `CREATE TABLE IF NOT EXISTS backtests (...)` with PRIMARY KEY (run_id). Columns: run_id VARCHAR NOT NULL, strategy_id VARCHAR NOT NULL, symbol VARCHAR NOT NULL, timeframe VARCHAR NOT NULL, from_ts TIMESTAMPTZ NOT NULL, to_ts TIMESTAMPTZ NOT NULL, param_hash VARCHAR NOT NULL, equity_curve_path VARCHAR NOT NULL, total_return DOUBLE, cagr DOUBLE, sharpe DOUBLE, sortino DOUBLE, calmar DOUBLE, max_dd DOUBLE, max_dd_duration_bars BIGINT, win_rate DOUBLE, expectancy DOUBLE, profit_factor DOUBLE, trade_count INTEGER, avg_hold_bars DOUBLE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP.
    - `CREATE TABLE IF NOT EXISTS trades (...)` with PRIMARY KEY (trade_id). Columns: trade_id VARCHAR NOT NULL, run_id VARCHAR NOT NULL, signal_id VARCHAR NOT NULL, strategy_id VARCHAR NOT NULL, side VARCHAR NOT NULL, entry_price DOUBLE NOT NULL, exit_price DOUBLE NOT NULL, exit_reason VARCHAR NOT NULL, entry_ts_utc TIMESTAMPTZ NOT NULL, exit_ts_utc TIMESTAMPTZ NOT NULL, pnl DOUBLE NOT NULL, size INTEGER NOT NULL, slippage_ticks INTEGER NOT NULL, mae DOUBLE NOT NULL, mfe DOUBLE NOT NULL, stop_price DOUBLE, target_price DOUBLE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP.
      Note: stop_price and target_price are nullable (DOUBLE with no NOT NULL constraint) because they are sourced from Signal.stop and Signal.target (Decimal fields on ORBStrategy-emitted signals) — non-ORB strategies that do not emit stop/target can write NULL here.

    In `packages/trading-core/src/trading_core/storage/duckdb_store.py`:
    - Add module-level constants WRITE_BACKTEST_SQL and WRITE_TRADE_SQL using `?` placeholders for every column (no string interpolation). Order of placeholders MUST match the column order above.
    - Add method `write_backtest(self, *, run_id, strategy_id, symbol, timeframe, from_ts, to_ts, param_hash, equity_curve_path, total_return, cagr, sharpe, sortino, calmar, max_dd, max_dd_duration_bars, win_rate, expectancy, profit_factor, trade_count, avg_hold_bars) -> None`. All keyword-only. Call self._conn.execute(WRITE_BACKTEST_SQL, [run_id, ...]) with the 20 positional values in column order. Mirror the docstring style used for write_run.
    - Add method `write_trades(self, trades: list[dict]) -> int`. Return early `return 0` when `len(trades) == 0`. Build a list of 17-tuple rows from each trade dict (the 15 base D-02 fields plus stop_price and target_price); coerce numerics to `float()` / `int()` to match the DOUBLE / INTEGER column types (mirror the upsert_bars pattern at lines 122-141); use `trade.get("stop_price")` and `trade.get("target_price")` (None for strategies that do not emit these). Use `self._conn.executemany(WRITE_TRADE_SQL, rows)`. Return `len(rows)`.
    - Do NOT use ON CONFLICT — backtests.run_id and trades.trade_id are uuid7 and unique per run; plain INSERT is correct per 03-RESEARCH.md DuckDB schema note.

    Create `packages/trading-core/tests/test_duckdb_backtests_trades.py` with the behavior bullets above. Use `tmp_path / "test.duckdb"` (the pattern from test_duckdb_store.py). Use a fixed uuid string (`"01923456-...uuid7..."`) so the test is deterministic. For the SQL injection assertion, after the (rejected) attempt the table must still exist and the prior valid row must still be queryable.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_duckdb_backtests_trades.py -x -q</automated>
  </verify>
  <done>
    `from trading_core.storage.duckdb_store import DuckDBStore` exposes `write_backtest` and `write_trades`. After `ensure_schema`, the `backtests` and `trades` tables exist with all D-01 / D-02 columns including stop_price and target_price. SQL injection via parameter does not corrupt the table. Existing test_duckdb_store.py tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add no-direct-vbt-from-signals pre-commit hook + create Wave 0 empty test files</name>
  <files>
    scripts/hooks/no_direct_vbt.py,
    .pre-commit-config.yaml,
    packages/trading-core/tests/test_safe_signals.py,
    packages/trading-core/tests/test_backtest_engine.py,
    packages/trading-core/tests/test_paper_executor.py,
    packages/trading-core/tests/integration/test_lookahead.py,
    packages/trading-core/tests/integration/test_reproducibility.py,
    packages/api/tests/test_routes.py,
    packages/api/tests/test_ws_stream.py
  </files>
  <read_first>
    .pre-commit-config.yaml,
    scripts/hooks/no_naive_tz.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-VALIDATION.md (Wave 0 Requirements section),
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (Pre-commit Hook Pattern section)
  </read_first>
  <behavior>
    - `python scripts/hooks/no_direct_vbt.py path/with/violation.py` exits 1 and prints `path/with/violation.py:N: direct vbt.Portfolio.from_signals() call blocked. Use safe_from_signals() instead.` (write a tiny tmp_path fixture file with the literal line `vbt.Portfolio.from_signals(` to exercise this).
    - `python scripts/hooks/no_direct_vbt.py path/clean.py` (no matching pattern) exits 0 and prints nothing.
    - `pre-commit run no-direct-vbt-from-signals --all-files` (after `uv run pre-commit install`) exits 0 on the current repo (no violations exist yet; safe_from_signals.py lands in Plan 02).
    - The hook does NOT scan `packages/trading-core/src/trading_core/backtest/safe_signals.py` (the exclude pattern covers it preemptively so when Plan 02 lands the file the wrapper itself does not trip the guard).
    - Wave 0 test files exist as valid Python (importable, zero collection errors); each contains exactly one xfail/skip placeholder with a clear reason so the file is non-empty.
  </behavior>
  <action>
    Create `scripts/hooks/no_direct_vbt.py` (UTF-8, LF). Structure mirrors `scripts/hooks/no_naive_tz.py` but uses regex (not AST — the call shape is unambiguous and a literal docstring containing this exact phrase is not a real risk):
    - Module docstring explains purpose (D-13 enforcement; safe_signals.py is the only legitimate call site).
    - `PATTERN = re.compile(r"vbt\\.Portfolio\\.from_signals\\s*\\(")`.
    - Iterate `sys.argv[1:]`, open each path with `encoding="utf-8", errors="replace"`, enumerate lines from 1. For every match append `f"{path}:{lineno}: direct vbt.Portfolio.from_signals() call blocked. Use safe_from_signals() instead."` to an errors list.
    - On non-empty errors: `print("\\n".join(errors))` then `sys.exit(1)`. Otherwise `sys.exit(0)`.
    - Skip unreadable files silently (mirror the OSError handling in no_naive_tz.py).

    Edit `.pre-commit-config.yaml`: append a new local hook under the existing `- repo: local` block (alongside no-naive-tz):
      - id: no-direct-vbt-from-signals
      - name: Block direct vbt.Portfolio.from_signals() calls (D-13)
      - entry: python scripts/hooks/no_direct_vbt.py
      - language: python
      - types: [python]
      - require_serial: true
      - exclude: |
          (?x)^(
            packages/trading-core/src/trading_core/backtest/safe_signals\.py|
            scripts/hooks/no_direct_vbt\.py|
            packages/trading-core/tests/test_safe_signals\.py|
            packages/trading-core/tests/integration/test_lookahead\.py
          )$

      Rationale: safe_signals.py is the only legitimate call site; the hook script itself contains the pattern as a regex; the two test files reference the wrapper API but may emit the literal phrase in error-message assertions.

    Create the following Wave 0 test files, each containing a module docstring referencing the requirement IDs and the file path of the analog test, plus exactly one xfail placeholder so the file imports without error:
    - `packages/trading-core/tests/test_safe_signals.py` — covers BT-02; xfail "Wave 2 Plan 02 implements safe_from_signals".
    - `packages/trading-core/tests/test_backtest_engine.py` — covers BT-01/BT-04/BT-05/BT-06; xfail "Wave 3 Plan 03 implements BacktestEngine".
    - `packages/trading-core/tests/test_paper_executor.py` — covers BT-03/BT-08; xfail "Wave 2 Plan 02 implements PaperExecutor".
    - `packages/trading-core/tests/integration/test_lookahead.py` — covers BT-07 (BL-1 gate, D-14); xfail "Wave 3 Plan 03 wires safe_from_signals + leaking entry fixture".
    - `packages/trading-core/tests/integration/test_reproducibility.py` — covers BT-09 + FND-08; xfail "Wave 3 Plan 03 implements run_backtest CLI + byte-stable Parquet".
    - `packages/api/tests/test_routes.py` — covers UI-01; xfail "Wave 4 Plan 04 implements /bars + /backtests routes".
    - `packages/api/tests/test_ws_stream.py` — covers SP-01 / D-04 / D-05; xfail "Wave 4 Plan 04 implements WS /stream".

    Each xfail placeholder is the pattern:
    ```
    import pytest

    @pytest.mark.xfail(reason="Wave N Plan NN — not yet implemented", strict=True)
    def test_placeholder_until_wave_N():
        raise NotImplementedError
    ```
    Note: do NOT embed the literal pattern `vbt.Portfolio.from_signals(` anywhere in these stubs (it would trip the new hook). Reference it only via a comment using `safe_from_signals` or split tokens.

    For the `packages/trading-core/tests/integration/` directory: if a sibling `__init__.py` is absent, do NOT add one — Phase 1 Plan 01-01 locked `--import-mode=importlib` with no tests/__init__.py.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_safe_signals.py packages/trading-core/tests/test_backtest_engine.py packages/trading-core/tests/test_paper_executor.py packages/trading-core/tests/integration/test_lookahead.py packages/trading-core/tests/integration/test_reproducibility.py packages/api/tests/test_routes.py packages/api/tests/test_ws_stream.py --collect-only -q</automated>
  </verify>
  <done>
    `python scripts/hooks/no_direct_vbt.py` rejects a tmp file containing `vbt.Portfolio.from_signals(`. `uv run pre-commit run no-direct-vbt-from-signals --all-files` exits 0 on the current repo. All 7 Wave 0 test files are collected without error (each has 1 xfailed placeholder). The current 244-test suite still passes (`uv run pytest -x -q` green; xfail placeholders count as xfailed, not failed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI / API args → DuckDBStore | untrusted user input crosses into the database |
| Pre-commit hook → source files | hook reads source bytes from the working tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | DuckDBStore.write_backtest / write_trades | mitigate | All inserts use `?` placeholder parameterized queries (executemany / execute); zero string interpolation of caller-supplied values. Validated by SQL injection test in Task 2 behavior bullets. |
| T-03-01-02 | Tampering | Fill / RiskDecision / RiskState / RiskConfig models | mitigate | Pydantic v2 frozen + extra="forbid" rejects extra fields; Literal/AwareDatetime validators reject malformed exit_reason / side / ts_utc. Test_models_phase3_fields.py covers each rejection path. |
| T-03-01-03 | Tampering | Pre-commit hook bypass (developer commits direct vbt call) | mitigate | `no-direct-vbt-from-signals` hook fails the commit; CI re-runs `pre-commit run --all-files` on PRs (Phase 8 expands the CI lane — Phase 3 just installs the local hook per ROADMAP guardrail). |
| T-03-01-04 | DoS | Hook regex DoS on large source files | accept | Pattern is anchored substring (`vbt\\.Portfolio\\.from_signals\\s*\\(`); linear scan; no catastrophic backtracking. Source files are bounded by repo size. |
</threat_model>

<verification>
- Models: `uv run pytest packages/trading-core/tests/test_models_phase3_fields.py -x -q` → all pass
- DuckDB schema: `uv run pytest packages/trading-core/tests/test_duckdb_backtests_trades.py -x -q` → all pass
- Pre-commit hook + Wave 0 stubs: `uv run pre-commit run no-direct-vbt-from-signals --all-files` → exit 0; `uv run pytest --collect-only -q` for the 7 Wave 0 files → all collect successfully
- Regression: `uv run pytest -x -q` → all 244 existing tests still pass; new tests pass; xfail placeholders show as xfailed (not failed)
</verification>

<success_criteria>
- Fill, RiskDecision, RiskState, RiskConfig carry exactly the D-10 minimal fields (and the D-11 exit_reason Literal)
- backtests + trades tables exist after `ensure_schema()` with all D-01 / D-02 columns including stop_price and target_price
- write_backtest and write_trades methods exist on DuckDBStore and use parameterized queries
- no-direct-vbt-from-signals hook installed and active; rejects a synthetic violation file; passes the current repo
- All 7 Wave 0 test files exist and collect without error
- All existing 244 tests still pass; new tests pass; zero new ruff/lint regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-vertical-mvp-slice-backtester/03-01-SUMMARY.md` documenting the schema/model decisions and any deviations.
</output>
