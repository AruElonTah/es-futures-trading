---
phase: 03-vertical-mvp-slice-backtester
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01-PLAN.md"
files_modified:
  - packages/trading-core/src/trading_core/backtest/__init__.py
  - packages/trading-core/src/trading_core/backtest/safe_signals.py
  - packages/trading-core/src/trading_core/risk/pass_through.py
  - packages/trading-core/src/trading_core/risk/__init__.py
  - packages/trading-core/src/trading_core/execution/paper.py
  - packages/trading-core/src/trading_core/execution/__init__.py
  - packages/trading-core/tests/test_safe_signals.py
  - packages/trading-core/tests/test_paper_executor.py
  - packages/trading-core/tests/test_pass_through_risk.py
autonomous: true
requirements:
  - BT-02
  - BT-03
  - BT-08
tags:
  - backtest
  - vbt
  - paper-executor
  - risk
  - slippage
  - eod-flatten
must_haves:
  truths:
    - "safe_from_signals raises ValueError when price is a string"
    - "safe_from_signals applies entries.shift(1) and exits.shift(1) internally"
    - "PaperExecutor fills at next-bar open price, adjusted by session-phase slippage in adverse direction"
    - "PaperExecutor resolves intrabar stop+target conflict as stop-first (D-12)"
    - "PaperExecutor closes any open position at the last RTH bar with exit_reason='eod_flat'"
    - "PassThroughRiskManager always approves and clamps adjusted_size to RiskConfig.max_contracts"
  artifacts:
    - path: "packages/trading-core/src/trading_core/backtest/safe_signals.py"
      provides: "safe_from_signals wrapper enforcing shift(1) + array-price guard"
      contains: "def safe_from_signals"
    - path: "packages/trading-core/src/trading_core/execution/paper.py"
      provides: "PaperExecutor: next-bar fill, slippage, intrabar stop-first, EOD flatten"
      contains: "class PaperExecutor"
    - path: "packages/trading-core/src/trading_core/risk/pass_through.py"
      provides: "PassThroughRiskManager satisfying RiskManager Protocol"
      contains: "class PassThroughRiskManager"
  key_links:
    - from: "safe_from_signals"
      to: "vbt.Portfolio.from_signals"
      via: "entries=entries.shift(1).fillna(False).infer_objects(copy=False) + price array"
      pattern: "entries\\.shift\\(1\\)"
    - from: "PaperExecutor._slippage_ticks"
      to: "trading_core.instruments tick_size"
      via: "get_instrument(symbol).tick_size * Decimal(slippage_ticks)"
      pattern: "tick_size.*Decimal"
    - from: "PaperExecutor.check_exit"
      to: "Fill.exit_reason"
      via: "stop-first when both touched (D-12); 'eod_flat' on last RTH bar (BT-08)"
      pattern: "exit_reason.*stop|target|eod_flat"
---

<objective>
Build the three core Phase 3 components that consume the foundation from Plan 01: the look-ahead-safe `safe_from_signals` VectorBT wrapper, the minimal `PassThroughRiskManager`, and the `PaperExecutor` that simulates next-bar-open fills, session-phase-aware slippage, intrabar stop-first conflict resolution, and EOD flatten.

Purpose: These three classes are the Phase 3 implementations of the four `Protocol` seams (`Strategy` was filled in Phase 2; `RiskManager` + `Executor` get their minimal v1 implementations here; the `safe_from_signals` wrapper hardens the only legitimate VBT call site). Without these, Plan 03 has nothing to drive.

Output:
- `safe_from_signals(close, entries, exits, *, price, …)` wrapper that raises `ValueError` on string `price` and applies `entries.shift(1).fillna(False).infer_objects(copy=False)` internally
- `PassThroughRiskManager(config: RiskConfig)` whose `async check()` always approves and clamps `adjusted_size = min(signal.size_hint, config.max_contracts)`
- `PaperExecutor(symbol, …)` exposing `async fill(signal, decision, next_bar)` for entries, and a helper that returns `Fill` with the correct `exit_reason` on stop/target/EOD exits
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-VALIDATION.md
@.planning/phases/03-vertical-mvp-slice-backtester/03-01-PLAN.md
@packages/trading-core/src/trading_core/strategy/models.py
@packages/trading-core/src/trading_core/strategy/orb.py
@packages/trading-core/src/trading_core/risk/protocols.py
@packages/trading-core/src/trading_core/execution/protocols.py
@packages/trading-core/src/trading_core/instruments.py
@packages/trading-core/tests/fixtures/orb_day.py
@packages/trading-core/src/trading_core/data/models.py
@packages/trading-core/src/trading_core/logging.py

<interfaces>
<!-- Contracts the executor needs. Lifted from existing code. -->

From packages/trading-core/src/trading_core/risk/protocols.py:
- class RiskManager(Protocol):
    async def check(self, signal: Signal, state: RiskState) -> RiskDecision

From packages/trading-core/src/trading_core/execution/protocols.py:
- class Executor(Protocol):
    async def fill(self, signal: Signal, decision: RiskDecision) -> Fill

From packages/trading-core/src/trading_core/instruments.py:
- get(symbol: str) -> Instrument
- Instrument fields: tick_size:Decimal, tick_value:Decimal, point_value:Decimal, calendar_name:Literal["CME_Equity","NYSE"], rth_open_et:str ("09:30"), rth_close_et:str ("16:00")

From packages/trading-core/src/trading_core/data/models.py:
- class Bar(BaseModel, frozen=True, extra="forbid"):
    symbol:str, timeframe:str, ts_utc:AwareDatetime, open:Decimal, high:Decimal, low:Decimal, close:Decimal, volume:int, rollover_seam:bool

From packages/trading-core/src/trading_core/strategy/models.py:
- Signal fields (frozen, extra="forbid"): strategy_id, strategy_version, ts_utc, side:Literal["long","short"], entry:Decimal, stop:Decimal, target:Decimal, size_hint:Decimal, signal_id:str

From Plan 01 (D-10 / D-11):
- Fill fields: signal_id:str, fill_price:Decimal(gt=0), fill_qty:int(gt=0), side:Literal["long","short"], slippage_ticks:int(ge=0), ts_utc:AwareDatetime, exit_reason:Literal["target","stop","eod_flat","manual"]
- RiskDecision fields: approved:bool, reason:str, adjusted_size:int
- RiskState.realized_pnl_today:Decimal default Decimal("0")
- RiskConfig.max_contracts:int default 1

VBT 1.0.0 Portfolio.from_signals critical params (from 03-RESEARCH.md, VERIFIED by live testing):
- entries, exits: boolean pd.Series, MUST be pre-shifted via .shift(1)
- price: pd.Series of fill prices (NEVER the string 'nextbar' — crashes Numba)
- sl_stop, tp_stop: FRACTIONS (entry-stop)/entry not absolute prices
- open, high, low: pd.Series required for intrabar stop/target
- freq: '1min' required for Sharpe etc.
- direction: 'longonly' | 'shortonly' | 'both'
- VBT default behavior matches D-12 (stop-first on intrabar conflict) — no special config needed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: safe_from_signals wrapper (BT-02, D-13)</name>
  <files>
    packages/trading-core/src/trading_core/backtest/__init__.py,
    packages/trading-core/src/trading_core/backtest/safe_signals.py,
    packages/trading-core/tests/test_safe_signals.py
  </files>
  <read_first>
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (safe_from_signals Wrapper Pattern + Pitfall 1 + Pitfall 3 + Pitfall 4),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (safe_signals.py section),
    packages/trading-core/tests/test_safe_signals.py (Wave 0 stub from Plan 01),
    packages/trading-core/src/trading_core/storage/duckdb_store.py (module-docstring style)
  </read_first>
  <behavior>
    - safe_from_signals(price="nextbar", ...) raises ValueError whose message contains "price must be an array" and "Numba JIT".
    - safe_from_signals(price=1.0, ...) (a float, not pd.Series) does NOT raise (VBT may accept a scalar in some paths) — only string is forbidden. If you choose stricter typing, the test should reflect that; the locked spec is "isinstance(price, str)".
    - Entries are shifted by 1 internally: passing entries=Series([True, False, False, False]) (4 bars; entry at bar 0) feeds VBT entries=Series([False, True, False, False]) — verify by monkey-patching vbt.Portfolio.from_signals to capture the kwargs and assert `kwargs["entries"].iloc[0] == False and kwargs["entries"].iloc[1] == True`.
    - Same shift applied to exits.
    - When short_entries / short_exits are supplied, both are shifted by 1; when None, neither key is passed to vbt.Portfolio.from_signals.
    - No FutureWarning emitted under pandas 2.2.x when calling the wrapper (use pytest.warns(None) or filterwarnings to assert).
    - The wrapper returns the object that vbt.Portfolio.from_signals returns (passthrough).
    - End-to-end smoke: call safe_from_signals on a 30-bar pd.Series with one entry / one exit and `price = close.shift(-1).fillna(close)` + freq='1min' + init_cash=10_000.0 + size=1 + direction='longonly' — assert returned portfolio has `pf.trades.count() >= 0` (no crash, finite Sharpe).
  </behavior>
  <action>
    Create `packages/trading-core/src/trading_core/backtest/__init__.py` exporting nothing initially (touch file with a one-line module docstring).

    Create `packages/trading-core/src/trading_core/backtest/safe_signals.py` matching the spec in 03-RESEARCH.md §safe_from_signals Wrapper Pattern verbatim — adapted to the codebase conventions:
    - Module docstring follows 03-PATTERNS.md §safe_signals.py "Module-level docstring" template: cite D-13, cite 03-RESEARCH.md §safe_from_signals + §Pitfall 1, note that the no-direct-vbt-from-signals hook explicitly excludes this file.
    - Function signature: `def safe_from_signals(close: pd.Series, entries: pd.Series, exits: pd.Series, *, price, sl_stop=None, tp_stop=None, short_entries=None, short_exits=None, open=None, high=None, low=None, **kwargs) -> "vbt.Portfolio":` — note `vbt` imported inside the function or at module top from `vectorbt as vbt`.
    - Guard: `if isinstance(price, str): raise ValueError("safe_from_signals: price must be an array of fill prices, not a string. Pass the next-bar open prices as a pd.Series. Passing price='nextbar' crashes Numba JIT.")` — message must contain the substrings `"price must be an array"` and `"Numba JIT"` so the test can match on either.
    - Shift: `shifted_entries = entries.shift(1).fillna(False).infer_objects(copy=False)`; same for shifted_exits. Apply same shift to short_entries / short_exits only when not None (else pass `None` through — do NOT inject keys with None values into vbt.Portfolio.from_signals).
    - Build call_kwargs dict starting from `dict(close=close, entries=shifted_entries, exits=shifted_exits, price=price, **kwargs)` and conditionally add sl_stop / tp_stop / short_entries / short_exits / open / high / low only when the caller supplied them.
    - Return `vbt.Portfolio.from_signals(**call_kwargs)`.
    - Add a sentinel comment: `# NOTE: This is the ONLY legitimate call site for vbt.Portfolio.from_signals — pre-commit hook excludes this file (D-13).`

    Create `packages/trading-core/tests/test_safe_signals.py` replacing the Wave 0 xfail stub with real tests covering every behavior bullet above. Use `import vectorbt as vbt`, `import pandas as pd`, `from unittest.mock import patch`. For the kwargs-capture tests, use `with patch.object(vbt.Portfolio, "from_signals", wraps=vbt.Portfolio.from_signals) as spy:` and assert on `spy.call_args.kwargs["entries"]`.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_safe_signals.py -x -q</automated>
  </verify>
  <done>
    safe_from_signals is importable from `trading_core.backtest.safe_signals`; rejects string price; shifts entries and exits internally; passes the smoke test against the 30-bar fixture. No FutureWarning under pandas 2.2.x. `pre-commit run no-direct-vbt-from-signals --all-files` exits 0 (safe_signals.py is excluded; no other source file calls the unwrapped API).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PassThroughRiskManager + PaperExecutor (BT-03, BT-08, D-10, D-12)</name>
  <files>
    packages/trading-core/src/trading_core/risk/pass_through.py,
    packages/trading-core/src/trading_core/risk/__init__.py,
    packages/trading-core/src/trading_core/execution/paper.py,
    packages/trading-core/src/trading_core/execution/__init__.py,
    packages/trading-core/tests/test_pass_through_risk.py,
    packages/trading-core/tests/test_paper_executor.py
  </files>
  <read_first>
    packages/trading-core/src/trading_core/risk/protocols.py,
    packages/trading-core/src/trading_core/execution/protocols.py,
    packages/trading-core/src/trading_core/strategy/orb.py (Decimal arithmetic + ET timezone pattern, lines 34-46 and 150-211),
    packages/trading-core/src/trading_core/instruments.py,
    packages/trading-core/src/trading_core/data/models.py,
    packages/trading-core/tests/fixtures/orb_day.py,
    .planning/phases/03-vertical-mvp-slice-backtester/03-RESEARCH.md (PaperExecutor section + Pitfall 4 + Architectural Responsibility Map),
    .planning/phases/03-vertical-mvp-slice-backtester/03-PATTERNS.md (paper.py + pass_through.py sections),
    .planning/phases/03-vertical-mvp-slice-backtester/03-CONTEXT.md (D-10, D-11, D-12, Specifics → slippage rule)
  </read_first>
  <behavior>
    PassThroughRiskManager:
    - check(signal, state) returns RiskDecision(approved=True, reason="pass_through", adjusted_size=min(int(signal.size_hint), config.max_contracts)).
    - With RiskConfig(max_contracts=1) and signal.size_hint=Decimal("5"), adjusted_size==1.
    - With RiskConfig(max_contracts=3) and signal.size_hint=Decimal("2"), adjusted_size==2.
    - Always approved=True regardless of state.realized_pnl_today (Phase 5 owns rejection logic).
    - check() is awaitable (async def) so it satisfies the RiskManager Protocol.

    PaperExecutor (fill entries):
    - fill_entry(signal, decision, next_bar) returns Fill(signal_id=signal.signal_id, fill_price=next_bar.open + tick_size*slippage_ticks (long) or next_bar.open - tick_size*slippage_ticks (short), fill_qty=decision.adjusted_size, side=signal.side, slippage_ticks=slippage_ticks, ts_utc=next_bar.ts_utc, exit_reason="target" — placeholder; entry fills use "target" as a sentinel OR an alternative literal: see action for the locked rule).
    - For a 9:35 ET bar (during 9:30-9:45 window), slippage_ticks==2. For a 10:30 ET bar, slippage_ticks==1.
    - For SPY (tick_size=0.01), a long fill against next_bar.open=Decimal("471.50") with 2-tick slippage returns fill_price=Decimal("471.52").
    - For ES (tick_size=0.25), same with 1-tick slippage returns Decimal("471.75").
    - The boundary 9:30 ET is inclusive; 9:45 ET is exclusive (matches the 03-PATTERNS.md _OPEN_WINDOW_START <= et_time < _OPEN_WINDOW_END check).
    - On a DST-spring-forward day (2026-03-08), a UTC bar that lands at 9:30 ET (=13:30 UTC during EDT) is correctly identified as in the open window (test with explicit UTC timestamps for the EDT case).

    PaperExecutor (check_exit) — D-12 stop-first and BT-08 EOD flatten:
    - For an OPEN long position with stop=Decimal("470.25") and target=Decimal("472.50"):
        - Bar (low=470.00, high=471.00) → exit_reason="stop", fill_price = stop +/- adverse slippage (for long, stop is sold — adverse means LOWER fill, so fill_price = stop - tick_size*slippage_ticks).
        - Bar (low=471.00, high=472.75) → exit_reason="target", fill_price = target +/- adverse slippage (favorable side: target hit, fill at target with same slippage_ticks magnitude in adverse direction; favoring conservatism: fill_price = target - tick_size*slippage_ticks for long sell).
        - Bar (low=470.00, high=472.75) → exit_reason="stop" (BOTH touched; D-12 stop-first).
        - Bar (low=470.75, high=471.50) (neither touched) → returns None.
    - For a long position open at the last RTH bar (BT-08), check_exit returns exit_reason="eod_flat" with fill_price=last_bar.close (no slippage applied to EOD market-close exit per minimal Phase 3 — keep simple, document deviation if any).
    - The "last RTH bar" check is parameterized: caller passes `is_last_rth_bar: bool` (BacktestEngine computes it). The executor does NOT own the calendar; this preserves the Architectural Responsibility Map and matches Phase 1's RthFilter ownership.
    - After EOD flat fill, the test asserts `sum(positions) == 0` when summed over a list of Fills' net qty (entries +, exits -).
  </behavior>
  <action>
    Create `packages/trading-core/src/trading_core/risk/pass_through.py` matching the spec in 03-PATTERNS.md §pass_through.py:
    - Imports: `from __future__ import annotations`; `from trading_core.logging import get_logger`; `from trading_core.risk.models import RiskConfig, RiskDecision, RiskState`; `from trading_core.strategy.models import Signal`.
    - `class PassThroughRiskManager:` — structurally satisfies the RiskManager Protocol; no inheritance.
    - `__init__(self, config: RiskConfig) -> None: self._config = config`.
    - `async def check(self, signal: Signal, state: RiskState) -> RiskDecision:` body: `adjusted_size = min(int(signal.size_hint), self._config.max_contracts)`; log.debug with signal_id / approved / adjusted_size; return `RiskDecision(approved=True, reason="pass_through", adjusted_size=adjusted_size)`.
    - Update `packages/trading-core/src/trading_core/risk/__init__.py` to re-export PassThroughRiskManager + the protocol + models.

    Create `packages/trading-core/src/trading_core/execution/paper.py`:
    - Imports: `from __future__ import annotations`; `from datetime import time as dt_time`; `from decimal import Decimal`; `from zoneinfo import ZoneInfo`; `from trading_core.data.models import Bar`; `from trading_core.execution.models import Fill`; `from trading_core.instruments import get as get_instrument`; `from trading_core.logging import get_logger`; `from trading_core.risk.models import RiskDecision`; `from trading_core.strategy.models import Signal`.
    - Module constants: `_ET = ZoneInfo("America/New_York")`; `_OPEN_WINDOW_START = dt_time(9, 30)`; `_OPEN_WINDOW_END = dt_time(9, 45)`.
    - Helper `def _slippage_ticks(bar_ts_utc, symbol: str) -> int`: convert to ET via `bar_ts_utc.astimezone(_ET).time()`; if in `[_OPEN_WINDOW_START, _OPEN_WINDOW_END)` return 2 (>= 1.5 ticks adverse — FR-1 pitfall); else return 1 (off-peak default, per CONTEXT specifics rounded up from 0.5).
    - Helper `def _adverse(side: str, price: Decimal, slippage: Decimal) -> Decimal`: for `side=="long"` return `price + slippage` (entry: buy higher; exit: sell lower — the caller decides direction); design choice: for fills entering long, adverse is +; for fills exiting long, adverse is -. To keep the helper simple, the caller passes the explicit sign — see entry/exit methods below.
    - `class PaperExecutor:`
      - `__init__(self, symbol: str)`: load and store `self._instrument = get_instrument(symbol)`; store symbol.
      - `async def fill_entry(self, signal: Signal, decision: RiskDecision, next_bar: Bar) -> Fill`: compute slippage_ticks via _slippage_ticks(next_bar.ts_utc, symbol); compute `adj = self._instrument.tick_size * Decimal(slippage_ticks)`; for long, `fill_price = next_bar.open + adj`; for short, `fill_price = next_bar.open - adj`. Return Fill(signal_id=signal.signal_id, fill_price=fill_price, fill_qty=decision.adjusted_size, side=signal.side, slippage_ticks=slippage_ticks, ts_utc=next_bar.ts_utc, exit_reason="target"). NOTE: Per Phase 3 D-11, exit_reason has only four legal values — for entry fills we set `exit_reason="target"` as a structural placeholder. The driver loop in Plan 03 distinguishes entry fills from exit fills by maintaining its own list separately; Phase 5 may split entry vs exit into separate models if it becomes a footgun. CONTRACT NOTE for SUMMARY: Document exit_reason="target" as a sentinel (not exit semantics) — flag for Phase 5 refinement when entry/exit Fill distinction is addressed.
      - `def check_exit(self, *, side: str, entry_price: Decimal, stop: Decimal, target: Decimal, bar: Bar, is_last_rth_bar: bool) -> tuple[str, Decimal] | None`: returns (exit_reason, exit_price) or None.
        - For side=="long":
          - hit_stop = bar.low <= stop
          - hit_target = bar.high >= target
          - if hit_stop and hit_target: exit_reason="stop" (D-12 stop-first), exit_price=stop with adverse slippage (fill below stop): `stop - tick_size * slippage_ticks(bar.ts_utc, symbol)`.
          - elif hit_stop: same as above.
          - elif hit_target: exit_reason="target", exit_price=target with adverse slippage (fill below target): `target - tick_size * slippage_ticks(...)`.
          - elif is_last_rth_bar: exit_reason="eod_flat", exit_price=bar.close (no slippage on EOD market-close exit — keep simple for Phase 3).
          - else: return None.
        - For side=="short": mirror the comparisons (hit_stop = bar.high >= stop; hit_target = bar.low <= target; adverse direction flipped).
      - `async def fill_exit(self, *, signal: Signal, exit_reason: str, exit_price: Decimal, exit_ts_utc, fill_qty: int) -> Fill`: returns Fill(signal_id=signal.signal_id, fill_price=exit_price, fill_qty=fill_qty, side=("short" if signal.side=="long" else "long"), slippage_ticks=_slippage_ticks(exit_ts_utc, self._symbol) if exit_reason != "eod_flat" else 0, ts_utc=exit_ts_utc, exit_reason=exit_reason). Slippage_ticks=0 on EOD-flat because exit_price is bar.close (no adverse adjustment applied).

    Update `packages/trading-core/src/trading_core/execution/__init__.py` to re-export PaperExecutor + the protocol + models.

    Create `packages/trading-core/tests/test_pass_through_risk.py` covering the three PassThroughRiskManager bullets. Use `pytest`; the async tests use the `asyncio_mode=auto` already in pyproject.toml — no `@pytest.mark.asyncio` decorator.

    Replace the Wave 0 stub in `packages/trading-core/tests/test_paper_executor.py` with real tests covering every PaperExecutor behavior bullet above:
    - Use `from fixtures.orb_day import orb_day_bars as _orb_day_bars` (import-mode=importlib pattern, locked in Phase 1).
    - Use class-organized tests: `TestSlippageWindow`, `TestEntryFill`, `TestIntrabarConflict`, `TestEODFlatten`.
    - For the DST-EDT test, construct two Bar fixtures with explicit `datetime(2026, 3, 9, 13, 30, tzinfo=timezone.utc)` (=9:30 EDT) and `datetime(2026, 3, 9, 13, 45, tzinfo=timezone.utc)` (=9:45 EDT) and assert slippage_ticks==2 on the first and ==1 on the second.
    - For `sum(positions) == 0` assertion, build a fills list of (entry, eod_flat_exit) and verify net quantity is zero by summing signed quantities.
  </action>
  <verify>
    <automated>uv run pytest packages/trading-core/tests/test_pass_through_risk.py packages/trading-core/tests/test_paper_executor.py -x -q</automated>
  </verify>
  <done>
    PassThroughRiskManager and PaperExecutor implementations import cleanly. All slippage / intrabar conflict / EOD flatten / DST cases pass. The `sum(positions) == 0` assertion for an entry+EOD-flat-exit fill pair returns 0. Existing 244-test suite still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| safe_from_signals → VBT Numba JIT | wrong arg shape crashes the inner loop with cryptic errors |
| PaperExecutor → Decimal/float arithmetic | float drift could break the trust-the-numbers reproducibility invariant |
| PaperExecutor → instruments.py | hardcoding tick_size anywhere else corrupts dollar math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | safe_from_signals price arg | mitigate | `isinstance(price, str)` guard raises ValueError before reaching VBT; covered by test_rejects_string_price. |
| T-03-02-02 | Tampering | Look-ahead leakage via missing shift(1) | mitigate | Wrapper applies shift internally; pre-commit hook from Plan 01 blocks any direct call site that could bypass the shift. BL-1 integration test (Plan 03) is the runtime gate. |
| T-03-02-03 | Tampering | Hardcoded tick_size in PaperExecutor | mitigate | All tick_size reads go through `get_instrument(symbol).tick_size`; pytest assertion uses Decimal compare on a known SPY/ES fixture so any literal sneaking back in fails. |
| T-03-02-04 | Information Disclosure | Float in price path producing reproducibility drift | mitigate | All price math uses Decimal; no `float()` in the executor body; round-trips through `Decimal(str(round(x, 4)))` only at the (yet-to-come) VBT boundary in Plan 03. |
| T-03-02-05 | DoS | PassThroughRiskManager unbounded approval | accept | Phase 3 is paper-only single-operator. PassThroughRiskManager intentionally approves everything; Phase 5 replaces it with the real risk manager (max DD, daily DD breaker, per-strategy concurrency cap). Documented in the class docstring. |
</threat_model>

<verification>
- safe_from_signals: `uv run pytest packages/trading-core/tests/test_safe_signals.py -x -q` → all pass
- Risk + Executor: `uv run pytest packages/trading-core/tests/test_pass_through_risk.py packages/trading-core/tests/test_paper_executor.py -x -q` → all pass
- Pre-commit guard still happy: `uv run pre-commit run no-direct-vbt-from-signals --all-files` → exit 0 (safe_signals.py is the only call site and is excluded)
- Regression: `uv run pytest -x -q` → 244 existing + new tests pass; no FutureWarning floods
</verification>

<success_criteria>
- safe_from_signals rejects string `price`; applies shift(1) to entries and exits internally; passes a smoke 30-bar VBT run with finite Sharpe
- PassThroughRiskManager.check approves every signal and clamps adjusted_size to max_contracts (1 by default)
- PaperExecutor.fill_entry produces a Fill with the correct slippage_ticks (2 during 9:30-9:45 ET window, 1 off-peak) and adverse-direction fill_price
- PaperExecutor.check_exit returns ("stop", _) when both stop and target are touched on the same bar (D-12); returns ("eod_flat", bar.close) on is_last_rth_bar=True; returns None when neither levels nor EOD apply
- All test classes pass; sum-of-positions assertion after entry+EOD-flat is zero
</success_criteria>

<output>
After completion, create `.planning/phases/03-vertical-mvp-slice-backtester/03-02-SUMMARY.md` documenting any deviations on slippage ticks (e.g., if you locked 0.5-rounded-to-1 differently) and the exit_reason="target" sentinel for entry fills. IMPORTANT: explicitly flag in the SUMMARY that exit_reason="target" on entry Fill is a sentinel (not exit semantics) — this is a known Phase 5 refinement item and should be listed in the SUMMARY's "Known Debt" section for Phase 5 to address.
</output>
