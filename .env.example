# Phase 0 spike — Twelve Data REST API key.
# Register at https://twelvedata.com/register (free tier sufficient).
# Copy the key from https://twelvedata.com/account/api-keys.
# This file is the committed template; place the real value in .env (gitignored).
TWELVEDATA_API_KEY=

# Future Phase 5 broker / live-execution keys land here when (and if) the
# paper-only constraint is relaxed. The TWELVEDATA_API_KEY entry above is
# loaded by trading_core.config.Settings via the SecretStr field
# `twelvedata_api_key`; repr() / str() redact the value (T-01-02-01).
