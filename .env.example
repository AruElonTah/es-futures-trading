# Phase 0 spike — Twelve Data REST API key.
# Register at https://twelvedata.com/register (free tier sufficient).
# Copy the key from https://twelvedata.com/account/api-keys.
# This file is the committed template; place the real value in .env (gitignored).
TWELVEDATA_API_KEY=
